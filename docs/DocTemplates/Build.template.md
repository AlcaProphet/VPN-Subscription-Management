# <项目名> 功能构建计划（BuildN：当前构建方案）

> **文档定位：** 本文档是 <项目名> 的**当前构建方案**（依据 AGENTS.md §12.1：Build 文档为详细构建方案，非强规则），承接已存档的 [Build1-(N-1)](HistoryDocs/)（见 `HistoryDocs/`，仅核查）。
> - 设计记录：[DesignN.md](./DesignN.md)（当前设计记录；与 AGENTS.md 或用户决策冲突时以用户确认为准）
> - 编码指令：[AGENTS.md](./AGENTS.md)（**唯一强要求**）
> - 问题追踪：[IssueN.md](./IssueN.md)（当前问题记录）
> - 历史构建与问题记录：见 [HistoryDocs/](./HistoryDocs/)（均已存档，仅核查）
>
> **执行原则（与 Build1-(N-1) 一致）：**
> - 每一步完成后均可编译、可测试。不跳步、不并行多步。
> - AI 执行指令：每次仅执行一个 Step，完成后运行验收命令，确认通过后再进入下一步。
> - **排序原则：先修复后构建、先安全后优化、先依赖后独立**。
> - 每步的新增逻辑必须配套单元测试（用户决策）。

---

## 一、构建进度追踪

| Step | 内容 | 设计依据 | 状态 |
|------|------|---------|------|
| — | <当前无进行中的构建 Step（上一轮已全部验收通过并归档）> | — | — |
| 1 | <候选构建项（见五、候选列表，待用户决策后逐项转 Step）> | DesignN §三 | ☐ 未开始 |

> 状态标记：☐ 未开始 / ◧ 进行中 / ✅ 验收通过

---

## 二、构建概要（文件清单总览）

| Step | 涉及文件 | 要点 |
|------|---------|------|
| <Step> | <文件路径列表> | <要点> |

---

## 三、构建顺序依赖图

```
候选构建项（DesignN §三）逐项经用户确认 → 转为 Step（含目标/前置/参考代码/验收命令）→ 按序执行
```

> 若有跨 Step 依赖，在此补充图示：
> Step 1 → Step 2 → Step 3（Step 3 依赖 Step 2 产出）

---

## 四、分步构建计划

当前无进行中的构建 Step。候选构建项经用户确认后在本节追加，每个 Step 包含：

**Step N：<Step 标题>**

- **目标：** <本步要实现什么>
- **前置条件：** <依赖的 Step 或环境条件>
- **产出文件与操作：**
  - <文件路径 1>：<改动说明>
  - <文件路径 2>：<改动说明>
  - <可直接使用的参考代码块>
- **测试与验收命令：**
  ```bash
  <验收命令 1>
  <验收命令 2>
  ```
- **验收标准：** <通过条件>

---

## 五、候选构建项（待用户决策，逐项转 Step）

| # | 候选 | 说明 | 来源 |
|---|------|------|------|
| 1 | <候选 1> | <说明> | DesignN §三-1 |
| 2 | <候选 2> | <说明> | DesignN §三-2 |

> 候选转 Step 流程：用户确认 → 在本文件追加 Step（含目标/前置/参考代码/验收命令）→ 按序执行。

---

## 六、变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | <日期> | 初始版本：作为当前构建方案（承接已存档 Build1-(N-1)），候选构建项见第五节 |
