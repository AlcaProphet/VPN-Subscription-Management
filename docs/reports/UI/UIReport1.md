# VPN 订阅管理系统 UI / UX 研究报告（UIReport1）

> **文档定位：** 本报告是一次仅针对前端视觉系统与用户体验的研究记录。依据为 [Design1-UI.md](../../reports/Design/Design1-UI.md)、[Design2-UI.md](../../../Design2-UI.md)、2026-08-28 的前端源码及本地浏览器走查结果。本文不改变产品功能、权限模型或后端契约，也不是已批准的 Design / Build 文档；候选项仍需用户确认后才能进入设计与构建。
>
> **结论先行：** 当前 UI 已具备完整路由、明暗主题和主要移动端分支，功能密度也适合小团队工具，但仍接近“Ant Design 默认组件拼装”的工程界面。最值得优先改进的是：建立完整视觉 Token、提高字号与内容层级、重组管理导航、拆分超长设置页、统一页面骨架与状态容器、改善移动端操作收纳，以及把技术性文案改成任务导向文案。

---

## 一、研究目标、范围与方法

### 1.1 目标

- 让界面在不更换 Vue 3、Ant Design Vue 与 Tailwind 技术栈的前提下更美观、更易读、更容易完成任务。
- 覆盖所有现有前端路由，检查视觉层级、信息架构、响应式、交互反馈、中文文案与无障碍。
- 将事实、判断与假设分开，并给出可落地的组件方案、伪代码、实施顺序和验收方法。

### 1.2 范围边界

- **包含：** 页面布局、导航、组件表现、状态表达、主题、响应式、交互提示、文案、键盘可用性和颜色对比。
- **不包含：** API 正确性、数据库、同步/装配算法、权限规则本身、后端错误码设计、安全逻辑和数据保留。
- 本报告可以评价“某个后端状态在 UI 上是否清楚”，但不评价该状态是否应该存在或后端是否正确。

### 1.3 环境与工具

| 项目 | 结果 |
|------|------|
| 仓库 | `/Users/kylechen/Desktop/Repo/VPN-Subscription-Management` |
| 前端 | Vue 3.5、Vite 7、Ant Design Vue 4.2、Tailwind CSS 3.4、Pinia、Vue Router |
| 运行方式 | `docker compose up -d --build`，单容器 `127.0.0.1:8080` |
| 构建结果 | Docker 多阶段构建成功；前端 `vue-tsc -b && vite build` 成功 |
| 浏览器 | Codex in-app browser，本地页面 DOM 与截图走查 |
| 视口 | 桌面约 1440×900、平板约 1024×768、手机约 390×844 |
| 主题 | Setup、首页、管理布局检查浅色与暗色；其余页结合浏览器和源码核验 |
| 测试数据 | 新建 Docker 测试卷；快速开始；虚构管理员账号；基础模式 |
| 文件权限 | 可读写；执行前目标报告不存在 |

### 1.4 调查方式与证据标记

- **[浏览器]**：实际页面 DOM 或截图观察。
- **[代码]**：前端组件、路由、样式或测试的静态核验。
- **[设计]**：Design1-UI / Design2-UI 已写明的规格。
- **[推断]**：根据目标用户与界面上下文做出的合理判断，未被真实用户研究验证。

本次没有采集真实用户数据，也没有执行删除、外部登录、邮件发送、正式导入或其他无必要副作用操作。平板视口截图采集发生一次非致命浏览器错误，按约定跳过；对应断点仍通过浏览器 DOM 与源码响应式分支核验。

---

## 二、规格、代码与实际呈现摘要

| 主题 | 设计规格 | 代码实现 | 浏览器呈现 | 结论 |
|------|----------|----------|------------|------|
| 主题 | 科技蓝、浅色布局、支持暗色 | 仅配置 `colorPrimary` 与 AntD 明暗算法 | 明暗切换可用；暗色大块区域接近纯黑 | 功能达标，视觉层次不足 |
| 排版 | 正文 14、标题 20、内容内边距 24 | `PageHeader` 使用 `text-lg`，大量辅助字为 12px | 1440px 下文字和控件整体偏小 | 应提高标题、正文与触控尺度 |
| 页面背景 | 浅灰页面、白色卡片、8px 圆角 | 布局卡片与页面组件层层叠加 | 空页面出现很大的单色内容块 | 需要明确 Page / Section / Card 三层 |
| 管理导航 | Design1 规定平铺；Design2 增至 10–12 项 | `AdminLayout` 单层 Menu | 扫描成本高；收起后只能依赖图标 | 现设计已不适合当前模块数量 |
| 移动列表 | `<768` 表格转卡片 | 多数高频页实现 `hidden md:block` + `md:hidden` | 手机首页纵向可读 | 基线良好，但页面实现不完全统一 |
| 页面头 | 标题、副标题、操作区 | 管理页普遍复用 `PageHeader` | 标题主次较弱，操作按钮语义不统一 | 扩展为统一 PageShell |
| 空状态 | Empty + 引导动作 | 有的用 `TriStateList`，有的纯文字/段落 | 空内容常居中于巨大容器且缺说明 | 统一 EmptyState 并限制内容高度 |
| 设置页 | 分区设置 | 十余 Tabs 内容在同一页面连续展开 | 文本极长，难定位和保存 | P0 级信息架构问题 |
| 技术术语 | 设计允许专业管理能力 | UI 直接出现 OIDC、Realm、Token、URI、Xray | 对新管理员学习成本高 | 保留术语但补人话解释 |
| 无障碍 | Design1 要求基础无障碍 | 有语义 heading/menu，但 emoji Switch、图标按钮和 placeholder 仍有缺口 | DOM 基础结构可识别 | 需要系统性补齐焦点与名称 |

### 2.1 已实现且应保留的优点

- 路由级代码分割完整，公共、用户、管理三类布局职责清楚。[代码]
- 管理端页面普遍复用 `PageHeader`，危险操作复用 `ConfirmModal`，错误/空/加载已有 `TriStateList` 基础。[代码]
- 多个复杂列表已实现桌面表格、手机卡片双态，避免手机横向表格。[代码]
- Setup 手机端步骤条会转为纵向，用户首页卡片自然降为单列。[浏览器][代码]
- 明暗主题由 `ConfigProvider` 全局联动，不需要逐组件维护两套主题。[代码]
- 异步任务、部分失败、轮询中断等状态文案已覆盖，工程完整度高。[代码]

---

## 三、当前 UI 总体评价

### 3.1 视觉风格

当前界面干净、克制、没有无关装饰，但视觉辨识度较低。主色、圆角、阴影、字号和中性色大多沿用组件默认值，导致不同业务模块看起来像同一组表格与弹窗的排列。[浏览器][代码]

桌面截图中主要内容只占可视区域上部，文字尺寸偏小，空白不是经过设计的呼吸感，而是内容容器缺少最大宽度、最小高度和空状态节奏造成的“空”。暗色模式虽没有明显不可读区域，但页面背景、卡片和侧栏之间差异小，层级主要靠细边框，长时间使用容易疲劳。[浏览器]

### 3.2 信息架构

- 管理端基础模式已有 10 个平铺入口，高级模式再增加“用户组”和“Xray 实例”。“订阅、分享、平台、规则、订阅装配、节点”属于同一业务链，却在单层列表里和“用户、审批、日志、配置”混排。[浏览器][代码]
- “面板配置”包含身份认证、验证码、SMTP、站点、模式、限流、日志、内容、导入导出、备份与危险操作，远超单页可快速理解的范围。[浏览器]
- “订阅装配”内部同时有规则素材池、代理组和多类装配器，页面副标题直接描述实现步骤，适合作为内部开发注释，不适合作为面向用户的页面说明。[浏览器]

### 3.3 交互反馈与状态

Notify、ConfirmModal、loading 和 Empty 已覆盖大多数基础状态，属于可靠基线。问题在于状态表达的“质量”不统一：有的空状态有插图和动作，有的只有一行灰字；有的按钮表达任务（“新建订阅”），有的表达实现（“装配生成”）；有的禁用按钮附 Tooltip，有的没有下一步指引。[代码]

### 3.4 文案

管理员页面偏技术说明，用户页面相对友好。主要问题有四类：

1. 把内部概念当作用户已知，例如 `product_type` 对应的 `yaml / subs / generic-subs`。
2. 用“暂无”“失败”“未开启”描述状态，却没有告诉用户如何继续。
3. 危险提示堆叠技术后果，缺少“先说会发生什么，再说为何”的结构。
4. AntD 中文按钮出现“登 录、返 回、删 除”式字符间距，视觉上不自然。[浏览器]

### 3.5 无障碍

- 优点：页面有 heading、banner、main、menu 等语义结构，表单大多有 Form.Item label。[浏览器]
- 风险：主题 Switch 的可访问名称主要表现为 emoji；纯图标侧栏收起后依赖图形辨认；辅助文本常使用很浅的灰色和 12px；部分规则只放在 placeholder；颜色 Tag 可能成为唯一状态线索。[代码][浏览器]
- 键盘焦点、弹窗回焦、跳过导航和 `prefers-reduced-motion` 未形成全局规范。[代码]

---

## 四、建议的全局视觉系统

### 4.1 视觉方向

建议采用“清晰的运维工作台”方向：保留科技蓝和 Ant Design 的稳定感，但用更成熟的中性色、较大的排版、分层表面、任务型图标和一致的页面节奏建立产品辨识度。不建议改成高饱和渐变、大玻璃拟态或重动效，这些风格会降低长表格和配置页的可读性。

### 4.2 Token 建议

```ts
// 伪代码：最终值需在 Design 阶段确认并做 WCAG 对比度测试
export const uiTokens = {
  color: {
    primary: '#2563EB',
    primaryHover: '#1D4ED8',
    page: '#F6F8FB',
    surface: '#FFFFFF',
    surfaceSubtle: '#F8FAFC',
    border: '#E2E8F0',
    text: '#0F172A',
    textSecondary: '#475569',
    textTertiary: '#64748B',
    success: '#15803D',
    warning: '#B45309',
    danger: '#B91C1C',
  },
  dark: {
    page: '#0B1120',
    surface: '#111827',
    surfaceRaised: '#172033',
    border: '#2B3548',
    text: '#F8FAFC',
    textSecondary: '#CBD5E1',
  },
  type: {
    pageTitle: { size: 24, lineHeight: 32, weight: 650 },
    sectionTitle: { size: 18, lineHeight: 26, weight: 600 },
    body: { size: 14, lineHeight: 22 },
    helper: { size: 13, lineHeight: 20 },
  },
  radius: { control: 8, card: 12, modal: 14 },
  spacing: { page: 24, section: 24, card: 20, mobilePage: 16 },
  shadow: { card: '0 1px 2px rgb(15 23 42 / 6%)', raised: '0 12px 32px rgb(15 23 42 / 12%)' },
}
```

### 4.3 页面骨架

所有管理页建议统一为四层：全局导航 → PageHeader → 可选 ContextBar → Section/Card。页面背景不再额外套一个覆盖全高的大白卡，空状态放进有合理最小高度的 Section。

```vue
<PageShell max-width="1440">
  <PageHeader
    title="订阅管理"
    description="创建订阅并管理已发布版本"
    :breadcrumbs="[{ label: '分发管理' }, { label: '订阅' }]"
  >
    <template #primaryAction>
      <Button type="primary">新建订阅</Button>
    </template>
    <template #secondaryActions>
      <Button>前往装配</Button>
    </template>
  </PageHeader>

  <StateContainer :loading="loading" :error="error" :empty="items.length === 0">
    <ResponsiveCollection :items="items" />
    <template #empty>
      <EmptyState title="还没有订阅" description="可以上传现有配置，或从装配器生成第一份订阅。">
        <Button type="primary">新建订阅</Button>
        <Button>从装配器开始</Button>
      </EmptyState>
    </template>
  </StateContainer>
</PageShell>
```

### 4.4 组件规范

| 组件 | 建议 |
|------|------|
| PageHeader | 标题 24px；副标题用用户目标描述；主操作仅一个；危险操作不得进入主操作位 |
| SectionCard | 12px 圆角、1px 边框、轻阴影；标题区与内容区间距固定 |
| DataTable | 表头更轻、行高至少 48px；主字段固定左侧；次要操作收进“更多”菜单 |
| MobileCard | 关键字段 3–4 项；底部一个主操作；次要/危险操作放菜单；触控目标至少 44px |
| FormSection | 每组不超过 6–8 个字段；组标题说明影响；帮助文本不只放 placeholder |
| EmptyState | 标题说明“现在是什么状态”，描述说明“为什么/如何继续”，最多两个动作 |
| StatusBadge | 图标/文字/颜色三者至少两种同时表达；状态词全局统一 |
| ConfirmModal | 第一段说明结果，第二段列影响；确认按钮使用具体动词，如“删除节点” |
| Toast | 成功只说明结果；失败说明原因与下一步；长任务用可持续 Notification |
| CopyField | 明确内容类型与敏感性；复制后提示“已复制订阅链接，请勿公开分享” |

---

## 五、导航与信息架构改进

### 5.1 管理导航分组

当前平铺菜单是 Design1 时期的明确决定，但 Design2 增加模块后已不再适配。建议下一轮设计允许分组，并保留收起态 Tooltip：

```text
概览
  返回主界面

分发管理
  订阅
  分享
  平台
  规则

装配资源
  订阅装配
  节点
  用户组（高级）
  Xray 实例（高级）

成员与权限
  用户
  审批中心

系统
  面板配置
  日志
```

推荐默认展开当前分组；其他分组保持可见但压缩。收起侧栏时每个图标必须有 Tooltip，组分隔仍以细线体现。手机 Drawer 在顶部显示当前页面和关闭按钮，底部固定“返回主界面”。

### 5.2 设置页重组

建议保持 `/admin/settings` 路由不变，在页内改成左侧锚点导航或 Tabs + 分组卡片：

- 身份与访问：OIDC、本地认证、验证码。
- 通知：SMTP。
- 外观与内容：站点名称、ICON、公告、页脚。
- 运行与安全：模式、限流、连接超时、日志、调试。
- 数据管理：导入导出、备份。
- 危险操作：始终独立红色区域，默认折叠。

每组提供独立保存条，保存后仅反馈该组。页面头显示“未保存更改”状态，切换分组或离开页面时提醒。

### 5.3 装配页重组

- 页面说明改为“组合节点、代理组和规则，生成可发布的订阅或分流规则”。
- 顶层导航以三个任务命名：“规则素材”“代理组”“生成配置”。
- “生成配置”内部再选择 Clash / Shadowrocket / 通用节点 / 分流规则，避免标题里出现“四类装配器”。
- 步骤条只显示当前任务需要的步骤；移动端用紧凑步骤标题和固定底部“上一步/下一步”。

---

## 六、全路由 UI / UX 审计矩阵

> “实机”表示浏览器直接到达或由正常流程到达；“静态”表示受状态/动态参数限制，依据组件源码核验。

### 6.1 公共与认证路由

| 路由 | 证据 | 主要问题 | 建议 | 优先级 |
|------|------|----------|------|--------|
| `/setup` | 实机 | 1440px 下卡片和文字偏小；导入区直接引用 `Design1 §3.4.8`；三张选择卡的可点击范围与选中状态不明显 | 提高容器至 760–800px、标题 24px；整卡可选；把内部文档引用改成人话 | P1 |
| `/login` | 实机 | 品牌区弱；主题开关游离在卡片右下；首管理员提示和登录任务竞争注意力 | 统一 AuthShell；主题按钮固定右上；管理员提示压缩为卡片内上下文提示 | P1 |
| `/register` | 实机 | 密码规则未在提交前说明；页面与登录页品牌结构不一致 | 字段下常驻密码要求与实时满足状态；复用 AuthShell | P0 |
| `/forgot` | 实机 | 只有标题和表单，缺少“会发生什么”的说明 | 增加“输入账号邮箱，我们会发送 1 小时有效的链接” | P1 |
| `/reset` | 实机 | 无 token 时仍展示可提交表单的风险；密码规则只依赖校验 | 无有效链接时显示 Result；补密码要求与显示/隐藏按钮名称 | P0 |
| `/pending` | 实机 | 状态明确，但没有刷新或下一步说明 | 增加“审批后重新登录即可”，可选“返回登录”与“刷新状态” | P2 |
| `/login/callback` | 静态 | 中转状态主要是通用 loading/result，缺少阶段与恢复路径 | 显示“正在完成登录”；失败时提供重试和返回登录 | P1 |
| `/emergency` | 静态 | 高风险页面字段密集；红色警示易掩盖操作步骤 | 使用编号步骤、固定风险摘要、清晰区分“重置密码”和“重新初始化” | P0 |
| `/:pathMatch` | 实机 | 404 文案可用但品牌和帮助弱 | 增加站点名、返回首页和返回上一页；不展示技术路径 | P2 |

### 6.2 用户路由

| 路由 | 证据 | 主要问题 | 建议 | 优先级 |
|------|------|----------|------|--------|
| `/` | 实机 | 桌面信息集中在页面上部；卡片主次弱；管理员首页仍出现用户态卡片但操作均禁用；手机顶栏按钮拥挤 | 页面最大宽度 1200–1280px；增强卡片标题/状态；管理员显示“预览视图”提示；手机将管理入口放用户菜单 | P1 |
| `/rules` | 实机 | 警告“请谨慎分发，请勿外发”语义重复；空状态无返回后的下一步 | 改为“规则链接可被任何获得者访问，请只分享给可信对象”；空状态说明由管理员配置 | P1 |
| `/profile` | 实机 | 页面不使用通用顶栏；基本信息、密码管理缺少用户身份摘要；“修改”按钮语义重复 | 复用用户端 Header；顶部增加头像/用户名摘要；按钮改为“修改用户名/邮箱” | P1 |

### 6.3 管理路由

| 路由 | 证据 | 主要问题 | 建议 | 优先级 |
|------|------|----------|------|--------|
| `/admin/subscriptions` | 实机 | 空状态位于巨大容器中央；“前往装配”和“新建订阅”主次接近 | 使用任务型 EmptyState；“新建订阅”为主操作，“从装配生成”为次操作 | P1 |
| `/admin/groups` | 静态 | 分配节点、配额和候选集概念密集；仅高级模式可达但缺少上下文 | 页头解释用户组影响；编辑分为“基础信息/流量/可用节点”；显示来源说明 | P1 |
| `/admin/shares` | 实机 | “分享订阅”与“订阅”差别不直观；空状态没有用途解释 | 副标题说明“生成可独立分发和撤销的分享链接”；空态提供创建说明 | P1 |
| `/admin/platforms` | 实机 | `yaml / subs / generic-subs` 暴露内部枚举；Slug 占据主表格 | 格式显示“Clash YAML / Shadowrocket 节点 / 通用节点”；标识移入详情或复制控件 | P1 |
| `/admin/platforms/new` | 静态 | 长表单和安装包配置同时出现，首次创建负担大 | 分“基础信息/订阅格式/客户端与导入”三段；格式选项配示例 | P1 |
| `/admin/platforms/:id/edit` | 静态 | 编辑与新建共用结构但风险、只读项不突出 | 显示平台摘要；不可改字段用描述列表，不用 disabled 输入框 | P2 |
| `/admin/users` | 实机 | 页头有两个重量接近的动作；“操作▾”隐藏所有操作；表格角色/来源/状态标签密集 | “新建用户”为主操作；批量邮件移入更多；保留一个高频行操作，其余进菜单 | P1 |
| `/admin/approvals` | 实机 | 空列表仍显示分页“共 0 条 / 1”；批量通过在无选择时占据页面头 | 空态隐藏分页和批量操作；说明审批来源和审核重点 | P1 |
| `/admin/rules` | 实机 | “装配生成/创建规则”难以理解差别；空态无解释 | 改为“从素材生成”和“手动创建”；空态呈现两条路径 | P1 |
| `/admin/settings` | 实机 | 单页内容极长、保存按钮重复、危险区难定位；术语密度最高 | 按 §5.2 重组；固定页内导航、未保存提示、分区说明 | P0 |
| `/admin/logs` | 实机 | “显示唯一值”不明确；清空日志与查询并列；空态仍显示分页 | 改为“合并重复记录”；清空移至更多/危险区；空态隐藏分页 | P1 |
| `/admin/assembly` | 实机 | 副标题像实现说明；顶层任务与产品类型层级混杂 | 按 §5.3 重组；使用任务名称和分步摘要 | P0 |
| `/admin/nodes` | 实机 | URI、协议字段对新用户门槛高；空态仅“暂无节点” | 空态区分“手动添加/批量导入/从 Xray 检测”；表单提供协议示例和字段解释 | P1 |
| `/admin/xray` | 静态 | 实例、刷新节点、初始化、对账、独立账号同时出现，任务层级复杂 | 顶部实例状态摘要；实例详情内分“节点/账号/对账”；高风险动作进更多菜单 | P0 |
| `/admin/*/:id/versions` | 静态 | 标题显示内部 `resourceName` 英文；上传、编辑、装配、预览、激活操作密集 | 显示真实资源名与类型；主操作按当前状态变化；时间线化版本列表 | P1 |
| `/admin/customs/:id/versions` | 静态 | 返回用户列表但页面上下文可能丢失 | 面包屑显示“用户 > 用户名 > 自定义订阅 > 版本” | P1 |

### 6.4 装配内嵌视图

| 视图 | 主要问题 | 建议 | 优先级 |
|------|----------|------|--------|
| 规则素材池 | 列表、同步状态、定时任务信息密度高 | 卡片头只显示健康状态与最近同步；次要统计放详情 | P1 |
| 素材池详情 | 手工条目、URL 条目、任务回执连续堆叠 | 用 Tabs 或折叠区分“条目/来源/同步记录” | P1 |
| 代理组 | 选择、引用、健康检查字段术语化 | 按“基本信息/成员/健康检查”分段，字段配帮助文本 | P1 |
| 装配步骤 | 步骤多、移动端长页面；错误可能离当前操作较远 | 固定步骤摘要和底部导航；字段错误滚动定位；保存草稿提示 | P0 |
| 预览 | 纯文本可读性依赖等宽块，差异与错误不突出 | 支持行号、搜索、错误定位和复制；长内容虚拟化属于候选优化 | P2 |

---

## 七、中文文案优化表

| 当前位置 | 现有文案 | 建议文案 | 修改理由 |
|----------|----------|----------|----------|
| Setup | 首次配置 | 初始化 VPN 订阅管理 | 说明配置对象，不只是状态 |
| Setup 快速开始 | 本地账号模式，零配置一键完成 | 使用邮箱和密码登录，无需接入外部账号系统 | 避免“零配置”与仍需注册的矛盾 |
| Setup 导入提示 | 避免模拟 OIDC 等调试配置外流（Design1 §3.4.8） | 开发模式不支持导入，以免测试身份配置被误用 | 移除内部文档引用 |
| Setup 确认 | 点击「确认完成」后系统立即进入已配置状态，此操作不可撤销 | 确认后将启用本地账号登录。之后仍可在“面板配置”中调整认证方式 | 更准确地描述下一步，避免无必要恐吓 |
| 登录 | 系统尚未配置管理员，首个注册用户将成为管理员 | 尚未创建管理员。请由部署者注册第一个账号 | 明确行动主体 |
| 注册 | 注册 | 创建账号 | 更自然、更任务导向 |
| 首页流量 | 不限流量 | 本月不限流量 | 补时间范围 |
| 首页规则卡 | 分流规则为 Shadowrocket 客户端专用 | Shadowrocket 分流规则 | 标题简短，说明放副标题 |
| 首页规则说明 | 使用指引：先添加订阅获取节点，再导入分流规则 | 使用 Shadowrocket 时，请先导入节点订阅，再添加分流规则 | 动作和对象更明确 |
| 首页版本空态 | 暂无可用版本，请联系管理员 | 暂时没有可用订阅，请联系管理员完成发布 | “版本”是管理概念，普通用户更关心订阅 |
| 管理首页入口 | 管理面板 | 进入管理后台 | 动作明确 |
| 订阅空态 | 还没有订阅 | 还没有可发布的订阅 | 说明对象状态 |
| 订阅操作 | 前往装配 | 从装配器生成 | 说明前往目的 |
| 分享页 | 分享订阅 | 分享链接 | 用户主要管理的是可撤销链接 |
| 平台格式 | yaml / subs / generic-subs | Clash YAML / Shadowrocket 节点 / 通用节点 | 隐藏内部枚举 |
| 用户批量操作 | 为所有无密码用户发送密码设置链接 | 向未设置密码的用户发送邀请 | 简短并解释用途 |
| 规则操作 | 装配生成 | 从素材生成 | 降低术语感 |
| 日志选项 | 显示唯一值 | 合并重复记录 | 说明实际行为 |
| 装配副标题 | 四类装配器：副 Tab 选类型 → 填头部 → 勾选节点/组 → 规则 → 预览 → 生成 | 组合节点、代理组和规则，生成可发布的订阅或分流规则 | 从实现说明改为用户价值 |
| 节点空态 | 暂无节点 | 还没有可用于装配的节点 | 补用途 |
| 设置页 | 面板配置 | 系统设置 | 更符合内容范围 |
| 高级模式 | 未开启 | 基础模式 | 用当前正向状态替代缺失状态 |
| Xray | 刷新节点 | 检测节点 | “刷新”容易误解为页面刷新 |
| 删除确认 | 删除后不可恢复 | 删除后将无法恢复，请确认已不再使用 | 增加判断条件 |
| 轮询超时 | 任务仍在后台执行，请稍后刷新查看结果 | 操作仍在后台进行。你可以稍后回到本页查看结果 | 避免命令式“刷新” |

### 7.1 文案规则

- 页面标题使用名词任务：“订阅”“用户”“系统设置”；主按钮使用动宾结构：“新建订阅”“发送邀请”。
- 先说用户结果，再说技术原因；技术缩写首次出现时补全中文用途。
- 空状态必须回答三件事：现在没有什么、为什么值得创建、从哪里开始。
- 错误信息采用“发生了什么 + 是否已保存 + 下一步怎么办”。
- 避免把源码枚举、数据库标识、Design/Build 编号暴露给最终用户。

---

## 八、响应式专项建议

### 8.1 桌面（≥1200）

- 内容区设置 `max-width: 1440px` 并居中；表单型页面建议 `max-width: 960px`，避免字段无限拉宽。
- 页面标题 24px，主内容正文至少 14px/22px；不要以大量 12px 灰字承担核心说明。
- 空状态 Section 的建议高度为 280–360px，不让空插图漂浮在整屏大容器中。
- 三列卡片仅用于信息对等的平台；设置和装配继续使用单列任务流。

### 8.2 平板（768–1199）

- 不能简单沿用桌面表格。操作列超过两个按钮时统一折叠；非关键列允许隐藏或进入展开行。
- 侧栏建议默认收起，但保留 Tooltip；内容内边距从 24 降为 20。
- 设置页使用顶部下拉分组代替左侧锚点，避免双侧栏挤压。
- 本次平板截图采集失败，以上关于视觉比例的结论属于**源码与断点推断**，实施前需补实机截图复核。

### 8.3 手机（<768）

- 手机双态列表方向正确，应扩展到所有列表，不允许局部回退为可横向滚动的大表格。
- 顶栏保留菜单、站点名、用户菜单三个核心元素；管理入口和主题切换进入用户菜单或设置抽屉。
- 页面头动作超过两个时只保留主操作，其余进入“更多”。
- 表单弹窗在手机改为全屏 Drawer；底部固定提交区并尊重安全区 `env(safe-area-inset-bottom)`。
- 触控目标至少 44×44px；当前大量 `size="small"` 按钮只适合桌面表格，不适合作为手机主操作。
- 装配步骤和长设置页需提供页内进度/目录，避免用户失去位置。

---

## 九、无障碍专项建议

1. 所有图标按钮提供稳定的 `aria-label`；收起侧栏同时提供 Tooltip 与可访问名称。
2. 主题控件使用“切换到深色模式/切换到浅色模式”，emoji 仅作装饰并 `aria-hidden`。
3. 页面只保留一个 `h1`；Section 使用 `h2`，卡片标题使用 `h3`，避免管理页从 `h2` 开始。
4. 所有 Form.Item 帮助文本与输入框通过 `aria-describedby` 关联；规则不能只放在 placeholder。
5. 成功/警告/失败除颜色外同时使用图标和文字；Tag 文本必须独立可理解。
6. 键盘焦点使用 2px 高对比 focus ring，不用浏览器默认样式被 Tailwind/AntD 清除。
7. Modal 打开后焦点进入标题或首字段，关闭后回到触发按钮；危险确认词输入需有明确说明。
8. Toast 对关键失败使用 `role="alert"`，普通成功使用非打断式 live region。
9. 全局支持 `prefers-reduced-motion`，关闭进度条和 Drawer 的非必要过渡。
10. 浅色次要文字和暗色边框需通过 WCAG AA 对比度检查；正文目标至少 4.5:1。

---

## 十、候选改进项与实施顺序

### P0：先解决任务完成与可访问性

| # | 候选项 | 目标 | 验收摘要 |
|---|--------|------|----------|
| P0-1 | 重组设置页 | 让管理员可在 10 秒内找到目标设置并知道保存范围 | 六个分组可直达；离开未保存区有提示；危险区独立 |
| P0-2 | 重组装配信息架构 | 区分资源维护与生成任务，降低步骤迷失 | 顶层三个任务；移动端固定导航；错误可定位 |
| P0-3 | 建立表单可访问性基线 | 字段规则、错误、焦点和弹窗可被键盘/读屏理解 | axe 无 critical；全流程键盘可完成 |
| P0-4 | 统一移动端主操作 | 避免小按钮、拥挤表格和不可见操作 | 390px 无横向溢出；触控目标 ≥44px |
| P0-5 | 修复无效重置链接状态 | 无 token 不展示可提交的重置表单 | 无 token 显示明确 Result 和返回登录 |

### P1：建立一致的产品体验

| # | 候选项 | 目标 | 验收摘要 |
|---|--------|------|----------|
| P1-1 | 设计 Token 与主题层级 | 统一颜色、排版、间距、圆角、阴影 | 浅/暗主题核心页面视觉快照通过 |
| P1-2 | PageShell / EmptyState / FormSection | 消除页面骨架和状态表达差异 | 全管理页复用；空态均有下一步 |
| P1-3 | 管理菜单分组 | 降低 10–12 项平铺的扫描成本 | 分组清晰；收起态 Tooltip 完整 |
| P1-4 | 技术文案翻译层 | 不暴露内部枚举和文档编号 | 文案表中高频项全部替换 |
| P1-5 | 表格与移动卡片统一 | 主字段、操作和状态一致 | 所有列表在 390/1024/1440 下可用 |
| P1-6 | AuthShell / UserShell | 统一登录、注册、找回、个人中心品牌结构 | 公共页主题按钮与返回路径一致 |

### P2：完成视觉润色

- 为首页与管理概览补更有信息量的摘要卡片，但不新增后端指标。
- 统一图标风格、空状态插图和加载 Skeleton 形态。
- 改进版本预览的行号、搜索和差异阅读。
- 使用 120–180ms 的轻量状态过渡，并支持减少动态效果。
- 对 404、待审批、回调页补齐品牌和恢复路径。

---

## 十一、参考实施伪代码

### Step 1：视觉 Token 与 PageShell

- **目标：** 建立所有后续页面共享的视觉语言。
- **影响范围：** `theme.ts`、全局样式、`App.vue`、三类 Layout、`PageHeader`。
- **伪代码：**

```ts
const antdTheme = computed(() => ({
  algorithm: dark.value ? theme.darkAlgorithm : theme.defaultAlgorithm,
  token: {
    colorPrimary: uiTokens.color.primary,
    borderRadius: uiTokens.radius.control,
    fontSize: uiTokens.type.body.size,
    controlHeight: 36,
    colorBgLayout: dark.value ? uiTokens.dark.page : uiTokens.color.page,
  },
  components: {
    Button: { controlHeightSM: 32 },
    Card: { borderRadiusLG: uiTokens.radius.card },
    Table: { cellPaddingBlock: 13 },
  },
}))
```

- **验收：** Setup、首页、订阅、设置在浅/暗主题下无硬编码冲突；1440px 正文可读；`git grep` 不再新增散落主色值。

### Step 2：状态与列表组件

- **目标：** 统一加载、错误、空、内容四态及桌面/手机呈现。
- **影响范围：** `TriStateList`、新增 `EmptyState` / `ResponsiveCollection`、所有列表页。
- **伪代码：**

```vue
<StateContainer :state="state" @retry="load">
  <template #empty>
    <EmptyState
      title="还没有节点"
      description="添加节点后即可在订阅装配中选择。"
      primary-label="新建节点"
      secondary-label="批量导入"
    />
  </template>
  <DesktopTable class="hidden md:block" />
  <MobileCardList class="md:hidden" />
</StateContainer>
```

- **验收：** 所有列表空态隐藏分页；手机无横向溢出；错误态有重试；加载态不导致明显布局跳动。

### Step 3：导航与设置页

- **目标：** 缩短模块和设置项查找路径。
- **影响范围：** `AdminLayout`、`SettingsView`，不改路由与 API。
- **伪代码：**

```ts
const navigationGroups = [
  { label: '分发管理', items: ['subscriptions', 'shares', 'platforms', 'rules'] },
  { label: '装配资源', items: ['assembly', 'nodes', 'groups', 'xray'] },
  { label: '成员与权限', items: ['users', 'approvals'] },
  { label: '系统', items: ['settings', 'logs'] },
]

const settingSections = [
  { key: 'identity', label: '身份与访问' },
  { key: 'notifications', label: '通知' },
  { key: 'appearance', label: '外观与内容' },
  { key: 'runtime', label: '运行与安全' },
  { key: 'data', label: '数据管理' },
  { key: 'danger', label: '危险操作' },
]
```

- **验收：** 桌面和手机均可两次操作内到达任一模块；设置锚点可深链；未保存状态清楚。

### Step 4：文案与无障碍收口

- **目标：** 让核心任务无需理解内部实现，并满足基础键盘/读屏要求。
- **影响范围：** 页面可见字符串、表单帮助、按钮名称、状态 Badge。
- **伪代码：**

```ts
const productTypeLabels = {
  yaml: 'Clash YAML',
  subs: 'Shadowrocket 节点订阅',
  'generic-subs': '通用节点订阅',
}

const statusLabels = {
  pending: { label: '等待处理', icon: ClockIcon },
  running: { label: '处理中', icon: LoadingIcon },
  succeeded: { label: '已完成', icon: CheckIcon },
  failed: { label: '失败', icon: ErrorIcon },
}
```

- **验收：** 内部枚举不直接展示；按钮具有唯一可访问名称；Tab 键顺序符合视觉顺序；颜色不是唯一状态信号。

---

## 十二、验收清单、未验证项与假设

### 12.1 报告验收清单

- [x] 覆盖全部公开、用户和管理路由，包括动态版本路由和装配内嵌视图。
- [x] 将浏览器事实、代码事实和推断分开标注。
- [x] 不评价或设计后端逻辑。
- [x] 给出全局视觉系统、导航、逐页问题、文案替换、响应式和无障碍建议。
- [x] 候选项按 P0/P1/P2 分级，并附伪代码与验收方式。
- [x] 除本报告外不修改前后端代码、设计文档或项目配置。

### 12.2 未验证项

- 高级模式未开启，因此 `/admin/groups`、`/admin/xray` 及独立账号实机状态以静态审计为主。
- 没有创建真实订阅、版本、节点、规则、素材池或 Xray 实例；复杂有数据状态主要依据源码。
- 没有执行 OIDC、验证码、SMTP、应急恢复、密码邮件和文件导入流程。
- 平板截图采集发生非致命错误；1024px 的视觉比例需在实施前补充人工复核。
- 未使用真实读屏软件与自动化 axe 扫描；无障碍结论是 DOM、源码和通用标准审计。

### 12.3 假设记录

1. **[推断]** 主要用户是管理小团队 VPN 订阅的管理员，以及只需获取订阅和客户端的普通用户。
2. **[推断]** 管理员可能熟悉 Clash、Shadowrocket、Xray，但不应被要求理解源码枚举或数据库字段。
3. **[推断]** 这是高频工具型产品，清晰、稳定和信息密度比营销式视觉更重要。
4. **[假设]** 后续 UI 改造继续使用 Ant Design Vue 与 Tailwind，不更换框架、不引入重型组件库。
5. **[假设]** 现有功能、路由和权限模型保持不变；导航分组与设置页重组仅改变前端呈现。
6. **[假设]** Design1 的“侧栏平铺不分组”已因 Design2 模块增长成为可重新讨论的历史设计，不在本报告中擅自改写设计结论。

---

## 十三、最终建议

不要从单页“美化”开始。最稳妥的顺序是：先确认视觉 Token 与信息架构，再落地 PageShell / EmptyState / FormSection，随后改设置与装配两个复杂页面，最后批量统一其余列表和文案。这样可以避免每个页面各自调整颜色、间距和按钮，减少后续返工，也符合项目“简单轻量化、重复逻辑抽取复用”的原则。

建议下一步由用户从 P0/P1 候选中确认首轮范围，再将已确认项同步到 Design 文档，并按 `Build.template.md` 生成单独的 UI 构建计划；本报告保持为研究依据，不直接充当构建指令。
