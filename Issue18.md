# Issue18.md — 邮件运行语义与站点信息问题

> **文档定位：** 本文档记录 2026-09-16 发现的邮件与站点信息问题。R32-01 记录站点名称一致性修复与自动化验收；R32-02 记录业务邮件同步发送阻塞主流程的问题、已确认处理方案及实施前验收合同。已实施的邮件内容设计仍见 [Design5.md](Design5.md) §7，历史构建记录仍以 [Build28.md](docs/reports/Build/Build28.md) 为准。

---

## 一、R32-01 邮件预览、实际发送与站点信息的名称语义不一致

- **现象：** 邮件即时预览中的 `{{site_name}}` 恒定渲染为“示例站点”，不跟随已保存站点名称；未保存名称时，页面标题回退为“VPN 订阅管理”，但站点信息接口与实际邮件变量仍得到空串。
- **根因：** Build28 把预览站点名与示例 URL 一并固定为合成值；前端另有独立默认名称；正常/应急站点信息和欢迎/审批发信路径则直接读取原始 `system_config.site_name`，没有共用有效值规则。
- **影响范围：** 面板站点信息、公共站点信息、正常/应急页面标题、五分支邮件模板元数据与预览、欢迎邮件、审批通过/拒绝邮件。密码重置分支不含 `site_name`，仅保留合成重置 URL 预览。原始配置导入导出与备份语义不变。
- **用户决策：** 有效站点名称优先使用已保存的非空站点名称；未设置或去除首尾空白后为空时，统一回退“VPN 订阅管理”。
- **修复方案与实施：** `config.Service` 新增唯一 `EffectiveSiteName`；管理/公共/应急站点信息、邮件模板列表元数据、预览 POST、欢迎与审批发信均改用该入口。预览的登录/重置 URL 仍固定为 `example.invalid`，不读取真实链接或令牌；未保存站点名称草稿不进入预览。导出继续读取原始 `site_name`，不把运行时回退值写入导出文件。
- **相关门禁补强：** 邮件发送路径对 `LoadTemplate` 读取错误改为显式处理；该服务已在内部记录安全告警并返回内置默认模板，发信仍按原设计继续，仅补齐 errgate 的显式错误处理证据。
- **自动化证据：** `go build ./...`、`go vet ./...`、`go test ./... -count=1`、`go run ./cmd/errgate ./...`、前端 `npm run test`（47 个测试文件、311 项测试）与 `npm run build` 通过。新增/更新回归覆盖配置缺失、纯空白、已保存非空名称、nil 隔离渲染、站点信息回退、邮件模板元数据默认值及已保存名称的预览渲染。
- **本地运行证据：** `docker compose build` 与 `docker compose up -d` 完成，保留原数据卷；`/health` 返回 200，当前未保存站点名称的 `/api/site/info` 返回“VPN 订阅管理”。本地浏览器重载后，站点信息输入框显示“VPN 订阅管理”，审批通过模板即时预览主题显示“VPN 订阅管理 审批通知”，不再出现“示例站点”。
- **验收边界：** 上述是编译、静态检查、隔离数据库/API/组件测试和前端生产构建证据；不等同于正式 SMTP、真实收件箱或外部邮件客户端验收。
- **状态：** ✅ 已修复并完成自动化验收（2026-09-16）。

---

## 二、R32-02 业务邮件同步发送阻塞主流程且缺少短期发送记录

### 2.1 现象与根因

- **现象：** 新用户直接激活后，注册请求会同步等待欢迎邮件完成；SMTP 响应慢时注册页面长时间等待，超过前端通用 15 秒请求超时后会显示网络异常。此时用户写库事务已经提交，再次注册会得到邮箱冲突，形成“页面认为失败、账号实际已创建”的不一致体验。
- **静态调用链：** `user.Service.Register` 提交用户事务后同步调用 `sendWelcomeIf`，生产回调进入 `mail.Service.SendWelcome`；邮件完成后注册 Handler 才继续签发会话并返回。SMTP 单封总超时为 30 秒，长于前端通用请求的 15 秒超时。
- **错误边界：** 当前欢迎邮件的普通 SMTP 错误会被 `sendWelcomeIf` 记录并吞掉，不会回滚用户；真正导致流程表象中断的是同步等待、请求取消与后续会话签发共用请求 Context。问题不等于“SMTP 错误直接回滚注册”。
- **同类范围：** 同一同步模式还覆盖 OIDC 首次激活、管理员创建本地用户、审批通过/拒绝通知、公共忘记密码、管理员单用户重置邮件及批量密码链接，不能只在注册按钮或单个 HTTP Handler 上局部启动 goroutine。
- **现有附带缺口：** 管理员密码邮件入口只检查 SMTP 配置完整性，没有同时检查 `password_reset` scope；scope 关闭时仍可能生成重置令牌并向页面报告“已发送”。批量接口的 `sent` 实际也只表示令牌生成路径未返回错误，不构成 SMTP 已接受证据。

### 2.2 已确认设计

- **统一异步边界：** 所有业务邮件通过邮件领域的统一异步派发器提交，不等待 DNS、TCP、TLS/STARTTLS、SMTP 认证、收件人确认或 DATA 传输；注册、审批、密码重置等调用方只完成业务事务与任务提交，不自行创建 goroutine，也不与按钮实现绑定。
- **同步诊断例外：** 设置页 SMTP 测试邮件保持同步。其目标是立即验证当前 SMTP 配置并返回安全的阶段化结果，不进入业务邮件队列；该例外属于后端诊断合同，不与具体前端按钮耦合。
- **投递保证：** 采用进程内、best-effort 模式，不新增数据库 outbox、外部消息队列或自动重试；进程退出时尚未完成的邮件允许丢失。业务成功、任务已入队、SMTP 已接受、真实收件箱收到与邮件客户端展示是五个独立证据层，禁止互相替代。
- **日志范围：** 用户确认“收发日志”中的“收”为误写；新增功能正式命名为“邮件发送日志”，只记录出站邮件提交、发送、SMTP 接受或失败，不记录外部来信、最终投递、垃圾邮件分类、退信回执或打开状态。

### 2.3 后端职责划分

1. **同步 SMTP 传输器 `mail.Service`：** 继续负责 SMTP 配置、模板读取与回退、变量渲染、MIME 构造、SMTP 会话及现有 30 秒总超时；不启动 goroutine。
2. **统一异步派发器 `mail.Dispatcher`：** 提供 `DispatchWelcome`、`DispatchApprovalApproved`、`DispatchApprovalRejected`、`DispatchPasswordReset`，负责 scope 判断、非阻塞入队、固定 worker、请求 Context 解绑定、发送调用、短期日志状态更新、停止和运行态复位。
3. **短期日志 `mail.ActivityLog`：** 保存最近 500 条邮件任务的并发安全内存快照，供日志面板查询；不写 SQLite，不进入备份、配置导入导出或长期审计。
4. **类型化任务：** 队列只接受封闭的 `JobKind` 与结构化字段，不接受任意闭包或动态注册。完整收件地址、登录 URL 和重置 URL 只存在于进程内任务对象，不序列化、不进入 API 或日志。

### 2.4 队列、并发与生命周期合同

- 派发器使用 **2 个固定 worker + 容量 100 的内存队列**。最多同时建立两个 SMTP 会话；队列满时立即返回，不阻塞业务请求等待空位，也不无限创建 goroutine。
- 派发器在入队前使用 `context.WithoutCancel` 解除 HTTP 请求取消；worker 内 SMTP 发送仍受 30 秒总超时约束。
- scope 在入队时检查并固定：`welcome`、`approval_notify`、`password_reset` 分别控制对应任务。scope 未启用时不创建任务、不创建邮件日志，也不视为失败；已经入队的任务不因随后关闭 scope 被静默丢弃。
- 队列满或派发器停止时，注册、OIDC 创建、管理员创建用户和审批业务仍成功，只记录安全失败；公共忘记密码继续返回统一防枚举响应。管理员单用户密码邮件应立即返回“队列繁忙”，批量入口分别统计 `queued` 与 `queue_failed`。
- 服务退出时停止接收新任务、取消活动 SMTP、丢弃未开始任务并等待 worker 退出，不为清空整个队列延长容器关闭。
- “一键清空所有数据”须调用派发器运行态复位：取消旧活动任务、丢弃旧队列、清空邮件短期日志并建立新的空队列/worker，避免清空用户和配置后继续发送旧邮件。普通“清空邮件发送日志”只清除可见记录，不取消正在发送的邮件。

### 2.5 业务入口语义

- **欢迎邮件：** 本地自注册、首管理员、OIDC 首次激活和管理员创建用户在事务提交且用户为 `active` 后最多提交一次；待审批用户不提交欢迎邮件。
- **审批通知：** 通过只提交一封 `approval_approved`，拒绝只提交一封 `approval_rejected`；不得在审批通过时叠发欢迎邮件。单个或批量审批结果不等待邮件，也不把邮件失败计入审批失败数。
- **密码重置：** 必须先成功写入一小时有效的重置令牌，再提交邮件；令牌写库失败时不得创建任务或邮件日志。公共忘记密码继续统一返回“若该邮箱已注册，重置邮件将发送”。
- **管理员密码邮件：** 可用性同时要求 SMTP 配置完整和 `password_reset` scope 启用。单用户成功文案改为“重置邮件已提交发送”；队列满立即返回安全错误。批量响应由 `sent` 改为 `queued`，新增 `queue_failed`，前端显示“已提交发送”，不得声称已送达。
- **SMTP 测试：** 绕过业务队列同步执行，但写入同一短期日志；成功状态显示“SMTP 已接受”，失败显示安全阶段。测试不受已有业务队列积压影响。

### 2.6 短期邮件发送日志

- **状态：** `queued`（已入队）、`sending`（开始发送）、`accepted`（SMTP 会话完成并接受邮件）、`failed`（发送或队列失败）。`accepted` 不得展示为“已送达”。
- **字段：** 单调递增 ID、创建/开始/完成时间、邮件类型、业务来源、用户 ID、掩码收件人、状态、安全失败阶段、排队耗时和发送耗时。
- **安全边界：** 不保存完整收件/发件邮箱、主题、正文、登录 URL、重置 URL/Token、SMTP 用户名/密码、模板 JSON 或服务商原始响应。失败阶段只允许封闭枚举，如队列已满、连接、握手、STARTTLS、认证、发件人、收件人、DATA、QUIT、超时或取消。
- **容量与清理：** 最多保留最近 500 条，满后淘汰最旧项；服务重启自动清空。`Clear` 只删除记录，不重置进程内自增 ID，防止旧任务完成后误更新清空后复用的 ID；已被清空或淘汰的记录完成更新时静默跳过，不重新插入。
- **管理 API：** 新增管理员专用 `GET /api/admin/logs/mail?page=&size=&kind=&status=` 与 `POST /api/admin/logs/mail/clear`，叠加 session/admin 中间件并返回 `Cache-Control: no-store`；列表沿用统一 `{list,total}` 包裹并返回 `[]` 而非 `null`。
- **日志面板：** 在“访问日志”和“实时日志流”之间增加“邮件发送日志”页签，提供类型/状态筛选、手动刷新、活动页签内 5 秒轮询、独立清空确认、桌面表格和移动端卡片；离开页签或卸载时停止轮询，不新增 SSE 连接。
- **页面提示：** 固定说明“SMTP 已接受仅表示发件服务器接受邮件，不代表进入收件箱；本页为当前进程短期记录，服务重启后清空”。

### 2.7 实施范围与顺序

1. 新增并测试 `mail.ActivityLog`，先冻结容量、ID、清空、淘汰、脱敏与并发合同。
2. 新增并测试 `mail.Dispatcher`，完成两个 worker、容量 100、非阻塞入队、scope、停止和运行态复位。
3. 将欢迎邮件三个生产入口迁移到 `DispatchWelcome`。
4. 将审批通过/拒绝迁移到具名异步派发接口。
5. 将公共、管理员单用户与批量密码重置迁移到 `DispatchPasswordReset`，同步修复 scope 可用性和 `queued/queue_failed` 回执。
6. 保留 SMTP 测试同步语义并接入 ActivityLog。
7. 增加邮件日志管理 API、日志面板第三页签与所有“已发送/已送达”文案修正。
8. 接通服务退出与“一键清空所有数据”的派发器复位，最后执行定向、race、全量及前端联合门禁。

### 2.8 验收合同

- **异步性：** 阻塞 SMTP 不得拖延注册 token、OIDC 首次激活、管理员创建、审批或密码重置业务响应；不允许以单纯提高前端超时替代异步修复。
- **并发：** 自动化证明 SMTP 同时发送数不超过 2、第三个任务进入队列、队列满时立即返回，并通过 `go test -race`。
- **一致性：** 用户/审批/重置令牌事务失败时不提交对应邮件；成功事务的任务最多提交一次。邮件失败不得改变已提交业务状态。
- **隐私：** API、短期日志与普通安全日志均不得出现完整邮箱、邮件内容、登录/重置 URL、Token、SMTP 凭据或服务商原始响应。
- **生命周期：** 派发器停止和全量数据清空能取消旧活动任务并丢弃旧队列；单独清空可见邮件日志不影响发送。
- **接口/UI：** 管理员权限、`no-store`、分页/筛选、空数组、响应式页面、轮询启停、`queued/queue_failed` 与“SMTP 已接受”文案均有自动化覆盖。
- **验证命令：** 后端执行 `go test -race ./internal/mail ./internal/auth ./internal/user ./internal/approval ./internal/server`、`go test ./...`、`go build ./...`、`go vet ./...`；前端执行 `npm test -- --reporter=dot`、`npm run build`；根目录执行 `git diff --check`。
- **证据边界：** 隔离 mock SMTP 只能证明异步、超时、状态和脱敏合同；正式 SMTP、真实收件箱及外部邮件客户端仍须独立人工验收。
- **当前状态：** 📝 问题与方案已确认并完成实施前审核；暂未修改代码，未执行实现后门禁（2026-09-16）。

---

## 三、变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.1 | 2026-09-16 | 新增 R32-02：记录业务邮件同步阻塞、统一有界异步派发、SMTP 测试同步例外、短期邮件发送日志、生命周期/隐私边界及实施前验收合同；方案已确认，暂未实施。 |
| v1.0 | 2026-09-16 | 记录 R32-01 现象、用户决策、实施、自动化证据与验收边界；闭环后在根目录保留。 |
