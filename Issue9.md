# Issue9.md — VPN 订阅管理系统 问题追踪（当前）

> **文档定位：** 本文档是 VPN 订阅管理系统的**当前问题记录**（记录错误与修复方案，非强制，经验参考），承接已归档/历史问题记录（Issue1～Issue8，见 `docs/reports/Issue/`）。
> 设计记录见 [Design2.md](Design2.md) 与 [Design2-UI.md](Design2-UI.md)；构建方案见 [Build11.md](Build11.md)、[Build12.md](Build12.md)；编码指令见 [AGENTS.md](AGENTS.md)（**唯一强要求**）。

---

## 〇、本轮说明

- **创建时间：** 2026-08-28
- **创建背景：** 在 Build1～Build12 全量核验中发现 Build11 新增管理员概览相关后端存在可复现死锁；已完成源码研究，尚未修改任何业务代码。
- **当前状态：** R24-01、R24-18、R24-19、R24-20 标记为需要更多分析，本轮不进入实现；R24-02、R24-12、R24-15 的方案已由用户确认并已同步至 `Design2-UI.md`；其余问题按各条记录的范围继续待后续实施。

---

## 一、进行中问题

### R24-01 Build11 `/api/admin/overview` 及独立账号相关 SQLite 单连接嵌套查询死锁

- **现象：**
  - 执行 `cd backend && go test -timeout 120s ./internal/server/...` 时，`TestOverviewEndpointAggregatesStableData` 超时并 panic：
    ```text
    panic: test timed out after 2m0s
    running tests: TestOverviewEndpointAggregatesStableData
    ```
  - 高级模式开启且存在独立账号时，`/api/admin/overview` 可能一直不返回，前端管理员概览页持续加载。
  - 进一步静态扫描发现以下路径同样存在同类风险：
    - `/api/admin/xray/ext` 独立账号列表；
    - 定时采集 `runXrayCollect` 中的独立账号配额检查；
    - 用户超限时 `PushUser` 内部写入超限原因的路径。
- **根因：**
  - `backend/internal/store/store.go` 中 `Open` 使用 `db.SetMaxOpenConns(1)`，即 SQLite 单连接模型。
  - 在 `for rows.Next()` 尚未结束、外层 `*sql.Rows` 仍占用唯一连接时，再次通过同一个 `*sql.DB` 发起查询/写入，会导致内层操作等待唯一连接，而外层游标又不会结束，形成死锁。
  - 当前确认存在该模式的具体代码：
    1. `backend/internal/xray/ext.go` 的 `ExtService.ListExt`：
       - 外层查询 `xray_ext_accounts` 后，在循环内调用 `pushTargetsFor`（再次查询）和流量汇总查询。
    2. `backend/internal/xray/ext.go` 的 `ExtService.CheckAllExtQuota`：
       - 外层查询独立账号 ID 后，在循环内调用 `CheckExtQuota`，而 `CheckExtQuota` 内部继续执行多个数据库查询/写入。
    3. `backend/internal/xray/sync.go` 的 `SyncService.writeQuotaExceededError`：
       - 外层查询 `xray_users` 后，在循环内逐行执行 `ExecContext` 更新。
- **影响范围：**
  - `/api/admin/overview`：高级模式 + 独立账号场景下管理员首页概览可能挂起。
  - `/api/admin/xray/ext`：存在独立账号时列表接口可能挂起。
  - 定时任务：`CheckAllExtQuota` 可能让采集任务卡死。
  - 用户超限路径：`writeQuotaExceededError` 可能导致推送/配额处理卡死。
  - 当前后端 `go test ./...` 不能全绿，阻塞 Build11 的完整后端验收结论。
- **修复方案（推荐，尚未实施）：**
  - 总体原则：**先读完外层 `Rows` 并关闭游标，再进行任何补充数据库操作**。仓库内 `subscription.Service.List`、`user.AdminService.List`、`home.Service.ListPlatforms`、`config.ExportService.exportInstances` / `exportAccounts` 已采用该正确范式，可作为参照。
  - **`ExtService.ListExt`：**
    1. 先读取全部独立账号基础行到内层 slice；
    2. 调用 `rows.Close()`；
    3. 再遍历 slice，分别调用 `pushTargetsFor` 与流量汇总查询。
    4. 可选优化：将推送目标和流量改为批量 `IN` 查询或 JOIN 聚合，减少 N+1 查询。
  - **`ExtService.CheckAllExtQuota`：**
    1. 先读取全部独立账号 ID 到 slice；
    2. 关闭游标；
    3. 再逐个调用 `CheckExtQuota`。
  - **`SyncService.writeQuotaExceededError`：**
    1. 优先改为单条 SQL 批量更新：
       ```sql
       UPDATE xray_users
       SET sync_status = 'failed',
           last_error = '已超限，请先重置配额',
           updated_at = CURRENT_TIMESTAMP
       WHERE user_id = ?
       ```
    2. 如保留逐条处理，也必须先读取目标到内存、关闭游标后再执行。
  - **测试建议：**
    - 为 `ListExt`、`CheckAllExtQuota`、`writeQuotaExceededError` 增加“非空数据”单测；
    - 增加覆盖“高级模式开启 + 存在独立账号 + 调用 `/api/admin/overview`”的回归测试；
    - 修复后执行：
      ```bash
      cd backend && go build ./... && go vet ./... && go test ./...
      ```
- **涉及文件：**
  - `backend/internal/xray/ext.go`
  - `backend/internal/xray/sync.go`
  - `backend/internal/server/overview.go`
  - `backend/internal/server/overview_test.go`
  - `backend/internal/store/store.go`（理解约束，不必然修改）
- **状态：** ◧ 需要更多分析：已确认单连接嵌套查询死锁及初步修复方向，但用户要求本轮不处理，待补充事务边界、错误传播与回归覆盖分析后再决定实施范围（未修改任何代码）

---

### R24-02 首页桌面端信息密度偏低，顶部卡片与平台卡片空间利用不足

- **来源：** 浏览器标注 2、3、4、6（`http://localhost:8080/`，桌面视口约 1361×961）。
- **现象：**
  - 流量卡片仅显示标题「流量」和「不限流量」，仍占用约 100px 高度。
  - 分流规则卡片仅显示规则名/版本和一个复制按钮，仍单独占用约 92px 高度。
  - 两张顶部卡片以全宽纵向方式排列，平台卡片网格被推到下方；首屏平台卡片结束后页面下半部分出现较大空白。
  - 当前页面主体实际采用「流量 → 分流规则 → 平台卡片网格 → 公告」的独立卡片堆叠结构，用户反馈需要重新利用这部分空间、优化主页整体结构。
- **快速验证：**
  - 浏览器 DOM 实测（1361×961）：主体内容宽约 1120px；流量卡片约 `1120×100px`，规则卡片约 `1120×92px`，平台卡片约 `363×213px`。
  - 两张顶部卡片的 `.ant-card-body` 均使用 AntD 默认 `24px` 内边距；当前 `HomeView.vue` 在第 187、204、223 行附近分别渲染三块区域，顶部两块各自占满一行。
- **根因/判断：**
  - 这不是单个溢出或计算错误，而是当前布局方案在数据较少的桌面场景下信息密度偏低。
  - Design2-UI §3.1 明确规定了「流量卡片 → 分流规则卡片 → 平台卡片网格 → 公告栏卡片」的纵向顺序和 16px 卡片间距，因此“合理利用空间”与现有规格存在设计层面的待决策项。
- **影响范围：**
  - 首页首屏视觉重心偏低，用户首先看到两个内容很少但高度较大的空白卡片。
  - 平台订阅信息和主要操作入口被下推；当平台数或公告增加时，页面纵向长度会继续放大。
  - 可能影响桌面端首页的可扫读性；移动端是否采用同样结构尚未由本问题确定。
- **修复方向（仅建议，尚未实施）：**
  - **推荐方案：** 桌面端将流量与分流规则改为同一行的响应式摘要区（各占一列），平台卡片继续使用整行栅格；`<768px` 回退为纵向堆叠。对简单状态卡适当收紧内边距，但保留清晰的区块层级。
  - 低改动方案：保持纵向顺序，仅降低流量/规则卡片内边距和最小高度；结构收益有限，不能完全解决整页首屏空白。
  - 需要用户确认的内容：是否允许调整 Design2-UI §3.1 的桌面端卡片顺序/并排规则，以及简单卡片的目标高度；在确认前不应进入实现。
- **涉及文件/规格：**
  - `frontend/src/views/HomeView.vue`
  - `frontend/src/style.css`（仅可能涉及通用卡片间距/布局样式）
  - `Design2-UI.md` §3.1
- **用户决策：** 采用推荐方案：桌面端将流量与分流规则改为同一行的响应式摘要区，`<768px` 回退为纵向堆叠；平台卡片网格与公告栏保持后续顺序。
- **状态：** ✓ 方案已确认并同步 `Design2-UI.md` §3.1；尚未实施（未修改任何代码）

### R24-03 管理员首页无订阅平台的空态文案仍使用用户态「联系管理员」

- **来源：** 浏览器标注 5。
- **现象：** 管理员登录首页时，未配置订阅的平台（当前示例为 `v2rayNG`）显示「暂无可用版本，请联系管理员」。管理员本身就是配置者，该文案不符合当前角色场景。
- **快速验证：**
  - 浏览器页面实际显示上述文案，且「按平台预览当前版本」按钮处于禁用状态。
  - `frontend/src/views/HomeView.vue` 管理员分支中，`card.subscription` 为空时复用「暂无可用版本，请联系管理员」；该分支与普通用户的 `unassigned` 空态使用相同措辞。
  - Design2-UI §3.1.3 对管理员订阅存在但没有激活版本的状态定义为「未激活」；本条标注对应的是 `card.subscription` 为空的更早空态，不能与“已有订阅但未激活”混为一谈。
- **根因：** 管理员预览态和普通用户下载态共用了面向用户的空态措辞，没有按角色和配置阶段区分“等待管理员配置”与“管理员等待添加版本”。
- **影响范围：** 管理员无法从文案直接判断下一步是创建订阅、上传版本还是激活版本；「联系管理员」在管理员自己操作时产生误导。
- **修复方向（仅建议，尚未实施）：**
  - 仅在管理员 `card.subscription` 为空的分支改为用户指定的「暂无版本，等待添加」；普通用户仍保留「暂无可用版本，请联系管理员」。
  - 已存在订阅但 `current_version` 为 0 的管理员状态继续沿用「未激活」，除非用户另行确认要统一成另一套文案。
- **涉及文件：** `frontend/src/views/HomeView.vue`
- **状态：** ◧ 文案问题已确认，目标文案已由用户明确指定（未修改任何代码）

### R24-04 首页顶栏「管理面板」与用户名按钮视觉尺寸不统一

- **来源：** 浏览器标注 1。
- **现象：** 同一顶栏右侧的「管理面板」按钮与用户名按钮高度不同，视觉上不属于同一组控件。
- **快速验证：**
  - 浏览器 DOM 实测：`管理面板`约 `88×32px`，`Admin` 用户名按钮约 `73×44px`。
  - `frontend/src/components/AppHeader.vue` 中，用户名按钮带 `.touch-target`，而「管理面板」按钮仅使用 `hidden md:inline-flex`；`.touch-target` 在 `frontend/src/style.css` 中规定 `min-height: 44px`。
  - 主题切换开关约 `48×22px`，属于开关控件，尺寸不应直接与按钮等同比较；本条重点是两个相邻按钮的高度层级不一致。
- **根因：** 用户名按钮同时承担可点击账户入口和触控命中区，复用了全局 `touch-target`；管理面板入口未复用同一视觉/命中区策略，导致桌面端一个为 44px、一个为 AntD 默认 32px。
- **影响范围：** 顶栏控制组的对齐和视觉节奏不一致；后续若继续在顶栏加入入口，尺寸规则会进一步分裂。
- **修复方向（已撤回）：**
  - 原先提出的按钮尺寸统一方案仅作为分析建议，未实施。
  - 用户已确认保持当前设计，不统一「管理面板」与用户名按钮的尺寸；不新增桌面/移动端分层尺寸策略。
- **为何目标高度属于设计取舍：**
  - “视觉高度”和“实际触控命中区”不是同一个概念。用户名按钮当前通过 `.touch-target` 直接把按钮本体撑到最小 44px；这同时解决了移动端可点击面积和桌面端视觉尺寸问题，但也把账户按钮做得比 AntD 默认中号按钮更高。
  - 「管理面板」入口在 `<768px` 下隐藏，用户名入口在所有尺寸都存在。因此管理按钮可以只在桌面端追求紧凑，而用户名按钮需要保留移动端的 44px 命中区；若不区分断点，统一一个高度就会牺牲其中一项。
  - Design2-UI/Build11 只规定了用户名入口和移动端触控可用性，没有规定顶栏两个按钮的统一像素高度；现有 AntD 默认按钮约 32px、全局触控规范为 44px，中间的 36px 还需要额外自定义样式，故不能从现有规格唯一推导出目标值。
- **各方案可能影响：**
  - 统一为 **44px**：实现最简单，保留现有用户名按钮的移动端命中区，桌面端两个按钮外观一致；代价是管理入口视觉更重，且与管理页其他默认 32px 按钮形成新的层级差异。当前 64px 顶栏可以容纳 44px 控件，不预计引发顶栏高度变化。
  - 统一为 **32px**：桌面端最紧凑，和 AntD 默认按钮体系一致；但若直接移除用户名按钮的 `.touch-target`，移动端账户入口会降至约 32px，违反当前 44px 触控命中区意图。只有在“桌面 32px、移动 44px”的断点策略下才可接受。
  - 统一为 **36px**：视觉上可在紧凑与可触控之间折中，但 AntD 没有现成的中间尺寸，需要自定义高度、内边距、行高和状态样式；后续还需检查焦点环、Dropdown 对齐及暗色主题，维护成本高于 32/44px。
- **用户决策：** 保持现有设计，不实施按钮尺寸统一；`.touch-target` 继续按当前用途使用，不为消除桌面截图中的高度差而调整。
- **涉及文件：**
  - `frontend/src/components/AppHeader.vue`
  - `frontend/src/style.css`
- **状态：** ✓ 用户确认保持当前设计，撤回尺寸统一改造（未修改任何业务代码）

---

### R24-05 管理员概览中的「首次发布清单」与「服务状态」面板信息价值不足

- **来源：** 浏览器标注 1、3（`http://localhost:8080/admin`）。
- **现象：**
  - 「首次发布清单」占据概览首屏右上区域，包含创建平台、创建订阅、添加节点、生成版本和普通用户检查五项；当前示例中前四项已完成，仅保留一项人工检查，用户认为该清单对日常管理员概览没有实际意义。
  - 「服务状态」面板仅显示运行模式、是否开启高级模式和应急状态，信息量较少，且与面板配置/侧边栏已有信息存在重复，用户希望移除。
- **快速验证：**
  - `frontend/src/views/admin/AdminOverviewView.vue` 在第 65～106 行附近固定渲染两个 `Card`，分别绑定 `overview.status` 和 `overview.checklist`；页面顶部描述也直接写有「查看服务状态、首次发布进度与近期管理活动」。
  - Design2-UI §10.5 和 Build11 D6/Step 5 将这两个面板作为管理员概览的既定组成部分，因此本条是产品信息架构调整，不是单纯的样式问题。
  - 后端 `backend/internal/server/overview.go` 仍会聚合并返回 `status`、`checklist` 及其相关数据；仅移除前端展示不会改变资源计数、审批摘要和访问日志等其他概览功能。
- **额外实现发现：**
  - `overview.go` 返回的 `overviewStatus.Emergency` 当前硬编码为 `false`，因此服务状态卡中的「应急状态 正常」不是从当前应急状态实时计算得出；保留该面板会产生状态可信度问题，移除它反而避免展示静态状态。
  - `advanced_mode` 虽然是真实配置值，也已经用于侧边栏菜单显隐和路由守卫；`app_mode` 也可在设置/部署上下文中获取，日常概览的重复价值有限。
- **影响范围：**
  - 面板移除后可显著减少概览首屏的非核心内容，资源计数和近期管理活动会成为主要信息入口。
  - 如果连后端字段、聚合查询和 API 契约一起删除，会波及 `GET /api/admin/overview`、相关后端测试及当前 R24-01 涉及的概览调用路径；这不是仅删除卡片渲染的同等操作。
  - 若移除服务状态卡而没有在其他位置补充应急状态提示，管理员在应急状态下缺少概览页的快速可见提醒；当前前端路由守卫仍会将请求导向 `/emergency`，但两者是不同层级的反馈。
- **修复方向（仅建议，尚未实施）：**
  - **推荐分两阶段：** 第一阶段只移除 `AdminOverviewView.vue` 中两个面板的展示，并同步调整概览标题描述；保留后端响应字段，避免扩大改动范围，也不把 UI 精简误当作 R24-01 死锁修复。
  - 第二阶段如确认这些数据永久不再被任何界面使用，再评估从 `overview.go` 和测试中删除 `status/checklist` 字段及对应查询；需要同步修订 Design2-UI §10.5 与 Build11 D6/Step 5。
  - 需要用户确认的内容：是否仅移除两张卡片，还是连同概览描述、后端 API 字段及首次发布聚合逻辑一并废弃；本轮用户意见已明确支持移除界面设计，但尚未要求删除 API 契约。
- **涉及文件/规格：**
  - `frontend/src/views/admin/AdminOverviewView.vue`
  - `backend/internal/server/overview.go`（若后续决定清理 API，不是第一阶段必改）
  - `Design2-UI.md` §10.5
  - `Build11.md` D6、Step 5
- **状态：** ◧ 用户已提出移除两张面板；记录为信息架构调整，尚未实施，API 是否同步收缩待确认（未修改任何业务代码）

### R24-06 管理员概览「刷新」按钮的 Reload 图标与文字基线未对齐

- **来源：** 浏览器标注 2。
- **现象：** 「刷新」按钮中的 Reload 图标视觉上低于“刷新”文字，图标和文字没有处于同一垂直中心线。
- **快速验证：**
  - 浏览器 DOM 实测（1361×961）：按钮约 `82×32px`；图标区域约 `14×14px`，文字行盒约 `28×22px`，图标中心比文字中心低约 `3.06px`。
  - 当前实现为 `AdminOverviewView.vue` 第 60 行附近的 `<Button><template #icon><ReloadOutlined /></template>刷新</Button>`；页面底部“返回主界面”“收起菜单”等带图标按钮也测得约 `3.06px` 的同类偏移，说明更可能是通用 AntD 图标 inline 基线排版现象，而非 Reload 图标资源本身损坏。
  - 计算样式显示按钮为普通 `block` 布局，图标为 `inline-block` 且 `vertical-align` 为约 `-1.75px`，文字按 22px 行高排版，二者中心自然不一致。
- **根因判断：** 图标通过 AntD Button 的 icon 插槽以行内元素参与基线对齐，当前没有对按钮内容统一使用 flex 垂直居中；现象可能在多个“图标 + 文字”按钮中复现。
- **影响范围：** 主要影响按钮的视觉精度和一致性，不影响刷新动作、点击区域或加载状态；如果采用全局按钮样式修复，可能影响侧边栏、表格操作、弹窗页脚等所有带图标按钮。
- **修复方向（仅建议，尚未实施）：**
  - 优先评估在通用按钮包装或局部按钮上采用稳定的 flex 对齐规则，并复核 icon-only、loading、small/middle、disabled 状态及 Dropdown 触发器。
  - 若仅修复本页，可限制在概览页刷新按钮，风险较低但会留下其他图标按钮的同类偏移；若修复 AntD 全局样式，需补充跨页面视觉回归。
- **涉及文件：**
  - `frontend/src/views/admin/AdminOverviewView.vue`
  - 可能的通用按钮/主题样式文件（需实施时进一步确定）
- **状态：** ◧ 对齐问题已通过浏览器实测确认，尚未实施修复（未修改任何业务代码）

---

### R24-07 订阅管理列表的「状态」列及常驻入池提示占用空间、信息层级过重

- **来源：** 浏览器标注 1、2（`http://localhost:8080/admin/subscriptions`）。
- **现象：**
  - 订阅列表额外展示一列「状态」；普通行该列为空，只有通过装配生成或上传后处于临时入池状态的行显示较宽的 `a-alert`「已入池未生效，请激活」。
  - 当前示例中状态提示和操作按钮同时占据一行，导致表格列数增加、操作区被挤压；用户认为状态列本身没有意义，希望移除。
  - 用户建议将「已入池未生效，请激活」改为 hover 或浮窗类提示，以减少常驻布局占用。
- **快速验证：**
  - `frontend/src/views/admin/SubscriptionsView.vue` 桌面表格在第 165 行附近固定渲染 `Table.Column title="状态"`，并在 `pooled(record.id)` 时渲染 `Alert`；移动端卡片也重复渲染同一 `Alert`。
  - `pooled()` 通过 `sessionStorage` 的 `pooled_sub_{id}` 标记判断临时状态；提示的主要动作是「去激活」，点击后进入该订阅的版本管理页，并不是单纯的信息展示。
  - Design2-UI §4.1 的订阅列表列定义为平台、订阅名称、产物格式、内容形态、当前版本、操作，没有独立「状态」列；§4.1 的入池引导口径是临时高亮 + 行内提示 + 「去激活」快捷链接，因此当前实现属于把临时提示扩展成固定表格列。
- **根因/判断：**
  - “状态列”与“当前版本”存在一定语义重叠：当前版本负责表达是否已有激活版本，入池提示表达的是“有候选内容但尚未生效”的临时流程状态。
  - 当前使用宽度较大的 `a-alert` 作为表格单元格内容，视觉权重高于普通状态标记，且把一次性流程状态固化为常驻列。
- **影响范围：**
  - 桌面端表格横向信息密度降低，操作区空间减少；列标题为空的普通行也会持续占用宽度。
  - 移动端虽然不展示表格列，但卡片内仍会常驻较高的 Alert，增加卡片高度。
  - 如果只删除状态列而完全删除提示，管理员可能看不到“已入池但未激活”的关键后续动作；这部分状态不能因为去掉列而丢失。
- **修复方向（仅建议，尚未实施）：**
  - **推荐：** 移除独立「状态」列；保留入池行的背景高亮或轻量 Tag/图标，在该标记上使用 `Tooltip`/`Popover` 展示「已入池未生效，请激活」和「去激活」入口。
  - 浮层不得只支持鼠标 hover，至少还应支持键盘 focus；移动端应支持点击打开，以避免触控设备无法触发或重要动作不可访问。
  - 移动端卡片可保留轻量状态标记，或将「去激活」放入操作区/更多菜单，避免继续使用整块常驻 Alert 撑高卡片。
  - 仅删除前端状态列和常驻 Alert 不需要改后端 API；`sessionStorage` 标记的生命周期和“去激活”动作仍需保留。
- **涉及文件/规格：**
  - `frontend/src/views/admin/SubscriptionsView.vue`
  - `Design2-UI.md` §4.1（若最终采用新的状态呈现方式，需要同步更新入池引导口径）
- **状态：** ◧ 用户已明确要求移除状态列、将入池提示改为浮层类呈现；尚未实施，提示触发方式和移动端呈现待实现时确认（未修改任何业务代码）

### R24-08 订阅列表行内「装配生成」入口与其他装配入口重复

- **来源：** 浏览器标注 3。
- **现象：** 订阅管理页同时存在：
  - 页头「前往装配」按钮；
  - 每条订阅行内的「装配生成」按钮；
  - 进入版本管理页后，创建区仍有「装配生成」入口。
  用户认为订阅列表行内入口重复，应移除。
- **快速验证：**
  - `frontend/src/views/admin/SubscriptionsView.vue` 页头在第 133 行附近渲染 `前往装配`，桌面表格和移动端卡片分别在第 171、207 行附近渲染行内 `装配生成`。
  - 行内按钮调用 `goAssembly(record)`，会根据当前订阅的 `product_type` 选择 `clash-yaml` / `sr-subs` / `generic-subs`，并携带 `platform_id`；它比页头入口拥有更多上下文。
  - `goAssemblyHeader()` 没有指定订阅时会选择 `subs.value[0]`，多订阅场景下页头入口默认使用第一条订阅；版本管理页则能天然保留当前订阅上下文。
  - Design2-UI §4.1 规定页头「前往装配」，§4.2 规定订阅版本管理页提供「装配生成」；没有规定订阅列表每行还要提供一个同名入口，因此当前行内按钮属于额外重复入口。
- **根因：** 装配功能入口分散在订阅列表页头、订阅列表行操作和版本管理页三个层级，缺少明确的“全局进入”和“针对当前订阅进入”边界。
- **影响范围：**
  - 操作区按钮过多，降低订阅列表的可扫读性；移动端还会增加操作按钮换行概率。
  - 如果直接移除行内按钮并同时依赖页头入口，多订阅场景可能进入错误的默认平台/产物类型；如果保留版本页入口，则当前订阅上下文不会丢失。
- **修复方向（仅建议，尚未实施）：**
  - 按用户意见移除订阅列表每行「装配生成」按钮。
  - 保留页头「前往装配」作为不带特定条目的通用入口；保留版本管理页「装配生成」作为带当前订阅上下文的精确入口。
  - 若未来希望从列表行直接进入特定订阅装配，应改为更明确的单一入口归属，而不是同时保留同名按钮；这属于后续信息架构决策。
- **涉及文件/规格：**
  - `frontend/src/views/admin/SubscriptionsView.vue`
  - `Design2-UI.md` §4.1、§4.2
- **状态：** ◧ 用户已明确要求移除行内装配生成入口；保留页头和版本页入口，尚未实施（未修改任何业务代码）

---

### R24-09 规则管理列表的「客户端类型」列属于冗余信息

- **来源：** 浏览器标注 1（`http://localhost:8080/admin/rules`）。
- **现象：** 规则管理表格展示「客户端类型」列，当前页面示例值为 `shadowrocket`；用户指出规则当前仅服务 Shadowrocket，列信息没有实际区分度，希望移除。
- **快速验证：**
  - `frontend/src/views/admin/RulesView.vue` 桌面表格在第 226～227 行附近固定渲染 `客户端类型` 列；移动端卡片顶部也重复展示 `r.client_type` 标签。
  - 创建表单中的客户端类型 Select 在第 290～292 行附近被明确禁用，唯一选项为「Shadowrocket（当前唯一支持）」；这证明当前产品层面没有可供管理员选择的第二种客户端类型。
  - `client_type` 仍参与后端规则创建校验、数据库存储、装配目标识别，并在用户端 `frontend/src/views/RulesView.vue` 展示；因此本问题只针对管理员列表的展示列，不应直接删除数据字段或 API 字段。
- **根因/判断：** 数据模型为未来多客户端类型预留了 `client_type`，但当前业务实际只有 Shadowrocket；管理列表继续展示该固定值，增加了列宽和视觉噪声，却没有帮助管理员完成操作。
- **影响范围：**
  - 桌面表格减少一列后可释放横向空间，操作区更宽松；移动端可同时移除卡片顶部重复标签，降低信息密度。
  - 如果误将列移除扩大为删除 `client_type` 字段，会影响规则创建接口、Shadowrocket 装配逻辑、数据库兼容和用户端规则展示，超出本次 UI 调整范围。
- **修复方向（仅建议，尚未实施）：**
  - 移除管理员规则列表桌面端「客户端类型」列；移动端同步移除同一字段的重复 Tag。
  - 保留创建表单的唯一类型约束、后端 `client_type` 字段和用户端规则标签，待未来真正支持多客户端时再恢复管理端区分展示。
- **涉及文件：**
  - `frontend/src/views/admin/RulesView.vue`
  - `frontend/src/views/RulesView.vue`（仅核对，不建议本条同步删除用户端标签）
- **状态：** ◧ 管理员列表冗余列已确认，限定为展示层调整，尚未实施（未修改任何业务代码）

### R24-10 规则管理「首页默认」应从独立单选列改为操作按钮

- **来源：** 浏览器标注 2、3、4（`http://localhost:8080/admin/rules`）。
- **现象：**
  - 当前表格首列固定展示「首页默认」标题和 Radio；默认行还在操作区额外显示「取消默认」按钮。
  - 用户希望移除「首页默认」列，将设置/取消首页默认统一收口到「操作」区的按钮中；标注 4 的「取消默认」与标注 3 一并处理。
- **快速验证：**
  - `frontend/src/views/admin/RulesView.vue` 桌面表格第 219～222 行附近渲染 `首页默认` Radio 列，第 241 行附近仅对默认行渲染「取消默认」按钮；移动端卡片第 260 行附近仍在名称旁渲染 Radio，第 279 行附近渲染「取消默认」按钮。
  - `askSetDefault()` 对非默认规则打开确认流程，`cancelDefault()` 对默认规则打开取消确认流程；两条业务流程都调用同一个 `setHomeDefault` API，后端通过事务和部分唯一索引保证最多一个默认规则。
  - Design2-UI §4.6 当前明确规定 Radio 列和默认行专设「取消默认」操作，因此本条是已确认规格的交互布局调整，不是后端默认规则机制变更。
- **根因/判断：** 默认状态被拆成“表格选择列 + 操作区取消按钮”两种交互形态；在只有一个默认规则的前提下，Radio 列占用独立宽度，同时又不能直接完成切换（非默认项还要弹确认），交互收益有限。
- **影响范围：**
  - 移除独立列可释放表格横向空间，操作入口集中，桌面与移动端的交互语义更一致。
  - 如果只保留默认行的「取消默认」而不为非默认行提供「设为首页默认」，管理员将失去切换默认规则的入口；按钮方案必须覆盖两种状态。
  - 不能删除 `is_home_default` 数据字段或 `setHomeDefault` API；首页分流规则卡片仍依赖默认规则，且后端唯一性和事务语义必须保持。
- **修复方向（仅建议，尚未实施）：**
  - 移除桌面端「首页默认」Radio 列；移动端移除名称旁 Radio。
  - 在操作区按行状态展示明确按钮：非默认行显示「设为首页默认」，默认行显示「取消默认」；继续沿用现有两个 ConfirmModal，避免把有影响的默认切换变成无确认操作。
  - 由于规则列表操作按钮已较多，按钮排序和“更多”收口需在实施时重新核对，避免移除一列后操作区过度拥挤。
- **涉及文件/规格：**
  - `frontend/src/views/admin/RulesView.vue`
  - `Design2-UI.md` §4.6（最终实施后需同步默认设置交互口径）
- **状态：** ◧ 用户已明确要求移除独立列并改为操作按钮；业务机制保持不变，尚未实施（未修改任何业务代码）

---

### R24-11 订阅版本管理页页头「订阅」类型标签与标题信息重复

- **来源：** 浏览器标注 1（`http://localhost:8080/admin/subscriptions/1/versions`）。
- **现象：** 版本管理页标题已经显示「ClashSubs1 · 版本管理」，右侧操作区又显示一个「订阅」类型 Tag；在当前订阅版本页语境下，用户认为该标识没有额外信息价值。
- **快速验证：**
  - `frontend/src/views/admin/VersionManageView.vue` 在第 246 行附近通过 `owner` 渲染 `<Tag v-if="owner" color="blue">{{ owner.type_label }}</Tag>`；同一 `owner.type_label` 还用于页面副标题「订阅资源的版本、预览与激活管理」。
  - 页面标题由 `owner.name` 与固定「版本管理」组成；对订阅资源而言，资源名称和当前路由已经足以表达对象类型，页头 Tag 属于重复提示。
  - `VersionManageView` 是订阅、规则、分享、自定义订阅四类资源共用视图；规则/分享/自定义订阅的标题语义可能不如订阅明确，因此不能仅凭本页面现象删除所有类型 Tag。
  - Build11/Design2-UI 当前曾要求版本页显示中文类型 Badge；本条是针对订阅资源的局部规格调整，不涉及 `owner.type_label` 数据来源和版本管理 API。
- **根因/判断：** 通用版本页为了覆盖四类资源统一显示类型 Tag，但订阅页的资源名称和“版本管理”标题已完成同样的语义表达，导致该类型 Tag 在订阅场景重复。
- **影响范围：**
  - 移除订阅页 Tag 只影响页头视觉信息密度，不影响版本预览、编辑、重新编辑、激活/分发、删除或返回操作。
  - 如果误改为全局删除 `owner.type_label` 或类型反查接口，会削弱规则/分享/自定义订阅版本页的资源辨识，并扩大到后端接口和测试。
- **修复方向（仅建议，尚未实施）：**
  - **推荐：** 仅在 `ownerType === 'subscription'` 时隐藏页头类型 Tag；保留 `owner.type_label` 用于副标题及其他资源类型页面。
  - 若希望四类资源统一不显示类型 Tag，需要另行确认，因为这会改变 Build11/Design2-UI 对通用版本页的展示约定。
- **涉及文件/规格：**
  - `frontend/src/views/admin/VersionManageView.vue`
  - `Design2-UI.md` §4.2、Build11.md Step 5（若实施局部隐藏需同步说明）
- **状态：** ◧ 订阅版本页类型标签冗余已确认，限定为展示层问题，尚未实施（未修改任何业务代码）

---

### R24-12 素材池详情中的 URL 同步条目应默认折叠并延迟查询

- **来源：** 浏览器标注 1（`http://localhost:8080/admin/assembly?tab=pool`）。
- **现象：** 素材池详情弹窗同时展示手动条目和 URL 同步条目。示例素材池 G1 有 1 个 URL、112 条条目，打开详情后 URL 同步条目区域直接展开并占用大量纵向空间。用户要求该板块默认折叠，点击后再展开；折叠期间不查询 URL 条目，展开时才查询。
- **快速验证：**
  - `frontend/src/views/admin/assembly/PoolDetail.vue` 在组件挂载时直接执行 `loadEntries()`；该函数请求 `GET /admin/pools/:id/entries?page=&page_size=`，因此打开弹窗即会查询条目，而不是等待 URL 区域展开。
  - 当前前端只有一个 `entries` 状态，随后通过 `source === 'manual'` / `source === 'url'` 计算出两个展示区；URL 条目是在同一次请求返回后才由前端分组，不存在独立的 URL 延迟加载边界。
  - `frontend/src/api/pool.ts` 的 `listEntries` 仅支持 `page`、`page_size`，没有 `source` 筛选参数；`backend/internal/server/pool.go` 也只解析分页参数。
  - `backend/internal/pool/pool.go` 的 `ListEntries` 同时统计全部 `pool_entries` 并按 `pool_id` 分页查询混合来源的记录。当前详情页在手动区和 URL 区共用一个 `total` 与一个分页控件。
  - Design2-UI §5.2.2 已要求条目列表采用后端分页并支持手动/URL 双态展示，但当前 §9.1 的接口约定仍是单一混合列表接口，尚未定义折叠区的懒加载或按来源分页。
- **根因/判断：** 当前数据接口和组件状态以“一个混合条目列表”为边界，展示层只是请求完成后的前端分组；因此 CSS 折叠只能隐藏已查询的数据，不能实现“折叠时不查 URL 条目”。同时，直接增加一个 `source=url` 参数也需要重新处理来源分区的计数、分页、加载状态和错误重试，不能只改查询条件后沿用当前共享分页。
- **影响范围：**
  - 打开详情时即查询并渲染 URL 条目，会增加大规模 URL 素材池的数据库读取、响应数据量和前端渲染/滚动成本；当前虽有每页 20 条的后端分页，但仍会在首次打开时返回当前混合页中的 URL 记录。
  - 如果只做前端折叠，数据库查询和内存占用不变，无法满足本条后端要求。
  - 如果仅给现有接口追加来源筛选而保留一个共享 `total` / 分页，可能出现手动区和 URL 区各自数据量与页码不一致、某页只有单一来源或分页总数错误的问题。
  - 手动条目在前、URL 同步条目在后的系统维护顺序需要保留；桌面端和移动端详情布局都需要同步处理折叠入口及加载状态。
- **修复方向（仅建议，尚未实施）：**
  - **推荐：** 手动条目区域按现有详情打开流程加载，URL 同步条目区域默认折叠；为条目查询增加 `source=manual|url` 过滤能力或提供等价的来源独立接口，分别维护 URL 区域的 `loaded/loading/page/total/error` 状态，首次展开时再请求 URL 条目，成功后缓存已加载结果，失败时提供重试。
  - 后端的来源过滤必须同时作用于 `COUNT(*)` 和列表查询，并结合现有 `pool_id`、`sort_order` 索引检查查询性能；前端应为来源分区使用准确的分页总数，不能继续把两区强行绑定到一个总数。
  - 折叠状态下“不查库”建议明确解释为“不查询 URL 条目”；弹窗仍可查询素材池基础信息及默认展示的手动条目。若用户要求连手动条目也不查，需要另行设计整个详情弹窗的按区块加载边界。
  - 移动端同样默认折叠，并确保展开按钮可聚焦、可通过键盘操作且能表达加载中/空数据/错误重试状态。
- **涉及文件/规格：**
  - `frontend/src/views/admin/assembly/PoolDetail.vue`
  - `frontend/src/api/pool.ts`
  - `backend/internal/server/pool.go`
  - `backend/internal/pool/pool.go`
  - `backend/migrations/1009_xray.sql`、`backend/migrations/1014_pool_sync_optimize.sql`（若实际查询计划证明需要补充或调整索引）
  - `Design2-UI.md` §5.2.2、§9.1（实施后需同步懒加载与来源分页约定）
- **用户决策：** “不查库”限定为折叠期间不查询 URL 同步条目；采用现有条目端点新增 `source=manual|url` 的可选筛选参数，两区独立分页，不新增平行端点。
- **状态：** ✓ 方案已确认并同步 `Design2-UI.md` §5.2.2、§9.1；尚未实施（未修改任何业务代码）

---

### R24-13 代理组高级开关「引入全部 Provider」文案发生换行

- **来源：** 浏览器标注 1（`http://localhost:8080/admin/assembly?tab=proxy-groups`）。
- **现象：** 代理组创建/编辑弹窗的「高级字段」区域中，开关选项「引入全部 Provider」在当前弹窗宽度下被拆成两行，`Provider` 落到下一行；同组其它开关保持单行，导致该选项与其它选项的垂直对齐和扫描节奏不一致。用户要求该文案保持同一行。
- **快速验证：**
  - `frontend/src/views/admin/ProxyGroupsView.vue` 第 334～341 行附近将六个开关放入 `grid grid-cols-2 md:grid-cols-4 gap-3`；在当前桌面断点下使用四列，弹窗内部每个网格单元的可用宽度有限。
  - 「引入全部 Provider」位于最长的开关文案之一，模板中的 `<label>` 没有 `whitespace-nowrap`、最小内容宽度或其它防止文本换行的约束；`<Switch>` 与文本处于同一个 label 内容流中，因此文本会在单元格剩余宽度不足时自然换行。
  - 该问题只涉及表单布局和文案呈现；`form.include_all_providers` 的状态字段、提交时的 `include-all-providers` 数据映射以及后端 `IncludeAllProviders` 语义均已独立存在，不需要改变业务数据结构。
  - Design2-UI §7.2 将这些能力定义为高级字段中的 `include-all` / `include-all-proxies` / `include-all-providers` 开关，但没有规定开关标签换行或弹窗窄宽下的具体列数。
- **根因/判断：** 四列网格在弹窗内容宽度下给单个开关分配的空间不足，而 label 默认允许文本换行；Provider 文案长度刚好超过该列的剩余宽度，故出现局部断行。
- **影响范围：**
  - 当前只影响视觉对齐、信息扫描和弹窗高度，不影响开关值保存、代理组生成或配置渲染。
  - 如果简单对 label 设置不换行而不调整网格列宽，窄屏或更窄弹窗中可能出现横向溢出、选项互相遮挡或点击区域难以区分的问题；需要同时验证桌面窄窗口和移动端布局。
  - 如果只缩短「引入全部 Provider」文案，虽然可以缓解当前断行，但会造成同类开关的命名风格不一致，也可能降低与配置字段 `include-all-providers` 的对应关系。
- **修复方向（仅建议，尚未实施）：**
  - **推荐：** 将高级开关项作为统一的 `flex items-center` 单行内容处理，并为标签增加不换行约束；同时在弹窗可用宽度不足时降低列数或采用带最小宽度的自适应网格，确保不以横向溢出换取单行展示。
  - 所有开关项应保持一致的垂直居中、间距、焦点和点击区域；若仅处理 Provider 一项，需要确认是否接受同一区域其它长文案在不同宽度下继续换行。
  - 不修改 `include_all_providers` 的字段名、接口载荷或后端校验，仅调整表单布局；完成后应在当前桌面宽度、窄桌面宽度和移动端分别核对单行、溢出及可操作性。
- **涉及文件/规格：**
  - `frontend/src/views/admin/ProxyGroupsView.vue`
  - `Design2-UI.md` §7.2（若落实统一单行与响应式列数，需补充高级开关布局约定）
- **状态：** ◧ 用户已明确要求「引入全部 Provider」保持单行；已完成浏览器现象核对与前端静态定位，尚未实施（未修改任何业务代码）。实施前需确认是仅保证该文案单行，还是统一要求高级开关文案全部单行并相应调整窄屏列数。

---

### R24-14 订阅装配目标类型 Tab 使用内部标识，整体文案不够面向用户

- **来源：** 浏览器标注 1～5（`http://localhost:8080/admin/assembly?tab=clash-yaml`）。
- **现象：** 构建订阅/规则区域的四个目标 Tab 直接显示内部标识 `clash-yaml`、`sr-subs`、`generic-subs`、`sr-conf`，包含连字符和英文缩写，用户不容易从名称判断输出格式与适用客户端。用户提出整体文案需要改进，并给出了四个目标的展示文案：`Clash - V2Ray/Mihomo（新版）`、`Shadowrocket 订阅组`、`通用V2Ray格式`、`Shadowrocket规则组`。
- **快速验证：**
  - `frontend/src/views/admin/AssemblyView.vue` 第 35 行定义 `SUB_TABS` 为稳定的内部值数组；第 678 行使用 `<Tabs.TabPane ... :tab="tab">`，因此把内部值直接作为界面文案渲染。
  - 同文件第 125～130 行已经存在 `targetSyntaxLabels` 映射：`clash-yaml` → `Clash YAML`、`sr-subs` → `SR 节点订阅`、`generic-subs` → `通用节点订阅`、`sr-conf` → `SR 分流规则`；该映射主要用于装配草稿来源等场景，没有用于当前四个 Tab。
  - `targetSyntax` 同时参与路由 query、表单分支、规则类型筛选、预览与生成请求；因此应只替换 Tab 的显示 label，不能修改 `SUB_TABS` 值、API `TargetSyntax`、后端 target_syntax 或 URL query 语义。
  - `HeaderStep.vue`、`PreviewStep.vue` 也分别按这些 target 值决定头部字段和预览目标名称，说明这些是稳定的业务枚举，当前问题属于显示层命名不一致，而不是目标类型识别错误。
- **根因/判断：** Tab 模板直接复用了用于逻辑判断和路由的枚举值，没有经过面向用户的展示文案映射；同时页面已有的人类可读标签与 Tab 实际展示没有统一使用。
- **影响范围：**
  - 直接影响目标类型的识别、可发现性和首次使用理解，尤其是 `generic-subs` 与 `sr-conf` 对非开发用户不直观。
  - 只替换显示文案不会影响已存在的路由深链、草稿恢复、目标分支、预览和生成 API；如果误改内部值，会造成 query 参数失效、表单状态切换错误或后端拒绝目标类型。
  - 「整体文案需要改进」的范围目前只通过标注明确覆盖四个 Tab；页面副标题、步骤标题、字段说明和按钮等其它文案是否需要一并重写尚未具体化。
- **修复方向（仅建议，尚未实施）：**
  - **推荐：** 新增/复用专门的 Tab 展示映射，保持内部 key 不变，按用户给出的四个名称显示：`clash-yaml` → `Clash - V2Ray/Mihomo（新版）`、`sr-subs` → `Shadowrocket 订阅组`、`generic-subs` → `通用V2Ray格式`、`sr-conf` → `Shadowrocket规则组`。
  - Tab label 与预览区/草稿来源的目标名称应统一口径，但不能为了统一显示而替换 `TargetSyntax` 的内部值；必要时保留一个内部值到显示文案的单向映射。
  - 若要处理“整体文案”而不仅是四个 Tab，需要先确认是否扩大到装配页副标题、步骤标题、头部字段、规则素材和预览按钮等区域，避免在本条中擅自改变未标注的产品术语。
- **涉及文件/规格：**
  - `frontend/src/views/admin/AssemblyView.vue`
  - `frontend/src/views/admin/assembly/PreviewStep.vue`（若统一预览目标文案）
  - `Design2-UI.md` §5.3（若落实新的目标类型展示命名，需同步 Tab 文案约定）
- **状态：** ◧ 四个目标 Tab 的展示名称已由用户明确提出；已完成前端静态定位，尚未实施（未修改任何业务代码）。页面其它区域是否纳入“整体文案”调整，实施前需确认范围。

---

### R24-15 Clash 头部参数中的 `Allow LAN` 开关应独立成开关区域

- **来源：** 浏览器标注 6（`http://localhost:8080/admin/assembly?tab=clash-yaml`）。
- **现象：** Clash YAML 的头部参数区域中，`Allow LAN` 以开关字段形式与 `Port`、`SOCKS Port`、`Mode`、`Log Level` 等输入字段混排在同一个两列网格里，开关控件单独占据一格且视觉重量与文本输入框不一致。用户要求将开关类按钮独立为单独区域。
- **快速验证：**
  - `frontend/src/views/admin/assembly/HeaderStep.vue` 的 `headerFields` 将 Clash 的 `allow-lan` 定义为 `type: 'bool'`，与 number/text 字段共用同一数组。
  - 模板第 88～97 行统一使用 `grid grid-cols-1 md:grid-cols-2 gap-3` 遍历所有字段；bool 字段仅渲染 label + `Switch`，没有独立的容器、标题或开关项布局。
  - 同一组件的 `sr-conf` 目标还存在 `ipv6` bool 字段，说明这不是只针对 Allow LAN 的偶发写法；后续若统一结构，可能需要让不同目标的布尔型头部参数也遵循同一布局规则。
  - `setField()` 仍按字段名写回 `fixed_params_text`，Clash 的 `allow-lan` 后端默认值和生成语义与表单布局无关。
- **根因/判断：** 头部字段当前按数据类型直接循环渲染，未区分输入型参数和开关型参数；两列表单网格适合输入框，但不适合单独的 Switch，导致 `Allow LAN` 缺少明确的分组层次。
- **影响范围：**
  - 主要影响头部表单的结构层次、视觉扫描和控件对齐，不影响 `allow-lan` 的 JSON 键名、默认值、保存和最终 Clash 输出。
  - 如果仅把 Allow LAN 移到新区域，`sr-conf` 的 IPv6 开关可能继续呈现旧布局，造成同类 bool 参数规则不一致；如果统一迁移所有 bool 字段，则需要核对各目标在结构化表单与高级 JSON 模式之间的切换表现。
  - 独立区域会改变表单垂直高度，需确认在分步模式、单页模式和移动端是否仍能保持操作按钮可见性与合理滚动。
- **修复方向（仅建议，尚未实施）：**
  - **推荐：** 将文本/数字字段保留在“头部参数”网格，将 bool 字段集中到独立的“开关参数”区域；区域内采用统一的横向或自适应布局，保证 Switch、标签、焦点和点击区域对齐。
  - 默认按字段类型统一处理 Allow LAN 与 IPv6；若用户只希望拆出 Clash 的 Allow LAN，需要明确接受 SR-conf 的 IPv6 仍使用旧布局。
  - 仅调整展示结构，不改变 `fixed_params_text` 的字段映射、默认值、目标枚举或生成接口。
- **涉及文件/规格：**
  - `frontend/src/views/admin/assembly/HeaderStep.vue`
  - `Design2-UI.md` §5.3.1～§5.3.3（若落实头部输入/开关分区，需补充分组规则）
- **用户决策：** 所有目标的 bool 型头部字段统一迁移到独立“开关参数”区域，包括 Clash YAML 的 `Allow LAN` 与 SR 分流规则的 `IPv6`。
- **状态：** ✓ 方案已确认并同步 `Design2-UI.md` §5.3；尚未实施（未修改任何业务代码）

---

### R24-16 头部参数的高级 JSON 与默认值操作入口需要改为明确的状态/危险操作表达

- **来源：** 浏览器标注 7～8（`http://localhost:8080/admin/assembly?tab=clash-yaml`）。
- **现象：** 头部参数区域右上角的「高级 JSON」是普通按钮，当前用于在结构化表单与 JSON 文本框之间切换；用户希望改为开关形式。旁边的「一键采用默认值」文案强调“一键”但实际会覆盖现有头部参数，用户要求改为「使用默认值」、使用警告样式，并需要确认。
- **快速验证：**
  - `frontend/src/views/admin/assembly/HeaderStep.vue` 通过本地 `advanced` 状态控制 `Input.TextArea` 与结构化表单，当前第 80～83 行使用 `Button`，按钮文案根据状态在「高级 JSON」和「结构化表单」之间切换。
  - `frontend/src/views/admin/AssemblyView.vue` 已通过 `headerConfirmOpen` 接收默认值操作；第 760～766 行的 `FormOverlay` 会提示“将覆盖当前已填头部字段，确定采用默认值吗？”并提供「确认采用」按钮。因此“需要确认”这一行为目前已经存在，标注主要是要求提高入口的警示程度和文案准确性，而不是发现缺少确认逻辑。
  - `applyDefaultHeader()` 会用当前 target 对应的 `DEFAULT_HEADERS[targetSyntax.value]` 整体替换 `form.fixed_params_text`，不是只补齐空字段；Clash 默认 JSON 还包含结构化表单未单独展示的 `external-controller`，所以确实存在覆盖和配置内容变化风险。
  - `generic-subs` 没有头部参数表单，当前高级 JSON/默认值操作只在其它目标的头部区域出现；改造不能改变该目标的“本步无需填写”分支。
- **根因/判断：** 高级 JSON 是一个双态视图切换，却使用瞬时操作按钮表达；默认值操作具有覆盖性，但入口使用普通按钮和“一键”措辞，风险提示弱于实际效果。确认弹窗已经存在，问题重点是状态表达、危险级别和文案，而非后端行为缺失。
- **影响范围：**
  - 将高级 JSON 改成 Switch 会影响结构化表单/JSON 文本框的可发现性、状态同步、键盘操作和移动端布局，但不应改变 `fixed_params_text` 的数据格式。
  - 默认值按钮改为 warning/danger 样式只改变操作提示层级；若误删现有确认弹窗，则可能导致用户填写的头部 JSON 被直接覆盖，属于明显行为回归。
  - 默认值内容按四类目标不同，文案应表达“当前目标的默认头部参数”，不能暗示只恢复某一个字段。
- **修复方向（仅建议，尚未实施）：**
  - **推荐：** 用带明确双态标签的 Switch 表达“结构化表单 / 高级 JSON”，切换时保留当前文本状态，并验证从 JSON 切回结构化表单时非法或额外字段的处理提示；不改变高级模式仅影响编辑视图的现有语义。
  - 将「一键采用默认值」改为「使用默认值」，使用 warning 样式，同时保留现有确认弹窗；弹窗标题/正文应明确说明会覆盖当前头部参数，确认按钮可继续使用「确认采用」或同等明确动作词。
  - 若用户希望默认值操作支持“仅填充空字段”而不是整体覆盖，这是业务行为变化，不能从当前标注推断，应另行确认后再设计。
- **涉及文件/规格：**
  - `frontend/src/views/admin/assembly/HeaderStep.vue`
  - `frontend/src/views/admin/AssemblyView.vue`
  - `Design2-UI.md` §5.3.1～§5.3.3（需同步高级模式切换控件和默认值覆盖确认约定）
- **状态：** ◧ 用户已明确提出高级 JSON 改为开关、默认值改名并使用警告样式；已确认现有默认值操作已有覆盖确认弹窗，尚未实施（未修改任何业务代码）。默认值是否仍采用“整体覆盖”语义，以及“整体文案改进”是否扩大到其它装配文案，实施前需确认。

---

### R24-17 装配步骤中的节点与代理组选区需要统一视觉层次，`manual` 文案需改为中文

- **来源：** 浏览器标注 1、4（`http://localhost:8080/admin/assembly?tab=clash-yaml`）。
- **现象：** 装配步骤②“节点与代理组”将手动节点、Xray 节点、代理组和国外流量成员连续排列在同一块普通内容流中，仅通过小字号标题分段；节点和代理组均使用简单的多列 Checkbox 网格，选中状态、不可用状态、强制状态和自建状态的视觉区分较弱。手动节点区标题仍显示为 `manual 节点`，用户要求改为“手动添加的节点”，并希望优化整个选择区域的样式使其更美观。
- **快速验证：**
  - `frontend/src/views/admin/assembly/NodesGroupsStep.vue` 第 84～131 行依次渲染手动节点、Xray 节点、代理组和“🌎国外流量成员（仅节点）”四个区域；每个区域只有文字标题和 `grid md:grid-cols-3 gap-2` 网格，没有 Card/Section 容器、计数、说明或统一的状态摘要。
  - 手动节点标题在第 87 行硬编码为 `manual 节点`；空态第 93 行也使用 `暂无 manual 节点`，因此只替换标题而不处理空态会留下中英文混用。
  - 代理组网格同时包含 disabled 的三类强制组、可勾选的预设组和自建组；选中后还在 Checkbox 内嵌“选择与排序”按钮。节点网格则同时显示协议 Tag、Xray 显示名/系统名和不可用 Tag，当前没有统一的选项行高、辅助说明或操作区域。
  - 该组件只负责更新 `node_names`、`group_names`、`group_node_orders` 和 `overseas_members`，当前结构调整不会天然改变这些表单字段，但选区重排可能影响分步模式高度与移动端滚动。
- **根因/判断：** 选择项按数据来源和业务角色分段，但缺少统一的分组容器与状态表达；同时直接使用后端来源枚举 `manual` 作为用户可见文案，导致信息层级和术语不够友好。
- **影响范围：**
  - 主要影响选区的可读性、选中状态识别、不可用项理解和操作效率，不影响节点/代理组勾选数据或排序数据的保存语义。
  - 如果通过卡片、背景色或更多 Tag 强化分组，需要注意已选、禁用、失效和强制等状态的颜色对比，避免把“不可选”误解为“未选中”。
  - 如果统一重排所有选项，可能改变“🌎国外流量成员”与代理组选择之间的关系；该区域当前是 Clash 特有的独立选择，不应被误并入普通代理组列表。
- **修复方向（仅建议，尚未实施）：**
  - **推荐：** 将节点与代理组选择拆为清晰的 Section/Card：例如“手动添加的节点”“Xray 节点”“代理组”“🌎国外流量成员”，为每区提供数量/选择状态摘要，选项行统一对齐并区分已选、不可用、强制和自建状态；保留现有拖拽排序入口。
  - 将 `manual 节点` 及空态 `暂无 manual 节点` 统一改为“手动添加的节点”及对应中文空态；Xray 节点可继续保留专业名，但应提供简短说明解释其来源和不可用原因。
  - 移动端应由三列网格降级为单列或自适应列，保证 Checkbox、Tag 和“选择与排序”按钮不挤压；结构化样式调整不应改变生成请求字段。
- **涉及文件/规格：**
  - `frontend/src/views/admin/assembly/NodesGroupsStep.vue`
  - `Design2-UI.md` §5.3.1（若落实新的分区、状态摘要和中文术语，需补充选区视觉约定）
- **状态：** ◧ 用户已明确要求优化整个节点/代理组选区并将 `manual` 改为“手动添加的节点”；已完成前端静态定位，尚未实施（未修改任何业务代码）。具体分区样式、状态颜色和移动端布局仍需在实施前按整体装配页视觉规范确认。

---

### R24-18 强制代理组的可选成员诉求与当前固定成员设计存在冲突

- **来源：** 浏览器标注 2、3（`http://localhost:8080/admin/assembly?tab=clash-yaml`）。
- **现象：** 当前代理组列表将 `🚀直接连接`、`🌎国外流量`、`🛟无法归属的流量` 都显示为已勾选且 disabled，并附带“强制” Tag。用户提出：`🌎国外流量` 应可以选择节点；`🛟无法归属的流量` 应可以选择某个节点或 `DIRECT`。其中前者在页面下方已经存在“🌎国外流量成员（仅节点）”选择区，后者则可能要求改变兜底组的成员生成规则。
- **快速验证：**
  - `frontend/src/views/admin/assembly/NodesGroupsStep.vue` 第 30 行定义三类 `FORCE_GROUPS`，第 111 行以 `:checked="true" disabled` 渲染，因此当前 UI 不允许勾选/取消这些组本身。
  - 同组件第 123～130 行允许从本次候选节点中选择 `overseas_members`，所以「🌎国外流量」目前是“组固定存在、组成员由装配员选择”的双层语义；但 UI 将其与另外两个固定成员组同样标注为“强制”，容易让用户误以为其节点成员也不可选。
  - `frontend/src/views/admin/AssemblyView.vue` 的表单只有 `overseas_members` 字段，没有 `direct_members` 或 `fallback_members`；后端 `backend/internal/assembly/validate.go` 要求 Clash 的 `overseas_members` 至少有一个已勾选节点，并要求其属于 `node_names`。
  - Design2.md §3.3 明确规定：`🚀直接连接` 内含固定 `DIRECT`；`🛟无法归属的流量` 成员固定为 `[🚀直接连接, 🌎国外流量]`；只有 `🌎国外流量` 的成员在装配器中从候选节点勾选。Design2.md §3.3/§3.6 还规定强制组不可移除、空强制组会阻止生成。
  - 后端 `backend/internal/assembly/clash_plan.go` 在最终渲染收敛时会把强制组保留，并在强制组成员为空时回退到 `DIRECT`；因此“允许兜底组选择某个节点或 DIRECT”不是单纯换 Tag 或放开 Checkbox，而是组成员模型、验证和下载重渲染语义的潜在变化。
- **根因/判断：** 当前设计将“强制存在”与“成员是否可配置”混在同一个 disabled Checkbox + “强制”标签中。用户标注 2 可能是在要求澄清/保留已有的国外成员选择能力；标注 3 则与现行兜底组固定成员设计直接冲突，具体意图尚不能从截图唯一确定。
- **影响范围：**
  - 若仅调整 UI 表达：可以保留三组强制存在，同时把 `🌎国外流量` 的成员选择能力在组旁明确提示，并保留 `🛟无法归属的流量` 固定兜底语义；影响主要是展示层。
  - 若允许 `🛟无法归属的流量` 选择节点或 `DIRECT`，需要明确是新增单选兜底目标、允许多个成员，还是替换现有两个强制组引用；这会影响 `GenerateInput`、装配蓝图 `selection_json` / `render_plan_json`、后端空组校验、Clash 下载重渲染、规则 `MATCH` 目标以及相关测试。
  - 若允许 `🚀直接连接` 添加节点，也会改变其“固定 DIRECT”语义，并可能影响所有将其作为子组引用的预设/自建组；当前设计和验证逻辑均未提供对应成员字段。
- **修复方向（仅建议，尚未实施）：**
  - **推荐（保持当前业务设计）：** 不改变三类强制组的存在性和成员生成规则；把“强制”改成更准确的分层表达，例如区分“系统固定组”和“成员可配置”，在 `🌎国外流量` 组项旁明确提示“节点成员见下方选择”，并保留下方节点选择区；`🛟无法归属的流量` 继续展示为系统兜底组。
  - 如果用户确实要让兜底组选择某个节点或 `DIRECT`，需先确认成员模型（单选/多选、是否允许组引用、空值行为、是否替代两个现有成员），再同步 Design2、前端表单字段、蓝图快照、后端校验/渲染和测试；本条不能按纯 UI 问题直接实施。
  - “🌎国外流量应可选节点”若只是希望解除误解，建议只改善组项说明和下方选择区的关联；若要改变选择位置或范围，也需确认是否仍要求成员必须来自本次已勾选节点。
- **涉及文件/规格：**
  - `frontend/src/views/admin/assembly/NodesGroupsStep.vue`
  - `frontend/src/views/admin/AssemblyView.vue`
  - `backend/internal/assembly/validate.go`
  - `backend/internal/assembly/clash_plan.go`
  - `Design2.md` §3.3、§3.6；`Design2-UI.md` §5.3.1（若确认改变强制组成员语义，需先同步设计）
- **状态：** ◧ 需要更多分析：用户要求本轮不处理。后续需先明确这是仅优化“强制组/成员可配置”的界面表达，还是正式改变强制组成员模型；在此之前不修改现有 Design2 固定兜底成员规则，也不进入实现（未修改任何业务代码）。

---

### R24-19 节点新增/编辑弹窗字段混排，开关参数缺少独立分区

- **来源：** 浏览器标注 1、2（`http://localhost:8080/admin/nodes`）。
- **现象：** 新建/编辑节点弹窗使用约 720px 宽的长表单；基础字段、凭据、传输参数、TLS/UDP/XUDP/Packet Address 等开关、协议对象参数均由同一个二维网格连续渲染。开关字段仅作为普通网格单元中的一个控件出现，没有独立的开关区域；随着协议字段增多，弹窗内容纵向拉长，层级和扫描路径不清晰，用户认为整体页面结构崩坏并要求重新排版。
- **快速验证：**
  - `frontend/src/views/admin/NodesView.vue` 的节点弹窗以 `FormOverlay` 渲染，宽度为 `720`；协议动态字段通过单个 `v-for` 循环放入 `grid ... md:grid-cols-2`，只有对象类型字段使用 `col-span-2`，没有按基础信息、传输、安全、开关和高级参数分区。
  - 同一渲染循环中，`bool` 字段直接渲染为 `Switch`，文本、选择、列表和对象字段没有独立的视觉容器；VLESS schema 同时包含 `tls`、`udp`、`packet-addr`、`xudp` 等开关，以及多个传输/协议对象字段，因此截图中的混排并非单一字段偶发问题。
  - `backend/internal/node/registry.go` 的协议注册表还通过公共字段为多种协议追加 `tfo`、`mptcp` 等布尔参数，说明若只针对 VLESS 调整，其他协议仍可能出现同类结构不一致。
- **根因/判断：** 当前前端按字段类型做通用网格映射，但 schema 只提供扁平字段列表，没有“字段分区/顺序/布局角色”等元数据；`FormOverlay` 的固定宽度配合较长的动态 schema，进一步放大了信息层级不足的问题。该问题主要是展示层和表单组织问题，不应因重新分区而改变 `protocol_json` 的字段名、默认值或敏感字段处理。
- **影响范围：**
  - 影响节点新增/编辑时的理解成本、填写效率、纵向滚动和移动端可用性；预计不直接影响保存数据，但重排字段时必须保持现有 schema 驱动、必填校验、默认值、密码字段和对象字段的行为。
  - 若只整理 VLESS，VMess、Trojan、Hysteria 等同样包含布尔字段或复杂字段的协议仍会产生不一致；若为全部协议引入分区，需要考虑不同 schema 的字段缺失和条件字段。
  - 如果为了分区修改后端 schema，可能扩大为协议注册表 API 与前端类型契约变更；单纯的前端分类也存在无法准确判断字段业务归属的风险。
- **修复方向（仅建议，尚未实施）：**
  - **推荐：** 保持 `protocol_json` 和现有字段语义不变，在前端建立“连接基础信息 / 凭据与认证 / 传输与 TLS / 开关选项 / 协议高级参数”等清晰分区；将所有布尔开关统一放入独立区域，并按桌面双列、移动端单列进行类型化排版。
  - 分区不应仅依赖字段类型推断；可以先在前端为已知协议维护轻量布局映射，或在确认跨协议通用规则后再扩展 `FieldSchema` 的布局元数据。对象字段的编辑方式另由 R24-20 处理，避免两个问题相互覆盖范围。
  - 重新排版后需验证 VLESS/VMess 等协议的新增、编辑、协议切换、默认值和移动端 Drawer/Modal 展示，确保仅改变布局而不丢失字段。
- **涉及文件/规格：**
  - `frontend/src/views/admin/NodesView.vue`
  - `frontend/src/api/node.ts`
  - `backend/internal/node/registry.go`
  - `Design2-UI.md` §6.2（当前仅规定 schema 驱动字段渲染，未规定分区布局）
- **状态：** ◧ 需要更多分析：用户要求本轮不处理。后续需补充评估前端布局映射与扩展 `FieldSchema` 布局元数据两种方案、公共 bool 字段范围及全协议兼容性后，再决定实现边界（未修改业务代码）。

### R24-20 节点协议对象参数使用原生 JSON 文本框，缺少结构化编辑方式

- **来源：** 浏览器标注 3（`http://localhost:8080/admin/nodes`）。
- **现象：** VLESS 节点表单中的 REALITY、HTTP、H2、gRPC、WebSocket 等对象参数以多行文本框展示，默认内容为 `{}`，用户需要直接输入 JSON；用户要求改为结构化表格，避免原生 JSON 形式输入。
- **快速验证：**
  - `frontend/src/views/admin/NodesView.vue` 对 `type === 'object'` 的字段统一执行 `JSON.stringify(..., null, 2)` 展示，并在失焦时通过 `JSON.parse` 解析；当前前端主要能发现 JSON 语法错误，未提供键值行、字段说明、嵌套结构或类型化编辑。
  - `frontend/src/api/node.ts` 的 `FieldSchema` 只有字段名、类型、标签、默认值、帮助文本和选项；对象字段没有嵌套字段 schema，因此前端无法仅凭当前接口可靠地生成协议专用结构化表格。
  - `backend/internal/node/registry.go` 将对象字段定义为扁平的 `object` 类型；`backend/internal/node/node.go` 校验对象主要接受 map/array 并保存在 `protocol_json` 中，未对 REALITY、WS headers、peers、smux 等对象内部键提供统一结构约束。不同协议的对象可能是映射、数组或包含嵌套值的复杂结构。
- **根因/判断：** 协议注册表把复杂参数视为不透明 JSON，前端因此只能复用通用文本框。要实现真正的“结构化表格”，必须决定是通用键值编辑器，还是基于协议 schema 的专用字段编辑器；两者的复杂度、可编辑范围和后端契约影响不同。
- **影响范围：**
  - 当前问题直接影响复杂协议参数的可发现性、输入正确率和非技术用户的操作负担；已有 `protocol_json` 数据的字段名、嵌套层级、数组形态和未知键不能因 UI 改造被静默丢弃。
  - 通用键值表需要处理新增/删除键、空对象、嵌套对象、数组、数字/布尔类型以及已有不规则数据；若只支持平面字符串，可能无法覆盖现有协议参数。
  - 若改为完整的协议专用表单，需要扩展 `FieldSchema` 的嵌套类型、字段标签、选项和校验规则，并同步后端注册表接口、服务端校验及相关测试；这已不再是单纯换一个前端控件。
- **修复方向（仅建议，尚未实施）：**
  - **推荐的渐进方案：** 先提供通用结构化对象编辑器（键值行、增删、类型选择，并能处理嵌套对象/数组），保存时继续生成与现有 `protocol_json` 相同的结构；对无法安全结构化的复杂或未知数据保留可回退的高级 JSON 入口，并明确提示，不得静默覆盖未知字段。
  - 若目标是完全移除 JSON 输入，则需要先扩展协议注册表的嵌套 schema，再按协议渲染专用表格/表单，同时补充后端类型校验、已有数据兼容和迁移测试。该方案覆盖更完整，但实施范围明显更大。
  - 无论采用哪种方案，都应保留服务端最终校验，并验证新建、编辑已有对象、协议切换、空对象和非法输入的行为。
- **涉及文件/规格：**
  - `frontend/src/views/admin/NodesView.vue`
  - `frontend/src/api/node.ts`
  - `backend/internal/node/registry.go`
  - `backend/internal/node/node.go`
  - `Design2-UI.md` §6.2（需补充复杂对象字段的编辑形态与兼容约定）
- **状态：** ◧ 需要更多分析：用户要求本轮不处理。后续需完成通用递归结构化编辑器与协议专用嵌套 schema 两种方案的兼容性、敏感嵌套字段和既有 `protocol_json` 回写风险分析后，再决定实现边界（未修改业务代码）。

---

## 二、已修复/已闭环问题

暂无。

---

## 三、变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-08-28 | 初始版本：记录 Build11 后端 SQLite 单连接嵌套查询死锁的现象、根因、影响范围与推荐修复方案；仅研究，未改动代码。 |
| v1.1 | 2026-08-28 | 新增 R24-02 首页桌面端空间利用问题、R24-03 管理员无订阅空态文案问题、R24-04 顶栏按钮尺寸不一致问题；完成浏览器实测与静态定位，未修改业务代码。 |
| v1.2 | 2026-08-28 | 补充 R24-04 的视觉高度/触控命中区区分、三种目标高度的影响及推荐分层方案；未修改业务代码。 |
| v1.3 | 2026-08-28 | 用户确认保持当前顶栏设计，撤回 R24-04 尺寸统一改造；保留分析证据，标记为不实施。 |
| v1.4 | 2026-08-28 | 新增 R24-05 管理员概览两类面板移除诉求、R24-06 刷新按钮图标与文字对齐问题；完成浏览器实测与静态定位，未修改业务代码。 |
| v1.5 | 2026-08-28 | 新增 R24-07 订阅列表状态列/入池提示优化诉求、R24-08 行内装配生成入口重复问题；完成规格对照与静态定位，未修改业务代码。 |
| v1.6 | 2026-08-28 | 新增 R24-09 规则管理客户端类型列冗余问题、R24-10 首页默认列与操作入口调整诉求；完成规格对照与静态定位，未修改业务代码。 |
| v1.7 | 2026-08-28 | 新增 R24-11 订阅版本管理页类型标签冗余问题；完成通用视图复用范围核对，未修改业务代码。 |
| v1.8 | 2026-08-28 | 新增 R24-12 素材池详情 URL 同步条目默认折叠与展开后延迟查询问题；完成前后端查询边界、来源分页与设计影响核对，未修改业务代码。 |
| v1.9 | 2026-08-28 | 新增 R24-13 代理组高级开关 Provider 文案换行问题；完成前端网格布局与响应式影响核对，未修改业务代码。 |
| v2.0 | 2026-08-28 | 新增 R24-14～R24-16 订阅装配目标 Tab 文案、头部开关分区、高级 JSON 与默认值操作入口问题；完成前端静态定位与行为影响核对，未修改业务代码。 |
| v2.1 | 2026-08-28 | 新增 R24-17～R24-18 装配节点/代理组选区样式与术语、强制组成员可选诉求；完成前端展示及强制组前后端语义核对，标记 Design2 冲突点，未修改业务代码。 |
| v2.2 | 2026-08-28 | 新增 R24-19～R24-20 节点弹窗整体排版/开关分区、协议对象参数结构化编辑诉求；完成前后端字段渲染与数据契约静态核对，保留实施范围确认点，未修改业务代码。 |
| v2.3 | 2026-08-28 | 用户确认 R24-02 桌面摘要区、R24-12 来源筛选懒加载、R24-15 全目标 bool 开关分区，并同步至 Design2-UI；R24-01、R24-18～R24-20 标记为需要更多分析，本轮不进入实现。 |
