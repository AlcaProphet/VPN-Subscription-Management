# Issue19.md — Build32 Step21 浏览器检查问题追踪

> **文档定位：** 本文档记录 Build32 Step21 浏览器检查中发现、尚待处理的问题。Step21 的验收合同见 [Build32.md](Build32.md) §八；本记录仅依据隔离本地实例中的浏览器观察，不替代 API、自动化、真实客户端或用户人工验收。

---

## 一、进行中问题

### R33-01 WireGuard peers 模式缺少 Peer 条目级排序交互

- **发现方式：** 2026-09-23，在 Build32 Step21 隔离本地浏览器检查中发现，并在 `375 × 785` 与 `920 × 785` Chrome DevTools 设备模拟视口复核。使用临时 `DATA_DIR=/tmp/vpn-sub-build32-step21-20260923-920`、`APP_MODE=dev`、`PORT=18080` 启动本地实例，并通过 Setup 创建合成管理员；未使用或修改仓库内 `backend/data`。
- **复现步骤：** 管理面板 → 节点 → 新建节点 → 协议选择 WireGuard → Peer 模式选择 `peers`。
- **现象：** 在两个视口的 `peers` 模式下，都只显示一个名为「Peer 列表」的 JSON 多行文本框；界面没有逐条 Peer 的新增、删除或排序控件。浏览器页面的可访问性树将该字段呈现为单个 `entry area`，截图也确认模式切换后未出现条目控件。因此无法从界面验证或操作 Step21 要求的 WireGuard Peer 排序。
- **根因：** 当前表单把 WireGuard 的 `peers` 数组作为整体 JSON 文本编辑，没有提供条目级列表编辑交互。此为浏览器可见行为归因；更深层 schema／组件调用链尚未核验。
- **影响范围：** WireGuard `peer_mode=peers` 的新建／编辑体验，以及 Step21 所要求的 Peer 稳定条目身份和排序浏览器验收。尚未提交表单，未验证保存后端顺序或装配结果。
- **修复方案：** 待确认并安排至 Build32 对应构建步骤；至少需按条目呈现 Peer、支持新增／删除／排序，并在重排后保持稳定条目身份与凭据状态。修复后补充浏览器核验，确认重排结果及 PSK 状态不会串项。
- **状态：** ☐ 待处理；未改代码，未执行保存、API、自动化或真实客户端验证。

### R33-02 375px 节点表单中的 UUID 与安全控件被压窄

- **发现方式：** 2026-09-23，在 Chrome DevTools 设备模拟视口 `375 × 785` 下浏览器核验。此视口由用户缩小浏览器窗口后，经设备工具栏精确设为 375px；测试仍使用本 Issue R33-01 记录的隔离本地实例和合成管理员。
- **复现步骤：** 管理面板 → 节点 → 新建节点，保持默认 VLESS 协议。
- **现象：** 窄屏表单中「UUID」与「传输」仍并排显示；UUID 输入框被压缩到几乎仅剩右侧显示／隐藏图标的宽度。「安全」与「Flow」也并排，其中安全选择器宽度过窄，当前只能露出部分选项文字。UUID 难以正常输入，安全选项难以辨认和操作。名称、服务器、端口字段在截图中仍为可用的整行宽度。
- **根因：** 窄屏下 UUID／传输、安全／Flow 表单组仍采用并排列布局，未为长输入控件提供足够宽度。此为浏览器可见行为归因；具体断点和布局规则尚未沿代码核验。
- **影响范围：** 375px 窄屏下节点新建／编辑表单的凭据输入及安全模式选择；可能影响使用同一布局结构的其他协议，尚待逐项浏览器核验。
- **修复方案：** 待确认并安排至 Build32 对应构建步骤；在窄屏将这些字段组改为可用宽度充足的布局，随后于 375px 浏览器重新核验 UUID 输入、选择器选项显示及常用协议分支。
- **状态：** ☐ 待处理；未保存节点，未改代码，未执行 API、自动化或真实客户端验证。

### Step21 浏览器核验边界

- 本轮隔离实例中，在 Chrome DevTools 设备模拟视口 `920 × 785` 下检查管理员节点页和节点新建表单。VLESS 字段组（UUID／传输、安全／Flow）在此宽度显示和操作空间正常；WireGuard `peers` 模式仍缺少条目级新增、删除和排序交互，见 R33-01。
- 920px 下展开并滚动协议选择器，浏览到完整的 19 个协议选项，未发现协议缺项。此为浏览器可见结果，不替代后端 schema/API 覆盖验证。
- 375px 表单布局问题见 R33-02；375px 与 920px 均确认的 WireGuard 条目级排序问题见 R33-01。
- 引用任务已有 OpenVPN diagnostics、多行长文本和主题的浏览器核验记录；本轮未重复验证这些项目。
- **验收状态：** Step21 整体仍未完成浏览器验收。

### 2026-09-23 自动化回归补测

- **定向回归：** 在 `frontend/` 执行 `npm test -- --run node-form-layout node-features protocol-field-editor node-check-panel nodes-view`，5 个测试文件、116 个测试通过。
- **全量回归：** 执行 `npm test -- --run`，48 个测试文件、344 个测试通过。
- **生产构建：** 执行 `npm run build`，`vue-tsc -b` 与 Vite 构建均通过。Vite 发出非阻断体积提示：`index-CUTDvqOU.js` 为 635.22 kB，超过 600 kB 阈值；未导致构建失败，本次未改代码处理。
- **浏览器边界：** 本轮未重复创建测试管理员或设置新的登录凭据。此前的隔离浏览器结果仍按上文记录；R33-01／R33-02 的浏览器缺陷未修复，自动化测试通过不等于这些实际界面问题关闭。
- **验收状态：** 自动化测试与生产构建通过；Step21 整体仍未完成，待处理 R33-01／R33-02 并完成相应浏览器复验。

---

## 二、变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-09-23 | 新建记录：登记 WireGuard peers 模式缺少条目级排序交互，并记录 Step21 浏览器验收边界。 |
| v1.1 | 2026-09-23 | 用户缩小窗口后继续进行 375px 浏览器核验，新增窄屏节点表单控件被压窄问题；同步更新精确视口验收状态。 |
| v1.2 | 2026-09-23 | 完成 920px 浏览器检查；确认 VLESS 字段布局正常、协议菜单完整显示 19 项，并在 375px／920px 复核 WireGuard peers 缺少条目级交互。 |
| v1.3 | 2026-09-23 | 补跑 Step21 定向与全量前端测试、生产构建并记录结果；测试和构建通过，记录 Vite 非阻断 chunk 体积提示，保留两个浏览器问题及未完成状态。 |
