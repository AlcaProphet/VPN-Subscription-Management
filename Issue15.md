# Issue15.md — VPN 订阅管理系统问题记录（人工测试与交叉审核补充，当前）

> **文档定位：** 本文记录 [ProdTestList.md](ProdTestList.md) 执行过程中由用户发现的 Production、浏览器和真实客户端错误；2026-09-10 起并登记文档交叉审核发现、尚未在 Issue14 独立立项的工程/验证/文档追溯问题。默认只登记发现事实，经用户明确授权分析或修复后，可在对应问题中同步根因、修复内容、自动化证据和用户人工核验结论。工程问题、既有 R28 问题和通用自动化证据仍分别由 [Issue14.md](Issue14.md) 及相关文档跟踪；交叉审核项必须在条目内注明工程主跟踪文档。
> 关联测试清单：[ProdTestList.md](ProdTestList.md)；编码约束：[AGENTS.md](AGENTS.md)；当前工程问题：[Issue14.md](Issue14.md)。

---

## 一、当前记录范围

- **创建时间：** 2026-09-09
- **记录来源：** [ProdTestList.md](ProdTestList.md) 中 PT-28-01～PT-28-05、R27、R26 和其他人工测试项目的实际执行结果；2026-09-10 起也登记文档交叉审核新发现、且尚未形成独立 Issue14 主跟踪条目的问题。
- **记录原则：** 人工测试项只登记用户实际观察到的现象、测试项目、环境、复现步骤和证据；没有实际测试证据时不创建问题结论。交叉审核项应给出代码/文档证据、主跟踪文档和后续验证入口，不把静态检查写成人工通过。
- **处理边界：** 新问题默认只记录，不自行探索或修复；用户明确授权后，可在同一问题中补充根因、修复方案、实施结果和验收证据。自动化通过不得代替 Production、浏览器或真实客户端人工核验。
- **编号约定：** 本文件新增问题使用 `R29-*` 编号；`PT-*` 继续表示 [ProdTestList.md](ProdTestList.md) 的测试项目，不与问题编号混用。
- **当前状态：** 已登记 12 条：R29-01～R29-08 来自 [ProdTestList.md](ProdTestList.md) 及实际运行核查；R29-09～R29-12 来自 2026-09-10 文档交叉审核，主跟踪分别在 Issue14 R28-06B、R28-06C、R28-05 与 Build/Docker 文档。R29-02～R29-05、R29-07、R29-08 已完成修复、自动化验证和用户人工核验；R29-01、R29-06、R29-09、R29-10 已完成工程修复与自动化验证，等待 ProdTestList 人工复验；R29-11、R29-12 仍未关闭。此前已经记录在 [Issue14.md](Issue14.md) 的 R28-01～R28-04 不重复迁移。

---

## 二、人工测试错误记录

> 后续发现错误时先追加发现记录；只有收到用户明确授权后，才在对应问题中补充根因、修复方案、实施结果或验收结论。人工测试记录必须关联一个或多个 `ProdTestList.md` 测试项目并保留原始证据位置；交叉审核登记项若没有对应人工测试项目，则必须注明工程主跟踪文档和后续复验入口。

### R29-01 Setup 新库导入被错误要求同时输入 IMPORT 和 DISABLE

- **关联测试项目：** [ProdTestList.md](ProdTestList.md) §三“Production 专项人工核查”前两项：v2 导出文件导入、新库数据引用核对
- **发现日期：** 2026-09-09
- **测试环境/版本：** 2026-09-11 本地 Docker Production，`http://localhost:8080/setup`；导入文件 `vpn-sub-config-20260911.enc`，截图由用户提供
- **现象：** 未配置且未启用高级模式的 Setup 页面使用 v2 文件导入时，输入 `IMPORT` 后提示“该导入会清空高级模式数据，请输入 DISABLE 确认”；Setup 页面没有第二确认入口，导入被阻断。
- **复现步骤：** 在未启用高级模式的状态下，使用 v2 导出文件向新库执行导入。
- **实际结果：** 后端要求 `IMPORT + DISABLE`，但 Setup 只发送 `IMPORT`，导致无法继续。早期记录中的 `RESET` 和“提示高级模式已开启”经 2026-09-11 截图核对后均不准确。
- **预期结果：** Setup 入口已经先行保证目标库 `configured=false`；该入口导入 v2 基础模式文件时只要求 `IMPORT`，不得误报会清空既有高级数据。已配置系统的管理面板覆盖导入继续保留 `IMPORT → DISABLE` 双确认。
- **根因：** `ImportV2` 仅依据导入 payload “无实例/账号且 `advanced_mode != true`”判断是否要求 `DISABLE`，没有区分 `setupMode=true` 的未配置新库与管理面板已有库。`SetupView.vue` 按原入口合同只发送 `IMPORT`，因此该 payload 在 Setup 中必然进入无法完成的第二确认死路。
- **数据库核查：** 当前 8080 Docker Production 使用独立 `/data/app-prod.db`；只读快照确认 `configured`、`advanced_mode` 均不存在，users/groups 及 xray_instances/xray_ext_accounts/xray_users/xray_ext_users/traffic_records/xray_ext_traffic 均为 0，没有任何高级模式数据可清理。导出文件可成功解密，结构为 format_version=2、advanced_mode 缺省、instances=0、accounts=0；未输出配置值或敏感密文。
- **修复内容：** `ImportV2` 的 `DISABLE` 守卫限定为 `setupMode=false`；Setup 新库仍只要求 `IMPORT`。管理面板双确认、事务内 OFF 清理、签名密钥保护、认证可用性校验和异步导入流程均保持不变。
- **回归测试：** 新增入口语义测试，覆盖 Setup 相同 v2 文件只凭 `IMPORT` 可提交、管理面板缺 `DISABLE` 仍拒绝、补 `DISABLE` 后可提交并完成。旧实现上的 Setup 分支先稳定失败，修复后三分支通过。
- **自动化证据：** 2026-09-11 定向 `go test ./internal/config -run 'TestImportV2DisableConfirmationByEntry|TestImportV2' -count=1` 通过；后端 `go test ./...`、`go build ./...`、`go vet ./...` 全部通过；前端 `npm run build` 通过；根目录 `git diff --check` 通过。
- **真实文件隔离验证：** 使用用户提供的文件和密码，在临时数据目录、独立 18080 端口启动当前代码的 Production 服务；真实 `POST /api/setup/import` 仅发送 `IMPORT` 返回 HTTP 200 与 `task_id`，异步导入完成后公开状态为 `configured=true`、`advanced_mode=false`、本地登录可用，未自动开启高级模式。临时服务随后停止并清理，当前 8080 Docker 数据卷未改动。
- **状态：** ◐ 代码修复与自动化/隔离 Production 真实文件验证通过；等待当前 Docker 镜像重建后的用户浏览器人工复验
- **历史只读复核（2026-09-10，修复前）：** 当时 `backend/internal/config/export.go:285-306` 对“v2 无实例/账号且 `advanced_mode != true`”分支强制要求 `disable_confirm_word=DISABLE`；`frontend/src/views/SetupView.vue:64-71` 的 Setup 新库导入只发送 `confirm_word=IMPORT`，没有第二步 `DISABLE` 通道，因此该场景被第二确认词阻断。`SettingsView.vue` 的面板导入已有两步流程，但 Setup 入口不同；当时 `ImportV2` 尚无入口级测试。2026-09-11 已由用户截图确认原报告中的 `RESET` 应为 `DISABLE`，并按上述范围完成修复。

> 本条不把临时 Production API 验证替代为当前 8080 Docker 浏览器人工验收；最终关闭仍以 [ProdTestList.md](ProdTestList.md) 复验结果为准。

### R29-02 不同素材池的同步任务被列表页全局状态错误阻断

- **关联测试项目：** [ProdTestList.md](ProdTestList.md) 原 §二“Issue15 问题修复后的人工复验”的 R29-02（用户确认修复后已从当前待办移除）
- **发现日期：** 2026-09-09
- **测试环境/版本：** 用户报告，具体环境和版本待补充
- **现象：** 单个素材池同步任务工作正常，但同时运行多个同步任务时，似乎不能并行执行。
- **复现步骤：** 在素材池列表启动池 A 的同步；池 A 尚未完成时点击池 B 的“同步”。
- **实际结果：** 旧前端在池 A 运行时直接提示“同步进行中，请等待完成”，池 B 的 `submitSync()` 请求不会发往后端。
- **预期结果：** 不同素材池可以分别提交并执行同步任务，各行独立显示运行状态、保存任务号并执行取消；同一素材池的重复提交仍由前后端共同阻止。
- **根因：** `PoolTab.vue` 使用单个全局 `syncingID` 和 `syncTaskID` 表示整个页面的同步状态，`doSync()` 只要发现任意池正在运行就提前返回。后端 `SubmitSync()` 的运行中检查带有 `pool_id=?`，只拒绝同一池重入；其按任务启动独立 goroutine 的实现与 Design2“同一池不并发”的契约一致，因此确定缺陷位于列表页状态粒度。原有 `pollHandles` 已按池 ID 建模，但被外围全局状态限制为实际只能跟踪一个任务。
- **修复内容：** 将列表页同步状态改为按 `poolID` 保存任务号的响应式 Map；同池本地防重保持不变，不同池可并行提交、轮询、显示和取消，任一任务结束只清理自身状态。页面加载发现后端返回 `running` 的素材池时，会读取该池最新任务号并恢复独立轮询及取消能力；组件卸载仍逐一取消所有前端轮询。后端业务逻辑未改动。
- **影响范围：** 修改仅涉及素材池列表的手动同步状态管理及相应测试；同池 HTTP 409、定时同步、单池内部 URL 顺序处理和 SQLite 单写者事务语义保持不变。允许不同池的网络拉取与解析并行，不承诺数据库写事务并行提交，也未增加全局并发上限。
- **回归测试：** 前端覆盖同池再次点击继续阻断、池 A/B 同时提交、两行独立显示“取消”、各自任务号取消、刷新/重进页面恢复运行中任务，以及卸载逐一取消轮询；后端受控双请求测试证明两个不同池可同时进入 HTTP 拉取阶段并分别完成，同时断言同池第二次提交仍返回 `ErrSyncRunning`。
- **自动化证据：** 2026-09-10 后端定向 `go test ./internal/pool ./internal/server ./internal/cron` 通过；后端 `go build ./...`、`go vet ./...`、`go test ./...` 全部通过；前端全量测试 42 个文件、223 个用例通过，`npm run build` 与根目录 `git diff --check` 通过。随后使用用户提供的 Loyalsoldier `direct-list.txt` 与 `proxy-list.txt` 执行临时数据库真实 URL 集成测试，两个不同池均成功提交并完成：前者识别为 `legacy-domain-text/common`，`accepted=111052, excluded=0, rejected=69, duplicates=47`；后者为 `legacy-domain-text/common`，`accepted=26903, excluded=0, rejected=335, duplicates=0`。受控测试继续证明两个不同池可同时进入 HTTP 拉取阶段、同池重入仍返回 `ErrSyncRunning`；后端全量测试与 `git diff --check` 再次通过。
- **人工核验：** 用户于 2026-09-10 明确确认“问题已修复”；该确认用于关闭 R29-02，不扩展为未陈述的具体设备、浏览器或 Production 环境证据。
- **证据：** 原始用户报告、只读研究结论、前后端回归测试、用户提供的两个真实测试 URL、自动化集成回执及用户修复确认。
- **状态：** ✅ 已闭环（根因已确认，代码与回归测试已修复，自动化真实 URL 验证和用户确认均通过）

> 本条区分任务层并发与 SQLite 写事务串行：修复保证不同池可同时处于运行状态并重叠执行网络拉取/解析，不改变单池 URL 顺序或数据库事务模型。

### R29-03 批量导入回执 UI 出现列错位和长文本溢出

- **关联测试项目：** [ProdTestList.md](ProdTestList.md) 原 §二“Issue15 问题修复后的人工复验”的 R29-03（真机验证通过后已从当前待办移除）
- **发现日期：** 2026-09-09
- **测试环境/版本：** 原始问题由用户提供的界面截图确认；修复后用户于 2026-09-09 确认真机验证通过，具体设备和版本未补充
- **现象：** 批量导入本身无问题，但导入回执界面出现 UI 错位：名称列内容发生异常换行，回执中的长 URI 文本横向溢出并超出弹窗内容区域，表格列与内容显示不协调。
- **复现步骤：** 执行节点批量导入并打开导入回执；具体输入和完整操作步骤待补充。
- **实际结果：** 截图中显示导入结果为 `1 成功 / 0 跳过`，但回执表格的名称列和长 URI 内容出现错位或溢出。
- **预期结果：** 批量导入回执的表头、列内容和操作区域应在弹窗范围内正确对齐；长 URI 应在界面内合理换行、截断或通过其他方式完整查看，不应破坏弹窗布局。
- **根因：** `NodesView.vue` 的回执表格使用默认 `table-layout: auto`；行号、名称和结果列的声明宽度在长 URI 形成较大最小内容宽度时被压缩，未限定宽度的说明列则把表格撑出 760px 弹窗。Ant Design Vue 单元格默认的 `overflow-wrap: break-word` 未能在该自动布局下约束连续 URI，因而同时造成名称异常换行、列宽失衡和横向溢出。手机 Drawer 继续渲染同一四列表格，问题在窄屏下更明显。
- **影响范围：** 仅影响批量导入后的逐行回执展示；服务端 `line/raw/ok/name/reason` 回执、成功/跳过统计、URI 解析、节点创建和列表刷新不受影响。
- **修复内容：** 桌面端为导入回执启用固定表格布局，稳定保留行号、名称和结果列宽度；名称、长 URI 与长失败原因使用回执局部的强制断行规则。`<768px` 改为逐条卡片展示行号、名称、结果和说明；回执区域限制为 `40vh` 并支持纵向滚动，避免大量结果挤出底部操作区。修改仅作用于导入回执，没有调整全局 Ant Design 样式或接口合同。
- **回归测试：** `nodes-view.spec.ts` 新增长 VLESS URI 与长失败原因的成功/跳过混合回执，覆盖桌面表格、手机卡片、完整文本保留和滚动容器。
- **自动化证据：** 失败回归在旧实现上稳定失败；修复后定向测试 31 个用例通过，前端全量测试 42 个文件、220 个用例通过，`npm run build` 与 `git diff --check` 通过。
- **人工验收：** 用户于 2026-09-09 确认真机验证通过；批量导入功能结果与本条 UI 验收继续分开记录。
- **证据：** 用户提供的原始批量导入回执截图、本条自动化证据及用户真机验收结论
- **状态：** ✅ 已闭环（代码修复、自动化验证和用户真机验证均通过）

> 本条区分“批量导入功能成功”和“导入回执 UI 显示异常”，不将 UI 错位写成导入失败。

### R29-04 手机宽度下主页丢失进入管理面板按钮

- **关联测试项目：** [ProdTestList.md](ProdTestList.md) §二“Issue15 问题修复后的人工复验”的 R29-04
- **发现日期：** 2026-09-09
- **测试环境/版本：** 本地页面 `http://127.0.0.1:8080/`，416×928 浏览器视口；真实手机与 Production 构建核验仍由 [ProdTestList.md](ProdTestList.md) 保留
- **现象：** 在手机宽度（≤430px）下，当前主页丢失进入管理面板的按钮。
- **复现步骤：** 使用不超过 430px 的手机宽度打开当前主页；具体设备、浏览器和操作步骤待补充。
- **实际结果：** 主页未显示进入管理面板的按钮。
- **预期结果：** 手机宽度下主页仍应提供可见、可操作的进入管理面板入口。
- **根因：** `HomeView.vue` 已正确传入 `manageBtn`，管理员判断和 `/admin` 路由也正常；`AppHeader.vue` 却给管理入口添加了 `hidden md:inline-flex`，使其在 `<768px` 时变为 `display:none`，而账户菜单中又没有等价入口。该隐藏规则来自 Build11 Step 4 的手机顶栏精简，不是权限、数据或路由故障。
- **影响范围：** 仅影响在用户主页显示管理入口的管理员；普通用户、管理页自身、后端权限和管理路由不受影响。
- **修复内容：** 移除管理入口的手机端隐藏规则，使用同一个按钮在所有宽度下进入 `/admin`；手机端以 `min-h-11` 保持 44px 触控高度，`md` 起以 `md:min-h-0` 保留桌面端默认高度，并增加 `items-center justify-center` 保证文案在按钮内水平、垂直居中。
- **回归测试：** 新增 `app-header.spec.ts`，覆盖管理员入口不再移动端隐藏、44px 触控高度、横纵居中类、`/admin` 跳转，以及普通用户或 `manageBtn=false` 时不显示。
- **自动化证据：** 定向测试 2 个用例通过；前端全量测试 42 个文件、219 个用例通过；`npm run build` 与精确路径 `git diff --check` 通过。
- **人工核验：** 用户于 2026-09-09 在 416×928 浏览器视口明确确认管理面板按钮已显示且文案对齐，问题已修复；本结论不替代后续真实手机/Production 核验。
- **状态：** ☑ 已完成（根因已确认，代码与回归测试已修复，自动化验证和用户浏览器核验均通过）

### R29-05 节点条件表单重复渲染输入框且下拉元数据文案粘连

- **关联测试项目：** [ProdTestList.md](ProdTestList.md) §五“R27 后续人工核查”的 R27-05 额外尺寸与交互边界
- **发现日期：** 2026-09-09
- **测试环境/版本：** 本地浏览器 `http://localhost:8080/admin/nodes`；节点编辑弹窗；用户通过浏览器标记指出重复输入框和选项文案显示问题
- **现象：** 节点编辑表单的“传输”“安全”“Flow”“客户端指纹”等推荐值字段同时出现可编辑下拉和额外普通输入框；数字字段和列表字段也会额外出现普通输入框。下拉选项的显示名称、验证来源和分组文案缺少明确分隔，例如连成 `无mihomo-1.19.29常用`。
- **复现步骤：** 打开管理端节点页面，编辑手动节点并查看“连接方式与当前参数”及高级参数中的推荐下拉、数字或列表字段；展开推荐下拉观察选项名称、验证来源和分组文案。
- **实际结果：** 非敏感的专用字段控件后又渲染一个普通输入框，允许绕过原字段的枚举或类型入口；选项元数据视觉和可访问文本粘连，语义不清。
- **预期结果：** 每个标量字段只能渲染所属类型的唯一编辑入口；推荐下拉、数字和列表字段不得附带额外普通输入框。选项验证来源与分组应使用可读名称和明确分隔，例如 `Mihomo 1.19.29 · 常用`。
- **根因：** `ProtocolFieldEditor.vue` 的第一条 `v-if / v-else-if` 链负责选择密码、数字、可编辑下拉、普通下拉或列表控件，但凭据状态文案使用了独立的 `v-if="sensitive"`，使后续长文本和普通输入框的 `v-else-if / v-else` 绑定到凭据文案。对所有非敏感字段，第二条条件链都会落入普通输入框分支。`EditableCombobox.vue` 又将选项名称、验证来源和分组拆成仅靠外边距区分的相邻文本，未提供可读名称和文本分隔。
- **影响范围：** 重复控件不仅造成视觉错误，还可能让下拉限制被普通输入框绕过、列表值被字符串覆盖、数字字段收到字符串值，从而引发草稿类型异常或保存校验失败；普通短文本和敏感字段不受重复控件影响。文案问题影响推荐项辨识和辅助技术读取。
- **修复内容：** `ProtocolFieldEditor.vue` 将密码控件与凭据状态文案包装进同一个敏感字段模板，使数字、推荐下拉、普通下拉、列表、长文本和普通文本恢复为唯一且连续的互斥渲染链；`EditableCombobox.vue` 将 Mihomo 验证来源转换为可读形式，并以 `·` 明确分隔验证来源和中文分组。
- **回归测试：** `protocol-field-editor.spec.ts` 增加推荐下拉、数字、非空列表和空列表的输入入口数量断言；`editable-combobox.spec.ts` 增加 `Mihomo 1.19.29 · 常用` 的可读文案与分隔断言。
- **自动化证据：** 定向前端测试 2 个文件、33 个用例通过；前端全量测试 41 个文件、212 个用例通过；`npm run build` 与 `git diff --check` 通过。
- **人工核验：** 用户于 2026-09-09 明确确认问题已修复。
- **状态：** ☑ 已完成（根因已确认，代码与回归测试已修复，自动化验证和用户人工核验均通过）

> 本条区分旧运行资源中的原始复现证据、代码自动化验证和用户最终人工核验；完成状态以用户本次明确确认的人工核验结果为准。

### R29-06 节点推荐字段缺少标准选择框语义并伴随数字/列表误写

- **关联测试项目：** [ProdTestList.md](ProdTestList.md) §五“R27 后续人工核查”的 R27-05 额外尺寸与交互边界
- **发现日期：** 2026-09-09
- **测试环境/版本：** 本地浏览器 `http://localhost:8080/admin/nodes`；用户通过浏览器标记指出推荐字段应使用与协议字段一致的标准下拉框，并确认“其他（自定义）”后显示独立内联表单
- **现象：** “传输”“安全”等推荐字段原由原生输入框承载，缺少下拉箭头和明确的整框展开语义；首轮改为标准 Select 后，用户真实运行进一步发现其关闭态因始终启用 `show-search` 而仍呈现输入式语义，未与“协议”的按钮式关闭态完全一致。同步检查还发现 ALPN 推荐项仅为不可点击文字、可选数字未设置时显示为 `0`、WireGuard `int-list` 新增空项立即因 `Number('')` 写成 `0`。
- **预期结果：** 推荐字段使用与“协议”一致的标准按钮式单值下拉，整框可展开；当前候选数量有限，不启用搜索输入。显式允许自定义时提供“其他（自定义）”和独立应用/取消草稿。列表推荐项可直接追加，自定义列表项明确应用；未设置数字保持为空，整数列表输入合法值前不写入。
- **根因：** `EditableCombobox.vue` 自行实现原生输入和绝对定位候选层，选择、搜索和自定义录入语义混合；`ProtocolFieldEditor.vue` 将可选数字以 `?? 0` 强制回退，并在整数列表新增空字符串后立即执行 `Number('')`。页面此前只跟踪未应用 JSON，无法阻止未应用自定义或列表草稿进入保存/检查流程。
- **修复内容：** `EditableCombobox` 改为复用 `AppSelect` 的标准单值选择结构，并按用户对有限候选的决策完全停用搜索输入层，使关闭和展开状态均与“协议”采用相同的按钮式 Select；“其他”使用仅存在于前端的内部值，独立输入需应用后才一次性回写，旧自定义值原样回填。递归字段编辑器区分未设置与显式 0，整数和自定义列表新增先进入草稿，ALPN 等推荐项使用标准添加下拉；节点页统一跟踪控件草稿并阻止保存与目标检查，分支清空或取消编辑时丢弃草稿。后端 schema、数据库和输出合同未改变。
- **回归测试：** 覆盖标准 Select/箭头/整框展开、候选选择、`allow_custom=false`、空值候选、自定义应用/取消/旧值回显、可选数字未设置与显式 0、整数列表不自动生成 0、列表推荐/自定义追加、保存与目标检查阻断。
- **自动化证据：** 2026-09-10 在当前 `beta` 基线复测，定向前端测试 4 个文件、71 个用例通过；前端全量测试 42 个文件、220 个用例通过；`npm run build`、后端 `go build ./...`、`go vet ./...`、`go test ./...` 与根目录 `git diff --check` 通过。
- **人工核验：** 待用户按真实运行环境执行；自动化结果不替代浏览器、移动端、主题及实际保存/重开核验。
- **状态：** ◐ 代码修复与自动化验证完成，等待用户真实运行核验

> 本条按用户决定保留真实运行核验边界；在用户回报前不得标记人工验收通过或完整闭环。

### R29-07 节点分支清空提示脱离表单且 Select 切换导致弹窗滚动跳顶

- **关联测试项目：** [ProdTestList.md](ProdTestList.md) §五“R27 后续人工核查”的 R27-05 焦点与交互边界
- **发现日期：** 2026-09-10
- **测试环境/版本：** 本地浏览器 `http://localhost:8080/admin/nodes`；用户通过浏览器标记指出清空提示的位置和选择框切换后的页面位移
- **现象：** 切换协议、传输、安全方式或插件后，清空说明以页面顶部的短暂全局消息展示，没有持久归属于“新建节点/编辑节点”模块；在弹窗中下部切换任意 Select 选项时，弹窗会被自动拖回顶部，打断连续编辑。
- **预期结果：** 清空规则在节点表单标题下方持久展示，不再因每次切换产生顶部动态消息；正常选择选项不改变用户当前滚动位置，Escape 关闭浮层仍正确回到触发控件且不触发滚动。
- **根因：** `NodesView.vue` 的 `warnResetScope()` 将本应长期可见的风险说明统一交给全局 `Notify.warning()`；`overlayManager.unregisterOverlay()` 又把“注销浮层”和“恢复旧焦点”错误绑定，导致 Select 正常关闭时也聚焦打开前记录的元素。真实浏览器测量中，弹窗 `.ant-modal-wrap.scrollTop` 从 `311` 跳到 `0`，焦点同时被送到顶部的“关闭新建节点”按钮。
- **修复内容：** 节点表单在冲突提示之后、基本信息之前增加持久 warning Alert，并移除协议/分支切换的清空 Toast，清空参数、凭据、扩展、草稿和 `reset_scopes` 合同保持不变。全局浮层管理改为普通注销只移除记录，Escape 路径才显式回焦；程序化聚焦统一使用 `preventScroll`。`AppSelect` 提供自身输入作为 Escape 回焦目标，并移除当前 Ant Design Vue Select 未声明的冗余 `openChange` 监听；Modal、Drawer 和确认框的合法回焦同步防止滚动。
- **影响范围：** 提示变更仅作用于节点新建/编辑表单；焦点修复位于全局浮层基础设施，覆盖 `AppSelect`、Dropdown、Popover、日期/时间选择器及 Modal/Drawer 的关闭路径。后端、数据库、协议 schema、节点保存和目标输出合同未改变。
- **回归测试：** `nodes-view.spec.ts` 覆盖持久清空说明、协议/网络切换继续清空且不再调用动态消息；`overlay-manager.spec.ts` 覆盖普通注销不回焦、Escape 仅回焦一次并携带 `preventScroll`。原节点控件和浮层回归继续纳入全量前端测试。
- **自动化证据：** 2026-09-10 定向前端测试 3 个文件、47 个用例通过；前端全量测试 42 个文件、221 个用例通过；`npm run build` 与根目录 `git diff --check` 通过。构建仅保留既有的大 chunk 提示。
- **人工核验：** 用户于 2026-09-10 明确确认两个问题均已修复；该确认覆盖节点表单内持久提示和选择框切换不再拖动页面。
- **状态：** ☑ 已完成（根因已确认，代码与回归测试已修复，自动化验证和用户人工核验均通过）

> 本条人工结论仅覆盖用户本次确认的两个浏览器问题，不替代 R29-06 全协议控件语义、移动端、主题、保存重开等仍待执行的人工范围。

### R29-08 素材池同步取消请求成功但任务继续运行

- **关联测试项目：** [ProdTestList.md](ProdTestList.md) 原 R29-02 素材池同步并发行为复验期间发现的取消分支问题；修复并经用户确认后未加入当前待办。
- **发现日期：** 2026-09-10
- **测试环境/版本：** 用户在素材池同步实际运行测试中发现；具体设备、浏览器和 Production 版本未补充。
- **现象：** 素材池开始同步后点击“取消”，界面提示正在取消，但任务仍继续执行并最终完成。
- **预期结果：** 取消接口只有在对应运行中任务已被可靠地标记为取消终态时才返回成功；下载、解析和后续写入应尽快停止，已取消任务不得再被工作线程覆盖为成功，同池随后应可重新同步。
- **根因：** `CancelSync()` 原实现只调用任务的 `CancelCauseFunc`；下载请求能够收到取消信号，但 `runSyncTask()`、`finishTask()` 和 `failTask()` 随后继续使用同一个已取消 context 写失败快照及任务/池终态，SQLite 操作返回 `context canceled`。受控慢响应稳定复现了“取消接口成功，但任务仍为 `running`、`error` 为空且 `finished_at` 未设置”。同时原终态更新没有 `status='running'` 条件，缺少取消与正常完成之间的原子胜负规则。
- **修复内容：** 提交任务时在同一事务写入池级 `running` 状态。取消入口先发送取消信号以中断网络读取和等待中的写入，再通过事务将仍为 `running` 的任务及所属池原子落为 `failed/同步任务已取消`；若工作线程已先完成则取消不再虚假成功。工作线程在来源处理前后、响应体读取失败、解析失败及写入前检查取消原因；任务终态使用独立 5 秒 context 写入，并通过 `WHERE status='running'` 条件更新保证已取消终态不可被成功/失败结果覆盖。取消后的同池任务可重新提交。
- **影响范围：** 修改集中于素材池同步任务的提交状态、取消、终态写入与取消检查；API 路径、前端按钮合同、同步结果 schema、同池防重、跨池并发、定时同步及 SQLite 单写者模型不变。取消发生前已经完成并提交的独立事务不能被追溯撤销，但取消信号发出后等待中的网络、解析后写入及最终状态不会继续冒充成功。
- **回归测试：** 新增受控慢响应取消测试，覆盖取消终态持久化、`finished_at`、取消错误文本、工作线程退出、模拟迟到成功写入不可覆盖，以及取消后同池重新提交；取消/完成竞态与跨池并发组合连续运行 20 轮。新增可选真实 URL 取消集成测试，使用 `R29_CANCEL_URL` 启用，普通全量测试不依赖外网。
- **自动化证据：** 2026-09-10 使用用户此前提供的 Loyalsoldier `direct-list.txt`，任务启动 200ms 后取消，验证终态为 `failed/同步任务已取消`、后台线程退出且首次同步未激活快照；测试使用临时 SQLite 数据库。后端 `go build ./...`、`go vet ./...`、`go test ./...` 全部通过；前端全量测试 42 个文件、223 个用例通过，`npm run build` 与根目录 `git diff --check` 通过。构建仅保留既有的大 chunk 提示。
- **人工核验：** 用户于 2026-09-10 明确确认测试通过；该确认覆盖本条“开始同步后执行取消”的修复结果，不扩展为未陈述的具体环境证据。
- **证据：** 用户原始问题报告、受控慢响应失败复现日志、取消/完成竞态回归、真实 URL 自动化取消结果及用户测试通过确认。
- **状态：** ✅ 已闭环（根因、代码修复、自动化验证和用户实际运行确认均完成）

> 本条与 R29-02 分开记录：R29-02 解决不同素材池被前端全局状态阻断；R29-08 解决单个运行中任务的后端取消与终态竞态。

---

## 三、文档交叉审核补充登记（2026-09-10）

> 以下条目由文档交叉审核发现，不是 [ProdTestList.md](ProdTestList.md) 的人工测试记录；工程实施与关闭仍以 Issue14、Build22、Build25 为准。未获授权修复前只记录事实、证据与复验入口。

### R29-09 WireGuard peers 高级 JSON 未放行内部 `_credential_id`

- **来源：** 2026-09-10 文档交叉审核（Build25 R28-06B / Issue14 R28-06 交叉核验）。
- **现象：** 编辑 WireGuard 节点时，`peers` 高级 JSON 默认包含内部稳定标识 `_credential_id`；点击“应用”会报 `字段 peers[0]._credential_id 未在协议注册表中声明`，随后保存和目标检查被前端校验阻断。结构化编辑路径不受影响。
- **证据：** `frontend/src/components/ProtocolFieldEditor.vue` 的 `validateFixedObjectProperties()` 只把 `field.properties` 的名称加入 known；`backend/internal/node/node.go` 与 `project.go` 显式放行 `field.ItemIDField`；`backend/internal/node/registry.go` 中 WireGuard `peers` 的 `ItemIDField` 为 `_credential_id`；节点读取响应会保留该键。
- **影响：** WireGuard peers 高级 JSON 无法正确应用；多 Peer、重排或需要直接编辑 Peer 列表时尤为明显。
- **预期：** 前端 known 集合加入 `field.item_id_field`，列表项应用/保存保留该内部标识且不进入客户端产物；补单测及多 Peer/重排回归。
- **修复结果（2026-09-10）：** `ProtocolFieldEditor.vue` 新增 `knownFieldNames()`，将 `properties` 与 `item_id_field` 统一纳入已知字段；WireGuard `peers._credential_id` 可原样应用/保存/检查。新增多 Peer 重排保留 ID、固定对象其他未知键仍拒绝、`NodesView` 保存集成回归；后端定向/全量/build/vet、前端定向/全量/build、Docker build、Production smoke 与 `git diff --check` 通过。目标输出仍按 schema 剥离内部 ID。
- **工程主跟踪：** [Issue14.md](Issue14.md) R28-06B / [Build25.md](docs/reports/Build/Build25.md)。
- **人工复验入口：** [ProdTestList.md](ProdTestList.md) `R28-06` §B 的 WireGuard peers 高级 JSON 项。
- **状态：** ☑ 已修复 / ☑ 自动化回归通过 / ◐ 人工复验入口保留在 ProdTestList，等待用户执行

### R29-10 高级 JSON 多草稿保存定位排序未统一

- **来源：** 2026-09-10 文档交叉审核（Build25 R28-06C / Issue14 R28-06 交叉核验）。
- **现象/证据：** `NodesView.vue` 保存时使用 `[...unappliedJsonPaths][0]`，即 Set 插入序；父对象阻断与后代定位使用“路径长度 + 字典序”的稳定排序。Build25 原文档曾称保存/检查/父阻断均复用稳定排序，实际不一致；多草稿保存定位没有专项测试。另：条件隐藏清理有代码路径，但缺少专项回归测试。
- **影响：** 多个后代 JSON 草稿并存时，保存定位顺序可能与提示和预期不一致；不造成数据丢失。
- **预期：** 统一使用稳定路径排序 helper，或明确接受插入序并修正文档；补多草稿保存定位与条件隐藏清理回归。
- **修复结果（2026-09-10）：** `NodesView.save()` 改为使用 `sortedUnappliedJsonPaths()[0]`，与父阻断/检查定位统一稳定排序；新增多草稿保存定位回归。`ProtocolFieldEditor` 增加模型值快照保护，父级因无关参数变化替换对象但内容不变时不清除仍有效 JSON 草稿；新增条件隐藏清理、保留无关有效草稿、折叠及组件卸载边界回归。后端定向/全量/build/vet、前端定向 5 文件/89 用例、全量 42 文件/259 用例/build、Docker build、Production smoke 与 `git diff --check` 通过。
- **工程主跟踪：** [Issue14.md](Issue14.md) R28-06C / [Build25.md](docs/reports/Build/Build25.md)。
- **人工复验入口：** [ProdTestList.md](ProdTestList.md) `R28-06` §C 的多草稿/条件隐藏项。
- **状态：** ☑ 已修复（采用稳定路径排序方案 A） / ☑ 自动化回归通过 / ◐ 人工复验入口保留在 ProdTestList，等待用户执行

### R29-11 Build22 Step 7 专属自动化证据矩阵缺失

- **来源：** 2026-09-10 文档交叉审核（Build22 / Issue14 R28-05）。
- **现象：** Build22 Step 1～6、8～10 的代码与测试声明成立，Step 11 的后端/前端全量、Docker build 与 Production smoke 门禁真实通过；但 Step 7 计划列出的专属自动化测试未落地，Step 11 不能声明 D3-1～D3-10 全部验收通过，Build22/Design3 不能归档。
- **已知缺口：** `ActivatePending`/`DiscardPending` 与 `activated_at` 语义；`SanitizeStoredSyncOutputs` 幂等/非破坏性清洗；`/sync/status`、`/sync/tasks`、`Pool.sync_error` 读时脱敏 raw JSON；`NormalizeDiagnostics` 19+1 与 200 rune 限额；v1 `rule_counts` 分项合计不变量、`previous_active` 比较、旧 stats `version 0` 解析；`latest_failed` 恢复与同时间戳 ID 排序；failed 快照写失败时 active/pending 不变、不虚报 snapshot ID；旧字符串数组 Clash plan 下载重渲染回退。
- **影响：** D3 工程闭环缺少可回归证据；不影响当前代码可构建/可运行，但不能按 Build22 自身验收标准关闭。
- **预期：** 按 Build22 Step 7 测试清单补齐自动化，重新执行 Step 11 门禁，再关闭 R28-05。
- **工程主跟踪：** [Issue14.md](Issue14.md) R28-05 / [Build22.md](Build22.md)。
- **人工复验入口：** 无（属于自动化证据补齐；完成后由 Step 11 门禁验证）。
- **状态：** ☐ 待补测试

### R29-12 R29-04 附带 Dockerfile Node 版本变更未登记

- **来源：** 2026-09-10 文档交叉审核。
- **现象：** 提交 `f8d7474`（R29-04 修复）除 `AppHeader.vue` 与测试外，还把 `Dockerfile` 前端构建镜像从 `node:22-alpine` 改为 `node:24-alpine`；Issue15 R29-04 记录和 ProdTestList 未记载该变更及原因。
- **影响：** 变更追溯不完整；Node 大版本升级可能影响构建、CI 或运行时行为，需要确认是否有意。
- **预期：** 确认该变更是否有意；如是，补记版本升级理由与验证边界；如否，另行决策回滚。该项不涉及业务运行时逻辑。
- **工程主跟踪：** Issue15 R29-04 / `Dockerfile`。
- **人工复验入口：** 无直接入口；如确认保留，可补构建/镜像验证记录。
- **状态：** ☐ 待确认/文档补记

---

## 四、与 ProdTestList 的同步约定

1. `ProdTestList.md` 继续记录测试是否执行、结果和测试证据。
2. 发现错误时，在 `ProdTestList.md` 对应项目的“结果或备注”中引用本文件的问题编号，例如 `R29-01`。
3. 在本文件对应问题中反向引用具体测试项目，例如 `PT-28-05` 或 `R27-03`。
4. 新问题默认只记录发现事实；根因探索、修复实施和修复验收须由用户明确授权，授权后可同步回对应问题。
5. 没有错误的测试项目只更新 `ProdTestList.md` 的人工结果，不在本文件新增问题。
6. 交叉审核登记项没有对应 `ProdTestList` 测试项目的，应在条目中注明工程主跟踪文档；后续人工复验动作再补测试项目关联。

---

## 五、变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-09-09 | 新建 Issue15，作为 ProdTestList 人工测试发现错误的只记录入口；当前未登记新的人工测试错误，不探索、不修复。 |
| v1.1 | 2026-09-09 | 根据用户人工测试报告登记 R29-01：未启用高级模式时 v2 数据导入被要求同时输入 IMPORT 和 RESET，并提示高级模式已开启，导致数据无法导入；本次仅记录。 |
| v1.2 | 2026-09-09 | 根据用户人工测试报告登记 R29-02：单个素材池同步正常，但多个同步任务似乎不能同时运行；本次仅记录，不确认根因。 |
| v1.3 | 2026-09-09 | 根据用户人工测试截图登记 R29-03：批量导入成功但导入回执 UI 出现列错位和长文本溢出；本次仅记录，不确认根因。 |
| v1.4 | 2026-09-09 | 根据用户人工测试报告登记 R29-04：手机宽度（≤430px）下当前主页丢失进入管理面板按钮；本次仅记录，不确认根因。 |
| v1.5 | 2026-09-09 | 登记并闭环 R29-05：节点条件表单重复输入框与下拉元数据文案粘连；记录已确认根因、组件和测试修复、前端全量自动化证据，以及用户人工核验通过。同步明确 Issue15 在用户授权后可记录问题闭环，但自动化不得代替人工验收。 |
| v1.6 | 2026-09-09 | 登记并实施 R29-06：推荐字段改为标准下拉 + “其他（自定义）”独立草稿，同步修复可选数字和列表项误写、列表推荐入口及页面级草稿阻断；代码与自动化验证完成，真实运行核验保留给用户。 |
| v1.7 | 2026-09-09 | 闭环 R29-04：确认 Build11 手机顶栏隐藏规则导致管理入口不可达；恢复手机端按钮，保留 44px 触控高度和桌面端原高度，补齐文案居中与回归测试；前端全量测试、构建及用户 416×928 浏览器核验通过，真实手机/Production 核验仍在 ProdTestList 保留。 |
| v1.8 | 2026-09-09 | 闭环 R29-03：确认自动表格布局被长 URI 最小内容宽度撑开；桌面回执改为固定布局和局部强制断行，手机改为逐条卡片，并限制回执区域高度；失败优先回归、前端全量测试、构建及用户真机验证通过。 |
| v1.9 | 2026-09-10 | 根据 R29-06 真实运行反馈继续收口推荐字段：当前候选数量有限，完全停用搜索输入层，使关闭和展开状态均采用与“协议”一致的按钮式 Select；保持候选、自定义草稿和数据合同不变。 |
| v1.10 | 2026-09-10 | 登记并闭环 R29-07：节点清空规则改为表单内持久提示，移除分支切换顶部动态消息；分离全局浮层注销与回焦并统一使用 `preventScroll`，修复 Select 正常选值后弹窗滚动跳顶。记录定向/全量前端测试、生产构建及用户浏览器人工核验通过。 |
| v1.11 | 2026-09-10 | 修复 R29-02：素材池列表同步状态改为按池隔离，支持不同池并发提交、独立轮询/取消和页面重进恢复；补充前端状态回归与后端真实并发契约测试，自动化门禁通过，按用户要求在真实链接测试前停止。 |
| v1.12 | 2026-09-10 | 闭环 R29-02：补充用户提供的 direct-list/proxy-list 真实 URL 自动化集成结果和解析计数，并记录用户明确确认问题已修复；从 ProdTestList 当前待办移除，不将确认扩展为未陈述的环境证据。 |
| v1.13 | 2026-09-10 | 登记并闭环 R29-08：修复素材池取消接口成功但任务继续运行的问题；取消终态改为原子持久化，工作线程终态使用独立 context 和条件更新，补充受控竞态及真实 URL 取消测试，并记录用户实际运行测试通过。 |
| v1.14 | 2026-09-10 | 新增“文档交叉审核补充登记”章节：R29-09 登记 Build25/R28-06B WireGuard `peers._credential_id` 高级 JSON 白名单缺口；R29-10 登记多草稿保存定位排序未统一与条件隐藏清理证据缺口；R29-11 登记 Build22 Step 7 自动化证据矩阵缺失；R29-12 登记 R29-04 附带 Dockerfile Node 22→24 变更未记录。同步更新记录范围、当前状态与 ProdTestList 同步约定；本轮只改文档，未修改任何代码。 |
| v1.15 | 2026-09-10 | 闭环 R29-09/R29-10 工程部分：`knownFieldNames()` 放行 `item_id_field`；保存定位统一稳定排序；补条件隐藏清理、保留无关有效草稿、折叠/组件卸载边界回归。重新执行后端定向 4 包/全量/build/vet、前端定向 5 文件/89 用例、全量 42 文件/259 用例/build、Docker build、Production smoke 与 `git diff --check`；R28-06 人工复验项仍保留 ProdTestList，未标记通过。 |
| v1.16 | 2026-09-11 | 修复 R29-01：用户截图确认第二确认词为 DISABLE；核验实际 v2 文件与当前未配置 Docker 数据库均无高级数据。将 DISABLE 守卫限定为管理面板导入，新增 Setup/管理面板三分支失败优先回归；后端定向/全量/build/vet、前端 build 和 diff-check 通过，并以临时 Production 服务和用户真实文件验证仅 IMPORT 即可导入且高级模式保持关闭；当前 8080 镜像重建后的浏览器复验仍保留 ProdTestList。 |
