# Issue15.md — VPN 订阅管理系统 Production 人工测试问题记录（当前）

> **文档定位：** 本文记录 [ProdTestList.md](ProdTestList.md) 执行过程中由用户发现的 Production、浏览器和真实客户端错误；默认只登记发现事实，经用户明确授权分析或修复后，可在对应问题中同步根因、修复内容、自动化证据和用户人工核验结论。工程问题、既有 R28 问题和通用自动化证据仍分别由 [Issue14.md](Issue14.md) 及相关文档跟踪。
> 关联测试清单：[ProdTestList.md](ProdTestList.md)；编码约束：[AGENTS.md](AGENTS.md)；当前工程问题：[Issue14.md](Issue14.md)。

---

## 一、当前记录范围

- **创建时间：** 2026-09-09
- **记录来源：** [ProdTestList.md](ProdTestList.md) 中 PT-28-01～PT-28-05、R27、R26 和其他人工测试项目的实际执行结果。
- **记录原则：** 只登记用户实际观察到的现象、测试项目、环境、复现步骤和证据；没有实际测试证据时不创建问题结论。
- **处理边界：** 新问题默认只记录，不自行探索或修复；用户明确授权后，可在同一问题中补充根因、修复方案、实施结果和验收证据。自动化通过不得代替 Production、浏览器或真实客户端人工核验。
- **编号约定：** 本文件新增问题使用 `R29-*` 编号；`PT-*` 继续表示 [ProdTestList.md](ProdTestList.md) 的测试项目，不与问题编号混用。
- **当前状态：** 已登记 5 条来自 [ProdTestList.md](ProdTestList.md) 及节点表单浏览器核查的人工测试问题：R29-01～R29-05；其中 R29-05 已完成修复、自动化验证和用户人工核验，R29-01～R29-04 仍保持原状态。此前已经记录在 [Issue14.md](Issue14.md) 的 R28-01～R28-04 不重复迁移。

---

## 二、人工测试错误记录

> 后续发现错误时先追加发现记录；只有收到用户明确授权后，才在对应问题中补充根因、修复方案、实施结果或验收结论。每条记录必须关联一个或多个 `ProdTestList.md` 测试项目，并保留原始证据位置。

### R29-01 未启用高级模式时数据导入被要求同时输入 IMPORT 和 RESET

- **关联测试项目：** [ProdTestList.md](ProdTestList.md) §三“Production 专项人工核查”前两项：v2 导出文件导入、新库数据引用核对
- **发现日期：** 2026-09-09
- **测试环境/版本：** 用户报告，具体环境和版本待补充
- **现象：** 当前项目在未启用高级模式的情况下执行数据导入时，界面要求同时输入 `IMPORT` 和 `RESET`，并提示高级模式已开启。
- **复现步骤：** 在未启用高级模式的状态下，使用 v2 导出文件向新库执行导入。
- **实际结果：** 导入流程要求同时输入 `IMPORT` 和 `RESET`，并提示高级模式已开启，导致数据无法导入。
- **预期结果：** 未启用高级模式时，v2 导出文件应能够按照正常导入流程处理，不应出现高级模式已开启的提示，也不应因该提示要求同时输入 `IMPORT` 和 `RESET`。
- **证据：** 当前用户报告；尚未附加截图、日志或导入响应。
- **状态：** ☐ 待后续处理（本条仅记录，未进行问题探索或修复）

> 本条仅记录当前人工测试观察到的现象，不包含根因判断、修复方案或验收结论。

### R29-02 素材池同步任务似乎不能同时运行

- **关联测试项目：** [ProdTestList.md](ProdTestList.md) §二“Issue15 问题修复后的人工复验”的 R29-02
- **发现日期：** 2026-09-09
- **测试环境/版本：** 用户报告，具体环境和版本待补充
- **现象：** 单个素材池同步任务工作正常，但同时运行多个同步任务时，似乎不能并行执行。
- **复现步骤：** 待补充
- **实际结果：** 多个同步任务的并行执行情况异常，具体表现待补充。
- **预期结果：** 在系统允许并行同步的前提下，多个同步任务应能按各自状态独立执行并返回结果；具体并发语义待后续确认。
- **证据：** 当前用户报告；尚未附加截图、日志或任务状态记录。
- **状态：** ☐ 待后续处理（本条仅记录，未进行问题探索或修复）

> 本条保留“似乎”表述，不将并发限制写成已确认结论。

### R29-03 批量导入回执 UI 出现列错位和长文本溢出

- **关联测试项目：** [ProdTestList.md](ProdTestList.md) §二“Issue15 问题修复后的人工复验”的 R29-03
- **发现日期：** 2026-09-09
- **测试环境/版本：** 用户提供的界面截图，具体环境和版本待补充
- **现象：** 批量导入本身无问题，但导入回执界面出现 UI 错位：名称列内容发生异常换行，回执中的长 URI 文本横向溢出并超出弹窗内容区域，表格列与内容显示不协调。
- **复现步骤：** 执行节点批量导入并打开导入回执；具体输入和完整操作步骤待补充。
- **实际结果：** 截图中显示导入结果为 `1 成功 / 0 跳过`，但回执表格的名称列和长 URI 内容出现错位或溢出。
- **预期结果：** 批量导入回执的表头、列内容和操作区域应在弹窗范围内正确对齐；长 URI 应在界面内合理换行、截断或通过其他方式完整查看，不应破坏弹窗布局。
- **证据：** [用户提供的批量导入回执截图](/var/folders/wb/5y2c_tcs37x1_z5bb99wpd500000gn/T/codex-clipboard-486b56e0-e178-4fab-b09c-1c14cd52ecee.png)
- **状态：** ☐ 待后续处理（本条仅记录，未进行问题探索或修复）

> 本条区分“批量导入功能成功”和“导入回执 UI 显示异常”，不将 UI 错位写成导入失败。

### R29-04 手机宽度下主页丢失进入管理面板按钮

- **关联测试项目：** [ProdTestList.md](ProdTestList.md) §二“Issue15 问题修复后的人工复验”的 R29-04
- **发现日期：** 2026-09-09
- **测试环境/版本：** 用户报告，具体环境和版本待补充
- **现象：** 在手机宽度（≤430px）下，当前主页丢失进入管理面板的按钮。
- **复现步骤：** 使用不超过 430px 的手机宽度打开当前主页；具体设备、浏览器和操作步骤待补充。
- **实际结果：** 主页未显示进入管理面板的按钮。
- **预期结果：** 手机宽度下主页仍应提供可见、可操作的进入管理面板入口。
- **证据：** 当前用户报告；尚未附加截图、录屏或浏览器信息。
- **状态：** ☐ 待后续处理（本条仅记录，未进行问题探索或修复）

> 本条仅记录移动端主页入口缺失，不推断其与复杂表单 Drawer 行为的关系。

### R29-05 节点条件表单重复渲染输入框且下拉元数据文案粘连

- **关联测试项目：** [ProdTestList.md](ProdTestList.md) §五“R27 后续人工核查”的 R27-05 额外尺寸与交互边界
- **发现日期：** 2026-09-09
- **测试环境/版本：** 本地浏览器 `http://localhost:8080/admin/nodes`；节点编辑弹窗；用户通过浏览器标记指出重复输入框和选项文案显示问题
- **现象：** 节点编辑表单的“传输”“安全”“Flow”“客户端指纹”等推荐值字段同时出现可编辑下拉和额外普通输入框；数字字段和列表字段也会额外出现普通输入框。下拉选项的显示名称、验证来源和分组文案缺少明确分隔，例如连成 `无mihomo-1.19.29常用`。
- **复现步骤：** 打开管理端节点页面，编辑手动节点并查看“连接方式与当前参数”及高级参数中的推荐下拉、数字或列表字段；展开推荐下拉观察选项名称、验证来源和分组文案。
- **实际结果：** 非敏感的专用字段控件后又渲染一个普通输入框，允许绕过原字段的枚举或类型入口；选项元数据视觉和可访问文本粘连，语义不清。
- **预期结果：** 每个标量字段只能渲染所属类型的唯一编辑入口；推荐下拉、数字和列表字段不得附带额外普通输入框。选项验证来源与分组应使用可读名称和明确分隔，例如 `Mihomo 1.19.29 · 常用`。
- **根因：** `ProtocolFieldEditor.vue` 的第一条 `v-if / v-else-if` 链负责选择密码、数字、可编辑下拉、普通下拉或列表控件，但凭据状态文案使用了独立的 `v-if="sensitive"`，使后续长文本和普通输入框的 `v-else-if / v-else` 绑定到凭据文案。对所有非敏感字段，第二条条件链都会落入普通输入框分支。`EditableCombobox.vue` 又将选项名称、验证来源和分组拆成仅靠外边距区分的相邻文本，未提供可读名称和文本分隔。
- **影响范围：** 重复控件不仅造成视觉错误，还可能让下拉限制被普通输入框绕过、列表值被字符串覆盖、数字字段收到字符串值，从而引发草稿类型异常或保存校验失败；普通短文本和敏感字段不受重复控件影响。文案问题影响推荐项辨识和辅助技术读取。
- **修复内容：** `ProtocolFieldEditor.vue` 将密码控件与凭据状态文案包装进同一个敏感字段模板，使数字、推荐下拉、普通下拉、列表、长文本和普通文本恢复为唯一且连续的互斥渲染链；`EditableCombobox.vue` 将 Mihomo 验证来源转换为可读形式，并以 `·` 明确分隔验证来源和中文分组。
- **回归测试：** `protocol-field-editor.spec.ts` 增加推荐下拉、数字、非空列表和空列表的输入入口数量断言；`editable-combobox.spec.ts` 增加 `Mihomo 1.19.29 · 常用` 的可读文案与分隔断言。
- **自动化证据：** 定向前端测试 2 个文件、33 个用例通过；前端全量测试 41 个文件、212 个用例通过；`npm run build` 与 `git diff --check` 通过。
- **人工核验：** 用户于 2026-09-09 明确确认问题已修复。
- **状态：** ☑ 已完成（根因已确认，代码与回归测试已修复，自动化验证和用户人工核验均通过）

> 本条区分旧运行资源中的原始复现证据、代码自动化验证和用户最终人工核验；完成状态以用户本次明确确认的人工核验结果为准。

---

## 三、与 ProdTestList 的同步约定

1. `ProdTestList.md` 继续记录测试是否执行、结果和测试证据。
2. 发现错误时，在 `ProdTestList.md` 对应项目的“结果或备注”中引用本文件的问题编号，例如 `R29-01`。
3. 在本文件对应问题中反向引用具体测试项目，例如 `PT-28-05` 或 `R27-03`。
4. 新问题默认只记录发现事实；根因探索、修复实施和修复验收须由用户明确授权，授权后可同步回对应问题。
5. 没有错误的测试项目只更新 `ProdTestList.md` 的人工结果，不在本文件新增问题。

---

## 四、变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-09-09 | 新建 Issue15，作为 ProdTestList 人工测试发现错误的只记录入口；当前未登记新的人工测试错误，不探索、不修复。 |
| v1.1 | 2026-09-09 | 根据用户人工测试报告登记 R29-01：未启用高级模式时 v2 数据导入被要求同时输入 IMPORT 和 RESET，并提示高级模式已开启，导致数据无法导入；本次仅记录。 |
| v1.2 | 2026-09-09 | 根据用户人工测试报告登记 R29-02：单个素材池同步正常，但多个同步任务似乎不能同时运行；本次仅记录，不确认根因。 |
| v1.3 | 2026-09-09 | 根据用户人工测试截图登记 R29-03：批量导入成功但导入回执 UI 出现列错位和长文本溢出；本次仅记录，不确认根因。 |
| v1.4 | 2026-09-09 | 根据用户人工测试报告登记 R29-04：手机宽度（≤430px）下当前主页丢失进入管理面板按钮；本次仅记录，不确认根因。 |
| v1.5 | 2026-09-09 | 登记并闭环 R29-05：节点条件表单重复输入框与下拉元数据文案粘连；记录已确认根因、组件和测试修复、前端全量自动化证据，以及用户人工核验通过。同步明确 Issue15 在用户授权后可记录问题闭环，但自动化不得代替人工验收。 |
