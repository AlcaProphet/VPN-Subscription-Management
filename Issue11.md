# Issue11.md — VPN 订阅管理系统问题追踪（当前）

> **文档定位：** 本文档是 VPN 订阅管理系统的当前问题记录，承接已归档的 [Issue1～Issue10](docs/reports/Issue/)。本轮仅记录浏览器调查发现、快速验证证据和大致修复方向，不在本轮修改业务代码或实施修复。
> 设计记录见 [Design3.md](Design3.md) 及已归档的 [Design2.md](docs/reports/Design/Design2.md)、[Design2-UI.md](docs/reports/Design/Design2-UI.md)；编码指令见 [AGENTS.md](AGENTS.md)（唯一强要求）。

## 〇、本轮说明

- **创建时间：** 2026-09-01
- **调查方式：** 应用内浏览器可见状态、必要的快速交互验证、前后端静态代码定位；按问题记录现象、快速验证、可能根因、影响范围和修复方向。
- **环境：** 本地 Dev 模式；Docker Compose 服务已停止，使用本地 Go 后端和 Vite 前端。
- **调试地址：** 前端 `http://127.0.0.1:5173/`，后端 `http://127.0.0.1:8080/`。
- **本地数据：** 使用仓库 `./backend/data` 的隔离 Dev 数据目录；已完成 Setup，并按 [TestPasswordList.md](docs/Reference/TestPasswordList.md) 注册合成管理员 `Admin / admin@test.com`，当前仅有该管理员和 Setup 预置平台。
- **基线验证：** 后端 `go build ./...`、`go vet ./...`、`go test ./...` 通过；前端 35 个测试文件/127 个用例通过，`npm run build` 通过（保留既有 chunk size warning）。
- **范围约束：** 本轮只调查、快速测试/静态分析和记录问题；不修改业务代码、不实施修复。问题编号沿用当前 R25 系列，具体问题从 `R25-10` 开始。

## 一、进行中问题

### R25-10 素材池同步历史的“新建 / 删除 / 跳过”计数为空

- **来源：** 用户浏览器评论 1；页面 `http://127.0.0.1:5173/admin/assembly` 的“素材池详情 · Google 直连”弹窗；标注的 URL 为 `https://raw.githubusercontent.com/Loyalsoldier/v2ray-rules-dat/release/google-cn.txt`。
- **现象：** 在素材池同步历史中，成功任务的 URL 后显示“新增 · 删除 · 跳过”，三个计数值均为空，没有展示本次同步实际新建、删除或跳过的条目数量。用户描述为同步过程中某个素材的同步历史计数器失效。当前问题也会影响同步完成后即时显示的同步回执，因为两处使用同一组字段。
- **快速验证：**
  - 浏览器 DOM 快速核对：同步历史节点实际文本为 `新增  · 删除  · 跳过 `；对应 URL 和任务状态“成功”均存在，但三个数值节点为空。
  - 本地数据库只读核对 `backend/data/app-dev.db`：对应 `pool_sync_tasks.per_url_json` 为 `[{"url":"...google-cn.txt","source_id":1,"ok":true,"format":"legacy-domain-text","profile":"common","accepted":112,"excluded":0,"rejected":0,"duplicates":0}]`，没有 `added`、`removed`、`skipped` 字段。
  - `frontend/src/views/admin/assembly/PoolDetail.vue:329-333` 在同步历史中直接渲染 `u.added`、`u.removed`、`u.skipped`；`:215-218` 的即时同步回执也直接渲染相同字段。
  - `frontend/src/api/pool.ts:39-55` 虽声明了这三个字段，但它们是可选字段，因此前端 TypeScript 检查不会发现后端缺字段；`backend/internal/pool/sync.go:28-40` 的 `PerURLResult` 没有对应字段，`syncOne:305-311` 只回写解析统计 `accepted/excluded/rejected/duplicates`。
  - `backend/internal/pool/sync.go:329-404` 的 `applyParseResultTx` 会读取旧快照的格式、profile、accepted 数量用于 pending 判断，但没有比较旧快照与新解析规则集合，也没有产生新建/删除/跳过计数；现有 `backend/internal/pool/sync_new_test.go` 只验证 `Accepted` 和同步后条目总数，没有覆盖三项回执字段。素材池专项测试 `go test ./internal/pool` 通过，前端 `tests/pool-detail.spec.ts` 的 4 个用例通过，但现有测试未覆盖该接口契约。
- **可能根因：** 前端已经按预期设计了三项展示字段，但后端同步回执模型仍停留在解析阶段统计，未实现“新建/删除”相对于上一份 active 快照的集合差异计算，也未定义并回写“跳过”的统一语义；字段在 TypeScript 端被声明为 optional，导致构建和现有测试均放行，最终 Vue 将 `undefined` 插值为空字符串。
- **影响范围：** 影响 `/admin/assembly` 素材池详情弹窗的同步历史，以及同步完成后的即时回执；影响管理员判断 URL 素材发生了多少条目变化、多少条目被忽略。当前同步快照本身仍能成功激活/进入 pending，已知证据不表明规则条目落库失败。
- **修复方向：** 在前后端建立明确且必填的同步回执契约：后端在每个 URL 同步事务中基于旧 active 快照与新解析规则的语义键集合计算 `added`、`removed`，并明确 `skipped` 的来源后写入 `per_url_json`；前端将回执字段改为必填并补充首轮同步、重复同步、条目增删、解析拒绝/平台排除和 pending 等场景的显示测试。保留现有 `accepted/excluded/rejected/duplicates` 解析诊断字段，避免用 `accepted` 直接冒充“新增”。
- **待确认事项：** “跳过”是否定义为 `duplicates + excluded + rejected` 的合计，还是只统计解析后未进入快照的某一类；当新快照进入 pending 时，新建/删除是否仍相对于当前 active 快照计算；删除是否按 canonical rule 的语义键还是按来源原始行计算。上述语义会影响后端回执契约和回归测试，本轮不擅自决定。
- **状态：** ☐ 待确认 / ☐ 待修复（本轮仅分析）

### R25-11 素材池手动条目的规则类型下拉列表为空

- **来源：** 用户浏览器评论 1；页面 `http://127.0.0.1:5173/admin/assembly` 的“素材池详情 · Google 直连”弹窗，点击“新增条目”后标注规则类型选择区 `DOMAIN-SUFFIX`。
- **现象：** “新增条目”弹窗中的规则类型选择器虽然显示默认值 `DOMAIN-SUFFIX`，但展开后下拉面板显示“暂无数据”，无法选择其他规则类型。默认值来自表单初始化，并不代表选项列表已加载。
- **快速验证：**
  - 浏览器 DOM 初始状态显示 `combobox` 和 `DOMAIN-SUFFIX`；点击展开后，DOM 明确出现 `listbox: paragraph: 暂无数据`。因此不是下拉菜单被遮挡或视口裁切，而是实际 options 为空。
  - `frontend/src/views/admin/assembly/PoolDetail.vue:72-79` 在挂载时请求规则元数据，并以 `meta.legacy.filter((m) => m.material_pool).map((m) => m.rule_type)` 生成选项；`:342` 将该数组传入选择器。当前接口请求失败时会提示错误，但本次页面没有错误提示，证据更指向响应字段未匹配。
  - `frontend/src/api/rulespec.ts:15-23` 定义前端期望的 snake_case 字段 `rule_type`、`material_pool`；`backend/internal/server/rulespec.go:14-18` 直接返回 `rulespec.LegacyMetadata()`。
  - `backend/internal/rulespec/legacy.go:5-13` 的 `LegacyCapability` 所有字段均未声明 JSON tag，而 `LegacyMetadata:75-86` 直接返回该结构体；Go 默认 JSON 输出字段名为 `RuleType`、`MaterialPool` 等首字母大写形式。因而前端读取 `m.material_pool` 为 `undefined`，每一项均被过滤掉；读取 `m.rule_type` 也无法获得后端字段。
  - `backend/internal/rulespec/legacy.go:16-26` 中多个可用于素材池的规则类型明确设置 `MaterialPool: true`，所以后端注册表本身并非没有规则类型。`go test ./internal/rulespec` 通过；前端 `tests/pool-detail.spec.ts` 通过，但其 `listCapabilityMeta` mock 在 `:16-18` 固定返回空 `legacy`，没有验证真实 JSON 字段映射。
- **可能根因：** 后端规则类型元数据结构的 JSON 命名与前端 API 契约不一致，导致素材池详情组件的过滤条件永远无法命中；同时测试 mock 直接使用空元数据，掩盖了真实接口字段命名问题。`DOMAIN-SUFFIX` 能显示是因为 `PoolDetail.vue:93` 和 `:97` 将其作为默认表单值，不是因为选项加载成功。
- **影响范围：** 影响素材池详情中的手动条目新增和编辑规则类型选择；用户无法从下拉列表选择其他类型，编辑已有条目时也可能出现当前值显示但无可选项。规则素材池同步、URL 条目读取和后端规则注册表本身未被当前证据证明受损。
- **修复方向：** 统一 `/api/admin/rulespec/meta` 的 JSON 字段契约，使 `legacy` 返回前端约定的 snake_case 字段，或在前端建立明确的响应适配层；补充真实响应形状的 API/组件测试，至少验证 `material_pool=true` 的 `DOMAIN`、`DOMAIN-SUFFIX`、`IP-CIDR` 等类型进入 options，并验证下拉展开后可选择。不要通过硬编码前端列表掩盖元数据接口契约问题。
- **待确认事项：** 无产品语义疑点；规则类型范围仍应以中央能力注册表中 `MaterialPool: true` 的既有定义为准。若希望同时调整可见文案、排序或是否保留 `USER-AGENT` 等类型，可在修复阶段另行确认，本轮不扩展。
- **状态：** ☐ 待确认 / ☐ 待修复（本轮仅分析）

### R25-12 同步历史按设计保留 7 天，但实际清理无独立调度且没有手动清理入口

- **来源：** 用户浏览器评论 1；页面 `http://127.0.0.1:5173/admin/assembly` 的“素材池详情 · Google 直连”弹窗，标注“同步历史”区域并询问是否应有清空历史记录按钮。
- **现象：** 同步历史区域只提供最近历史任务列表及分页，没有“清空历史记录”或“删除历史”按钮。管理员无法手动移除当前素材池的历史记录。
- **设计核对：** `Design3.md:319-323` 规定同步完成任务和诊断最多保留 7 天，且每个 URL 最多保留 20 条代表性诊断；`active/pending` 快照不受任务清理影响。`docs/reports/Build/Build16.md:220-231` 将“7 天清理且不删除 active/pending”列为验收要求。`docs/reports/Design/Design2-UI.md:301-313,531-533` 也将同步历史描述为后端保留 7 天的最近任务列表，未定义手动清空操作。因此当前设计不是永久保留，也没有要求必须提供一键清空按钮。
- **快速验证：**
  - `backend/internal/pool/sync.go:21-25` 将 `taskRetentionDays` 设为 7；`finishTask:407-434` 在任务完成写入终态后，按当前 `pool_id` 删除 `finished_at` 早于 7 天的任务，SQL 条件明确排除了未完成任务和快照状态记录。
  - 当前清理逻辑挂在 `finishTask` 的同步完成副作用中；`backend/internal/pool/sync.go:436-445` 的 `failTask` 早退路径没有执行同一清理；`backend/internal/cron/pool.go:75-147`、`backend/internal/cron/cleanup.go:10-62` 和 `backend/cmd/server/main.go:80-119` 均未发现独立的同步历史启动/每日清理任务。启动阶段只将运行中的同步任务标记为失败，已有的访问日志/重置令牌清理与同步历史无关。
  - `backend/internal/server/pool.go:20-39` 只有提交同步、查询状态/任务和取消任务等路由，没有删除同步历史路由；`frontend/src/api/pool.ts:96-105` 没有对应清理 API；`frontend/src/views/admin/assembly/PoolDetail.vue:175-183,320-336` 只加载、展示和分页同步历史。
  - 本地只读数据库当前有 2 条近期成功任务，不能证明历史永久保留；专项 `go test ./internal/pool ./internal/cron` 已通过，现有测试未覆盖“超过 7 天后由独立调度清理”或“手动清理保护 active/pending”的行为。
- **可能根因：** 7 天策略已作为 `finishTask` 内的按池、按下一次同步触发的清理逻辑实现，但没有独立的全局生命周期清理器；UI/API 设计也仅覆盖“最近 N 条历史任务”读取，没有手动删除契约。因此不活跃素材池的终态历史可能超过设计规定的 7 天仍保留；这属于清理时机与设计保留策略不完全对齐，不是“永久保留”的设计结论。
- **影响范围：** 影响素材池同步诊断历史的存留时长和管理可操作性。长期不再同步的素材池可能持续显示超过 7 天的终态历史；历史记录无法由管理员主动清理。当前证据表明清理 SQL 仅面向已完成终态任务，不能据此推断会删除 `active/pending` 快照。
- **修复方向：** 优先补齐与 7 天策略一致的独立启动/定时清理，在全局范围清理已结束的同步任务和诊断，并明确只清理终态记录、保留运行中的任务及 `active/pending` 快照；同时为清理失败增加可观测日志和测试。按当前设计建议暂不增加通用“一键清空”按钮，因为历史是短期运行诊断而非永久审计记录，新增删除入口会扩大破坏性操作和 API 契约。如果产品确实需要管理员即时整理历史，可另行增加措辞明确的“清理已完成历史”操作，要求二次确认、仅限终态任务，并明确按当前素材池还是全部素材池处理。
- **待确认事项：** 是否坚持当前设计并暂不增加手动按钮，先修正 7 天自动清理的实际触发时机；若需要按钮，还需确认作用范围（当前素材池/全部素材池）、是否包含失败任务（建议包含全部终态任务）、以及运行中任务和 `active/pending` 快照的保护规则。本轮不擅自选择，也不实施修复。
- **状态：** ☐ 待确认 / ☐ 待修复（本轮仅分析）

### R25-13 Shadowrocket 分流规则装配错误地要求至少一个可用节点

- **来源：** 用户浏览器评论 1；页面 `http://127.0.0.1:5173/admin/assembly?tab=sr-conf` 的“构建订阅/规则 → Shadowrocket规则组”页签；标注的提示为“构建前缺少以下前置条件：至少一个可用节点”。
- **现象：** 在没有任何可用节点的本地环境中，Shadowrocket 分流规则装配页显示节点前置条件告警，并通过该告警阻止装配器主体继续显示。用户无法仅维护规则素材、填写 `[General]`、选择 `PROXY/DIRECT` 兜底方向并生成 `.conf`。该行为把 SR 节点订阅装配的前置条件错误带入 SR 分流规则装配。
- **设计核对：** `docs/reports/Design/Design2.md:114-121` 明确将 Shadowrocket 拆分为“节点订阅”和“分流规则”两个可单独使用的装配器；其中分流规则装配器填写 `[General]`、选择规则素材和 `FINAL` 方向，产出 `.conf`，并明确标注“**不含节点**”。`docs/reports/Design/Design2.md:126-129` 规定 SR 规则目标为 `PROXY/DIRECT`；`docs/reports/Build/Build5.md:36` 也只对 Clash 的强制组和 SR subs/generic-subs 的节点链接设置空产物门槛，没有给 `sr-conf` 设置节点门槛。
- **快速验证：**
  - 浏览器 DOM 显示当前选中页签为 `Shadowrocket规则组`，告警包含 `至少一个可用节点`；该告警来自页面渲染，不是后端返回的生成错误。
  - `frontend/src/views/admin/AssemblyView.vue:160-174` 的 `buildPreflightMissing` 无条件计算 `hasNode` 并追加节点缺失项，随后才判断 `targetSyntax === 'sr-conf'`；`:165-166` 仅跳过 SR 规则实体前置条件，没有跳过节点检查。
  - 同组件 `:224-229` 已为 `sr-conf` 配置独立步骤：头部、规则素材、预览，没有节点步骤；模板 `:788-789` 却使用所有目标共用的 `buildPreflightMissing.length === 0` 作为装配器显示门槛，形成前置条件与步骤定义的矛盾。
  - 后端 `backend/internal/assembly/validate.go:19-49` 对 `SrConf` 只校验规则实体/规则名称，不校验节点或平台；`:50-59` 只遍历请求中实际传入的节点，空 `NodeNames` 不会触发错误；`:155-158` 的“至少一个可转换节点”只适用于 `SrSubs` 和 `GenericSubs`。
  - `backend/internal/assembly/render_sr.go:65-111` 的 SR 分流规则渲染只读取固定头部、素材池/手动规则和 `FINAL` 方向，不读取节点；现有 `backend/internal/assembly/assembly_test.go:657-666` 已用不传 `NodeNames` 的 `SrConf` 输入验证预览成功。后端 `go test ./internal/assembly ./internal/server` 通过，前端 `tests/assembly-view.spec.ts` 的 13 个用例通过，但现有前端测试未覆盖“无节点时 sr-conf 不应显示前置告警”。
- **可能根因：** 前端前置条件把“至少一个可用节点”作为所有四类装配器的公共门槛，而后续只对 `sr-conf` 放宽了规则实体要求；节点步骤虽已按目标类型正确隐藏，但节点 preflight 未按目标类型收窄。后端的目标校验和渲染逻辑与用户描述及设计一致，当前更像是前端门槛条件的范围错误，而不是 SR 规则生成链路本身要求节点。
- **影响范围：** 直接影响无节点或暂未配置节点的管理员使用 Shadowrocket 分流规则装配；也会使仅有规则目标和规则素材的环境无法进入 SR 规则预览/生成。当前证据不表明 Clash、SR 节点订阅或通用 V2Ray 节点订阅的节点前置条件应被移除；这些目标仍依赖节点内容。
- **修复方向：** 将节点可用性前置条件限定在实际需要节点的目标（至少 Clash YAML、SR subs、generic-subs），对 `sr-conf` 不显示该项；保持 `sr-conf` 的规则实体/名称、规则素材和规则语法校验不变。补充前端回归测试：无节点时四个目标分别验证，其中 `sr-conf` 可进入规则步骤，节点订阅目标仍保留节点门槛；同时保留后端已有的空 `NodeNames` SR 规则预览测试。
- **待确认事项：** 当前没有发现需要产品重新决策的语义疑点。需要确认的仅是是否按上述设计结论移除 `sr-conf` 的节点前置提示；本轮不擅自修改代码。
- **状态：** ☐ 待确认 / ☐ 待修复（本轮仅分析）

### R25-14 平台附加响应头编辑开关关闭时文案应指向“高级JSON"

- **来源：** 用户浏览器评论 1；页面 `http://127.0.0.1:5173/admin/platforms/1/edit` 的“附加响应头”区域，标注了关闭状态的模式开关及当前文案“结构化编辑”。
- **现象：** 附加响应头编辑器当前处于结构化模式，开关为关闭状态，但旁边显示“结构化编辑”。用户期望关闭开关时显示“高级JSON”，即文案表示点击开关后可切换到的目标模式，而不是当前已经处于的模式。
- **快速验证：**
  - 浏览器 DOM 初始状态显示 `switch "结构化编辑" [active]`，其下方文本也是“结构化编辑”；与用户标注截图一致。
  - 在不保存数据的情况下切换一次开关，DOM 变为 `switch "高级 JSON" [checked]`，说明当前实现的标签随当前模式变化：关闭时显示结构化编辑，开启时显示高级 JSON。切回后未保存任何业务数据。
  - `frontend/src/components/PlatformHeadersEditor.vue:18` 使用 `mode='structured'|'json'`；`:323-324` 根据当前模式渲染 `mode === 'json' ? '高级 JSON' : '结构化编辑'`。这表达的是当前状态，不是用户要执行的下一状态。
  - 同类模式开关也存在于 `frontend/src/components/ProtocolFieldEditor.vue:166-169`，当前对象默认结构化时同样显示“结构化编辑”；装配头部 `frontend/src/views/admin/assembly/HeaderStep.vue:84-87` 使用相同的当前态表达，但结构化态文案为“结构化表单”。因此若该诉求是统一模式切换语义，影响范围不应只看平台附加响应头组件。
  - `docs/reports/Design/Design2-UI.md:333,359` 定义该控件为“结构化表单 / 高级 JSON”双态 Switch，但没有明确标签表示当前状态还是切换目标；已归档 `Issue9.md:R24-16` 记录过将其改为 Switch 的背景。当前 `frontend/tests` 未发现平台附加响应头组件的独立文案测试；`tests/protocol-field-editor.spec.ts:22-33`、`tests/nodes-view.spec.ts:101` 和 `tests/header-step.spec.ts:19-28` 覆盖的是现有默认态文案。相关定向测试 2 个文件、6 个用例通过，但未验证本次期望的目标态文案。
- **可能根因：** 模式开关的标签实现采用了“当前所处模式”而不是“点击后将进入的模式”。这不是数据或编辑逻辑错误，而是控件状态语义与用户操作预期不一致；同时三个使用相近模式切换的组件文案也没有统一。
- **影响范围：** 直接影响平台附加响应头编辑器的可发现性和操作理解，用户可能误以为关闭开关代表关闭结构化编辑功能，而不是切换到高级 JSON。若统一调整，将同时影响节点协议对象编辑器和装配头部参数编辑器的默认态文案；不会改变 `mode`、`advanced` 状态切换、响应头数据或 JSON 存储格式。
- **修复方向：** 按用户指定的目标态语义调整标签：结构化模式/开关关闭时显示“高级JSON”，高级 JSON 模式/开关开启时显示“结构化编辑”；并统一平台附加响应头、协议对象和装配头部三个同类控件的文案口径。同步补充默认态与切换后态测试，确认标签、可访问名称和实际编辑区域保持一致。需要注意当前设计文档使用带空格的“高级 JSON”，而用户本次指定的是“高级JSON”，修复时应以确认后的文案为准。
- **待确认事项：** 是否将该文案规则统一应用到三个同类开关（平台附加响应头、节点协议对象、装配头部），还是只调整当前平台编辑页；以及最终采用用户指定的“高级JSON”还是设计文档中的“高级 JSON”排版。本轮不擅自修改代码。
- **状态：** ☐ 待确认 / ☐ 待修复（本轮仅分析）

### R25-15 代理组默认携带子组的文案及构建页选择排序展示不完整

- **来源：** 用户浏览器评论 1～3；代理组设置页面 `http://127.0.0.1:5173/admin/assembly?tab=proxy-groups` 标注表格列“成员”，以及“构建订阅/规则”页面 `http://127.0.0.1:5173/admin/assembly?tab=clash-yaml` 标注代理组“🎬YouTube 选择与排序”和“可选成员：本次已勾选节点、🚀直接连接”。用户已确认本条应按前端文案和信息展示问题记录，不是移除功能。
- **已确认的产品语义：**
  - 代理组设置中的选择表示：该代理组在“构建订阅/规则”中被携带时，默认一同携带的子代理组。
  - 代理组设置页面的表格列“成员”应改为“默认携带的代理组”，并增加说明文字，让用户知道这里配置的是构建时随当前代理组携带的子组。
  - 在“构建订阅/规则”页面的代理组区域中，已选择的代理组应展示其默认携带的子组；这些默认携带的子组还应与本次已勾选节点一起纳入代理组的选择与排序。
- **现象：** 当前代理组管理页将默认携带的子组统称为“成员”，构建页则主要展示本次装配节点和当前可操作的代理组成员，未明确展示普通代理组自身默认携带了哪些子组，也未将这些子组作为可一同选择、排序的成员呈现，容易使用户误以为代理组设置中的选择没有进入构建。
- **快速验证：**
  - 本地数据库只读核对 `backend/data/app-dev.db`：`proxy_groups.id=1` 的 YouTube `definition_json` 为 `{"type":"select","groups":["🚀直接连接","🌎国外流量"]}`，字段语义是子组引用。
  - 浏览器实际走过本地 Clash YAML 构建预览流程，选择节点 `us-1` 和预置组 `🎬YouTube` 后，预览中的 `proxy-groups` 包含 `name: 🎬YouTube` 和 `proxies: [🚀直接连接, 🌎国外流量]`，说明当前后端构建链路已经使用这些默认携带的子组；本次只生成预览，没有创建或保存版本。
  - `frontend/src/views/admin/ProxyGroupsView.vue:211-213` 的 `memberSummary` 读取 `g.definition.groups`，编辑区 `:299-310` 的实际字段为“子组引用”；`frontend/src/views/admin/assembly/NodesGroupsStep.vue:144-189` 的“选择与排序”当前主要维护本次装配的 `group_node_orders`，即节点成员顺序。
  - `backend/internal/assembly/render_clash.go:33-58` 渲染普通代理组时，会将本次装配节点与 `g.Groups` 子组引用共同写入输出；当前未发现后端覆盖或丢弃默认子组的证据。普通代理组若最终不可被规则或其他组到达，下载重渲染仍可能按现有可达性逻辑移除整组，这与本条要求展示并参与排序的前端交互需要分开处理。
  - 定向测试通过：后端 `go test ./internal/assembly ./internal/proxygroup ./internal/server` 通过；前端 `tests/proxy-groups-view.spec.ts`、`tests/nodes-groups-step.spec.ts`、`tests/assembly-view.spec.ts` 共 3 个文件、19 个用例通过。现有测试尚未覆盖“已选择普通代理组展示默认子组，并将默认子组纳入选择与排序”的新 UI 要求。
- **可能根因：** 代理组设置和构建页对同一组配置使用了不同层级的展示方式：前者保存并显示 `definition.groups`，后者的交互状态主要围绕本次装配节点排序建立，缺少将普通代理组的默认子组展开、说明并纳入同一排序上下文的前端映射。当前更像是文案和构建页信息架构不完整，不是后端构建链路失效。
- **影响范围：** 影响代理组设置页面的配置理解，以及 Clash 构建页面对代理组最终成员组成和顺序的可见性、可操作性；当前 SR 装配不读取 Clash 代理组配置，不应据此推导 SR 输出行为。
- **修复方向：** 代理组设置页将“成员”改为“默认携带的代理组”，补充构建语义说明；构建页在每个已选代理组下展示其默认携带的子组，并将节点和默认子组纳入同一选择与排序模型，同时保持当前后端将 `definition.groups` 写入 Clash 输出的行为。补充前端组件测试，覆盖默认子组展示、勾选/取消、排序及最终 `group_node_orders`/构建请求映射；补充一条从 `proxy_groups.definition_json.groups` 到预览/下载计划的回归测试。
- **待确认事项：** 产品语义已确认；实现阶段仍需保持默认子组与本次节点选择的边界清晰，并定义嵌套子组、重复引用和不可达普通组在排序/展示中的具体交互规则。本轮不实施代码修复。
- **状态：** ☐ 待确认 / ☑ 待修复（本轮仅记录，未实施）

### R25-16 组装器覆盖层默认展示过深，普通用户缺少高级配置提示

- **来源：** 用户浏览器评论 1～2；页面 `http://127.0.0.1:5173/admin/assembly?tab=clash-yaml` 的 Clash YAML 组装器，分别标注分步模式和单页模式中的覆盖层完整编辑区。
- **现象：** 分步模式进入“④ 覆盖层”时，Merge YAML、Rules Seq、Proxies Seq、Groups Seq 四个 YAML 文本框默认全部展开；单页模式中的覆盖层卡片也默认直接显示全部文本框。普通用户进入组装流程时会立即看到较复杂的覆盖语法，容易误以为这些字段是构建必填项。
- **用户已确认的需求：**
  - 分步模式和单页模式中的覆盖层均默认折叠。
  - 折叠状态需要明确提示这是“高级配置”，默认不需要填写。
  - 增加按钮，用户主动点击后再展开覆盖层配置；展开后继续提供现有四类 YAML 编辑和“填入空模板”操作。
  - 本轮只记录需求，不实施代码改动。
- **快速验证：**
  - 浏览器截图和 DOM 标注均显示两种布局下四个文本框同时可见，当前没有折叠状态、展开按钮或“默认无需填写”的提示。
  - `frontend/src/views/admin/assembly/OverlayStep.vue` 当前直接渲染覆盖层说明、空模板按钮和四个 `Input.TextArea`，没有展开状态；`frontend/src/views/admin/assembly/AssemblerShell.vue` 在分步和单页模式复用同一个 `OverlayStep` 插槽，因此收口应以覆盖层组件为主，避免两套行为不一致。
  - `docs/reports/Design/Design2-UI.md:337` 目前只规定 Clash 覆盖层包含四个文本框、空模板按钮和字段说明，没有规定默认折叠或高级配置提示；`docs/reports/Design/Design2.md:143-148` 确认覆盖层仅作用于 Clash YAML，且会参与预览、快照和重新编辑回填，折叠只应改变展示方式，不应改变表单值和请求契约。
- **可能根因：** 当前覆盖层组件按“功能完整可编辑区”直接展示，没有区分普通构建流程和高级覆盖能力；由于分步/单页只是外层布局切换，两个模式同时暴露了同一组高级字段。
- **影响范围：** 影响 Clash YAML 组装器分步模式和单页模式的首屏信息密度、字段理解和操作路径；不应改变覆盖层的四类 YAML 内容、预览指纹、草稿/蓝图回填、后端校验或下载渲染行为。
- **修复方向：** 在 `OverlayStep` 内增加默认关闭的展开状态，折叠态显示“覆盖层（高级配置）”“默认无需填写”及现有作用范围说明，并提供“添加覆盖层”/展开编辑按钮；已有内容时折叠态应提示已填写并提供展开编辑，避免隐藏已保存配置。展开后保留现有字段、占位提示和空模板按钮；补充组件测试覆盖分步/单页共用、默认折叠、主动展开、已有内容提示、收起不丢值等场景。必要时同步更新 Design2-UI 的覆盖层界面规格。
- **待确认事项：** 产品需求已确认；实现时仅需确定展开按钮的最终文案，以及已有覆盖层内容时采用“展开编辑”还是统一“展开”文案，不涉及后端数据或覆盖规则语义。
- **状态：** ☐ 待确认 / ☑ 待修复（本轮仅记录，代码改动已撤回）

### R25-17 Diff 视图的行号、变更符号与 YAML 代码缺少清晰的列分隔

- **来源：** 用户浏览器评论 1；页面 `http://127.0.0.1:5173/admin/assembly?tab=clash-yaml` 的预览区域，标注了包含行号和 YAML 内容的代码展示，并要求改进 Diff UI。
- **现象：** 当前预览代码区使用行号后直接显示 YAML 内容；Diff 视图使用 `+`/`-` 变更符号后直接显示代码。行号、变更符号和代码之间缺少醒目的竖向分割，且 YAML 本身大量使用列表前缀 `-`，容易让用户把 Diff 的删除符号与 YAML 语法中的列表符号混淆，降低新增/删除行的辨识度。
- **快速验证：**
  - `frontend/src/components/DiffView.vue:111-140` 的行结构目前只有一个宽度为 `w-4` 的变更符号 `span` 和后续文本，没有独立行号列，也没有行号列、变更列、代码列之间的边框或竖线。
  - 同文件 `:96-103` 只在顶部显示汇总的 `+新增数` 与 `-删除数`；`jumpTo` 通过 `data-diff-kind` 定位行，但没有提供行号定位信息。
  - `frontend/src/views/admin/assembly/PreviewStep.vue:97-119,121-132` 的普通预览/过期预览另有独立 `w-8` 行号列，但该布局不复用于 `DiffView`，因此预览和 Diff 的列结构不一致。
  - `frontend/tests/diff-view.spec.ts` 当前只验证三色行背景、`+`/`-` 文本、整体新增和大文本手动对比；定向执行 `npm test -- --run tests/diff-view.spec.ts` 结果为 1 个文件、3 个用例通过，未覆盖行号列和分隔线语义。
  - GitHub Pull Request 的 Files changed 视图提供统一视图/分栏视图切换，并以行号作为变更审阅和评论定位入口；可参考其“行号区—变更标记区—代码区”的分层方式，但本项目当前只需要改善阅读，不需要引入评论、审阅或提交操作。[GitHub 官方说明](https://docs.github.com/en/pull-requests/how-tos/review-pull-requests/reviewing-proposed-changes-in-a-pull-request)
- **可能根因：** Diff 组件沿用了最小化的 unified text 行布局，只把新增/删除符号作为普通行内文本渲染，没有将“旧行号/新行号、变更标记、代码内容”建模为独立视觉列；同时没有针对 YAML 列表语法中的 `-` 做符号层级区分。
- **影响范围：** 影响装配预览中“与当前激活版本对比”的 Diff 阅读、定位和代码审阅，尤其是 `proxy-groups`、`rules` 等包含大量 YAML 列表项的内容；不应改变 jsdiff 的差异计算、计数、超大文本保护、搜索或跳转逻辑。
- **修复方向：** 参考 GitHub Pull Request 的 Files changed 视觉层级，将 Diff 每行拆成独立的行号区、变更符号区和代码区，并在列之间加入较醒目的竖向分割线；变更符号列使用固定宽度、居中/右对齐和独立的颜色/底色，使 `+`/`-` 与代码里的 YAML `-` 有明确层级差异；保持新增/删除/上下文三色背景和横向滚动/自动换行行为。补充测试验证列结构、行号渲染、`+`/`-` 与代码文本分离，以及整体新增和删除行的行号显示。
- **待确认事项：** 删除行和新增行的行号显示口径需要确认：是采用统一 Diff 常见的“双行号列”（旧行号/新行号），还是只保留一列按新文本编号；推荐采用旧行号与新行号双列，删除行只显示旧行号、新增行只显示新行号、上下文行同时显示两者，更能准确表达 YAML 变更位置。分隔线的具体颜色和粗细可沿用现有主题 Token，在实现阶段确定。
- **状态：** ☑ 待确认 / ☑ 待修复（本轮仅分析和记录）

### R25-18 预览区副标题与差异对比入口层级重复，容易引发操作困惑

- **来源：** 用户浏览器评论 1；页面 `http://127.0.0.1:5173/admin/assembly?tab=clash-yaml` 的预览区域，标注第二个页签“差异对比”，并指出预览区副标题和两处差异对比入口需要重新思考。
- **现象：** 预览区顶部同时存在“与当前激活版本对比”按钮和“差异对比”页签。前者实际负责向后端加载当前激活版本并切换 `showDiff`，后者负责在已加载后切换预览/差异视图；虽然职责不同，但两者都使用“对比/差异对比”语义，用户可能不知道应该先点击哪个，也可能把页签理解为另一个独立的对比操作。当前“最近预览：时间 · 目标类型”提示也更像状态 Alert，而不是清晰说明当前预览对象与差异对比基准的副标题。
- **快速验证：**
  - `frontend/src/views/admin/assembly/PreviewStep.vue:81-89` 在工具栏直接渲染“刷新预览”和“与当前激活版本对比”；`:131-136` 又渲染“预览 / 差异对比”两个页签，并在未加载差异时显示“请点击‘与当前激活版本对比’加载差异数据”。这形成了“按钮触发加载 + 页签切换结果”的双入口视觉结构。
  - `frontend/src/views/admin/AssemblyView.vue:618-632` 的 `toggleDiff` 才执行激活版本对比请求、写入 `diffOld`/`diffMissing` 并设置 `showDiff`；`PreviewStep.vue:41-44` 仅根据 `showDiff` 自动将活动视图切到 `diff`。因此页签不是第二次加载，但页面没有把这个层级关系充分表达出来。
  - `PreviewStep.vue:34-37,91-93` 将预览时间与目标类型作为 `Alert success` 的 `message` 展示，未同时展示“当前配置预览”与“对比基准为当前激活版本”等对象关系；预览过期时又切换为“配置已变化，请重新预览”告警，状态、对象和操作提示分散在不同位置。
  - `docs/reports/Design/Design2-UI.md:41,369` 当前只规定使用 `DiffView` 和提供“与当前激活版本对比”开关，没有规定预览副标题、差异加载状态或页签与加载动作的层级关系。R25-17 已另行记录 Diff 内容本身的行号/分隔线视觉问题。
  - 定向执行 `npm test -- --run tests/diff-view.spec.ts tests/assembly-view.spec.ts` 结果为 2 个文件、16 个用例通过；现有测试验证了对比请求和 Diff 内容，但未覆盖“单一对比入口、页签状态、预览副标题信息层级”等交互语义。
- **可能根因：** 预览区把“加载对比数据”和“切换已加载视图”分别实现为工具栏按钮和页签，却使用了相近的用户文案；同时预览时间、目标类型、预览过期状态和对比基准没有形成统一的状态标题/副标题层级。
- **影响范围：** 影响 Clash 及其他支持当前激活版本对比的装配预览页面的操作发现性和状态理解；不应改变预览刷新、当前激活版本读取、`showDiff` 状态、Diff 计算或预览过期校验逻辑。
- **修复方向：** 参考 GitHub Pull Request 的 Files changed 阅读模式，将“当前预览对象”“对比基准”“差异是否已加载”集中为预览区的标题/副标题或状态条，并明确主操作。可选方向包括：
  1. 保留“预览 / 差异对比”页签，但将顶部动作改为“加载当前激活版本差异”，在差异加载后更新页签状态，明确页签只负责切换显示；
  2. 移除重复的差异页签，将“查看当前激活版本差异”作为单一按钮/可展开面板，加载后在同一区域展示 Diff；
  3. 将预览与差异对比改为统一的 Segmented/模式切换，未加载时差异模式显示明确的加载动作，避免同时出现两个同义入口。
  推荐优先评估方向 1 或 3：保留预览与 Diff 的清晰模式切换，同时把“加载对比数据”从“切换显示”中区分出来。实现时应保留刷新预览、复制、搜索、换行和过期状态能力，并补充两种状态（未加载/已加载/预览过期）的可访问名称与交互测试。
- **待确认事项：** 需要确认最终采用哪种入口模型：保留页签并改清楚层级、移除页签改为单面板，还是改为统一模式切换；同时确认副标题是否采用“当前预览：Clash YAML · 最近刷新时间 / 对比基准：当前激活版本”的双行状态表达。当前不擅自选择，也不实施 UI 修改。
- **状态：** ☑ 待确认 / ☑ 待修复（本轮仅分析和记录）

### R25-19 预览工具栏的自动换行应使用开关，复制入口应移除

- **来源：** 用户浏览器评论 1～2；页面 `http://127.0.0.1:5173/admin/assembly?tab=clash-yaml` 的预览工具栏，分别标注“自动换行”和“复制”。用户已明确要求将自动换行改为开关，并移除复制按钮。
- **现象：** 当前预览工具栏使用按钮在“取消换行”和“自动换行”之间切换状态；状态通过按钮文字表达，视觉上仍像一次性操作。工具栏还提供“复制”按钮，但预览内容本身已经是可选择的代码文本，按钮只复制整段预览，和当前预览/搜索/差异浏览功能没有明确的必要关联，增加了工具栏噪音。
- **快速验证：**
  - `frontend/src/views/admin/assembly/PreviewStep.vue:26-28` 使用本地 `wrap` 状态；`:81-89` 以 `<Button>` 渲染复制和换行操作，换行按钮文案由 `wrap ? '取消换行' : '自动换行'` 动态切换。
  - 同文件 `:65-75` 的 `copyPreview` 调用 `navigator.clipboard.writeText(props.previewText || '')`，复制对象是完整预览文本；该函数只服务于工具栏“复制”按钮，没有其他业务依赖。
  - `:113-119,121-132` 通过 `wrap` 控制预览内容的 `whitespace-pre-wrap`/`whitespace-pre`，说明换行状态本身适合表达为持续状态型控件；移除复制按钮不会影响预览内容、搜索、差异对比或生成逻辑。
  - 定向执行 `npm test -- --run tests/diff-view.spec.ts tests/assembly-view.spec.ts` 结果为 2 个文件、16 个用例通过；现有测试未覆盖自动换行控件类型、开关状态可访问名称或复制按钮是否存在。
- **可能根因：** 预览工具栏将持续状态（是否自动换行）和一次性动作（复制全文）都使用普通按钮承载，缺少对“状态开关”和“必要操作”的区分；复制功能与代码区域的原生文本选择能力重复，导致其产品价值不明显。
- **影响范围：** 影响预览区域工具栏的状态可见性、操作理解和视觉简洁性；不应改变换行对 `whitespace-pre-wrap`/`whitespace-pre` 的实际控制，也不应影响 Diff、搜索、预览刷新、过期状态和生成流程。
- **修复方向：** 将自动换行改为 Switch，明确 checked 状态表示“自动换行已开启”，并提供对应可访问标签；保留现有换行 CSS 分支和用户在预览/差异视图中的状态逻辑。移除复制按钮及其专用复制逻辑/无用依赖，让用户继续通过代码区域原生选择复制；同步调整工具栏间距和相关测试，验证 Switch 两态文案/可访问性以及复制按钮不再渲染。
- **待确认事项：** 用户需求已确认；实现时只需确定 Switch 的最终标签是“自动换行”还是“换行”，以及该状态是否在预览与 Diff 两个视图之间共用。当前建议标签使用“自动换行”，并在两个视图共用现有 `wrap` 状态。
- **状态：** ☐ 待确认 / ☑ 待修复（本轮仅分析和记录）

## 二、问题记录格式

```markdown
### R25-XX 问题标题

- **来源：** 浏览器页面、URL、用户标注和必要的截图/交互步骤。
- **现象：** 可复现的用户可见行为或逻辑结果。
- **快速验证：** 浏览器验证结果、接口响应、测试结果或最小静态代码证据。
- **可能根因：** 当前阶段基于证据的定位；未确认部分明确标注不确定性。
- **影响范围：** 受影响的页面、接口、数据或用户流程。
- **修复方向：** 仅给出大致方向，不在本轮实施。
- **待确认事项：** 需要用户决定的产品语义、兼容性或实现取舍。
- **状态：** ☐ 待确认 / ☐ 待修复（本轮仅分析）
```

## 三、调查与变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-09-01 | 新建当前问题记录；停止 Docker Compose，启动本地 Dev 前后端，完成 Setup 和合成管理员初始化；已完成基线构建/测试，尚未登记具体问题，未修改业务代码。 |
| v1.1 | 2026-09-01 | 新增 R25-10：复现素材池同步历史及即时回执的“新增/删除/跳过”计数为空；确认前端读取字段与后端回执模型不一致，补充数据库、DOM、前后端静态代码和专项测试证据；未修改业务代码。 |
| v1.2 | 2026-09-01 | 新增 R25-11：复现“新增条目”规则类型下拉展开后显示“暂无数据”；确认后端 `LegacyCapability` 默认 JSON 字段名与前端 snake_case 契约不一致，补充浏览器 DOM、注册表、组件和测试 mock 证据；未修改业务代码。 |
| v1.3 | 2026-09-01 | 新增 R25-12：核对同步历史为终态任务保留 7 天而非永久保留；确认现有清理仅在当前素材池完成同步时触发，缺少独立调度且没有手动清理 API/UI；给出暂不增加一键清空、优先补齐自动清理的建议；未修改业务代码。 |
| v1.4 | 2026-09-01 | 新增 R25-13：复核 Shadowrocket 分流规则页在无节点时错误显示节点前置条件；确认 Design2 明确 SR 分流规则不含节点，后端已允许空节点预览，问题集中在前端公共 preflight；补充浏览器 DOM、设计、前后端代码和测试证据；未修改业务代码。 |
| v1.5 | 2026-09-01 | 新增 R25-14：复核平台附加响应头模式开关关闭时显示“结构化编辑”的文案问题；确认当前实现表达当前状态，用户期望表达切换目标；补充浏览器两态 DOM、同类组件、设计文档和定向测试证据；未修改业务代码。 |
| v1.6 | 2026-09-01 | 新增 R25-15：核验代理组管理页 YouTube 的 `🚀直接连接、🌎国外流量` 在 Clash 构建预览中会进入 `proxies`；区分全局子组引用与装配级节点成员，记录普通组可达性裁剪这一独立行为及待确认语义；后端 3 个包和前端 3 个相关测试文件通过，未修改业务代码。 |
| v1.7 | 2026-09-01 | 根据用户确认撤回 R25-15 原先关于“可能移除功能”的问题表述，改记为默认携带子组的前端文案、说明、构建页展示及选择排序需求；明确默认子组应与本次节点一同参与构建页选择与排序；未修改业务代码。 |
| v1.8 | 2026-09-01 | 新增 R25-16：记录分步与单页模式覆盖层默认展开、缺少高级配置提示的问题；按用户要求撤回本轮临时业务代码和测试改动，仅保留问题记录。 |
| v1.9 | 2026-09-01 | 新增 R25-17：分析 Diff 视图中行号、变更符号与 YAML 代码缺少列分隔的问题；确认现有 Diff 无独立行号列，定向 3 个用例通过，记录双行号方案为待确认建议；未修改业务代码。 |
| v1.10 | 2026-09-01 | 根据用户补充，增加 GitHub Pull Request Files changed 作为 Diff 行号区、变更标记区和代码区分层的视觉参考；不引入评论/审阅操作，未修改业务代码。 |
| v1.11 | 2026-09-01 | 新增 R25-18：分析预览区副标题、对比加载按钮与“差异对比”页签的层级重复；确认按钮负责加载、页签负责切换，定向 16 个相关用例通过，记录三种 UI 收口方向；未修改业务代码。 |
| v1.12 | 2026-09-01 | 新增 R25-19：记录自动换行按钮改为 Switch、移除复制按钮的明确需求；确认当前实现与测试覆盖范围，未修改业务代码。 |
