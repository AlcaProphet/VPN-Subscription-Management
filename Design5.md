# Design5.md — 加密整站导出与一步迁移候选设计

> **文档定位：** 本文记录 VPN 订阅管理系统“导出完整加密数据，并通过 Setup 一步完成整站迁移”的后续设计构想。本文目前只承载产品目标、现状边界、风险与待研究/待决策项，不是 Build 文档，也不表示已经授权修改代码、数据库、导出格式或恢复流程。
> **编码约束：** 遵循 [AGENTS.md](AGENTS.md)（唯一强要求）。现行配置导入/导出与完整备份基线见 [Design1.md](docs/reports/Design/Design1.md) §3.4.8/§7.2；Xray 实例与独立账号的 v2 增量见 [Design2.md](docs/reports/Design/Design2.md) §5.4；当前节点编辑器设计继续以 [Design4.md](Design4.md) 为准。
> **设计状态：** v0.1，2026-09-11。用户已确认整站迁移目标并要求先登记、暂不实施；具体文件格式、恢复入口、安全模型、兼容策略与构建步骤均未研究定稿。

---

## 一、背景与现状差距

### 1.1 用户目标

通过一个加密导出文件携带完整站点数据，在另一套未配置部署的 Setup 页面上传该文件并输入密码后，一步完成整站迁移。迁移完成后的目标站点应恢复可登录账号、业务数据、内容文件、配置与必要引用关系，而不要求部署者手工进入容器解包数据卷。

### 1.2 当前能力边界

| 能力 | 当前范围 | 与目标的差距 |
|------|----------|--------------|
| 配置导出 `.enc` | 加密导出系统配置、签名密钥、站点 ICON；v2 另含 Xray 实例与独立账号 | 不含用户、组、平台、节点、订阅、规则、分享、自定义内容、各类 Token、装配数据和内容文件，不能完成整站迁移 |
| Setup 配置导入 | 解密 `.enc`，整体覆盖系统配置并创建默认组/默认平台 | 只完成配置初始化；旧账号与绝大多数业务数据不会恢复 |
| 下载备份 `tar.gz` | SQLite 一致性快照 + 内容目录 + public 资源 | 数据完整，但当前未加密，且恢复需要部署者手工解包到数据卷；Setup 无恢复入口 |

2026-09-11 的 R29-01 后续核查验证了上述边界：v2 基础模式 `.enc` 导入成功后，系统配置生效，但用户、节点、装配蓝图等业务表为空。这符合现行设计，但不满足本设计新增的整站迁移目标。

### 1.3 已确认目标与非结论

- **已确认目标：** 完整数据、加密文件、Setup 一步迁移。
- **暂不实施：** 本轮不修改现有导出/导入、备份、数据库或 UI。
- **尚未确认：** 是扩展现有 `.enc` 配置格式，还是为完整备份建立独立加密格式和入口。
- **尚未确认：** 是否允许向已配置实例恢复、是否只允许未配置新库、是否支持跨版本迁移及允许的版本跨度。
- **尚未确认：** 原站点运行期间导出的一致性、目标端失败回滚、恢复后的外部依赖重连和旧站点并行运行策略。

---

## 二、候选能力范围

完整整站迁移至少需要研究以下数据域，最终清单必须由 schema 与内容目录审计形成，不能只凭当前页面枚举：

| 数据域 | 候选迁移内容 | 主要风险 |
|--------|--------------|----------|
| 系统与认证 | `system_config`、签名密钥、OIDC/SMTP 等加密配置 | 文件密码成为站点密钥与敏感配置的安全边界；域名变化影响回调地址 |
| 用户与权限 | 用户、密码哈希、角色、状态、组归属、凭据版本 | 恢复后可直接登录；不得产生无管理员或权限漂移 |
| 业务结构 | 用户组、平台、节点、代理组、素材池、规则、分享、装配蓝图及版本元数据 | 外键、稳定标识、排序、当前版本指针与跨表引用必须一致 |
| Token 与凭据 | 下载 Token、节点凭据、Xray 用户/独立账号密文 | 必须与恢复后的签名/加密密钥配套；日志和错误响应不得泄露明文 |
| 内容文件 | 订阅/规则/分享/自定义版本文件、站点资源及必要的当前指针 | DB 与文件必须来自同一一致性时点；路径穿越与符号链接恢复需受控 |
| 运行时/历史数据 | 同步快照、任务记录、流量记录等 | 哪些属于迁移事实、哪些应在新实例重建或丢弃，需要逐项决策 |

日志、临时文件、进程内任务和可重建缓存是否迁移，暂不预设结论。

---

## 三、候选方案（待研究与用户决策）

### 3.1 路线 A：独立的加密完整备份格式（初步推荐，未确认）

在现有一致性备份能力上增加整体加密与带版本清单的封装，并为 Setup 提供专用“恢复完整备份”入口。现有“配置导入”继续保持轻量配置迁移定位。

- 优点：配置迁移与灾难恢复职责清楚；可以复用 SQLite 一致性快照和内容目录打包；不必把所有业务表重复编码为大型 JSON 合同。
- 风险：必须安全解包、验证清单、处理空间上限、原子替换数据卷和失败回滚；恢复时数据库连接与当前进程生命周期需要专门设计。

### 3.2 路线 B：扩展现有 `.enc` v2 为完整逻辑导出

逐表序列化全部业务数据和内容文件，升级格式版本，由导入事务重建数据。

- 优点：可以做字段级校验和选择性版本兼容，延续当前密码加密体验。
- 风险：合同庞大且容易漏表、漏字段、漏文件或破坏引用；每次 schema 变化都需要维护导出/导入映射；大内容经 JSON/base64 会增加体积和内存压力。

### 3.3 路线 C：保持手工数据卷恢复，仅改善文档

不满足“一步完成整站迁移”的已确认目标，只能作为现状保留或故障兜底，不作为目标方案。

---

## 四、设计前必须回答的问题

1. **入口范围：** 仅允许 `configured=false` 的 Setup 恢复，还是也允许管理员覆盖已有站点？
2. **文件关系：** 新增独立扩展名/格式，还是升级现有 `.enc`；旧 v1/v2 配置文件如何继续识别？
3. **一致性：** 如何保证 SQLite 快照与内容文件来自同一逻辑时点；导出期间新增版本如何处理？
4. **原子恢复：** 当前服务已打开数据库时如何验证、落盘、切换和重启；任一步失败如何完整回滚到未配置状态？
5. **版本兼容：** 是否只允许同版本恢复；若允许旧版本，迁移是在离线快照上还是首次启动时执行？新版本文件导入旧程序如何拒绝？
6. **加密封装：** KDF/AEAD 参数、文件头、版本、完整性校验、密码错误提示、体积上限和流式处理如何定义？
7. **敏感数据：** Token、密码哈希、节点凭据、OIDC/SMTP 密文是否原样迁移；密钥旋转与重加密是否需要支持？
8. **外部状态：** Xray 实例账号、OIDC 回调、SMTP、远程素材源及旧域名如何在迁移后核验或重新绑定？
9. **双站并行：** 原站点与新站点同时运行时，共用 Token、签名密钥和远端账号可能带来什么风险，是否必须展示停旧站提示？
10. **恢复后验收：** 如何自动核对表计数、外键、内容文件哈希、当前版本指针、管理员存在性与关键业务引用？

---

## 五、候选验收边界

后续若进入构建，至少应分别验证：

- 错误密码、篡改文件、截断文件、未知版本和超限文件均在变更前拒绝；错误信息不泄露内容。
- 恢复失败不会留下半库、半文件、已配置但无管理员或密钥不匹配状态。
- 恢复成功后，用户可用原账号登录，角色/组归属正确；节点、平台、规则、分享、装配版本和内容文件数量及引用一致。
- 数据库 `foreign_key_check`、schema 迁移、当前版本指针和文件哈希核对通过。
- 目标域名变化时，OIDC 回调、`frontend_url`、公开下载地址等有明确阻断或修正流程。
- 自动化测试、临时 Production 恢复、Docker 数据卷恢复和用户浏览器人工验收分开记录，不以单一测试替代完整接受标准。

---

## 六、后续流程与停止边界

1. 当前只登记构想，不创建 Build5/Build26，不修改实现。
2. 后续先审计完整 schema、内容目录、备份实现、启动迁移和外部状态依赖。
3. 将路线对比、威胁模型、文件格式和恢复状态机提交用户决策。
4. 用户确认设计后，再编写独立 Build 文档和逐步验收计划。

---

## 七、变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-09-11 | 根据用户确认登记“完整数据加密导出 + Setup 一步整站迁移”构想；记录现状差距、候选路线、安全与一致性问题及后续停止边界。本轮不实施。 |
