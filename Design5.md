# Design5.md — 后续增量设计：整站迁移候选与邮件内容定制

> **文档定位：** 本文分开记录两个后续专题：§一～§六是“加密导出并通过 Setup 一步整站迁移”的候选构想；§七是用户已确认的“可编辑邮件内容与完整 URL 超链接”增量设计。两项不共用构建范围。
> **编码约束：** 遵循 [AGENTS.md](AGENTS.md)（唯一强要求）。现行配置导入/导出与完整备份基线见 [Design1.md](docs/reports/Design/Design1.md) §3.4.8/§7.2；当前配置导出缺陷 R31-01 见 [ExportRelated1.md](ExportRelated1.md)；Xray 实例与独立账号的 v2 增量见 [Design2.md](docs/reports/Design/Design2.md) §5.4；当前节点编辑器设计继续以 [Design4.md](Design4.md) 为准。
> **设计状态：** v0.8，2026-09-16 更新。整站迁移仅面向 Production，仍处于研究候选阶段，未创建对应 Build、未授权实施；其文件格式、恢复入口、安全模型与兼容策略尚未定稿。邮件内容专题的首版范围、审批通过登录链接及完整 URL 显示方式已由用户确认，并已由 [docs/reports/Build/Build28.md](docs/reports/Build/Build28.md) 完成 Step 0.5～8 工程实现、联合门禁、隔离 smoke 及归档后严格 JSON/implicit TLS/前端边界/smoke 五键/64 KiB 与 API 负向回归；2026-09-16 又完成 [Issue18.md](Issue18.md) 的有效站点名称一致性修复。正式 SMTP、真实收件箱、邮件客户端链接表现与真实浏览器人工项见 [ProdTestList.md](ProdTestList.md)，不计入整站迁移候选范围。

---

## 一、背景与现状差距

### 1.1 用户目标

在 Production 通过一个加密导出文件携带完整站点数据，在另一套未配置的 Production 部署的 Setup 页面上传该文件并输入密码后，一步完成整站迁移。迁移完成后的目标站点应恢复可登录账号、业务数据、内容文件、配置与必要引用关系，而不要求部署者手工进入容器解包数据卷。Dev 不提供整站导出或导入。

### 1.2 当前能力边界

| 能力 | 当前范围 | 与目标的差距 |
|------|----------|--------------|
| 配置导出 `.enc` | 加密导出系统配置、签名密钥、站点 ICON；v2 另含 Xray 实例与独立账号 | 不含用户、组、平台、节点、订阅、规则、分享、自定义内容、各类 Token、装配数据和内容文件，不能完成整站迁移 |
| Setup 配置导入 | 解密 `.enc`，整体覆盖系统配置并创建默认组/默认平台 | 只完成配置初始化；旧账号与绝大多数业务数据不会恢复 |
| 下载备份 `tar.gz` | SQLite 一致性快照 + 内容目录 + public 资源 | 数据完整，但当前未加密，且恢复需要部署者手工解包到数据卷；Setup 无恢复入口 |

2026-09-11 的 R29-01 后续核查验证了上述边界：v2 基础模式 `.enc` 导入成功后，系统配置生效，但用户、节点、装配蓝图等业务表为空。这符合现行设计，但不满足本设计新增的整站迁移目标。

2026-09-14 用户真机、本地人工核验确认现有导入导出可用，但数据仍无法正确备份。该能力缺口转入本候选设计，待后续研究完整加密备份与整站迁移方案；Build26 R28-07 A 的导入确认词、20 MiB 边界和错误体验等相关人工复验一并作为本设计的后继范围，当前不作为 Issue15 问题追踪。

### 1.3 已确认目标与非结论

- **已确认目标：** 完整数据、加密文件、Setup 一步迁移；整站导出与导入仅限 Production，不考虑 Dev 导出/导入。
- **已确认认证边界：** Production 整站恢复在目标数据替换前拒绝非 Production 来源文件及任何 mock 配置（生效提供商为 mock，或存在 `oidc_params_mock` 键，即使值为空）；与 [Issue17.md](Issue17.md) R31-06 的现有 Production 配置导入整体拒绝规则一致。整站文件包含用户及 OIDC 身份，不能只检查当前提供商；来源模式须由新格式元数据表达，不从待恢复的 `system_config.app_mode` 推断。元数据如何验证仍随文件格式与安全模型设计，不能将其视为不可伪造的来源证明。现有配置导入的 R31-06 安全修复不等待本候选实施。
- **暂不实施：** 本轮不修改现有导出/导入、备份、数据库或 UI。
- **尚未确认：** 是扩展现有 `.enc` 配置格式，还是为完整备份建立独立加密格式和入口。
- **尚未确认：** 是否允许向已配置实例恢复、是否只允许未配置新库、是否支持跨版本迁移及允许的版本跨度。
- **尚未确认：** 原站点运行期间导出的一致性、目标端失败回滚、恢复后的外部依赖重连和旧站点并行运行策略。

---

## 二、候选能力范围

完整整站迁移至少需要研究以下数据域，最终清单必须由 schema 与内容目录审计形成，不能只凭当前页面枚举：

| 数据域 | 候选迁移内容 | 主要风险 |
|--------|--------------|----------|
| 系统与认证 | `system_config`、签名密钥、OIDC/SMTP 等加密配置 | 文件密码成为站点密钥与敏感配置的安全边界；域名变化影响回调地址 |
| 用户与权限 | 用户、密码哈希、角色、状态、组归属、凭据版本 | 恢复后可直接登录；不得产生无管理员或权限漂移 |
| 业务结构 | 用户组、平台、节点、代理组、素材池、规则、分享、装配蓝图及版本元数据 | 外键、稳定标识、排序、当前版本指针与跨表引用必须一致 |
| Token 与凭据 | 下载 Token、节点凭据、Xray 用户/独立账号密文 | 必须与恢复后的签名/加密密钥配套；日志和错误响应不得泄露明文 |
| 内容文件 | 订阅/规则/分享/自定义版本文件、站点资源及必要的当前指针 | DB 与文件必须来自同一一致性时点；路径穿越与符号链接恢复需受控 |
| 运行时/历史数据 | 同步快照、任务记录、流量记录等 | 哪些属于迁移事实、哪些应在新实例重建或丢弃，需要逐项决策 |

日志、临时文件、进程内任务和可重建缓存是否迁移，暂不预设结论。

上述 Production 与 mock 恢复边界适用于路线 A、B，尚不决定沿用 `.enc` 还是采用独立加密备份格式。归档 Design1 §3.4.8 曾因 Dev 不提供配置导出而认为无需拦截 mock 流入 Production；当前用户已明确选择现行配置导入及未来整站恢复均在 Production 整体拒绝 mock 配置，以本节与 Issue17 R31-06 的新结论为准，不改写历史归档。

---

## 三、候选方案（待研究与用户决策）

### 3.1 路线 A：独立的加密完整备份格式（初步推荐，未确认）

在现有一致性备份能力上增加整体加密与带版本清单的封装，并为 Setup 提供专用“恢复完整备份”入口。现有“配置导入”继续保持轻量配置迁移定位。

- 优点：配置迁移与灾难恢复职责清楚；可以复用 SQLite 一致性快照和内容目录打包；不必把所有业务表重复编码为大型 JSON 合同。
- 风险：必须安全解包、验证清单、处理空间上限、原子替换数据卷和失败回滚；恢复时数据库连接与当前进程生命周期需要专门设计。

### 3.2 路线 B：扩展现有 `.enc` v2 为完整逻辑导出

逐表序列化全部业务数据和内容文件，升级格式版本，由导入事务重建数据。

- 优点：可以做字段级校验和选择性版本兼容，延续当前密码加密体验。
- 风险：合同庞大且容易漏表、漏字段、漏文件或破坏引用；每次 schema 变化都需要维护导出/导入映射；大内容经 JSON/base64 会增加体积和内存压力。

### 3.3 路线 C：保持手工数据卷恢复，仅改善文档

不满足“一步完成整站迁移”的已确认目标，只能作为现状保留或故障兜底，不作为目标方案。

---

## 四、设计前必须回答的问题

1. **入口范围：** 仅允许 `configured=false` 的 Setup 恢复，还是也允许管理员覆盖已有站点？
2. **文件关系：** 新增独立扩展名/格式，还是升级现有 `.enc`；旧 v1/v2 配置文件如何继续识别？
3. **一致性：** 如何保证 SQLite 快照与内容文件来自同一逻辑时点；导出期间新增版本如何处理？
4. **原子恢复：** 当前服务已打开数据库时如何验证、落盘、切换和重启；任一步失败如何完整回滚到未配置状态？
5. **版本兼容：** 是否只允许同版本恢复；若允许旧版本，迁移是在离线快照上还是首次启动时执行？新版本文件导入旧程序如何拒绝？
6. **加密封装：** KDF/AEAD 参数、文件头、版本、完整性校验、密码错误提示、体积上限和流式处理如何定义？
7. **敏感数据：** Token、密码哈希、节点凭据、OIDC/SMTP 密文是否原样迁移；密钥旋转与重加密是否需要支持？
8. **外部状态：** Xray 实例账号、OIDC 回调、SMTP、远程素材源及旧域名如何在迁移后核验或重新绑定？
9. **双站并行：** 原站点与新站点同时运行时，共用 Token、签名密钥和远端账号可能带来什么风险，是否必须展示停旧站提示？
10. **恢复后验收：** 如何自动核对表计数、外键、内容文件哈希、当前版本指针、管理员存在性与关键业务引用？

---

## 五、候选验收边界

后续若进入构建，至少应分别验证：

- 错误密码、篡改文件、截断文件、未知版本和超限文件均在变更前拒绝；错误信息不泄露内容。
- 非 Production 来源文件或含任何 mock 配置的整站文件在目标数据替换前整体拒绝；失败后数据库与内容文件均不变。
- 恢复失败不会留下半库、半文件、已配置但无管理员或密钥不匹配状态。
- 恢复成功后，用户可用原账号登录，角色/组归属正确；节点、平台、规则、分享、装配版本和内容文件数量及引用一致。
- 数据库 `foreign_key_check`、schema 迁移、当前版本指针和文件哈希核对通过。
- 目标域名变化时，OIDC 回调、`frontend_url`、公开下载地址等有明确阻断或修正流程。
- 自动化测试、临时 Production 恢复、Docker 数据卷恢复和用户浏览器人工验收分开记录，不以单一测试替代完整接受标准。

---

## 六、整站迁移专题的后续流程与停止边界

1. 本专题当前只登记整站迁移构想，不创建整站迁移 Build、不修改相关实现；邮件内容构建计划另见 §七与 Build28。
2. 后续先审计完整 schema、内容目录、备份实现、启动迁移和外部状态依赖。
3. 将路线对比、威胁模型、文件格式和恢复状态机提交用户决策。
4. 用户确认设计后，再编写独立 Build 文档和逐步验收计划。

---

## 七、邮件内容定制（已确认设计；实施计划见 Build28）

### 7.1 目标、现状与范围

管理员在“通知”分组的 SMTP 设置与连接测试之后，通过独立“邮件内容”卡片编辑业务邮件主题和正文。SMTP 连接参数、认证密码与业务文案分别保存；连接测试邮件继续使用固定文案，不受模板草稿影响。当前 [`mail.Service`](backend/internal/mail/mail.go) 内置纯文本主题/正文，三类业务开关由 `smtp_enabled_scopes` 控制；此前预留见归档的 [Issue16.md](docs/reports/Issue/Issue16.md)“自定义发信内容”小节。参考 [Gitea 按事件选模板、缺失自定义项回退内置模板的做法](https://docs.gitea.com/administration/mail-templates/)，本项目选择更小的面板内纯文本编辑与预览，不引入完整模板语言或文件覆盖机制。

首版仅开放五个业务分支；每个分支分别编辑主题与正文，未自定义时使用程序内置默认文案：

| 分支 | 正文可用变量 | 必须保留 | 发送开关 |
|---|---|---|---|
| 密码重置 | `{{reset_url}}` | `{{reset_url}}` | `password_reset` |
| 审批通过 | `{{site_name}}`、`{{login_url}}` | `{{login_url}}` | `approval_notify` |
| 审批拒绝 | `{{site_name}}` | 无 | `approval_notify` |
| 本地欢迎 | `{{site_name}}`、`{{login_url}}` | `{{login_url}}` | `welcome` |
| OIDC 欢迎 | `{{site_name}}`、`{{login_url}}` | `{{login_url}}` | `welcome` |

审批通过的当前实际路径只调用欢迎邮件，虽然邮件服务已有“审批通过通知”的内置文案。用户确认首版改为：审批通过时只发送一封“审批通过通知”，由 `approval_notify` 控制；该分支默认正文和任何自定义正文都必须包含可用的 `{{login_url}}`，保留原欢迎邮件中的登录入口。其他首次激活继续由 `welcome` 控制并按本地/OIDC 来源选择欢迎文案。此项新决定明确覆盖归档 Design1 §3.4.6 对“审批通过发欢迎邮件”的旧描述。两个开关同时启用时，审批通过也只发一封。仅开启 `welcome` 的既有站点在实施后将不再收到审批通过邮件，设置页与实施说明须明确提示这一行为变化；不静默改动管理员已有开关。

### 7.2 编辑与预览体验

- 卡片一次选择一个分支，显示“默认文案／已自定义”状态。管理员可从默认文案开始修改主题与正文，点击变量按钮插入占位符；正文是保留换行的纯文本编辑区，不要求编写 HTML、CSS 或脚本。
- 旁边即时预览当前未保存草稿的主题与正文；预览和保存分开，不发送邮件、不改 SMTP 设置。`{{site_name}}` 使用当前有效站点名称：优先取已保存且去除首尾空白后非空的名称，否则回退为“VPN 订阅管理”。预览仍只使用显示完整路径的合成登录/重置 URL，不读取真实一次性令牌。未保存的站点名称表单草稿不进入邮件预览。预览须由与实际发信共用的变量解析与 HTML 转义规则产生，并显示链接的完整可见文字；标注“示意预览，最终显示取决于邮件客户端”。预览请求失败或草稿校验失败时展示错误，不继续显示可能过期的旧预览。
- “保存当前模板”只保存所选分支；“恢复默认”移除该分支覆盖，使后续内置文案更新仍可生效。切换分支或离开页面时提示未保存草稿。

### 7.3 变量与完整 URL 链接的邮件语义

- 只识别各分支白名单中的字面占位符；不执行任意 Go 模板表达式、HTML 或用户脚本。主题仅允许纯文本及具备站点名的分支使用 `{{site_name}}`；`{{reset_url}}`、`{{login_url}}` 只允许在正文。主题必须非空且为单行；正文有长度上限；未知变量、跨分支变量、缺失必需链接变量或非法控制字符在保存前明确拒绝。实际发送时从当前站点与本次业务调用取值；`{{site_name}}` 与预览、公共站点信息共用上述有效站点名称规则。若必需的实际链接为空或不是有效 HTTP(S) URL，停止该封邮件并返回不含令牌的安全错误，业务主流程仍按既有失败不阻断语义运行。绝不在模板配置、预览响应或普通日志中保存真实重置 URL/令牌。页面提示实际重置链接仅 1 小时有效，管理员自定义文案不得将其说成更长时间。
- 管理员仍只编辑纯文本模板。实际发送时由同一渲染结果生成 `text/plain` 与最小 `text/html` 两个版本，采用 MIME `multipart/alternative`（参见 [RFC 2046](https://www.rfc-editor.org/info/rfc2046/)）。每个链接占位符在纯文本版展开为**完整原始 URL**；在 HTML 版展开为 `<a href="完整 URL">完整 URL</a>` 形式的锚点。正文中直接书写的完整 HTTP(S) URL 也按相同规则生成锚点；为避免标点与网址边界歧义，编辑区提示将直接书写的网址独立成行，渲染器按空白/换行分隔识别。链接目标和收件人看到的文字在邮件客户端解码后必须与同一个完整 URL 一致，包含协议、域名、路径、查询参数及存在时的片段。系统不得缩短、截断、改写链接，也不得把可见文字换成“点击这里”等标签或 Markdown 链接语法。HTML 源码中的属性/文字要分别转义，普通模板文字一律作为文本转义并保留换行；不得把管理员输入解释成 HTML。纯文本邮件客户端是否自动识别链接取决于客户端，因此 HTML 版本提供明确的可点击锚点，纯文本版本保留可复制的完整 URL。
- 不添加站点 Logo 变量、图片、CID、远程图片或附件；发送报文仅包含上述两个正文版本。预览亦只展示文本与链接。用户撤回先前的图片 Logo 方案，以避免图片被默认拦截或增加邮件投递负担；这不作为邮件一定不会进入垃圾邮件的保证。

### 7.4 保存、影响与验收边界

模板覆盖按分支独立存入系统配置，缺失覆盖即使用代码内置默认值。保存/恢复只影响模板键，不读写 SMTP 连接键；模板领域规则应留在邮件服务层，配置层只提供通用持久化，避免当前 `mail → config` 依赖形成循环。后端管理端点继续要求会话和管理员权限，预览响应不缓存。配置导出/导入、完整备份与恢复须保留有效模板覆盖；旧文件缺少模板键时回退到默认文案。导入文件中若含非法模板覆盖，须在整体覆盖前拒绝导入，避免留下半配置；运行时遇到损坏覆盖或读取失败，则记录不含模板正文/链接的安全告警、管理页提示该分支异常，并按内置默认文案发送，不让损坏配置吞掉必需链接。实施时核对 [ExportRelated1.md](ExportRelated1.md) R31-01 的现有配置导出缺陷，不能把针对模板键的隔离往返测试写成整个真实随机密钥导出问题已修复。

两个正文版本的传输编码、CRLF/行长、唯一 MIME 边界及中文主题编码都必须由邮件报文构造统一处理，不把仅在本地测试中可解析的原始 UTF-8 字节视作跨客户端兼容证据（参见 [RFC 2045](https://www.rfc-editor.org/info/rfc2045/) 与 [RFC 2047](https://www.rfc-editor.org/info/rfc2047/)）。邮件不得包含图片或附件部分。

自动化验收须覆盖五分支主题/正文渲染、主题与正文各自变量白名单、审批通过必需登录链接、默认及损坏覆盖回退、审批通过单封/开关变更、缺失/纯空白/已保存站点名称的有效值一致性、预览不含真实令牌且不复用过期响应、纯文本与 HTML 两个 MIME 部分、占位符及独立成行直接书写的长 URL/含查询参数 URL 在解码后的链接目标/可见文字/纯文本正文三处完全一致、空白/换行边界、HTML 转义、没有图片或附件部分、SMTP 测试文案不变、导入非法覆盖的整体拒绝、缺键回退和配置覆盖保留。真实 SMTP 接受、收件箱投递、不同客户端显示及浏览器可用性分别记录，不以单元测试或面板预览代替。

### 7.5 暂不纳入

不提供自由 HTML/富文本编辑器、任意变量或条件语法、图片 Logo 或附件、群发/营销邮件、SMTP 平台 API 适配、自定义连接测试邮件，也不在 Build28 中实施本文件 §一～§六的整站迁移候选。

### 7.6 实施状态（2026-09-15）

- Build28 Step 0.5～8 已按用户一次性授权严格串行完成，工程实现、逐步验收、联合门禁与隔离 SQLite + 本地 SMTP mock smoke 均通过；完整记录见 [docs/reports/Build/Build28.md](docs/reports/Build/Build28.md)。
- 已落地：五分支固定模板与持久化、严格校验、统一安全渲染与合成预览、双版本 MIME、五分支真实发送与 scope、四类管理 API、前端邮件内容卡片、v1/v2 导入校验、导出/备份往返测试。
- 归档后补强：严格 JSON 大小写/重复字段、implicit TLS、前端组件边界、smoke 五键与 64 KiB/API 负向回归均已通过。
- 尚未完成：正式 SMTP 投递、真实收件箱、不同邮件客户端链接表现、真实浏览器交互与跨反代显示；这些项目记录在 [ProdTestList.md](ProdTestList.md)，不能以本地 mock、单元测试或构建结果替代。
- 本状态只同步已由 Build28 验证的 §七实施事实，不改变 §一～§六候选迁移范围，也不表示 R31-01 已闭环。
- 2026-09-16 后续修复统一了有效站点名称：公共/管理站点信息、邮件预览与实际发送均优先使用已保存非空名称，否则回退“VPN 订阅管理”；示例链接/真实令牌隔离边界不变。闭环记录见 [Issue18.md](Issue18.md)。


---

## 八、变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-09-11 | 根据用户确认登记“完整数据加密导出 + Setup 一步整站迁移”构想；记录现状差距、候选路线、安全与一致性问题及后续停止边界。本轮不实施。 |
| v0.2 | 2026-09-14 | 根据 Production 真机、本地人工核验结果补记：现有导入导出可用，但数据仍无法正确备份；该能力缺口及 Build26 R28-07 A 相关导入复验转入本候选设计待后续改进，当前不作为 Issue15 问题追踪，未实施代码或格式变更。 |
| v0.3 | 2026-09-14 | 增加已确认的邮件内容专题：五分支纯文本编辑与即时预览、审批通过单封及开关归属、可选 `{{site_logo}}` 与 PNG/JPEG 内嵌/文字回退；Build28 单独承接邮件功能，整站迁移仍为候选。 |
| v0.4 | 2026-09-14 | 设计复核：按用户确认补审批通过必需登录链接；明确主题/正文变量、HTML 链接、预览失效、损坏模板/导入回退、模块依赖和 MIME 传输边界。 |
| v0.5 | 2026-09-14 | 按用户新决定撤回图片 Logo/CID 方案；邮件使用完整 URL 可见文字与目标相同的 HTML 锚点，另保留完整 URL 的纯文本版本，不含图片和附件。 |
| v0.6 | 2026-09-15 | 按用户确认整站导出/导入仅限 Production，恢复前拒绝非 Production 来源及任何 mock 配置；关联 Issue17 R31-06 的现行配置导入修复，保留格式与恢复机制待定。 |
| v0.7 | 2026-09-15 | 同步 Build28 §七实施与归档状态；补充归档后严格 JSON、implicit TLS、前端边界、smoke 五键与 64 KiB/API 负向回归结果；整站迁移仍为候选，R31-01 仍独立跟踪。 |
| v0.8 | 2026-09-16 | 按用户确认将 `{{site_name}}` 与站点信息统一为“已保存非空名称优先，否则回退 VPN 订阅管理”；保留预览合成 URL 和真实令牌隔离，闭环见 Issue18。 |
