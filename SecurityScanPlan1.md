# SecurityScanPlan1.md — VPN 订阅管理系统第三期完整网络安全审查计划

> **文档定位：** 本文档是当前项目第三期网络安全审查的**执行计划与进度入口**，用于把完整审查拆分成可在单次约 200K 上下文内独立完成、独立验收和独立交接的 Steps。本文档只规定如何审查，不记录最终漏洞结论，也不授权修改业务代码。
>
> - 唯一强要求：[AGENTS.md](AGENTS.md)
> - 当前设计：[Design2.md](Design2.md)、[Design2-UI.md](Design2-UI.md)
> - 历史审查：[SecurityReport1.md](docs/reports/SecurityReport/SecurityReport1.md)、[SecurityReport2.md](docs/reports/SecurityReport/SecurityReport2.md)
> - 文档结构参考：[Build.template.md](docs/DocTemplates/Build.template.md)
> - 本地测试账号：[TestPasswordList.md](docs/Reference/TestPasswordList.md)
> - 本期结果载体：计划在 Step 1 创建根目录 [SecurityReport3.md](SecurityReport3.md)，审查期间逐 Step 追加，完成并经用户确认后再决定是否归档
> - OWASP 主口径：[OWASP Top 10:2025](https://owasp.org/Top10/2025/0x00_2025-Introduction/)；为兼容前两期报告，同时保留 OWASP Top 10:2021 编号映射
>
> **已确认执行方向（2026-08-30）：** 静态代码审查 + 自动化扫描 + 本地隔离动态验证；WAF 只作为背景条件，不替代应用安全控制；每步向同一份 `SecurityReport3.md` 写入独立检查点；审查与修复分离，发现问题先记录、分级并由用户确认，不在审查 Step 内直接修改业务代码。

---

## 一、审查目标、边界与授权

### 1.1 目标

1. 对当前项目的后端、前端、数据库迁移、内嵌静态资源、容器构建、CI、依赖与对外网络交互做一次可复核的完整应用安全审查。
2. 以 OWASP Top 10:2025 为主线，补齐前两期“未运行项目”的证据缺口，并回归历史修复和历史未处理项。
3. 结合本项目实际业务，重点覆盖 Setup、注册/登录、OIDC、Cookie/Bearer 双通道、权限实时校验、下载 Token、分享、规则、订阅装配、素材池、导入导出、备份、Xray 对接、应急恢复和日志流。
4. 每个发现必须包含可定位证据、成立条件、实际影响、现有缓解、严重度和建议；不得用搜索命中数量代替人工确认。
5. 最终形成当前基线下的 OWASP 对照表、历史项回归矩阵、发现项总表、正面安全资产清单、未验证边界与后续处置建议。

### 1.2 威胁模型

- 自部署、小团队或个人站，约 10–50 名使用者，非商业运营，不主动宣传或进行 SEO。
- 通常位于 WAF/反向代理之后；WAF 可降低常见噪声和容量型攻击，但不能作为认证、授权、输入校验、SSRF、数据保护等应用控制的替代品。
- 默认考虑匿名外部访问者、普通已登录用户、待审批用户、管理员凭据被滥用、低权限账号尝试水平/垂直越权、恶意输入与意外异常状态。
- 不按大型商业平台、超高并发、国家级攻击、长期定向渗透或极端资源耗尽场景设计审查强度。
- 管理员专用功能仍属于应用攻击面；“仅管理员可用”计入利用前置条件和较低可能性，但不自动从审查范围排除。

### 1.3 纳入范围

| 范围 | 主要对象 |
|------|----------|
| 后端 | `backend/cmd/`、`backend/internal/`、`backend/migrations/`、`backend/web/` |
| 前端 | `frontend/src/`、`frontend/tests/`、Vite/Vitest/Tailwind 配置、`package.json`/锁文件 |
| 数据与文件 | SQLite schema/迁移、`/data`、`/public`、导入导出、备份、版本文件、安装包 |
| 网络边界 | HTTP API、公开下载、OIDC 出站、素材池 URL 同步、SMTP、Xray gRPC、可信代理头 |
| 交付链 | `Dockerfile`、Compose 模板、GitHub Actions、Go/npm 依赖、内嵌前端产物 |
| 设计与历史 | AGENTS、当前 Design/Build/Issue、两期 SecurityReport 及其声称的修复状态 |

### 1.4 明确排除

- 宿主机操作系统更新、SSH、系统账号、内核、防火墙、安全组、端口白名单和服务器基线加固。
- WAF/CDN 厂商规则配置、源站隐藏、厂商 IP 段维护、容量型 DDoS 和流量清洗能力。
- TLS 证书申请、续签和外部反向代理的具体配置；但应用对可信 `X-Forwarded-*`、Secure Cookie、回调 URL 的处理仍纳入。
- 部署者对备份介质、日志平台、SMTP/OIDC/Xray 服务器本身的运维安全；但应用生成的备份、日志和凭据是否安全仍纳入。
- 针对真实生产站点、真实第三方 OIDC/SMTP/Xray 服务的攻击、扫描、压力测试或破坏性操作。
- 与本项目应用无关的物理安全、社工、SEO、营销滥用和商业风控。

### 1.5 操作授权与禁止事项

- 审查期间允许：只读源码/文档检查、编译测试、依赖扫描、在本地隔离实例中使用合成数据进行非破坏性动态验证、向 `SecurityReport3.md` 和本计划回写进度。
- 用户已确认当前项目的本地构建环境不存在任何真实用户或真实数据。进行本计划内的本地测试时，允许清空仓库根目录下的 `./backend/data` 临时数据库目录，并按 [TestPasswordList.md](docs/Reference/TestPasswordList.md) 重建测试用户；若清单中的测试用户已经存在，也可直接使用对应邮箱和密码登录。
- 上述清空授权只适用于当前仓库的本地 `./backend/data`。执行前必须确认当前仓库根目录和目标的规范化绝对路径、停止使用该数据库的本地进程，并使用解析后的精确目标；不得扩大到外部 `DATA_DIR`、Compose/Docker 数据卷、生产/预发布环境或任何包含真实数据的位置。
- 未经另行确认禁止：修改业务代码、依赖、迁移、Docker/CI 配置、历史报告和设计文档；访问生产环境；使用真实密钥或真实用户数据；向第三方服务发送测试载荷；执行重型压力、持久化利用或数据破坏。
- 如某项验证需要新增仓库内测试代码、安装新扫描器、联网查询漏洞库、启动外部模拟服务或扩大范围，必须先在该 Step 记录必要性和影响；可逆且只在临时目录中的辅助脚本优先。
- 若发现疑似 Critical/High 且继续验证可能扩大影响，立即停止利用链，保留最小证据并向用户报告，不追求完整武器化 PoC。

---

## 二、上下文控制与逐 Step 执行协议

### 2.1 单 Step 上下文预算

- 每次任务**只执行一个 Step**，不得顺手开始下一 Step；目标使用量控制在约 120K–160K，硬上限约 200K。
- 一个 Step 只审一个边界明确的安全域。若文件或端点数量超出本 Step 承载能力，先按服务/路由分片，在计划中新增子 Step，经用户确认后再继续；不得依赖上下文压缩硬塞完成。
- 每步开始前只加载该 Step 的前置检查点、目标文件和必要设计依据，不重复通读所有历史文档。
- 每步结束必须完成“证据胶囊”，使新的上下文只读报告中的该胶囊即可继续，不依赖聊天记录。

### 2.2 每步固定流程

1. **基线确认：** 记录分支、HEAD、`git status --short`、本 Step 开始时间；如代码基线相对 Step 1 变化，先判断是否影响已审结论。
2. **范围锁定：** 列出本步包含/排除的文件、端点、角色、状态和动态场景。
3. **静态走读：** 从入口追到接入层、业务层、数据层和输出/副作用，不只做关键词搜索。
4. **测试与扫描：** 先复用已有测试；需要动态验证时只使用隔离实例、合成账号和受控载荷。
5. **双向取证：** 同时记录缺陷证据和已正确实现的正面控制，避免只看异常路径。
6. **反证与去重：** 检查是否已有中间件、事务、编码器、限制器或测试消除了初步疑点；与历史 F/L/N 编号去重。
7. **报告写入：** 向 `SecurityReport3.md` 追加本 Step 的完整检查点和发现项，不覆盖其他 Step。
8. **验收与交接：** 执行本步验收命令，更新本计划状态，只在本步证据完整后标记 ✅。

### 2.3 证据胶囊模板

每个 Step 在 `SecurityReport3.md` 中必须以如下结构落地：

```markdown
### Step N 检查点 — <标题>

- 基线：<branch / commit / dirty paths / 时间>
- 实际范围：<文件、端点、角色、状态>
- 执行命令：<命令与关键版本>
- 结论：通过 / 有发现 / 部分阻塞
- 新发现：S3-XX（如无则写“无”）
- 历史项：Fxx/Lxx/Nxx → 已修复 / 回归 / 未落地 / 不适用 / 证据不足
- 正面控制：<至少列出已人工确认的有效控制>
- 未验证项：<原因与所需条件，不得省略>
- 证据索引：<文件:行、测试名、HTTP 请求/响应编号、日志/扫描输出摘要>
- 下一步交接：<后续 Step 只需知道的事实，建议不超过 800 字>
```

### 2.4 发现项格式与严重度

每个新发现使用连续编号 `S3-01`、`S3-02`……，至少包含：

- 标题、严重度（Critical / High / Medium / Low / Informational）和置信度（High / Medium / Low）。
- OWASP Top 10:2025 分类、兼容的 2021 分类、CWE；适用时给出 CVSS 向量，但不得用分数替代本项目语境分析。
- 受影响资产、角色、入口、前置条件、攻击路径、影响范围和是否可被 WAF 缓解。
- 精确源码证据与动态证据；无法动态验证时明确标注“仅静态成立”。
- 现有缓解、实际可利用性、10–50 人非商业部署下的风险校准。
- 最小修复建议、可能副作用、建议测试；不在本次审查中直接实施。
- 与历史 F/L/N 项、Issue/Build 的关系以及“新发现/历史回归/历史未落地/观察项”标签。

### 2.5 基线变化规则

- Step 1 固定审查基线；之后如工作区出现与本审查无关的用户改动，保留改动并在每步记录，不得清理或覆盖。
- 若变更命中尚未审查的文件：按新基线继续，并记录首次纳入的 commit/工作区状态。
- 若变更命中已完成 Step：将受影响 Step 标为“需增量回归”，只回归受影响结论，不从头重审全部内容。
- 最终 Step 28 必须给出“固定基线结论”和“审查期间增量变化”两套清单，禁止把漂移后的状态冒充初始基线。

---

## 三、审查进度追踪（TODO LIST）

> 状态：☐ 未开始 / ◧ 进行中 / ⛔ 阻塞 / ↺ 需增量回归 / ✅ 验收通过。每次只允许一个 Step 为 ◧。

| Step | 内容 | OWASP 2025 主映射 | 状态 |
|------|------|-------------------|------|
| 1 | 固定基线、创建报告骨架与证据目录 | 全局 | ☐ 未开始 |
| 2 | 前两期报告与当前实现逐项回归矩阵 | 全局 | ☐ 未开始 |
| 3 | 架构、资产、数据流与信任边界建模 | A06 | ☐ 未开始 |
| 4 | 后端 API、前端路由与保护链全量清点 | A01/A02 | ☐ 未开始 |
| 5 | 本地隔离动态环境与合成数据基线 | A02/A06 | ☐ 未开始 |
| 6 | Go/npm/容器/CI 软件供应链审查 | A03/A08 | ☐ 未开始 |
| 7 | 应用交付与运行时安全配置审查 | A02/A10 | ☐ 未开始 |
| 8 | Setup、本地认证、注册与应急模式审查 | A06/A07/A10 | ☐ 未开始 |
| 9 | OIDC 发现、登录、绑定与换票链审查 | A01/A04/A07 | ☐ 未开始 |
| 10 | 会话、Bearer/Cookie、CSRF 与凭据失效审查 | A01/A07/A08 | ☐ 未开始 |
| 11 | RBAC、对象所有权、管理员与高级模式授权审查 | A01 | ☐ 未开始 |
| 12 | 下载 Token、分享、公开内容与 SSE 凭据审查 | A01/A04/A07 | ☐ 未开始 |
| 13 | 密码重置、验证码、审批与邮件安全审查 | A06/A07/A10 | ☐ 未开始 |
| 14 | SQL、命令、配置、CRLF 与解析器注入审查 | A05/A10 | ☐ 未开始 |
| 15 | 前端 XSS、DOM、Markdown、URL 与第三方组件审查 | A05 | ☐ 未开始 |
| 16 | CSP、安全响应头、CORS、跳转与缓存策略审查 | A02/A04 | ☐ 未开始 |
| 17 | 路径穿越、文件上传/下载、静态资源与临时文件审查 | A01/A05/A08 | ☐ 未开始 |
| 18 | 配置导入导出、备份、清空与数据完整性审查 | A04/A08/A10 | ☐ 未开始 |
| 19 | 密码学、密钥派生、凭据存储与敏感数据生命周期审查 | A04/A08 | ☐ 未开始 |
| 20 | SSRF、DNS rebinding、重定向与全部出站网络审查 | A01/A02/A10 | ☐ 未开始 |
| 21 | 请求限制、超时、限流、任务与小规模 DoS 审查 | A02/A06/A10 | ☐ 未开始 |
| 22 | 业务一致性、并发事务、级联、预览/生成竞态审查 | A06/A08/A10 | ☐ 未开始 |
| 23 | Xray 集成、协议参数、凭据与对账副作用审查 | A01/A04/A06/A10 | ☐ 未开始 |
| 24 | 日志脱敏、错误处理、调试、审计与告警能力审查 | A09/A10 | ☐ 未开始 |
| 25 | 匿名、公开、Setup 与异常状态黑盒动态验证 | A01/A02/A07/A10 | ☐ 未开始 |
| 26 | 普通用户、管理员与跨角色黑盒动态验证 | A01/A07/A08 | ☐ 未开始 |
| 27 | 恶意输入回归、历史修复回归与全量验证 | 全局 | ☐ 未开始 |
| 28 | 发现去重、风险校准、OWASP 总结与最终交付 | 全局 | ☐ 未开始 |

---

## 四、产出文件与依赖关系

### 4.1 预期产出

| 产出 | 用途 | 允许变更阶段 |
|------|------|--------------|
| `SecurityScanPlan1.md` | 审查计划、进度和执行边界 | 每步仅更新状态/必要的范围修订 |
| `SecurityReport3.md` | 单一当前审查报告，逐 Step 追加证据 | Step 1 创建；Step 2–28 追加 |
| 临时证据目录 | 请求响应、扫描原始输出、测试数据；不得含真实秘密 | 使用 `mktemp -d`，默认不入库 |
| Issue/Build/Design | 修复和设计决策 | 本计划不创建；审查完成后由用户另行确认 |

### 4.2 顺序依赖

```text
Step 1 基线
  ├─> Step 2 历史回归
  ├─> Step 3 信任边界 ─> Step 4 路由清单 ─> Step 8–24 各安全域
  └─> Step 5 隔离环境 ─> Step 25–27 动态验证

Step 6–24 的静态结论 + Step 25–27 的动态证据
  └─> Step 28 去重、校准与最终交付
```

- Step 1–5 必须串行完成，确保所有后续 Step 使用相同基线、资产表和测试环境。
- Step 6–24 仍按编号串行执行；不得为了速度并行修改同一报告或在不同基线上审查。
- Step 25 依赖公开面清单（Step 4）和隔离环境（Step 5）；Step 26 依赖授权矩阵（Step 11）；Step 27 依赖所有领域审查结论。
- Step 28 只有在 1–27 全部 ✅，或明确列出并经用户接受的阻塞项后才能开始。

---

## 五、分步审查计划

### Step 1：固定基线、创建报告骨架与证据目录

- **目标：** 固定可复现的审查对象，建立 `SecurityReport3.md` 目录、发现编号规则、检查点模板和证据索引。
- **前置条件：** 用户已确认本计划；工作区任何既有变更都视为用户资产并保留。
- **审查内容：** 记录分支/commit/dirty paths、Go/Node/npm/Docker 版本、源文件和测试数量、主要配置文件哈希；区分仓库文件、生成产物和运行时数据；记录联网扫描的时间敏感性。
- **建议命令：** `git status --short --branch`、`git rev-parse HEAD`、`git diff --stat`、`rg --files`、`go version`、`node --version`、`npm --version`、`docker version`。
- **产出：** 创建 `SecurityReport3.md` 的范围、方法、基线、编号、严重度和 Step 检查点骨架；临时证据目录必须是明确的 `mktemp -d` 路径且不进入仓库。
- **验收标准：** 他人仅凭报告可定位同一代码基线；dirty files 无遗漏；未执行任何扫描或业务修改；本计划 Step 1 更新为 ✅。

### Step 2：前两期报告与当前实现逐项回归矩阵

- **目标：** 对 SecurityReport1 的 F01–F11/L01–L09、SecurityReport2 的 N01–N07/边界项/O1–O3 建立当前状态，不继承“已修复”文字结论。
- **前置条件：** Step 1。
- **审查内容：** 为每项追踪当前源码、测试、依赖与 CI；分类为已修复、回归、建议未落地、仍属已确认边界、被后续设计取代或证据不足；特别核对 OIDC 验签、安装包下载、日志脱敏、请求限制、可信代理、Cookie/CSRF、备份加密、验证码、重置 Token、CSP、依赖版本和 CI 门禁。
- **建议命令：** 按历史编号使用 `rg -n` 定位；只运行历史报告明确列出的定向测试和版本查询，不在本步扩展新漏洞搜索。
- **产出：** 历史项回归总表、存在漂移的文件清单、后续 Steps 必须重新验证的高风险假设。
- **验收标准：** 每个历史编号恰有一个当前状态和证据索引；历史“部署边界”与本期边界不一致时明确标注，交由对应领域 Step 重新评估。

### Step 3：架构、资产、数据流与信任边界建模

- **目标：** 用当前实现而非仅设计文档建立攻击面模型，防止后续遗漏跨模块链路。
- **前置条件：** Step 1–2。
- **审查内容：** 浏览器/WAF/反代/Go 服务/SQLite/数据卷/OIDC/SMTP/素材 URL/Xray 的信任关系；匿名、待审批、普通用户、管理员、高级模式、应急模式；凭据、个人信息、订阅正文、节点密钥、备份、日志等资产；数据进入、持久化、渲染、下载和外发路径。
- **方法：** 从 `cmd/server` 构造注入追踪各 Service/Handler/Store；对照 Design2 数据模型与迁移；标记跨信任边界的解析、解密、网络拨号和文件写入。
- **产出：** 文字化数据流图、资产清单、角色/状态表、信任边界编号 TB-01…，供后续发现引用。
- **验收标准：** 每个外部系统、公开入口、敏感资产至少出现在一条数据流中；设计与实现差异单列，不在本步自行选择结论。

### Step 4：后端 API、前端路由与保护链全量清点

- **目标：** 建立完整端点与页面清单，确认所有后续安全域都有可核对的入口集合。
- **前置条件：** Step 3。
- **审查内容：** 全部 Gin route/group、HTTP method、path 参数、handler、中间件顺序、角色/高级模式要求、请求体类型、响应类型和副作用；前端 public/admin 路由、路由守卫与 API 调用；健康检查、静态资源、SPA fallback、下载和 SSE 等非 JSON 入口。
- **方法：** 自动搜索只负责初始枚举，必须人工核对条件注册、应急模式替换路由、组级中间件和 handler 内二次授权。
- **产出：** API/页面攻击面矩阵，标出匿名、会话、管理员、高级模式、一次性 Token 和 Cookie/Bearer 认证来源。
- **验收标准：** 后端注册的每个路由恰有一条矩阵记录；前端调用但后端不存在、后端存在但未被矩阵覆盖的差异全部解释。

### Step 5：本地隔离动态环境与合成数据基线

- **目标：** 建立不会触及生产和第三方真实服务的可重复动态测试环境。
- **前置条件：** Step 1–4。
- **审查内容：** 使用唯一 Compose project/端口和临时 `/data`，或使用仓库本地 `./backend/data`；本地数据库状态不适合复用时，可以先清空 `./backend/data`，再按 [TestPasswordList.md](docs/Reference/TestPasswordList.md) 创建管理员、普通用户、待审批用户、禁用用户及其他专用测试用户；若这些用户已存在，可直接使用清单凭据登录。订阅/规则/分享/节点/素材池均使用合成样例；OIDC、SMTP、URL 源和 Xray 使用本地 mock 或保持关闭；记录清理方法。
- **安全要求：** 只绑定 `127.0.0.1`；删除前确认仓库根目录、停止占用数据库的本地进程，并核对目标的规范化绝对路径确实对应当前仓库的 `./backend/data`；不得使用未解析变量、宽泛 glob 或递归操作工作区根目录；不得清理外部 `DATA_DIR`、Compose/Docker 数据卷、生产/预发布数据；不得复用真实 Cookie/Token/邮箱/域名；不得把秘密或数据库提交到仓库；动态请求保存时对凭据做二次脱敏。
- **基础验证：** `/health`、Setup、使用测试账号登录、用户首页和管理员页的最小旅程；核对账号实际角色/状态与清单目标一致；记录 HTTP 客户端、浏览器和 mock 版本。
- **产出：** 环境启动/停止/清理命令、合成数据字典、角色凭据别名、已知不可模拟外部依赖。
- **验收标准：** 环境可从空数据重复创建；停止后不残留占用端口；清理目标是已解析的临时目录而非工作区或用户目录。

### Step 6：Go/npm/容器/CI 软件供应链审查

- **目标：** 覆盖 OWASP A03:2025 与 A08 的依赖、构建和发布完整性风险。
- **前置条件：** Step 1–5。
- **审查内容：** Go/npm 直接与间接依赖、可达漏洞、锁文件一致性、维护状态与生产可达性；Docker base image/运行时包；GitHub Actions 权限、可变 tag/digest、构建上下文、Registry 登录和产物标签；前端产物进入 `backend/web/dist` 的一致性。
- **建议命令：** `go list -m -json all`、记录版本后运行 `govulncheck ./...`、`npm ci`、`npm audit --audit-level=high`、`npm ls --all`、`docker build`；镜像扫描器仅在已安装或用户同意后使用，并记录漏洞库时间。
- **判定要求：** 区分代码可达、仅模块存在、仅 dev/build 链和运行时可达；不把宿主机补丁列为应用发现。
- **产出：** 依赖与构建链风险表、CI 门禁覆盖表、历史 N01/L05/L06 回归结论。
- **验收标准：** 每条扫描告警完成人工可达性判断；扫描器版本/数据库日期可复现；无“工具报 Critical 所以项目 Critical”的直接推断。

### Step 7：应用交付与运行时安全配置审查

- **目标：** 审查应用自身可控的安全默认值和异常配置，不进入宿主机/WAF 运维范围。
- **前置条件：** Step 3–6。
- **审查内容：** Docker 非 root、单卷权限、监听地址、Dev/Prod/Debug、默认配置、可信代理解析、`X-Forwarded-*` 信任一致性、HTTP server 超时、panic recovery、healthcheck、错误回显、环境变量边界、零配置启动；CI 产物与本地嵌入产物是否可能漂移。
- **动态验证：** 在隔离环境比较可信/不可信来源头、Dev/Prod 响应、异常启动和只读数据目录等安全相关状态；不测试真实 WAF 或证书。
- **产出：** 安全配置矩阵、默认值/失败模式/文档一致性结论，回归 F04/F05/F08/F09/L04/N06 相关部分。
- **验收标准：** 每个运维环境变量明确“应用责任/部署者责任”；仅因不符合大型平台最佳实践不得记为漏洞。

### Step 8：Setup、本地认证、注册与应急模式审查

- **目标：** 验证首次部署和救援状态不会形成未授权配置、首管理员竞态或 fail-open。
- **前置条件：** Step 4–5、7。
- **审查内容：** configured 判定、首管理员创建事务、Setup 端点匿名窗口、导入路径、注册开关、密码策略与 bcrypt、登录防枚举、限流、用户禁用、审批状态、应急操作码生成/比较/一次性消费、应急路由隔离和模式退出。
- **动态验证：** 并发首管理员、Setup 完成前后相同端点、重复/过期操作码、禁用与待审批账号、错误密码/不存在账号的响应等；限制为少量受控请求。
- **产出：** 状态机与测试矩阵，记录 fail-open/fail-closed 行为和历史 F02/F04/N03 关联。
- **验收标准：** Setup/正常/应急三种状态的公开面均有证据；并发结论由事务测试或可复现动态证据支持。

### Step 9：OIDC 发现、登录、绑定与换票链审查

- **目标：** 完整审查 OIDC 身份断言、出站发现和账户关联链。
- **前置条件：** Step 3–5、8。
- **审查内容：** discovery/JWKS 获取、issuer/audience/azp/exp/iat/nonce/state/PKCE/alg 校验、密钥轮换与缓存、回调错误、一次性 ticket、Cookie Secure 判定、账号自动合并/绑定/白名单/角色声明、首管理员 OIDC 竞态、测试端点授权和 SSRF 防护。
- **动态验证：** 仅用本地 mock OIDC 覆盖正确 token、错误签名、`alg=none`、错误 iss/aud、过期、nonce/state 重放、ticket 重放、声明类型异常和回调恢复；不连接真实提供商。
- **产出：** OIDC 时序与威胁矩阵，回归 F01/F06/L01/L04/N05/OIDC 相关历史结论。
- **验收标准：** 每个被信任 claim 均追踪到验证点和消费点；网络安全与身份安全证据分开记录。

### Step 10：会话、Bearer/Cookie、CSRF 与凭据失效审查

- **目标：** 验证浏览器和非浏览器认证双通道不会产生会话固定、CSRF、优先级混淆或失效不一致。
- **前置条件：** Step 8–9。
- **审查内容：** JWT 签发/算法/claims/期限、Bearer 与 Cookie 选择顺序、Cookie 的 HttpOnly/Secure/SameSite/Path/Max-Age、登录/登出/记住我、CSRF 自定义头/Origin/SameSite、防跨域表单、`credential_version` 实时查库、密码/邮箱/角色/禁用后的会话失效、多标签行为。
- **动态验证：** Cookie-only、Bearer-only、二者冲突、缺 CSRF 头、跨站简单请求模拟、登出重用旧凭据、权限降级后旧会话、过期和篡改 JWT。
- **产出：** 会话状态机和认证来源矩阵；回归 L01/N05/O1 及前端 auth store/request 封装。
- **验收标准：** 所有状态变更方法的 CSRF 结论有路由级证据；不能只检查前端是否加头，必须确认服务端拒绝路径。

### Step 11：RBAC、对象所有权、管理员与高级模式授权审查

- **目标：** 系统性发现水平/垂直越权、IDOR、批量操作绕权和仅前端隐藏的权限控制。
- **前置条件：** Step 4、10。
- **审查内容：** session/admin/advanced 中间件及顺序；用户对订阅、规则、分享、平台、个人资料、流量的所有权；管理员用户管理、审批、设置、日志、装配、节点、Xray；高级模式实时开关；批量 ID、路径 ID、查询 ID、间接引用和资源存在性泄露。
- **方法：** 从 Step 4 端点表逐行建立匿名/待审批/用户A/用户B/管理员/高级关闭的预期与实际响应；检查 service/store 层是否再次约束业务所有权。
- **产出：** 完整授权矩阵、疑似 IDOR 列表及反证、A01 正面控制清单。
- **验收标准：** 每个非公开端点至少有允许角色和拒绝角色证据；404/403 语义与资源枚举影响明确。

### Step 12：下载 Token、分享、公开内容与 SSE 凭据审查

- **目标：** 审查不依赖常规会话的公开能力及其泄露半径。
- **前置条件：** Step 4、10–11。
- **审查内容：** 订阅/规则/分享 Token 的生成熵、存储、业务键原子复用、轮换/撤销/过期、404 防枚举、组未分配的 200 注释语义、`no-store`、内容类型、文件名和日志脱敏；SSE 日志认证、URL query 凭据和一次性/短期属性；公开安装包与首页内容。
- **动态验证：** 无效/过期/撤销/跨资源 Token、并发创建/轮换、条件响应与缓存头、Referer/日志泄漏检查；不进行大规模 Token 猜测。
- **产出：** 各 Token 生命周期表和公开入口数据暴露矩阵，回归 F03/F07/L08/L09/O1。
- **验收标准：** 每类 Token 从生成到销毁均有证据；“高熵所以安全”不能替代日志、缓存和撤销检查。

### Step 13：密码重置、验证码、审批与邮件安全审查

- **目标：** 审查账号恢复和人工审批流程中的枚举、重放、fail-open 和通知缺口。
- **前置条件：** Step 8、10–12。
- **审查内容：** forgot/reset 响应一致性、Token 哈希/明文存储、TTL、一次性消费、清理、限流、fragment/query/path 暴露；验证码启用状态、密钥缺失、提供商错误和超时；审批令牌/状态；SMTP 头部/正文注入、失败语义、敏感内容和密码变更通知。
- **动态验证：** 不存在邮箱、重复请求、过期/重放 Token、并发消费、验证码配置不完整、SMTP mock 失败和包含换行的字段；不向真实邮箱发信。
- **产出：** 恢复链状态机，回归 F07/L08/N03/N04/N07。
- **验收标准：** 外部响应与内部日志两侧均检查账号枚举；成功事务与邮件失败的先后顺序明确。

### Step 14：SQL、命令、配置、CRLF 与解析器注入审查

- **目标：** 覆盖传统注入与本项目配置生成特有的语义注入。
- **前置条件：** Step 3–4。
- **审查内容：** SQL 参数化、动态表/列/排序/IN 条件白名单；`os/exec`/shell/模板；HTTP header/邮件/Content-Disposition CRLF；YAML/JSON/URI/Shadowrocket conf/Clash 规则/节点名称/代理组名/手动规则拼接；控制字符、重复键、类型混淆、超深嵌套和未知字段。
- **方法：** 搜索只是入口，必须沿 parser → validator → model → renderer → response/file 追踪编码是否匹配输出语境；对 goccy YAML 与手工字符串渲染分别判断。
- **动态验证：** 使用最小受控载荷验证引号、换行、Unicode 分隔符、YAML 特殊值、URI 编码和嵌套对象；不得生成可连接真实目标的恶意配置。
- **产出：** 输入—校验—输出语境矩阵，回归 L03/历史 A03 结论。
- **验收标准：** 每个手工拼接点有输入来源和控制结论；扫描命中必须去除测试/常量/不可控数据误报。

### Step 15：前端 XSS、DOM、Markdown、URL 与第三方组件审查

- **目标：** 验证全部用户/管理员可控数据在浏览器中的渲染和跳转安全。
- **前置条件：** Step 4、12、14。
- **审查内容：** `v-html`、Markdown 配置、组件 slot/tooltip/popover、Diff/预览/SSE/访问日志、富文本/公告/页脚、属性绑定、style/class、`window.open`、动态 href/src/scheme、下载文件同源执行、localStorage/DOM API、第三方 Ant Design/Vue 组件行为。
- **动态验证：** 在隔离环境注入无害的 XSS 探针，验证文本、属性、URL、Markdown、SVG/HTML 安装包和新窗口 opener；只证明执行与否，不窃取凭据。
- **产出：** 来源—汇聚点清单、CSP 之外的编码/净化证据，回归 F03/F10/N05 和前两期“唯一 v-html”结论。
- **验收标准：** 不以 Vue 默认转义概括所有场景；每个显式 HTML/URL/新窗口汇聚点均被人工检查。

### Step 16：CSP、安全响应头、CORS、跳转与缓存策略审查

- **目标：** 验证浏览器安全边界和敏感响应策略在所有响应类型上一致。
- **前置条件：** Step 7、12、15。
- **审查内容：** CSP header/meta 的实际生效范围、`frame-ancestors`/X-Frame-Options、`object-src`、`base-uri`、生产 `connect-src`、`nosniff`、Referrer-Policy、HSTS 的责任边界、CORS、开放跳转、Host/Forwarded Host、Cookie、敏感 API/下载的 Cache-Control/Pragma/Vary/ETag、错误响应是否漏头。
- **动态验证：** 对 HTML、JSON、纯文本下载、安装包、404/500、SPA fallback、应急模式分别抓取响应头；使用本地不同 Origin 测试预检和简单请求；不评价外部 WAF 注入的头。
- **产出：** 响应类型—安全头矩阵，回归 F03/F05/F09/L04/N06。
- **验收标准：** 结论以应用直接响应为准；meta CSP 能力限制与 header CSP 分开；缓存策略覆盖成功和业务错误正文。

### Step 17：路径穿越、文件上传/下载、静态资源与临时文件审查

- **目标：** 验证所有用户输入参与的路径和文件生命周期不能逃逸、覆盖或同源执行。
- **前置条件：** Step 3–4、16。
- **审查内容：** slug/filename/path 校验、绝对路径/`..`/分隔符/Unicode/双重编码、symlink/TOCTOU、`filepath.Join` 后边界、上传大小前后端限制、扩展名/MIME、原子写入和失败清理、权限、临时目录、版本文件、安装包、站点资源、删除级联和残留。
- **动态验证：** 使用临时数据目录测试遍历、冲突、危险扩展名、错误 MIME、失败中断和删除；不得针对工作区或用户目录使用递归删除。
- **产出：** 文件操作清单、路径规范化证据、失败清理矩阵，回归 F03 和 AGENTS 五项安全底线中的路径防护。
- **验收标准：** 每个写/读/删入口都追踪到最终绝对根和清理逻辑；单纯拒绝字符串 `..` 不等同完整通过。

### Step 18：配置导入导出、备份、清空与数据完整性审查

- **目标：** 审查高影响数据管理能力的保密性、完整性、认证不变量和异常恢复。
- **前置条件：** Step 11、17；本步只记录调用了哪些密码学机制，算法与密钥生命周期细审按顺序交给 Step 19。
- **审查内容：** 导出/导入格式、Argon2id/GCM 封装、密码/确认词、schema/version、未知字段、签名密钥与加密数据迁移、认证死锁校验、事务与回滚、备份快照/加密/临时文件、清空权限/确认/级联、恢复说明与敏感日志。
- **动态验证：** 正确/错误密码、截断/篡改/旧版/重复/超深/缺字段文件、事务中途失败、备份解密还原一致性、清空取消和最小合成数据清理；不触碰真实数据卷。
- **产出：** 数据管理威胁矩阵，回归 L02/N02 及 A08 历史结论。
- **验收标准：** 成功、失败和中断三类路径均有数据前后状态证据；“管理员发起”不自动消除完整性与秘密泄露风险。

### Step 19：密码学、密钥派生、凭据存储与敏感数据生命周期审查

- **目标：** 验证算法使用、随机性、密钥分离和敏感数据生命周期符合项目规模下的实际安全需求。
- **前置条件：** Step 3、6、10、18。
- **审查内容：** bcrypt 参数、随机 Token 熵、JWT 签名、HKDF、AES-256-GCM nonce/key 使用、Argon2id 参数/格式、SHA-256 Token 哈希、恒定时间比较、签名密钥生成与轮换、OIDC/SMTP/Xray/节点凭据加密、敏感字段解密范围、日志/响应/导出/备份/数据库残留。
- **方法：** 检查标准库/API 的正确调用和 key/nonce 来源，不自创密码算法；结合数据库 schema 与导入导出检查密钥依赖关系和不可恢复风险。
- **产出：** 敏感数据分类表、静态/传输/使用中保护矩阵、密钥派生关系图，回归 F01/N02/N04/N05 与 AGENTS 密钥底线。
- **验收标准：** 每种秘密都有生成、存储、读取、输出、轮换/失效和备份行为；算法名正确不能替代 nonce、AAD、错误处理与生命周期检查。

### Step 20：SSRF、DNS rebinding、重定向与全部出站网络审查

- **目标：** 统一审查应用代表用户/管理员发起的全部网络请求。
- **前置条件：** Step 3–4、9、19；本步记录 Xray 网络通道，协议、凭据与业务副作用细节按顺序交给 Step 23。
- **审查内容：** OIDC discovery/JWKS/token/userinfo/test、素材池 URL 同步、SMTP、Xray、任何 webhook/远程下载；scheme、userinfo、host/port、IPv4/IPv6、解析、私网/回环/link-local、DNS rebinding、重定向、代理环境变量、超时、响应大小、内容类型、错误回显和凭据跨域转发。
- **动态验证：** 使用本地多地址 mock/DNS 可控测试替代真实内网探测，覆盖重定向到不同地址、解析前后变化、超大/慢响应和凭据转发；不扫描宿主网络。
- **边界口径：** 即使素材 URL 仅管理员可配置也要审查；是否接受风险在报告中结合可信管理员和部署边界校准，不提前排除。
- **产出：** 出站请求总表和每通道 SSRF 控制矩阵，回归 F06/F11/历史边界-1/边界-4。
- **验收标准：** 所有 `http.Client`、自定义 Dial、gRPC dial 和 SMTP 连接都有归属；预检查与实际连接是否使用同一解析结果必须明确。

### Step 21：请求限制、超时、限流、任务与小规模 DoS 审查

- **目标：** 在 10–50 人部署尺度下检查现实的资源耗尽和异常阻塞，不做极端容量评测。
- **前置条件：** Step 4–5、7、20。
- **审查内容：** server read/header/write/idle timeout、分路由请求体上限、JSON/YAML/压缩包/递归对象、上传/导入/同步/预览/渲染/SSE、数据库连接数与游标、限流键/IP/可信代理、登录/reset/setup/公开下载、后台 cron/task 重叠、goroutine/ticker/context 取消、外部调用超时和响应大小。
- **动态验证：** 少量慢头/慢体、边界大小、深层输入、并发重复操作和客户端取消；设置严格本地时间/请求上限，禁止洪泛或长时间 soak。
- **产出：** 资源预算与限制矩阵、可被 WAF 缓解和只能由应用控制的区分，回归 F02/F08/N04。
- **验收标准：** 每条风险说明在小项目规模下为何现实；仅在极端流量才成立的项目降为观察或排除。

### Step 22：业务一致性、并发事务、级联、预览/生成竞态审查

- **目标：** 审查会导致越权内容、错误配置、孤儿数据或半完成副作用的不安全设计与异常条件。
- **前置条件：** Step 11–14、17–21。
- **审查内容：** 版本号/激活切换、下载 Token 复用、用户/组/平台/订阅/规则/节点删除级联、候选节点重算、组分配、预览摘要与生成一致性、蓝图/素材并发变化、直接上传与装配切换、事务后 Xray/文件副作用、幂等重试、SQLite 单连接嵌套查询和错误回滚。
- **动态验证：** 两客户端受控并发、依赖在 preview→generate 间变化、重复提交/删除/激活、数据库失败注入可用现有测试 fake；不直接破坏生产数据。
- **产出：** 关键不变量表、事务/副作用时序、R24-01/R24-18/R24-21/R24-22 及历史并发安全结论回归。
- **验收标准：** 每个不变量至少有源码事务证据或测试；“最终一致”必须说明失败重试和用户可见状态。

### Step 23：Xray 集成、协议参数、凭据与对账副作用审查

- **目标：** 独立覆盖项目最大外部特权集成，避免在通用 SSRF/业务 Step 中被稀释。
- **前置条件：** Step 19–22。
- **审查内容：** instance 地址和凭据、gRPC 明文已确认部署边界、连接超时/串行化、检测数据可信度、协议注册表、`protocol_json` 递归校验和嵌套敏感字段、REALITY 私钥处理、display/render name 冲突、Add/RemoveUser、配额重置、对账 push/clean、独立账号、OFF 清空、missing/allocatable/enabled 状态和失败重试。
- **动态验证：** 使用 fake/local mock Xray 覆盖恶意/异常返回、未知协议、重复 tag、无效嵌套对象、超时、部分成功和重试；不得连接真实 Xray 或操作真实账号。
- **产出：** Xray 信任边界与副作用矩阵、凭据流图、协议输入安全结论。
- **验收标准：** 明文 gRPC 只作为已确认部署边界记录，不掩盖应用凭据泄露、异常处理或授权问题；每个写副作用有失败状态与重试证据。

### Step 24：日志脱敏、错误处理、调试、审计与告警能力审查

- **目标：** 覆盖 A09:2025 与新增 A10:2025，确认异常不会泄密、fail-open 或静默丢失安全信号。
- **前置条件：** Step 7–23。
- **审查内容：** 请求 URI/path/query/header/body、`token/code/state/reset`、Authorization/Cookie、OIDC/SMTP/Xray/节点凭据脱敏；结构化字段与大小写/编码变体；5xx/debug/panic；业务错误码；审计日志覆盖的高影响操作；客户端 IP 与可信代理；日志 SSE 授权；验证码/同步/邮件/任务失败是否告警或仅静默继续。
- **动态验证：** 使用哨兵秘密贯穿错误请求、URL 编码、异常外部响应和 debug on/off，搜索应用 stdout、访问日志、SSE 和 HTTP 响应；保存证据前再脱敏。
- **产出：** 敏感字段—日志渠道矩阵、异常条件 fail-open/fail-closed 表、管理操作审计覆盖差距，回归 F07/L07/L09/N03/N07。
- **验收标准：** 至少覆盖正常失败、panic/500、外部依赖失败和后台任务失败；不得因“日志仅管理员可见”忽略凭据泄露。

### Step 25：匿名、公开、Setup 与异常状态黑盒动态验证

- **目标：** 从不了解内部实现的外部访问者视角验证公开攻击面，与静态结论交叉确认。
- **前置条件：** Step 4–5、7–24。
- **场景：** `/health`、SPA/静态资源、Setup 前后、登录/注册/forgot/reset/OIDC callback、公开订阅/规则/分享/安装包、404/405/OPTIONS、错误 Content-Type、Host/Forwarded headers、缓存和安全头、常见输入边界、异常状态恢复。
- **方法：** 根据 Step 4 矩阵逐项发送最小请求；使用浏览器验证页面级行为和 HTTP 客户端保存原始响应；请求频率维持正常人工/小型自动化规模。
- **产出：** 黑盒请求—响应证据表、静态与动态不一致清单、匿名面可枚举信息清单。
- **验收标准：** 所有匿名/Token 入口至少有成功、无凭据、无效凭据和异常输入证据；不以页面按钮不可见代替 API 验证。

### Step 26：普通用户、管理员与跨角色黑盒动态验证

- **目标：** 用真实 HTTP/浏览器会话验证 Step 11 授权矩阵和关键状态变化。
- **前置条件：** Step 5、10–13、25。
- **场景：** 待审批/禁用/用户A/用户B/管理员/高级模式开关；对象 ID 替换、管理员 API、用户资料、订阅和规则、分享、流量、版本、批量操作；Cookie/Bearer/CSRF；角色变更、密码修改、禁用和登出后的旧凭据；多标签和重复提交。
- **安全限制：** 只使用合成数据；高影响操作前记录快照，验证后恢复或重建临时实例；不执行真实邮件、URL 同步或 Xray 副作用。
- **产出：** 角色×端点×状态动态矩阵，静态授权反证或确认、任何 IDOR/越权最小复现。
- **验收标准：** Step 11 的每个高风险授权假设都获得动态证据或明确的不可验证原因；发现越权后停止扩大读取范围。

### Step 27：恶意输入回归、历史修复回归与全量验证

- **目标：** 汇总运行非破坏性恶意输入回归和项目全量质量门禁，发现跨领域组合问题。
- **前置条件：** Step 1–26。
- **审查内容：** 将 Steps 8–24 形成的最小载荷按入口重放；回归两期报告全部“已修复”项和本期疑点；检查 XSS+local session、日志泄密+reset、SSRF+redirect、导入+认证死锁、preview+并发变化等组合链；不得增加武器化程度。
- **验证命令：** `cd backend && go build ./... && go vet ./... && go test -timeout 180s ./...`；`cd frontend && npm ci && npm audit --audit-level=high && npm run build && npm test`；依赖可达扫描复跑；`docker build` 和隔离容器最小 smoke。
- **产出：** 全量验证表、失败归因、历史回归最终状态、审查期间代码变化的增量回归结果。
- **验收标准：** 每个失败区分安全发现、已有非安全缺陷、环境限制和工具波动；不得为了全绿修改代码或测试。

### Step 28：发现去重、风险校准、OWASP 总结与最终交付

- **目标：** 把 27 个检查点整合为可决策、可复核且不过度夸大的最终报告。
- **前置条件：** Step 1–27 全部 ✅，或用户已接受明确列出的阻塞项。
- **工作内容：** 去重 S3 与 F/L/N；验证每项证据链和置信度；按本项目 10–50 人、非商业、WAF 背景重新校准可能性与影响；区分应用修复、接受风险、长期硬化、部署边界和误报；完成 OWASP 2025 十类与 2021 历史映射、正面清单、未验证项、基线漂移和最终测试结果。
- **报告结构：** 执行摘要 → 范围/方法/限制 → 历史回归 → 发现总表 → 逐项证据与建议 → OWASP 对照 → 正面安全资产 → 部署边界/接受风险 → 验证结果 → 后续决策清单。
- **质量门槛：** Critical/High 必须有高置信证据和最小复现；Medium/Low 必须说明在本项目语境下的现实路径；无证据的推测只能进入观察/未验证项。
- **产出：** 完整 `SecurityReport3.md`、本计划全部状态、建议的 Issue/Build 候选列表；不直接创建修复文档或修改代码。
- **验收标准：** 用户可逐项决定“修复/接受/长期观察/部署边界”；所有结论能回链到 Step 检查点、源码或动态证据。

---

## 六、OWASP Top 10:2025 覆盖校验

| OWASP Top 10:2025 | 本计划主要覆盖 Step | 与 2021 历史报告兼容说明 |
|-------------------|----------------------|--------------------------|
| A01 Broken Access Control | 4、9–12、17、20、23、25–26 | 含 2021 A01，并吸收 2021 A10 SSRF 的访问边界部分 |
| A02 Security Misconfiguration | 4、7、16、20–21、25 | 对应并扩展 2021 A05 |
| A03 Software Supply Chain Failures | 6、27 | 扩展 2021 A06“易受攻击和过时组件” |
| A04 Cryptographic Failures | 9–10、12、16、18–19、23 | 对应 2021 A02 |
| A05 Injection | 14–15、17 | 对应 2021 A03，包含配置/CRLF/解析器语境 |
| A06 Insecure Design | 3、8、13、21–23 | 对应 2021 A04，加入业务不变量和小规模滥用路径 |
| A07 Authentication Failures | 8–13、25–26 | 对应 2021 A07 |
| A08 Software or Data Integrity Failures | 6、10、17–19、22、26 | 对应 2021 A08 |
| A09 Security Logging & Alerting Failures | 24、27 | 对应并扩展 2021 A09 |
| A10 Mishandling of Exceptional Conditions | 7–9、13–14、18、20–25 | 2025 新分类；重点检查 fail-open、错误处理、部分成功和恢复 |

> 覆盖表只是索引，不表示一个 Step 只检查表中单一类别。SSRF 在 2025 被并入 A01，但本计划仍保留独立 Step 20，便于与前两期 A10:2021 结论对照。

---

## 七、每步验收通用清单

每个 Step 标记 ✅ 前必须逐项满足：

- [ ] 已记录本步开始时的 branch、HEAD 和 dirty paths。
- [ ] 实际读取范围、未读取范围和动态场景明确。
- [ ] 搜索命中已沿调用链人工确认，误报已说明。
- [ ] 已检查正向成功路径、拒绝路径和异常/恢复路径。
- [ ] 发现项已与 F/L/N/S3 编号去重。
- [ ] 新发现含源码证据；适用时含最小动态证据。
- [ ] 已按小团队、非商业、WAF 背景校准风险，没有套用大型平台要求。
- [ ] 部署者运维边界没有被误写成应用漏洞，应用责任也没有被 WAF 掩盖。
- [ ] 未验证项及原因已记录，没有以“未发现”冒充“已证明安全”。
- [ ] 未修改业务代码、依赖、配置、设计或历史报告。
- [ ] `SecurityReport3.md` 已写入证据胶囊和不超过 800 字的交接摘要。
- [ ] 本步命令完成，失败已如实记录；本计划只更新本步状态。

---

## 八、阻塞、升级与计划变更规则

1. **需要用户决策：** 设计/文档冲突、是否接受管理员专用风险、需要真实第三方服务、需要新增仓库测试、需要修复代码或改变审查边界时，停止当前 Step 并给出推荐方案及影响。
2. **疑似高危：** 先保存最小脱敏证据；不继续获取更多数据、不建立持久化、不扩大到其他账号或外部系统；立即汇报。
3. **工具不可用：** 记录版本/网络/权限原因，使用人工走读和已有测试继续可完成部分；不得把“扫描器未运行”写成“无漏洞”。
4. **上下文将超限：** 在达到约 160K 前完成当前子域的证据胶囊，将 Step 标为部分阻塞，并提出拆分 Step N-a/N-b；经用户确认后更新本计划。
5. **代码漂移：** 按 §2.5 标记增量回归，不重写已完成证据；最终报告同时呈现初始基线与增量状态。
6. **计划调整：** 只允许增加必要子 Step、修正实际文件范围或更新依赖关系；不得静默删除未完成审查项。

---

## 九、最终完成标准

本次完整安全审查只有在以下条件全部满足时才可宣告完成：

1. Steps 1–28 全部 ✅，或阻塞项已由用户明确接受并在报告限制中列出。
2. OWASP Top 10:2025 十类均有当前代码证据、动态证据或明确不适用说明，同时提供 2021 历史映射。
3. Step 4 端点清单、Step 11 授权矩阵、Step 19 敏感数据表和 Step 20 出站请求表均无未解释条目。
4. 两期 SecurityReport 中每个 F/L/N/边界/观察编号都有当前状态。
5. 所有 S3 发现均有严重度、置信度、现实攻击路径、现有缓解、建议与证据回链。
6. 后端 build/vet/test、前端 clean install/audit/build/test、依赖可达扫描、Docker build 和隔离 smoke 的结果均如实记录。
7. 未对真实生产、真实第三方服务或真实数据执行攻击；临时测试数据和进程已安全清理。
8. 审查与修复保持分离；后续是否建立 Issue/Build 由用户逐项决策。

---

## 十、变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.1 | 2026-08-30 | 明确本地测试数据授权：当前本地构建无真实用户和数据，测试时可安全清空当前仓库的 `./backend/data` 临时数据库；测试用户可按 TestPasswordList 重建或直接使用现有清单账号登录，并补充精确路径、进程停止和禁止扩大到外部数据卷/真实环境的约束。 |
| v1.0 | 2026-08-30 | 初始版本：确认采用静态审查、自动化扫描与本地隔离动态验证的混合方法；以 OWASP Top 10:2025 为主并兼容 2021 历史映射；拆分为 28 个可在单次约 200K 上下文内独立执行和验收的 Steps；明确 WAF/部署边界、证据胶囊、基线漂移、停止条件与审查/修复分离规则。 |
