# Issue10.md — VPN 订阅管理系统问题追踪（当前）

> **文档定位：** 本文档是 VPN 订阅管理系统的当前问题记录，承接已归档的 [Issue1～Issue9](docs/reports/Issue/)。本轮仅记录浏览器或快速静态分析发现的问题，不在本轮修改代码或实施修复。
> 设计记录见 [Design3.md](Design3.md) 及已归档的 [Design2.md](docs/reports/Design/Design2.md)、[Design2-UI.md](docs/reports/Design/Design2-UI.md)；编码指令见 [AGENTS.md](AGENTS.md)（唯一强要求）。

## 〇、本轮说明

- **创建时间：** 2026-09-01
- **调查方式：** 应用内浏览器可见状态、必要的快速交互验证、前后端静态代码定位；按问题记录现象、证据、可能根因、影响范围和修复方向。
- **环境：** 本地 Dev 模式；数据库清理目标和服务启动状态以本轮实际核对结果为准。
- **范围约束：** 本轮不修改业务代码、不添加回归测试、不实施修复；如修复方向涉及产品语义、API 契约、数据模型或多种技术取舍，先与用户确认。

## 一、进行中问题

### R25-01 版本管理页“重新编辑”入口与装配生成语义重复

- **来源：** 浏览器评论 1；`http://localhost:8080/admin/subscriptions/1/versions`。页面当前显示 v6～v10 五个装配版本，每行均同时提供“编辑”和“重新编辑”入口。
- **现象：** 对已生成的装配蓝图点击“重新编辑”后，会回到订阅装配页恢复原蓝图内容；即使不改变配置，最终仍需按装配流程重新预览并生成一个新版本，原版本保留，版本号继续累加。用户反馈该入口的实际意义不大，希望移除以简化版本操作。
- **快速验证：**
  - 浏览器 DOM 快速核对得到 `重新编辑` 按钮数量为 5；v6～v10 的行文本均包含“装配 / 编辑 / 重新编辑”，当前版本 v10 也保留该入口。
  - `frontend/src/views/admin/VersionManageView.vue:61-75` 的 `reEdit` 先请求 `/api/admin/versions/{id}/blueprint`，再跳转 `/admin/assembly?tab={target_syntax}&edit_version_id={id}`；桌面按钮位于 `:320`，移动端菜单项和分发位于 `:135-149`。
  - `frontend/src/views/admin/AssemblyView.vue:410-455` 根据 `edit_version_id` 回填目标、节点、代理组、素材池、规则和覆盖层；`frontend/src/views/admin/AssemblyView.vue:656-680` 仍调用 `generateAssembly`。
  - `backend/internal/server/assembly.go:175-181` 的生成路径调用 `versionSvc.CreateVersion`，并在同一版本事务中 `SaveBlueprintTx`，因此“重新编辑”不会修改原版本，而是创建递增的新版本并保存新的蓝图快照。
- **可能根因：** 版本模型采用不可变版本记录；“重新编辑”只是旧蓝图回填 + 新版本生成的快捷入口，不存在原地更新蓝图的语义。因此该按钮与版本页已有的“创建新版本”→“装配生成”入口在产出结果上重叠，只是前者额外恢复旧蓝图字段。
- **影响范围：** 主要影响订阅/规则版本管理页的操作复杂度和用户对“编辑”与“重新编辑”区别的理解；不影响历史版本文件、当前激活切换和装配生成本身。若直接移除入口，仍需明确是否保留用户通过 `/admin/assembly?edit_version_id=...` 进入的旧蓝图回填能力，以及对应后端 blueprint 查询接口和测试。
- **修复方向：** 推荐先移除版本管理页桌面“重新编辑”按钮及移动端“更多”菜单项，仅保留“编辑”、预览、激活/分发、删除和页头/创建弹窗中的“装配生成”入口；同步删除 `VersionManageView.vue` 中仅服务该入口的 `reEdit` 分发逻辑与 `getVersionBlueprint` 调用。若产品确认彻底取消旧蓝图回填，再进一步清理 `edit_version_id` 处理、蓝图 GET 接口及对应测试/设计文档；不改变 `assembly_blueprints` 的历史快照和下载渲染用途。
- **待确认事项：** 本次是否只移除版本管理页上的用户入口，保留底层旧蓝图回填链路作为兼容路径；还是连同 `edit_version_id`、`GET /api/admin/versions/:id/blueprint` 和相关测试/文档一起移除？
- **状态：** ☐ 待确认 / ☐ 待修复（本轮仅分析）

### R25-02 平台“附加响应头”可编辑但 `Content-Disposition` 被系统文件名逻辑覆盖

- **来源：** 浏览器评论 1；`http://localhost:8080/admin/platforms/1/edit`。平台编辑页“附加响应头”中当前存在 `Content-Disposition` 输入，页面读取到的值为 `attachment; filename*=UTF-8''Luneflare`。
- **现象：** 页面允许编辑并保存 `Content-Disposition`，但订阅下载时文件名仍按订阅资源名称生成，例如 `ClashSubs.yaml`；平台配置中的自定义文件名不生效，因此用户观察到响应头编辑似乎无效。这里的“平台对应的订阅名”从当前代码看实际取自订阅记录的 `name`，不是平台记录的 `name`。
- **快速验证：**
  - 浏览器 DOM 快速核对确认“附加响应头”中存在 `Content-Disposition`、`profile-update-interval` 和 `profile-web-page-url` 三组键值；当前页面允许对这些通用行编辑。
  - `frontend/src/views/admin/PlatformEditView.vue:168-189` 将通用响应头组装到 `extra_headers` 提交；`backend/internal/platform/platform.go:177-213` 校验并持久化 `extra_headers`，当前证据不指向保存失败。
  - `backend/internal/download/download.go:168-185` 读取平台附加响应头；`:202-223` 从订阅记录读取 `name` 并生成下载文件名；`:365-373` 最终按资源名加扩展名拼接文件名。
  - `backend/internal/server/download.go:60-65` 先写入平台附加响应头，再用 `res.Filename` 调用 `BuildContentDisposition` 设置系统 `Content-Disposition`，因此平台同名键会被后写入的系统值覆盖。
  - `backend/internal/server/download_test.go:181-223` 已有测试明确插入平台旧的 `Content-Disposition`，并要求最终响应使用订阅名生成的 RFC5987 文件名，验证了当前覆盖行为是既有契约，而非偶发渲染问题。
  - 高级模式下，`backend/internal/download/download.go:186-200` 还会系统注入/覆盖 `profile-update-interval`、`profile-web-page-url` 和 `subscription-userinfo`；这些字段在对应模式下也可能表现为“可编辑但不生效”。
- **可能根因：** 前端和后端把 `Content-Disposition` 当作普通附加响应头开放编辑，但下载链路将它作为系统管理字段，用订阅名统一生成并覆盖平台配置；界面没有标明该保留字段的优先级，所以形成了“可编辑、可保存、但下载无效”的误导性操作。高级模式的几个 profile 头也存在相同的系统优先级与界面表达不一致问题。
- **影响范围：** 影响平台编辑页的 `Content-Disposition` 配置和订阅下载文件名；高级模式下影响上述系统注入的 profile 响应头的可配置预期。该覆盖链路不影响本问题范围之外的响应体生成或其他未被系统重写的自定义响应头。
- **修复方向：** 按当前下载契约，建议将系统管理的 `Content-Disposition` 从通用编辑器中移除/禁止编辑，或改为只读并明确“文件名由订阅名称生成”；同时将高级模式下由系统接管的 profile 响应头标记为系统管理、不可编辑或清楚说明优先级。若产品需要按平台自定义下载文件名，应先新增专用文件名字段，并明确覆盖优先级、扩展名处理、RFC5987 编码和安全校验，再调整下载链路，不能直接让任意附加响应头覆盖系统文件名。
- **待确认事项：** 请确认产品语义是继续保持“订阅名称生成系统文件名”并移除/禁用 `Content-Disposition` 编辑，还是要支持平台/自定义文件名；同时请确认高级模式下的 profile 响应头是否应改为系统管理字段。
- **状态：** ☐ 待确认 / ☐ 待修复（本轮仅分析）

### R25-03 规则素材池同步时操作区加载反馈造成布局位移

- **来源：** 浏览器评论 1；`http://localhost:8080/admin/assembly` 的“规则素材池”列表，标注区域为第一行“详情 / 同步 / 编辑 / 删除”操作区。
- **现象：** 点击“同步”后，同一行的同步按钮出现 loading 图标，并在“同步”和“编辑”之间临时插入“取消”按钮；操作区的按钮集合和宽度随之变化，用户看到“编辑”及后续操作被加载状态推移。同步完成后列表还会短暂进入整体加载态，操作区可能随表格重新渲染而再次跳动。移动端操作区还存在换行重排风险。
- **快速验证：**
  - 浏览器 DOM 初始状态为“详情 / 同步 / 编辑 / 删除”；点击第一行“同步”后约 300ms，DOM 变为“详情 / loading 同步 / 取消 / 编辑 / 删除”，同步按钮带有 `loading` 图标，确认位移来自加载状态和条件插入项，而不是编辑表单本身被打开。
  - 快速复现任务最终正常完成，第一行状态保持“成功”，上次同步时间更新为 `2026-09-01 10:05`；因此当前证据指向反馈布局问题，不指向同步业务失败。
  - `frontend/src/views/admin/assembly/PoolTab.vue:118-146` 使用共享的 `syncingID` 驱动同步状态，并在完成后执行 `await load()`；`:236-244` 的桌面操作列固定宽度为 `230`，操作容器为普通 flex，且同步按钮使用 `:loading`、取消按钮使用 `v-if` 动态插入；移动端对应逻辑在 `:249-268`，操作容器允许 `flex-wrap`。
  - `frontend/src/components/TriStateList.vue:15-19` 将列表委托给状态容器；`frontend/src/components/StateContainer.vue:23-37` 在 `loading` 时用 Skeleton 替换整个列表内容。因此同步完成后的刷新不只是更新行数据，还会让表格暂时卸载并重新渲染。
  - `frontend/src/style.css:86-99` 将移动端 `.mobile-actions` 内按钮和开关统一为至少 `44px` 的触控尺寸；这有利于触控，但会进一步放大操作项新增时的换行和高度变化。
- **可能根因：** 同步按钮、取消按钮、编辑和删除共用一个没有稳定槽位的操作区；`v-if` 新增取消项、Ant Design loading 图标改变按钮内部布局，再叠加固定桌面列宽和移动端自动换行，导致后续按钮发生视觉位移。同步完成后的 `load()` 又使用列表级 Skeleton 替换，造成第二次整体重排。当前问题更准确地说是操作状态与加载动画的布局设计不稳定，而不是“编辑”按钮自身触发了加载。
- **影响范围：** 影响规则素材池桌面表格和移动端卡片的操作可见性、点击稳定性、列表高度和用户对当前同步状态的判断；同步请求、取消逻辑和最终数据刷新目前仍可正常执行。若操作项继续增加，窄屏下的换行和误触风险会扩大。
- **修复方向：** 建议整体收口操作区：将“同步”作为主操作，详情可通过池名称或次级入口进入，编辑/删除收纳到“更多”菜单；同步中在原同步槽位内保持固定尺寸，仅替换为“同步中”状态，并把“取消”放入稳定的状态槽位或与同步控件形成同一组，避免在编辑按钮前插入新节点。刷新完成时保留当前表格和滚动位置，优先采用行级状态更新或表格内轻量遮罩，不用全列表 Skeleton 替换已展示内容；移动端将状态信息与操作按钮分成独立行/操作组，桌面端为操作组预留稳定宽度，并继续保留触控尺寸和减少动态效果偏好支持。
- **待确认事项：** 请确认是否采用“同步作为主按钮，编辑/删除进入更多菜单，详情通过名称或次级入口访问”的整体形式；以及同步取消是保留为独立按钮，还是合并为同步按钮的稳定状态切换。确认后再决定具体按钮样式、动画时长和列表级刷新策略。
- **状态：** ☐ 待确认 / ☐ 待修复（本轮仅分析）

### R25-04 移动端操作卡片中的开关被触控尺寸规则拉伸变形

- **来源：** 浏览器评论 1；`http://localhost:8080/admin/assembly`，视口约 `560 × 961`，标注第一条规则素材池卡片中的定时同步开关。
- **现象：** 移动端“规则素材池”卡片中的 small 开关没有保持正常的横向胶囊形状，关闭态呈现为近似圆形的灰色区域，开启态也显得过大，与旁边的“每日 04:00 UTC”文本和操作按钮比例不协调。该问题不局限于当前素材池页面，使用 `.mobile-actions` 容器的其他移动端列表也可能受到同一规则影响。
- **快速验证：**
  - 浏览器 DOM 快速核对确认目标元素为 `button.ant-switch-small.ant-switch`；在当前 560px 视口下，实际 small 开关的计算外框为 `44 × 44px`，`min-width` 和 `min-height` 均为 `44px`，而 Ant Design small 开关自身的默认高度约为 `16px`、最小宽度约为 `28px`，因此轨道被拉伸为近似圆形。
  - `frontend/src/style.css:86-99` 对 `.mobile-actions .ant-btn` 和 `.mobile-actions .ant-switch` 统一设置 `min-width: 44px; min-height: 44px`；按钮和开关共用该规则是变形的直接原因。
  - `frontend/src/views/admin/assembly/PoolTab.vue:249-268` 的移动端素材池卡片使用 `mobile-actions`，其中定时同步开关位于同一行的 `:259-262`；因此全局触控命中区规则直接作用于本评论标注的元素。节点、Xray 实例等其他移动端卡片也复用了 `mobile-actions` 与开关组合。
  - 当前 `.mobile-actions` 规则只扩大了开关元素本身，没有为内部轨道/滑块提供独立的触控包裹层，导致“触控命中区”和“视觉控件尺寸”被混为一体。
- **可能根因：** 为满足移动端至少 `44px` 的触控命中区，样式将按钮和开关直接统一设置最小宽高；按钮适合扩大矩形点击区域，但 switch 的外框尺寸同时决定视觉轨道形状，因而 small switch 被强制拉成 `44 × 44px`。这属于通用触控尺寸规则与控件视觉尺寸不匹配，不是数据状态或开关切换逻辑异常。
- **影响范围：** 影响规则素材池、节点管理、Xray 实例等复用 `.mobile-actions` 的移动端开关外观、行高和对齐；可能使卡片高度增加、操作行换行，并降低用户对开关开/关状态的辨识度。当前没有证据表明开关的点击或保存逻辑受影响。
- **修复方向：** 建议将“视觉 switch 尺寸”和“触控命中区”拆开：保留 small switch 的标准胶囊尺寸，在外层使用至少 `44px` 的可点击容器、透明 padding 或等效命中区；对按钮继续采用 `44px` 最小触控尺寸，但不要把同一最小高度直接施加给 switch 的视觉外框。移动端卡片可统一使用独立的设置行，将开关、状态说明和操作按钮分组对齐，并检查开启/关闭/加载三种状态以及窄屏换行；同时保留现有 `prefers-reduced-motion` 支持。
- **待确认事项：** 请确认移动端规范要求的是“可点击命中区至少 44px”，还是“开关视觉本身也必须达到 44px”；建议采用前者，以保持标准 switch 外观并扩大外围命中区。另需确认是否将该规则一次性收口到所有 `.mobile-actions` 页面，还是只调整规则素材池。
- **状态：** ☐ 待确认 / ☐ 待修复（本轮仅分析）

### R25-05 桌面侧边栏折叠后分组标题仍左对齐

- **来源：** 浏览器评论 1、2；`http://localhost:8080/admin/assembly`，桌面视口约 `1403 × 961`。标注了折叠侧边栏中的“分发”和“装配”分组标题；同样现象也适用于“成员”和“系统”。
- **现象：** 侧边栏折叠到窄栏后，菜单图标已进入折叠态，但分组标题文字仍然显示，并从标题区域左侧开始排列，没有在窄栏内居中；四个分组标题的视觉对齐方式不一致于折叠后的图标菜单，也占用了一部分有限宽度。
- **快速验证：**
  - 浏览器 DOM/计算样式核对确认当前 `.ant-layout-sider` 为 `64px` 宽，四个 `.ant-menu-item-group-title` 均保持 `display: block`、`text-align: start`、左右 `8px` 内边距；标题区域宽约 `63px`、高 `38px`，因此“分发”“装配”从左侧起始位置显示并非截图误差。
  - `frontend/src/layouts/AdminLayout.vue:117-126` 设置桌面侧栏展开/折叠宽度为 `220px / 64px`，并直接使用 `<Menu mode="inline" :items="menuItems" />`；`:71-80` 以 `type: 'group'` 提供“分发”“装配”“成员”“系统”标题，没有针对 `collapsed` 状态的标题渲染分支或自定义居中样式。
  - 当前移动端使用独立 Drawer（`AdminLayout.vue:140-160`），本问题针对桌面 Sider 折叠态，不应直接推断为移动端抽屉标题问题。
- **可能根因：** 侧栏只把折叠状态传给 Ant Design Menu 的菜单项布局，分组标题仍沿用展开态默认样式；项目仅为菜单项图标提供 Tooltip（`AdminLayout.vue:46-49`），没有为分组标题定义折叠态的隐藏、居中或替代表达。因此窄栏中的分组标题既没有被隐藏，也没有按 `64px` 视觉中心重排。
- **影响范围：** 影响桌面侧边栏折叠态的“分发 / 装配 / 成员 / 系统”四个分组标题的视觉对齐、侧栏层次和可读性；不影响菜单项路由、高亮、点击操作和展开态布局。若继续增加分组或标题文案，窄栏中的截断和拥挤会更加明显。
- **修复方向：** 建议先明确折叠态分组标题的产品角色：若分组标题仅用于展开态层级说明，折叠后可隐藏标题并通过分组间距保留层次；若需要继续展示，则应使用折叠态专用样式将标题内容在 `64px` 栏内居中，并处理多字符标题、Tooltip 和键盘/读屏语义。菜单项图标继续保持统一中心对齐，避免直接复用展开态的左内边距规则。
- **待确认事项：** 请确认折叠后分组标题是隐藏，还是保留并居中；建议优先隐藏，因为折叠栏主要承担图标导航，分组文字不承载点击功能，能够避免窄栏中的截断和视觉拥挤。
- **状态：** ☐ 待确认 / ☐ 待修复（本轮仅分析）

### R25-06 平台主列表展示内部标识的优先级偏低

- **来源：** 浏览器评论 1、2；`http://localhost:8080/admin/platforms`。标注了平台列表中的内部标识 `platform-nkehqb7p` 以及“编辑”入口；用户明确提出主列表不再优先展示标识，二级编辑页面保留即可。
- **现象：** 平台管理主列表同时展示“名称、产物格式、标识、安装包、操作”，每个平台的内部 `slug` 以代码样式占据独立列；但该标识主要用于下载地址、接口和调试定位，主列表的日常管理决策主要依赖平台名称、产物格式和安装包状态，当前信息层级显得偏技术化。编辑平台二级页面已经有只读“标识”字段，因此主列表重复展示的必要性较低。
- **快速验证：**
  - 浏览器 DOM 快速核对确认平台主列表表头包含“标识”，当前三条平台记录均直接展示 `platform-nkehqb7p`、`platform-ft6hnkqb`、`platform-3csjc6te`；“编辑”按钮均可进入平台二级编辑页。
  - `frontend/src/views/admin/PlatformsView.vue:94-118` 的桌面表格单独渲染 `slug` 列；`:121-139` 的移动端卡片也在名称下方重复渲染 `p.slug`，说明如果按本需求调整，应同时覆盖桌面表格和移动端卡片。
  - `frontend/src/views/admin/PlatformEditView.vue:205-234` 的二级编辑页已经以只读代码文本展示已有平台的 `form.slug`；新建平台则提示保存后自动生成。
  - `PlatformsView.vue:54-65` 的删除确认文案仍会引用平台标识；此外平台标识还参与下载链接、资源路由等系统契约。因此本问题是主列表展示层级调整，不应删除后端字段、自动生成逻辑或业务 URL 契约。
- **可能根因：** 主列表沿用了“管理信息完整展示”的表格结构，将面向接口/调试的稳定标识与用户日常管理字段放在同一层级；虽然二级编辑页已经提供足够的标识查看入口，但列表没有按使用频率和业务重要性做信息降噪。
- **影响范围：** 影响平台管理主列表桌面表格和移动端卡片的信息密度、列宽/卡片空间及非技术用户的阅读优先级；不影响编辑页面查看标识、删除确认中的风险提示、下载路由、接口参数和平台标识的持久化。
- **修复方向：** 按用户建议，主列表移除桌面“标识”列，并从移动端卡片的常规信息行中移除标识；保留编辑平台页的只读标识，必要时在详情/编辑入口或删除确认中继续提供，以便复制、排查和风险确认。主列表可将释放出的空间用于平台名称、产物格式、安装包状态或更清晰的操作区，不修改 API 返回字段和后端标识生成/使用逻辑。
- **待确认事项：** 请确认是否同时从移动端平台卡片隐藏标识，并保留删除确认中的标识提示；当前建议是桌面和移动端主列表统一隐藏，二级编辑页继续保留，删除确认继续保留用于核对目标。
- **状态：** ☐ 待确认 / ☐ 待修复（本轮仅分析）

### R25-07 规则主列表展示 slug 的优先级偏低，需转入改名入口只读展示

- **来源：** 浏览器评论 1、2；`http://localhost:8080/admin/rules`。标注了规则主列表中的 `rule-3rkupgjy` 以及“改名”按钮；用户要求主界面不展示 slug，并将 slug 转入改名按钮的子菜单/表单中保持只读。
- **现象：** 规则管理主列表当前同时展示“名称、标识、当前版本、Token 刷新时间、操作”，内部 slug 以代码样式占据独立列；该信息主要服务下载链接、接口调用和排查，不是日常规则管理的主要决策信息。当前“改名”入口已经存在，但打开的表单只有名称输入，尚未在其中提供 slug 的只读查看。
- **快速验证：**
  - 浏览器 DOM 快速核对确认主列表表头包含“标识”，当前规则直接展示 `rule-3rkupgjy`；同一行提供“改名”入口。
  - `frontend/src/views/admin/RulesView.vue:214-245` 的桌面表格单独渲染 `slug` 列；`:248-275` 的移动端卡片也在常规信息行直接渲染 `r.slug`，因此主界面隐藏需要同时覆盖桌面和移动端。
  - `frontend/src/views/admin/RulesView.vue:97-114` 的改名流程只提交 `renameRule(id, name)`；`:311-314` 的 `FormOverlay` 只有一个 `renameValue` 输入，没有 slug 的只读展示，因此需要在该二级入口补充信息而不改变改名 API。
  - `frontend/src/views/admin/RulesView.vue:116-124` 使用 `r.slug` 拼接规则下载链接，`frontend/src/api/rule.ts:4-15` 也将 slug 作为规则实体接口字段；因此这里只应调整展示层级，不应修改 slug、下载 URL 或 Token 关联契约。
- **可能根因：** 规则列表沿用了面向接口排查的完整字段展示，把稳定的内部标识与名称、版本等高频管理信息放在同一视觉层级；改名表单又没有承载目标确认信息，导致用户要么在列表中看到低优先级技术字段，要么在改名时缺少只读的标识核对入口。
- **影响范围：** 影响规则管理主列表桌面表格和移动端卡片的信息密度、阅读优先级以及改名时的目标确认体验；不影响规则 slug 的生成、下载链接、Token 刷新、版本管理和装配生成。
- **修复方向：** 按用户建议，移除桌面“标识”列，并从移动端规则卡片的常规信息行移除 slug；在“改名”打开的二级表单中增加只读代码样式的 slug，与规则名称并列展示，不提供复制按钮且不得允许修改。主列表释放出的空间可用于名称、当前版本、Token 刷新时间和操作区；后端接口、slug 字段和 URL 继续保留。
- **待确认事项：** 无。已确认用户所说的“改名按钮的子菜单”即现有“改名”弹窗；slug 直接在弹窗内只读展示，不提供复制按钮。
- **状态：** ☑ 已确认 / ☐ 待修复（本轮仅分析）

### R25-08 节点来源 `manual` 的用户可见文案需统一为“手动添加”

- **来源：** 浏览器评论 1；`http://localhost:8080/admin/nodes`。标注节点列表“来源”列中的 `manual` 标签；用户要求统一显示为“手动添加”。
- **现象：** 节点管理列表将手动录入/导入的节点来源直接显示为内部枚举值 `manual`，与面向用户的中文界面文案不一致；当前页面同时存在“批量导入”“新建节点”等用户操作，使用 `manual` 会让来源语义偏技术化。
- **快速验证：**
  - 浏览器 DOM 快速核对确认节点列表“来源”列中的多条记录均显示 `manual`，包括 `Node1`、`Node2`、`SS1` 和 `VL1`。
  - `frontend/src/views/admin/NodesView.vue:258-262` 的桌面来源标签直接输出 `record.source`；`:290-305` 的移动端卡片也直接输出 `n.source`，因此统一文案需要覆盖桌面表格和移动端卡片两种视图。
  - `frontend/src/api/node.ts:4-7` 将 `source` 定义为内部联合值 `'manual' | 'xray'`；`backend/internal/node/node.go:516-524` 仍按 `source=manual|xray` 过滤，批量导入也在 `backend/internal/node/uri_import.go:131-138` 写入 `source='manual'`。这些内部值属于接口和数据契约，不应改成中文。
  - 当前 `NodesView.vue:103-113` 也用 `n.source !== 'manual'` 判断手动节点是否允许编辑，`NodesView.vue:280-283` 按来源决定“编辑/命名”操作；因此本问题仅涉及显示标签，不涉及条件判断。
- **可能根因：** 节点来源标签直接复用了后端枚举值，没有建立面向用户的来源文案映射；内部状态值与展示文本耦合，导致 `manual` 直接暴露在界面上。
- **影响范围：** 影响节点管理桌面列表和移动端卡片的来源标签可读性；不影响手动节点与 Xray 节点的识别、筛选、编辑、命名、导入和装配逻辑。
- **修复方向：** 增加仅用于展示的来源文案映射，将 `manual` 统一显示为“手动添加”；`xray` 保留独立的用户可见中文文案或当前既定标签，内部 API、数据库枚举、筛选参数和业务判断继续使用 `manual` / `xray`。桌面与移动端应共用同一映射，避免再次出现两套文案。
- **待确认事项：** 无。已确认 `manual` 的用户可见文案统一为“手动添加”，仅调整显示文本，不修改内部 source 值。
- **状态：** ☑ 已确认 / ☐ 待修复（本轮仅分析）

### R25-09 分享订阅主列表缺少 slug，需在改名弹窗内只读展示

- **来源：** 当前分享订阅页；`http://localhost:8080/admin/shares`。用户要求沿用 R25-07 的已确认方式：主列表不展示 slug，在现有“改名”弹窗内增加只读 slug，不提供复制按钮。
- **现象：** 分享订阅主列表只展示名称、创建时间、当前版本、Token 状态和操作，无法直接查看该分享订阅的内部 slug；现有“改名”弹窗也只有名称输入。创建分享成功后的提示区域会短暂显示 slug，但关闭弹窗或进入既有记录后没有稳定的二级查看入口。
- **快速验证：**
  - 浏览器 DOM 快速核对确认 `/admin/shares` 的表头为“名称 / 创建时间 / 当前版本 / Token 状态 / 操作”，当前分享记录没有 slug 展示；同一行存在“改名”按钮。
  - `frontend/src/views/admin/SharesView.vue:194-223` 的桌面表格没有 slug 列；`:226-248` 的移动端卡片也没有 slug，符合主列表不展示内部标识的方向。
  - `frontend/src/views/admin/SharesView.vue:250-279` 只在创建成功结果 `createdShare` 中临时显示 `createdShare.slug`；`:281-285` 的“改名”`FormOverlay` 只有 `renameValue` 输入，没有分享 slug 的只读展示。
  - `frontend/src/api/share.ts:4-12` 的 `ShareItem` 仍包含 slug，`SharesView.vue:37-46` 使用 slug 拼接分享下载链接；`backend/internal/share/share.go:98-106` 的改名服务只更新名称，现有 `backend/internal/share/share_test.go:138-157` 也验证改名不改变 slug。因此本问题只需补充二级 UI 信息，不应修改 slug 或下载契约。
- **可能根因：** 分享列表为降低主界面技术信息密度没有展示 slug，但改名弹窗未像 R25-07 约定的规则改名弹窗一样承担稳定的标识核对功能；创建成功提示的临时展示不能覆盖既有分享记录的查看需求。
- **影响范围：** 影响分享订阅已有记录的 slug 查看和改名时的目标确认体验；不影响分享内容、版本管理、Token 刷新/吊销、复制链接和下载路由。移动端主列表当前已经不展示 slug，后续只需确保改名弹窗在桌面与移动端一致。
- **修复方向：** 沿用 R25-07：保持桌面和移动端分享主列表不展示 slug，在现有“改名”弹窗的名称输入附近增加只读代码样式的 slug，不提供复制按钮，也不允许修改；保留创建成功提示中的既有展示，后端 `ShareItem.slug`、分享 URL、Token 和改名 API 均不变。
- **待确认事项：** 无。已确认采用 R25-07 的交互语义：slug 只在现有“改名”弹窗中只读展示，不增加复制按钮。
- **状态：** ☑ 已确认 / ☐ 待修复（本轮仅分析）

### 问题记录格式

发现问题后按以下结构追加：

```markdown
### R25-XX 问题标题

- **来源：** 浏览器页面/URL/用户标注，以及必要的截图或交互步骤。
- **现象：** 可复现的用户可见行为。
- **快速验证：** 浏览器验证结果、接口响应或最小静态代码证据。
- **可能根因：** 当前阶段基于证据的定位；未确认部分明确标注不确定性。
- **影响范围：** 受影响的页面、接口、数据或用户流程。
- **修复方向：** 仅给出大致方向，不在本轮实施。
- **待确认事项：** 需要用户决定的产品语义、兼容性或实现取舍。
- **状态：** ☐ 待确认 / ☐ 待修复（本轮仅分析）
```

## 二、调查与变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-09-01 | 新建当前问题记录；准备清理本地调试数据并通过应用内浏览器开展问题调查，尚未登记具体问题，未修改业务代码。 |
| v1.1 | 2026-09-01 | 新增 R25-01：记录版本管理页“重新编辑”入口与装配生成重复、始终创建递增新版本的问题；完成浏览器 DOM 快速核对与前后端静态定位，未修改业务代码。 |
| v1.2 | 2026-09-01 | 新增 R25-02：确认平台 `Content-Disposition` 可保存但在下载响应中被系统按订阅名称生成的文件名覆盖；补充高级模式系统响应头的同类语义风险与待确认项，未修改业务代码。 |
| v1.3 | 2026-09-01 | 新增 R25-03：复现规则素材池同步时 loading 与取消按钮导致操作区位移、同步完成后全列表 Skeleton 重排的问题；补充桌面/移动端操作区整体优化方向，未修改业务代码。 |
| v1.4 | 2026-09-01 | 新增 R25-04：确认移动端 `.mobile-actions` 的统一 44px 触控规则将 small switch 拉伸为 44×44px，导致开关轨道变形；补充触控命中区与视觉尺寸拆分方向，未修改业务代码。 |
| v1.5 | 2026-09-01 | 新增 R25-05：确认桌面侧边栏折叠到 64px 后分组标题仍按默认左对齐显示，补充折叠后隐藏或居中的交互方向，未修改业务代码。 |
| v1.6 | 2026-09-01 | 新增 R25-06：记录平台主列表展示内部标识的优先级偏低；确认桌面表格与移动端卡片均重复展示 slug，二级编辑页已有只读入口，建议只调整展示层级，不改动标识契约，未修改业务代码。 |
| v1.7 | 2026-09-01 | 新增 R25-07：记录规则主列表桌面/移动端均展示 slug，而改名表单尚无只读 slug；补充移除主列表展示、改名入口只读保留和不改动下载契约的方向，未修改业务代码。 |
| v1.8 | 2026-09-01 | 确认 R25-07 的交互语义：slug 放入现有“改名”弹窗中只读展示，不提供复制按钮；仍未修改业务代码。 |
| v1.9 | 2026-09-01 | 新增 R25-08：确认节点来源 `manual` 的用户可见文案统一为“手动添加”，仅调整桌面/移动端显示映射，保留内部 source 契约，未修改业务代码。 |
| v2.0 | 2026-09-01 | 新增 R25-09：确认分享订阅主列表缺少 slug，沿用 R25-07 在现有“改名”弹窗内只读展示且不提供复制按钮；保留分享 URL 与 slug 契约，未修改业务代码。 |
