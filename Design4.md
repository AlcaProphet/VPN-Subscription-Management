# Design4.md — 节点编辑器条件表单与客户端兼容设计

> **文档定位：** VPN 订阅管理系统的节点编辑器增量设计记录，承接 [Node-Editor-Design-Research.md](docs/Reference/Node-Editor-Design-Research.md)、[Node-Editor-Improvement-Directions.md](docs/Reference/Node-Editor-Improvement-Directions.md) 和 2026-09-02 补充研究。本文记录已确认的产品方向、设计纲要、研究证据及后续范围，不是分步 Build 文档。
> **编码约束：** 遵循 [AGENTS.md](AGENTS.md)（唯一强要求）。[Design3.md](Design3.md) 的规则素材与装配设计继续有效；节点已实现基线见 [Design2.md](docs/reports/Design/Design2.md)、[Design2-UI.md](docs/reports/Design/Design2-UI.md) §6.2 和 [Build15.md](docs/reports/Build/Build15.md)。
> **设计状态：** v1.2，2026-09-02。用户已确认并授权写入：切换分支清空所属参数与凭据、切回不恢复、取消独立 `node_edit_states` 表，在 `nodes` 内保存最小当前状态；同时确认协议过渡、目标生成门槛、未知扩展保护和并发修订方案。当前有效决策见第二章，设计准备度见第十一章。**关键行为已确认，四协议组合矩阵及具体接口结构仍待补齐；尚未构建，未完成客户端连接验收。**
> **数据前提：** 按用户确认，当前项目无业务数据，因此旧节点/旧调用方的数据兼容、迁移、历史数据读改写不作为本次设计约束；但仍保留 schema 版本化、新节点初始化与“无数据环境下直接建库”的设计。历史版本快照语义继续适用。
> **证据基线：** 项目提交 `d76ee99`；补充研究使用 Clash Verge Rev **2.5.2** 的官方 URI 解析源码及本机携带的 Mihomo **1.19.29**，执行了 **41 个离线配置检查和 9 个 URI 样例检查**。结果含接受、拒绝、字段改写与静默回退，不是“50 项全部通过”；Shadowrocket 只有官方发布资料证据，没有本轮真机导入或连接结果。
> **状态用语：** “设计结论”表示本次采用的方案；“研究结果”表示指定版本、指定路径的观察；“待细化”表示构建前仍需补齐的契约或证据。文中的目标行为不表示当前代码已经实现。

---

## 一、当前功能全景与改进目标

### 1.1 已有能力与主要缺口

当前 manual 节点编辑器已有后端协议注册表、桌面 920px Modal／移动端全屏 Drawer、六个逻辑分区、递归对象编辑和凭据留空保留机制。原有能力继续作为改进基础。

| 当前能力或缺口 | 本次设计要解决的问题 |
|---|---|
| 按协议 schema 渲染，但主要依赖静态分区 | 继续按传输、安全、插件等当前选择展示对应参数 |
| 部分有限选项仍为普通文本，部分 Select 只能选固定值 | 推荐选项优先，同时允许手动填写和旧值回显 |
| UI 显隐不等于输出过滤 | 显式保存当前连接状态，清除失效分支，服务端只投影当前有效参数 |
| 协议级可导出标记不能证明字段完整输出 | 按协议组合、字段、目标格式和客户端版本检查 |
| 各类默认值、空输入与凭据保留语义不完全一致 | 明确未设置、显式值、保留、替换及清除 |
| 手工节点进入生成版本，Xray 节点另有动态注入路径 | 编辑改进需要保持历史版本和来源所有权边界 |

代码定位：[NodesView.vue](frontend/src/views/admin/NodesView.vue)、[ProtocolFieldEditor.vue](frontend/src/components/ProtocolFieldEditor.vue)、[registry.go](backend/internal/node/registry.go)、[node.go](backend/internal/node/node.go)、[render_clash.go](backend/internal/assembly/render_clash.go)、[links.go](backend/internal/assembly/links/links.go)。更完整的现状审计保留在两份 Reference 研究中。

### 1.2 用户目标

用户先选择 VLESS／VMess／Trojan／SS 等协议，表单随选择变化；优先展示多数常用方案需要填写的结构化字段，并展示当前连接组合需要的信息；不常用参数及其他功能默认折叠。开关参照订阅拼装样式，集中为独立区域。

常用不等于必填。合法缺省、空值条件和远端匹配要求须分别表达，不能用“放在首屏”替代协议校验依据。

### 1.3 交付范围

| 范围 | 设计结论 |
|---|---|
| 首批完整改进 | **VLESS、VMess、Trojan、Shadowsocks（SS）** 的条件表单、保存状态、目标检查及相关输出一致性 |
| 后续覆盖目标 | **完成当前全部 19 个已兼容 manual 协议的编辑体验改进**，清单见第九章；这是后续目标，不将其余协议标成已完成 |
| 扩展模型验例 | TUIC 认证互斥、WireGuard 多 Peer 与数组凭据，用于检验可扩展性，不加入首批 UI 交付 |
| 过渡期 | 全部 19 个 manual 协议接入统一保存契约；首批四协议完成条件表单，其余 15 个保留原有 schema 和编辑能力，使用最小当前状态，不建立分支恢复数据，不隐藏或删除入口 |
| Xray 来源 | 继续由实例检测维护，通过来源适配进入共享输出层，不套用 manual 编辑器的分支清空操作 |
| 独立 Xray 输出 | 后续单独设计固定验证 profile 的客户端 outbound 导出；首批记录边界 |
| 本次文档工作 | 仅维护 Design4 文档，不创建 Build、不改业务代码、不更新数据库或运行配置 |

“已兼容协议”指项目注册表现有入口，不表示每个协议都能导出到所有客户端。首批也不等于某协议所有历史传输、所有上游新功能均已获得完整兼容承诺。

---

## 二、设计决策记录

| 编号 | 决策点 | 结论 | 出处 |
|---|---|---|---|
| D1 | 入口与交互 | 协议优先，后续字段随当前连接组合动态变化，延续现有编辑浮层 | 用户初始要求及后续确认 |
| D2 | 常用与高级分层 | 基础、认证及当前连接常用参数默认展开；复杂扩展、兼容和性能区默认折叠 | 用户初始要求及后续确认 |
| D3 | 开关位置 | 协议业务开关集中为独立区域，参照订阅拼装；覆盖研究中“按语义分散到各区”的候选建议 | 用户明确指定 |
| D4 | 保存模型 | `nodes` 内保存最小当前选择元数据、活动参数、格式版本与编辑修订号；服务端投影为权威，同事务保存 | v1.2 用户确认，覆盖原路线 B 的分支恢复方案 |
| D5 | 分支切换 | 当前草稿清空所属参数、凭据、扩展和局部 JSON 草稿；新分支从未配置开始，切回不恢复 | v1.2 用户提出并确认 |
| D6 | 跨协议切换 | 首批沿用协议参数整体重填，清楚提示重置影响；不做会话内分协议草稿恢复 | 用户确认推荐方案 |
| D7 | 固定选项 | 可搜索推荐选项优先，允许手动输入；列表字段支持推荐条目与自定义条目 | 用户补充要求及写入授权 |
| D8 | 校验 | 确定性错误阻止；合法缺省、远端匹配和未知版本分别处理；保存与目标生成分级校验 | 用户确认推荐方案及补充研究 |
| D9 | 目标预览 | 高级区按需打开脱敏检查，复用服务端实际输出适配器 | 用户确认推荐方案 |
| D10 | 凭据与扩展 | 凭据区分保留／替换／清除，被重置分支禁止回填旧凭据；未知扩展标明来源、所属分区和目标，无法识别敏感路径时整体加密保护 | v1.2 用户确认 |
| D11 | 首批与后续 | 首批四协议，后续完成全部现有 19 协议；Xray outbound 独立推进 | 用户明确范围及确认 |
| D12 | 兼容结论 | 区分客户端能力、项目映射、实际导入和连接结果；记录版本与入口，不把解析成功当成语义正确 | 补充研究结论及写入授权 |
| D13 | 当前状态存储 | 取消新增 `node_edit_states` 表；必要元数据与活动配置保存在同一节点行，不保存恢复副本 | v1.2 用户确认，覆盖 v1.1 独立表决策 |
| D14 | 跨协议切换凭据 | 清空全部协议参数与凭据；保留名称、服务器地址、端口等基础信息；不做会话内分协议草稿恢复 | v1.2 用户确认清空边界 |
| D15 | 非激活分支 | 不持久化、不缓存待恢复参数；清空先作用于草稿，保存成功才落库，取消整个编辑不改数据库 | v1.2 用户确认，覆盖原保留分支决策 |
| D16 | 目标验证基线 | 首批以 Mihomo 1.19.29 与 Clash Verge Rev 2.5.2 为可判定基线；Shadowrocket 仅记录版本，标示待真机验证 | v1.1 用户确认，v1.2 沿用 |
| D17 | 节点检查接口 | 新增按需调用的节点检查接口，服务端复用实际适配器并返回脱敏产物与诊断 | v1.1 用户确认，v1.2 明确新建／编辑草稿边界 |
| D18 | SS 2022 范围 | 首批支持普通 AEAD、插件条件表单与插件诊断；SS 2022 完整 URI/密钥/客户端兼容列为后续阶段 | v1.1 用户确认，v1.2 沿用 |
| D19 | 旧数据兼容 | 当前项目无业务数据，旧节点/旧调用方数据兼容不作为本次设计约束；新库按新 schema 初始化 | 用户明确，本轮确认 |
| D20 | 目标生成门槛 | SR／generic 对核心语义无法表达的节点跳过并说明，零有效链接拒绝；Clash 对该类已选节点阻止生成；可选字段损失及仅缺证据分别警告 | v1.2 用户确认，细则见 §7.4 |
| D21 | 协议过渡 | 全部 manual 协议使用统一保存契约；其余 15 个保留原表单，Xray 保持来源所有权 | v1.2 用户确认 |
| D22 | 并发编辑 | 使用编辑修订号；旧修订保存返回 409，保留当前草稿并提示重新加载，不自动合并 | v1.2 用户确认 |
| D23 | 清空与折叠 | 关闭有子配置的功能清空子参数；面板折叠、结构化／JSON 模式切换不清空数据 | v1.2 用户确认，细则见 §3.4 |

### 2.1 与既有设计的覆盖关系

- Design4 是节点编辑器的后续增量，不改变 Design3 的规则来源识别、规则能力注册表或素材池职责。
- Design2-UI §6.2 的固定六区排列将由本文的动态层次替代；独立开关区、现有浮层、名称约束、管理员权限与 manual／xray 来源边界继续沿用。
- Design2-UI／Build15 中“数据库 schema 不变、无迁移”是当时实现的范围约束。本次取消独立状态表，但仍需在 `nodes` 中补充当前状态、格式版本和修订号，保留 schema 版本化与迁移设计，不能承诺零后端改动。
- 当前项目无业务数据（用户确认），因此旧节点读取、旧 API 数据兼容、旧字段迁移与冲突处理不作为本次设计必需项；只需保证新库初始化、新节点创建和 schema 版本化路径正确。
- 两份 Reference 中的待选路线仍保留研究历史；本章记录当前有效结论。v1.2 已覆盖 v1.0／v1.1 的分支保留、恢复及独立状态表方案，旧版决策只在变更记录中保留。旧研究关于 WS Path、ServiceName、Short ID 一律必填的候选规则不作为本次统一要求。
- 目标生成延续 Design2 §4.5 的 SR／generic 不可转节点跳过及空产物拒绝，并扩展为字段／组合级诊断；Clash 核心语义错误按 §7.4 阻止生成，不自动删节点或改组成员。无旧数据兼容不免除现有 URI 导入和 Xray 检测路径的来源适配。
- 历史 Design／Build／Issue 不在本次改写，也不将研究中发现的缺口标为修复完成。

---

## 三、界面与动态表单纲要

### 3.1 阅读顺序和默认展开状态

```text
新建／编辑节点                 当前组合：VLESS · WebSocket · TLS

协议选择、基础信息与认证        默认展开
  协议 → 名称 → 服务器／端口 → 当前协议凭据

连接方式与当前参数              默认展开常用内容
  传输／插件选择 → 当前传输／插件字段
  安全选择 → 当前安全方式的身份参数

开关参数                        独立区域
  当前组合适用的常用开关
  更多开关                      区内默认折叠

更多功能、兼容与性能参数        默认折叠，结构化编辑
  标题显示已配置项和诊断摘要

高级数据与目标检查              默认折叠
  未知扩展摘要、局部 JSON、脱敏输出检查

取消                            保存
```

基础信息内部可分“节点身份”和“认证”小组，避免合并成长列表。桌面保持合理的双列布局，复杂对象跨列；移动端单列，标签、说明和值上下排列，不强制横向三列表格。长算法名称可搜索和完整查看，不挤压保存操作。

### 3.2 动态显示与字段优先级

| 类别 | 展示方式 | 示例 |
|---|---|---|
| 节点核心信息 | 始终可见 | 地址、端口、当前协议凭据 |
| 当前连接常用信息 | 选择后直接展示 | WS Path／Host、gRPC ServiceName、TLS SNI、REALITY 身份参数 |
| 当前连接高级信息 | 对应高级区默认折叠 | Early Data、调优参数、复杂插件扩展 |
| 被切离的分支 | 清空所属数据，不提供恢复摘要或缓存 | WS 切换到 gRPC 后，WS 参数删除；切回 WS 重新填写 |
| 当前目标无法表达 | 保留编辑值，显示具体目标差异 | URI 无法完整承载的 Header 或插件选项 |

当前组合真正需要用户补充的字段不能藏在默认折叠区而没有提示。保存遇到错误时展开所属区域，定位首个错误；高级区已有配置时显示摘要，但不通过“自动打开所有已配置区”重新制造长表单。

### 3.3 独立开关区

参照 [HeaderStep.vue](frontend/src/views/admin/assembly/HeaderStep.vue) 的独立边框区域、紧凑文案与 Switch 排列、主题色和换行间距。业务 bool 在节点编辑器中集中管理；只有当前协议和当前组合适用的开关进入该区，不把所有协议的 bool 同时铺开。

- 常用开关直接呈现；不常用开关在同一区域内的“更多开关”折叠部分呈现。
- TLS 证书校验类选项仅在相应 TLS 能力成立时出现，不自动套用于 REALITY。
- 嵌套功能的开关标明所属功能，如“多路复用：启用／填充”，避免同名开关失去上下文；参数编辑仍位于对应结构化区域。
- 传输、安全、插件等多选一状态用 Select／Segmented，不用多个可能互相矛盾的 bool 表达。
- 结构化／JSON 属于编辑模式操作，使用明确的按钮或分段控件；不能混成节点运行参数。

### 3.4 分支清空、功能关闭与折叠

**切换先修改当前草稿，保存成功才写入数据库。** 新分支从未配置状态开始，切回不恢复原参数；合法缺省仍可显示，但不把展示默认值批量写入数据。清空范围如下：

| 操作 | 清空范围 | 保留范围 |
|---|---|---|
| 切换协议，如 VLESS → VMess | 全部协议参数、凭据、协议扩展及其局部 JSON 草稿 | 节点名称、服务器地址、端口等基础信息 |
| 切换传输，如 WS → gRPC | 原传输专属参数、凭据、扩展、局部草稿及依赖本次选择的参数 | 协议主凭据及与本次选择无关的配置 |
| 切换安全方式，如 REALITY → TLS | 原安全分区参数，包括 SNI、ALPN、指纹、专属身份参数及扩展；新安全分区重新填写 | 协议主凭据及不受影响的传输参数 |
| 切换或取消 SS 插件 | 插件参数、插件凭据、插件扩展和局部草稿 | SS 自身算法、主密码及其他无关配置 |
| 关闭有子配置的功能，如多路复用 | 该功能子参数、凭据、扩展和局部草稿；重新开启不恢复 | 必要的关闭状态及其他功能配置 |
| 收起面板、切换结构化／JSON 编辑方式 | 不因显示或编辑模式变化清空有效数据 | 当前有效状态；未应用 JSON 按 §6.4 处理 |

字段归属和重置依赖由条件元数据声明。Flow、ALPN 等跨维度字段随其声明依赖的选择变化而重置，不能只按“是否隐藏”决定清理，也不能无条件清空整节点。普通 bool 值变化不自动重置无关字段；功能关闭的清空仅适用于该功能声明的子配置。

界面应清楚提示“切换将清空该分区参数，切回需重新填写”。清空时同步移除受影响路径的校验错误与局部草稿，并使目标检查结果失效；新分支按当前条件重新校验。折叠仍激活的面板不影响校验或输出。

取消整个节点编辑会放弃本次草稿，数据库仍为上次保存状态；再次打开显示该已保存状态，不属于分支恢复。保存失败同样不落库。保存成功后，只持久化当前选择和当前有效数据，清空内容不进入节点的恢复副本，也不缓存到本地存储。

---

## 四、推荐选项与手动输入

### 4.1 单值字段交互

有稳定候选值的参数采用可编辑下拉框：空状态可浏览选项；输入后过滤；无匹配时提供明确的“使用自定义值”。自定义仍是一个字符串值，不把单值算法或模式做成多选标签。

1. 常用项靠前，扩展和旧版项分组；每个选项保存规范值，显示名称可更易读。
2. 搜索使用友好的大小写匹配；写入值的大小写、别名转换按具体字段规则处理，不能全局转小写或跨协议共用别名表。
3. 选择和手动输入进入同一校验链。搜索文本只有被选择或明确确认为自定义值后才成为字段值；保存前处理尚未确认的输入，不静默提交旧值。
4. 编辑旧值或自定义值时完整回显；失焦、切换模式和刷新选项列表不得自动改为第一项、`auto` 或空值。
5. `未设置` 与字面值 `none`、`auto` 分开。清空可选字段表达恢复未设置；必填字段清空后正常提示。
6. 支持键盘选择、输入法、Escape 关闭和焦点回归，遵循现有单浮层管理。交互参考 [W3C 可编辑 Combobox](https://www.w3.org/WAI/ARIA/apg/patterns/combobox/)。

协议入口本身继续来自项目支持的协议注册表；允许自定义参数值不等于支持用户创建任意新协议类型。

### 4.2 选项分组、默认值与兼容反馈

“常用”是显示优先级，不是统计结论，也不表示可以替换服务端配置。SS 新建时要求选择或填写服务端算法，不因常用列表首项而自动设为该算法。VMess 可继续表达协议已有的 `auto` 缺省语义，但区分显示缺省与显式设置。

个人连接模板仅在用户主动应用时预填，并说明将改动的字段；不把个人端口、指纹、Flow 或安全选择变成全协议默认值。打开、折叠或切换 JSON 不应批量写入全部展示默认值。

| 输入类别 | 反馈和处理 |
|---|---|
| 推荐列表中的已知值 | 按当前协议、组合和目标验证，不因入选列表而跳过检查 |
| 列表外但当前规则已知的值 | 显示其归属与限制，使用相同校验 |
| 未识别的自定义值 | 保留原值并标注“未验证”；仅不在推荐列表中不是删除或替换理由 |
| 确定不符合协议结构的值 | 指向字段并阻止保存 |
| 节点可以保存，但目标明确不接受或会改变连接含义 | 目标生成前报错或按已确认的剔除契约处理；不得显示完整支持 |
| 旧值被新能力表判为异常 | 显示原值和差异，不在读取时迁移或无声替换 |

仅缺少验证证据、尚无确定错误且适配器可以表达的自定义组合允许生成，并明确警告“未验证”；不能表达或确定改变核心连接语义时按 §7.4 处理。不得把未验证等同于完整支持，也不能用未验证标签掩盖已知映射缺失。

### 4.3 首批字段目录

以下是当前研究支持的候选目录和设计分组；每项仍需要记录目标范围，不代表表内选项对所有客户端统一有效。

| 字段 | 选项与结构 | 归属与限制 |
|---|---|---|
| SS `cipher` | 常用：`aes-128-gcm`、`aes-256-gcm`、`chacha20-ietf-poly1305`；其他／旧版兼容组；可手填；SS 2022 仅列后续／待验证 | 不使用 VMess 的 `auto`；必须匹配服务端 |
| SS 2022（后续专项） | `2022-blake3-aes-128-gcm`、`2022-blake3-aes-256-gcm`、`2022-blake3-chacha20-poly1305` | 算法、密钥格式和 URI 编码联动，不列为首批完整支持 |
| VMess `cipher` | `auto`、`aes-128-gcm`、`chacha20-poly1305`、`none`、`zero`；可手填 | 与 SS 独立；特定客户端 URI 导入有改写风险 |
| VLESS `flow` | 未设置、`xtls-rprx-vision`；可手填 | 按连接组合验证，不自动为所有 VLESS 启用 Vision |
| `client-fingerprint` | 按目标提供浏览器指纹候选，如 `chrome`、`firefox`、`safari`、`iOS`、`android`、`edge`、`360`、`qq`、`random`；可手填 | 目标版本和规范写法独立记录，不能据此认定 SR 接受同一全集 |
| `fingerprint` | 证书指纹实际值输入 | 与客户端 TLS 指纹不同，不提供浏览器名称选项 |
| `alpn` | 推荐条目＋自定义条目，有序列表 | 按传输建议 `h2`／`http/1.1` 等；不把 `h3` 全局预填 |
| SS `plugin` | 未使用插件、现有 `obfs`／`v2ray-plugin`／`shadow-tls`／`restls`；允许自定义并显式提示能力状态 | 新上游插件不因可手填而标为项目已支持；未知插件使用高级参数入口 |
| 插件 `mode`／`version` | 按插件显示候选，例如 obfs 的 `http`／`tls`、v2ray-plugin 的 `websocket` | 不让不同插件共用一个无条件模式表 |
| VLESS XHTTP `mode` | 未指定、`auto`、`stream-one`、`stream-up`、`packet-up`；可手填 | `none` 不作为“未指定”的存储替身 |
| 包编码与多路复用 | 已有 schema 中的候选按目标列出；允许手填 | 包编码、Mux 方言分别建模，不把 Mihomo SMux 直接映射为 Xray Mux |
| HTTP Method 等扩展字段 | 推荐常用方法＋手动输入 | 校验合法格式与目标限制；服务端是否接受仍需匹配 |

目录依据：[Mihomo SS](https://wiki.metacubex.one/en/config/proxies/ss/)、[VMess](https://wiki.metacubex.one/en/config/proxies/vmess/)、[VLESS](https://wiki.metacubex.one/en/config/proxies/vless/)、[TLS](https://wiki.metacubex.one/en/config/proxies/tls/)、[Transport](https://wiki.metacubex.one/en/config/proxies/transport/)。其他／旧版 SS 算法不从客户端 TypeScript 全集直接复制；按版本建立清单，旧值仍可回显。

### 4.4 SS 2022 的条件表单

选择 SS 2022 后，凭据说明相应改为预共享密钥要求。AES-128 和 AES-256 的单密钥分别按 Base64 解码后的 16／32 字节检查；带身份密钥链的配置另按其语义建模，不能以“整个字符串只允许一个 Base64 密钥”误拒绝合法场景。本轮检查覆盖三个算法的单密钥形态，没有完成全部身份密钥链验证。[SIP022](https://shadowsocks.org/doc/sip022.html)、[SIP023](https://shadowsocks.org/doc/sip023.html)

算法切换后原凭据不能被自动截断、重新编码或生成替代值。新算法与已有凭据不兼容时要求用户明确处理；编辑时“已保存凭据”仍由服务端合并后校验，浏览器不接收原密钥。

按本轮 D18，本节作为 SS 2022 后续专项研究保留，不列入首批实现范围。

---

## 五、首批四协议表单纲要

### 5.1 VLESS

| 层次 | 内容 |
|---|---|
| 基础与认证 | 名称、服务器、端口、UUID |
| 当前连接 | 传输选择、安全选择、对应传输与身份参数、适用时的 Flow |
| TLS | SNI、ALPN、客户端指纹等当前能力支持的常用字段 |
| REALITY | SNI、公钥、Short ID、客户端指纹；不照搬服务端私钥、target 或监听配置 |
| 更多参数 | XHTTP 扩展、Early Data、TCP 伪装、多路复用、其他目标相关字段 |

安全状态表达无外层安全／TLS／REALITY，但允许的组合由目标证据确定。WS、gRPC、H2、HTTP、XHTTP 各自展示专用字段；不把这些传输互相当作别名。VLESS `encryption` 的高级值需要核对适配器，当前 generic 输出固定 `none` 的事实不能被 UI 自定义输入掩盖。

### 5.2 VMess

常用区包含 UUID、独立的 VMess Cipher、当前传输与安全信息。内部加密和外层安全使用不同标签。AlterId 保留为兼容参数，默认语义与显式 `0` 分开；Padding、Authenticated Length、多路复用等在高级区域。

REALITY、传输扩展和包编码逐组合验证，不能照抄 VLESS 的安全菜单。URI 目标还须处理第八章确认的算法改写风险。

### 5.3 Trojan

常用区包含密码、当前传输及 TLS 身份信息。以指定 Mihomo 基线核实的 TCP／WS／gRPC 作为普通传输候选；其他自定义传输保留原值和诊断，不能把其被静默当作 TCP 的行为当成支持。Trojan 不强行复用“TLS bool 默认 false”的 VLESS／VMess 表单。

Trojan-Go 内层 SS 等属于高级条件功能。研究发现目标字段是 `ss-opts.enabled`／`method`／`password`，当前项目对象使用 `cipher` 且未声明 `enabled`；由于无旧数据兼容，构建时应以目标字段语义重新明确 Trojan 内层 SS 的表单结构与输出映射，不能仅增加算法下拉框。[Mihomo Trojan](https://wiki.metacubex.one/en/config/proxies/trojan/)、[v1.19.29 Trojan 源码](https://github.com/MetaCubeX/mihomo/blob/v1.19.29/adapter/outbound/trojan.go)

### 5.4 Shadowsocks

常用区包含算法、密码／密钥、插件选择和当前插件所需参数。插件决定子表单；切换或取消插件清空所属参数、凭据和扩展，切回重新填写，SS 主密码不因插件切换清空。UDP 等业务开关集中在独立区；UDP over TCP、SMux 和插件复杂扩展按层次折叠，功能关闭与面板折叠分别遵循 §3.4。

不因为 Xray 样例含 SS＋WS／gRPC，就给所有 SS 节点套用 VLESS 式传输与 REALITY 表单。插件内部 TLS、模式和认证各有语义，按当前插件激活。参考 [SS 插件说明](https://wiki.metacubex.one/en/config/proxies/ss/#plugin)。

按 D18，SS 2022 的完整 URI/密钥/客户端兼容不列入首批实现；首批 SS 以普通 AEAD、插件条件表单与插件诊断为主，SS 2022 在界面和输出中明确为后续/待验证。

### 5.5 共同校验原则

- 缺少活动分支的必要凭据、类型错误、端口越界或明确非法组合，阻止保存并返回字段路径。
- WS Path 的合法缺省、ServiceName 与服务端匹配、REALITY Short ID 的合法空值条件分别处理，不跨目标统一要求非空。
- 必要连接参数在目标输出中会丢失时，不得成功生成后仍标为完整支持；可选调优缺失与核心连接含义改变分别诊断。
- 条件切换、数组条目删除、JSON 模式关闭后重建有效性状态，避免已隐藏或已删除路径残留而锁死保存。

收窄必填规则的证据见 [Xray WS](https://xtls.github.io/en/config/transports/websocket.html)、[gRPC](https://xtls.github.io/en/config/transports/grpc.html)、[REALITY](https://xtls.github.io/en/config/transports/reality.html)。这些只证明对应目标的语义，不能直接充当所有客户端的统一约束。

---

## 六、保存状态、凭据与 JSON

### 6.1 活动配置与最小当前状态

采用单节点行保存模型，取消独立 `node_edit_states` 表及非激活分支恢复机制。职责如下：

```text
统一编辑草稿：当前选择＋当前参数＋扩展归属＋本次重置范围／凭据操作
  → 服务端解析、归一化；新建初始化，更新按修订号取得一致的旧状态
  → 从合并基底清除被重置数据；处理保留／替换／清除操作
  → 当前连接校验与活动投影
  → nodes 同一行保存当前元数据、活动 protocol_json、加密扩展及版本／修订信息
  → 按目标投影与能力检查
  → Clash YAML／SR URI／generic URI／后续 Xray outbound
```

| 数据 | 职责与持久化边界 |
|---|---|
| 当前选择元数据 | 显式表达当前传输、安全、插件和功能选择；只保留恢复当前表单所必需的信息，不能由 `reality-opts` 对象存在性猜测安全方式 |
| 活动 `protocol_json` | 服务端由当前选择和参数生成，是实际适配器的活动配置输入；不接受另一份可独立编辑、与草稿冲突的活动真值 |
| 当前未知扩展 | 保存来源、所属分区、目标归属及加密内容，按 §6.4 处理；不复制到非激活分支，不在活动配置中另存明文副本 |
| 格式版本 | 表示当前数据结构的解释方式，与数据库迁移版本、编辑修订号区分 |
| 编辑修订号 | 对节点配置的一致更新进行冲突检测；成功保存时更新，旧修订请求返回 409 |
| 仅本次草稿的数据 | 被重置分区标记、凭据操作、未应用 JSON、搜索文本和折叠状态不作为分支历史持久化 |

上述持久化内容归属 `nodes` 同一行，同事务写入；具体列名与 JSON 字段结构仍需在接口契约中细化。服务端投影必须清除失效的已知参数，目标输出不能全量透传内部元数据。保留显式当前状态的目的，是准确解释当前配置，而不是保存另一份完整参数副本。

REALITY → TLS 会清空原安全分区，TLS 参数重新填写；保存重开后仍为 TLS，随后切回 REALITY 也需重新填写。清空不会删除已经生成的版本快照，历史版本按 §7.3 处理。

编辑修订号覆盖同一次保存的当前状态、活动参数和扩展。冲突时不自动合并，前端保留当前草稿并提示重新加载；数据库不得出现部分写入。即使只写一行，也需要修订校验，不能把事务写入等同于防止旧页面覆盖。

### 6.2 条件元数据与选项元数据

继续扩展现有后端 `FieldSchema`，不在前端维护另一份协议字段全集。需表达：

| 概念 | 作用 |
|---|---|
| 活动条件／条件必填 | 由协议、传输、安全、插件、认证模式决定生效范围 |
| 所属分区／重置依赖 | 声明参数、凭据、扩展随哪些选择变化或功能关闭而清空；处理跨传输／安全依赖，与单纯显示条件分开 |
| 展示组／优先级 | 决定常用、高级和开关位置；不参与运行含义判断 |
| 推荐选项／允许自定义 | 区分候选目录与合法值边界，支持已知值和自定义值回显 |
| 规范值／显示名／分组 | 统一写入值与用户文案，避免各端擅自映射 |
| 默认与未设置 | 区分缺省、空串、false、0 和显式值 |
| 字段类型／格式／范围 | 前端提前反馈，后端最终验证 |
| 别名与规范路径 | 兼容输入、冲突诊断及目标映射 |
| 目标证据与验证级别 | 关联客户端、版本、入口、字段能力和已知差异 |

规则保持小型、声明式，不引入任意脚本求值。前端、Go 后端和适配器共享规则数据及正反样本，而不是复制三套条件逻辑。推荐列表不能被当成严格枚举集合；`allow_custom` 等具体名称只在后续接口契约中确定。

### 6.3 凭据操作与清空优先级

凭据在 UI 表达“未配置／已保存／待替换”。普通编辑且对应分区未被重置时，允许保留已保存凭据；请求必须明确区分保留、替换、清除，不能让空串同时承担三种含义。服务端仅能保留同一节点、同一修订、同一语义位置上确实存在且未被重置的凭据。

- 切换分支后，对应凭据转为未配置；检查与保存请求携带本次重置范围和凭据操作，不靠空输入推断删除。
- 服务端先从旧状态的合并基底清除被重置分区，再应用本次重新填写的值。重置不应删除用户切换后新填写的凭据；但不得用旧密文补齐空值。
- 同一次编辑中 A → B → A，或跨协议切换后再切回，重置标记仍有效；不能只比较最终选择与数据库相同，就恢复原参数或允许保留旧凭据。只记录被重置范围，不保存各分支草稿或切换历史。
- 可选凭据允许清除；必要凭据被清空后必须重新填写并通过当前模式校验，才能保存。切换传输不清空无关的协议主凭据，跨协议切换则全部清空。
- 节点检查与正式保存共享相同的重置、凭据处理和校验流程；检查不更新数据库，也不能重新带回被清空凭据。
- 加密、脱敏、合并、解密须覆盖同一组敏感路径和扩展块；诊断不得回显秘密。后续多 Peer 等场景仍需稳定条目身份，不能按重排后的数组索引保留他项凭据；取消分支恢复并不消除数组条目自身的身份问题。

### 6.4 未知参数与高级 JSON

未知扩展保留原结构和来源，不因更新时 schema 重建而丢失。必须声明所属范围：节点公共区、传输、安全、插件或具体功能；所属分支清空时连同扩展一并清除，不能把无法归属的内容静默当作公共字段长期保留。顶层无法归属的输入先在高级区提示补充归属。

首批对无法识别敏感路径的未知扩展采用整体加密块，支持保留／替换／清除。读取时返回归属和已配置状态等摘要，不完整回显保存后的内容；编辑替换需要重新提供该块。已知普通字段照常编辑，未知块不进行自动逐键凭据猜测。分支重置对扩展块的保留操作具有与 §6.3 相同的约束。

扩展仅在明确指定的目标中参与输出；未指定目标时不自动透传，检查须提示其未参与输出。指定目标并不构成兼容承诺，仍按 §7.4 判断能否表达和诊断风险。内部归属、加密包装和修订信息不得进入客户端产物；脱敏检查也不得因 Base64、URI 或嵌套对象包装而暴露扩展秘密。

结构化表单与局部 JSON 共用一份有效状态。JSON 编辑采用“草稿 → 校验 → 应用本区”，取消则放弃该草稿；同一范围不允许父区和子对象同时持有互相覆盖的未应用编辑。保存节点前明确处理未应用内容。分支清空会同时丢弃该范围的局部草稿；单纯切换编辑模式不清空有效值，也不允许旧草稿重新应用到已切换的分支。完整目标 JSON 首期用于只读脱敏检查，不成为第二份可编辑真值。

### 6.5 API 与初始化边界

当前项目无业务数据（用户确认），因此不要求旧 manual 节点 legacy 解码、旧字段别名冲突处理或旧数据迁移读取。新库按版本化 schema 初始化；全部 19 个 manual 协议接入统一保存契约，其余 15 个保留原表单并使用最小当前状态，不因首批改造而失去现有编辑和输出入口。

manual 表单和 URI 导入统一归一化到规范路径，例如 `ws-opts`，再执行当前状态初始化、校验和加密保存；批量导入继续保留逐行回执。新建不需要重置历史，已保存节点更新才使用修订号及本次重置操作。

Xray 节点不建立可人工编辑的分支状态。当前实例检测仍可能产生 `ws-path`、`ws-host` 等字段，必须在进入共享输出层前做来源适配；这属于现有生产入口，不属于可省略的旧数据兼容。不得把 manual 规范路径的收敛直接当作删除 Xray 来源转换的依据。

API 需覆盖编辑详情读取、创建、更新及新建／编辑草稿检查；读取应包含必要当前状态、修订号和脱敏凭据／扩展摘要。具体 DTO、条件元数据结构、诊断字段路径及接口路径见 §10.2 待补齐项。取消独立表后仍需覆盖 `nodes` 新字段的迁移、备份恢复和密钥保护；配置导出不等于完整节点备份。

---

## 七、客户端能力与输出检查

### 7.1 兼容判断的维度

每条结论至少关联：**协议＋连接组合＋字段／选项＋输出格式＋客户端及版本＋进入客户端的路径＋证据级别**。

例如“Clash Verge Rev 2.5.2／Mihomo 1.19.29 加载 Clash YAML”和“Clash Verge Rev 2.5.2 手动导入 VMess URI”是两条不同路径。generic 是项目 URI 约定，不是单一客户端，也不代表任意客户端都接受。

| 能力状态 | 含义 |
|---|---|
| 可完整表达 | 当前活动连接语义都能映射；另注明证据是文档、解析还是连接 |
| 等价转换 | 规范值或结构转换后语义相同，保留转换说明 |
| 部分表达 | 可选能力丢失或改变，指出具体字段与影响 |
| 不支持 | 核心认证、传输或安全语义无法表达 |
| 未验证 | 无足够目标版本或实际路径证据，不显示成功徽标 |

“内核接受”“客户端正确导入”“客户端完成连接”作为独立验证记录。HTTP 200、解析无异常或 `mihomo -t` 成功均不能独立证明连接组合正确。

### 7.2 目标检查体验

节点高级区按需检查当前草稿，新增的节点检查接口必须同时覆盖未保存的新建节点和已保存节点的编辑草稿；不能只依靠必须提供节点 ID 的路径。新建使用本次输入；编辑使用节点身份、基础修订号、当前草稿、重置范围及凭据操作，按 §6.3 处理已保存凭据，不读取被清空分区作为兜底。服务端复用实际适配器，只返回脱敏产物及诊断，不落库。

改变影响输出的字段、切换分支、改变凭据操作或扩展归属后，已有检查结果失效。请求与响应须关联对应草稿版本，迟到的旧响应不能覆盖新草稿的检查状态；具体字段形状由接口契约细化。节点检查仅使用固定 profile 的规则与实际适配器，不将本机研究实验的内核命令当作服务端已具备的检查能力。用户不必为了保存节点先选择全部目标。

日常只显示有意义的差异，例如“此链接入口会改变 VMess 算法”“此插件参数不能完整输出”。技术细节按需展开。脱敏产物明确标注不能直接用于连接。节点局部检查不能替代装配覆盖层应用后的最终检查。

### 7.3 历史版本与 Xray 来源

新建或重新装配的版本使用新的活动投影与目标检查。已生成 manual 快照不因节点编辑或能力表更新被静默重写；需要重新装配才能形成新产物。渲染计划解释须有新旧边界。

动态 Xray 节点涉及共享输出路径时一并评估，但配置所有权仍属于实例检测。本文不开放手工修改 Xray 来源节点，也不改变名称、组引用、权限或分配语义。

### 7.4 已确认的目标生成门槛

保存节点与生成目标产物分级处理：类型错误、当前必要凭据缺失及确定的通用非法结构阻止保存；合法节点对具体目标不兼容时，在目标检查和生成阶段处理。

| 诊断情况 | SR／generic 节点订阅 | Clash YAML |
|---|---|---|
| 核心认证、传输或安全语义无法表达，或确定会被错误改写 | 跳过该节点，列出节点、字段及原因；最终有效链接数为零时拒绝生成 | 对该已选节点报错并阻止生成，不自动删除节点或改写代理组成员 |
| 仅可选调优能力无法表达，核心连接语义仍完整 | 允许生成，明确提示丢失字段和影响 | 允许生成，明确提示丢失字段和影响 |
| 仅缺少目标验证证据，尚无确定错误且适配器能够表达 | 允许生成并标注未验证 | 允许生成并标注未验证 |
| 可以完整表达或已知等价转换 | 生成；转换需说明，验证证据级别单独显示 | 同左 |

上述规则延续 Design2 §4.5 的 SR／generic 跳过及空产物语义，并增加字段／组合级诊断。预览应展示相同的诊断，不能预览显示完整支持、保存版本时才无提示剔除。可选损失和仅缺证据均不能冒充完整兼容，也不能把已知错误降级为未验证。

节点局部检查与覆盖层应用后的最终检查分别执行；覆盖层不能绕过上述核心语义检查。历史 manual 快照和动态 Xray 下载路径仍按 §7.3 的边界处理，具体接入与错误回执在后续契约中明确。Shadowrocket 缺少真机证据本身不使所有节点被剔除；SS 2022 已知编码问题不能仅贴未验证标签后无条件放行，其完整兼容仍为后续范围。

---

## 八、2026-09-02 补充研究结论

### 8.1 实验方式与证据限制

本章承接写入前同一任务中的研究结果。离线检查调用本机 `/Applications/Clash Verge.app/Contents/MacOS/verge-mihomo` 的 `-t`，使用独立临时目录、通过 `-config` 传入合成配置；地址使用 `192.0.2.1`，配置无真实账号，规则为 `MATCH,DIRECT`。该二进制自报版本 `Mihomo Meta v1.19.29 darwin arm64`。

URI 样例在内存中加载官方仓库 `v2.5.2` 标签的解析文件，去除 TypeScript 类型后执行；没有把本地其他开发提交当成已安装版本源码。样例按当前项目编码形态或 SIP002 形态构造，**不是运行新适配器后的验收结果**。

临时目录已清理；未更改运行中的 Clash Verge 配置，没有进行真实代理连接。实验输出保留于本次任务记录；本章是结果摘要，尚未形成仓库测试夹具。本次文档写入没有重新执行这些实验。

### 8.2 41 个内核配置检查

| 检查组 | 数量 | 观察结果 |
|---|---:|---|
| SS 已接受算法 | 11 | 三个常用 AEAD、三个 SS 2022，以及 `aes-192-gcm`、`xchacha20-ietf-poly1305`、`aes-128-cfb`、`rc4-md5`、`none` 被接受 |
| SS 被拒绝算法 | 4 | `auto`、`dummy`、`chacha20-poly1305`、合成未知算法被拒绝 |
| VMess 已接受算法 | 5 | `auto`、`aes-128-gcm`、`chacha20-poly1305`、`none`、`zero` 被接受 |
| VMess 被拒绝算法 | 3 | `chacha20-ietf-poly1305`、`aes-256-gcm`、合成未知算法被拒绝 |
| SS 2022 无效密钥 | 2 | AES-128／AES-256 使用非 Base64 密钥被拒绝 |
| VLESS XHTTP Mode | 6 | `auto`、`packet-up`、`stream-up`、`stream-one` 被接受；`none`、合成未知模式被拒绝 |
| Trojan Network | 6 | `tcp`、`ws`、`grpc`、`h2`、`xhttp`、合成未知传输均通过配置检查；后三者不能据此认定支持 |
| Trojan 内层 SS Method | 4 | 三个常用 AEAD 被接受，合成未知算法被拒绝 |
| **合计** | **41** | 配置接受与拒绝记录，不是连接成功计数 |

SS／VMess 不能共用算法清单或别名规则。[Mihomo 算法实现](https://github.com/MetaCubeX/mihomo/blob/v1.19.29/transport/shadowsocks/core/cipher.go)和各协议文档提供解释依据；上述接受结果也不意味着应把旧版算法放进常用推荐组。

Trojan 的源码只对 WS／gRPC 作专门处理，其他值走 TCP。XHTTP 的 `none` 明确报模式未实现，而项目当前 schema 将它用作默认值。这两类证据分别要求补充语义校验和修正候选目录，不能用“配置检测通过”掩盖静默回退。[Trojan v1.19.29](https://github.com/MetaCubeX/mihomo/blob/v1.19.29/adapter/outbound/trojan.go)、[项目 XHTTP schema](backend/internal/node/registry.go)

### 8.3 9 个 URI 样例检查

| 样例 | Clash Verge Rev 2.5.2 官方解析器观察 |
|---|---|
| SS `aes-128-gcm`，普通 Base64 userinfo | 解析保留算法 |
| SS 2022，当前项目的 Base64 userinfo 形态 | 样例算法和密钥保留 |
| SS 2022，SIP002 的百分号编码 userinfo 形态 | 样例算法保留，但密钥未正确还原 |
| SS obfs，当前项目形态 `obfs;host=…;mode=tls` | 报插件名不支持 |
| SS obfs，`obfs-local;obfs=tls;obfs-host=…` | 正确解析插件及样例参数 |
| VMess `auto` | 算法保留 |
| VMess `aes-128-gcm` | 算法保留 |
| VMess `chacha20-poly1305` | 改写为 SS 风格 `chacha20-ietf-poly1305`；该值被上述 VMess 内核检查拒绝 |
| VMess `zero` | 改写为 `auto`，未保留原值 |

指定版本源码：[helpers.ts](https://github.com/clash-verge-rev/clash-verge-rev/blob/v2.5.2/src/utils/uri-parser/helpers.ts)、[ss.ts](https://github.com/clash-verge-rev/clash-verge-rev/blob/v2.5.2/src/utils/uri-parser/ss.ts)、[vmess.ts](https://github.com/clash-verge-rev/clash-verge-rev/blob/v2.5.2/src/utils/uri-parser/vmess.ts)。

这些观察证明必须区分 YAML 与 URI 入口。不能为适应某个 URI 解析器而全局将 VMess 算法改成 SS 写法，也不能把算法变成 `auto` 当作等价修复。

### 8.4 SS 2022 与插件编码的设计含义

[SIP002](https://shadowsocks.org/doc/sip002.html) 对 AEAD-2022 要求非 Base64 的 userinfo，并对 method／password 作百分号编码。项目目前所有 SS 输出统一 Base64；上述特定解析器又存在规范形态的密钥还原差异。因此：

- 标准 URI 与客户端特定兼容路径要有明确归属，不能把一个历史解析器的容错行为认定为标准。
- 不因本轮样例可读，就宣布项目 SS 2022 的全部客户端链路已完成。
- 目标选项需要覆盖密码中 `+`、`/`、`=`、身份密钥链等编码样例；不得通过改算法或改密钥回避差异。
- SS 2022 具体采用哪种客户端兼容输出、版本条件与提示方式，列为后续专项的设计前置；不作为首批 Build 的阻塞项，也不在本次静默改变历史链接。

项目 [pluginString](backend/internal/assembly/links/links.go) 将存储中的插件名与属性直接串联，不能代替 SIP003／目标客户端的字段映射。`obfs` 的名称及 `mode/host` 转换已有反例；布尔标志、复杂对象及分隔符转义也需要独立规则和样本。插件推荐选项必须与该映射能力一致。

按本轮用户确认，**SS 2022 的完整密钥/URI/客户端兼容不列入首批实现范围**；首批 SS 只保证普通 AEAD 与插件条件表单、插件诊断，SS 2022 在界面和输出中明确标为后续/待验证，不宣称完整支持。

### 8.5 Shadowrocket 与其他客户端

由 [App Store](https://apps.apple.com/us/app/shadowrocket/id932747118) 指向 [开发者官网](https://shadowlaunch.com/)，再经官网关联群组找到公告频道，确认了官方来源链。正式版 2.2.10 的公告说明加入 AEAD-2022；后续公告包含相关密钥修复、Vision 和 REALITY 等能力变化。[AEAD-2022 发布记录](https://t.me/ShadowrocketNews/384)、[后续发布记录](https://t.me/s/ShadowrocketNews?before=421)

这些是功能与版本证据，不是全部算法拼写、插件参数及 URI 组合的验收矩阵。**本轮没有 Shadowrocket 真机导入／连接结果**；不能给全部候选项统一打“SR 已验证”标记。

[sing-box 官方 SS 文档](https://sing-box.sagernet.org/configuration/outbound/shadowsocks/)列出其算法及插件范围，可作其他常用内核的交叉依据，但没有执行其客户端实验，也不据此新增项目 sing-box 输出目标。Clash Verge Rev 的版本关系另见 [v2.5.2 发布记录](https://github.com/clash-verge-rev/clash-verge-rev/releases/tag/v2.5.2)。

---

## 九、后续全协议计划与独立 Xray 输出

### 9.1 完成现有 19 协议的目标清单

| 协议 | 本次阶段 | 后续完成时的主要条件模型 |
|---|---|---|
| VLESS | 首批设计，待构建 | 传输、安全、Flow、目标能力 |
| VMess | 首批设计，待构建 | 内部加密、传输、安全与旧版参数 |
| Trojan | 首批设计，待构建 | TLS、传输、内层 SS 与目标映射 |
| SS | 首批设计，待构建 | 算法、密钥、插件与分支参数 |
| Hysteria | 后续必须覆盖 | 认证、混淆、端口与带宽模型 |
| Hysteria2 | 后续必须覆盖 | 独立认证、混淆和端口参数，不与 Hysteria 合并 |
| TUIC | 后续必须覆盖；首批模型验例 | v4／v5 认证互斥、凭据与目标输出 |
| WireGuard | 后续必须覆盖；首批模型验例 | 地址、多 Peer、稳定条目身份和数组密钥 |
| HTTP | 后续必须覆盖 | 认证模式、TLS 与 Header |
| SOCKS5 | 后续必须覆盖 | 认证、TLS 和 UDP 能力 |
| Snell | 后续必须覆盖 | PSK、版本与目标能力 |
| AnyTLS | 后续必须覆盖 | TLS 身份、会话调优和证书凭据 |
| Mieru | 后续必须覆盖 | 认证、传输、端口与多路复用选项 |
| MASQUE | 后续必须覆盖 | 独立核验密钥和隧道参数，不套用 WireGuard 必填规则 |
| OpenVPN | 后续必须覆盖 | 完整配置编辑、语法及内嵌凭据保护 |
| SSH | 后续必须覆盖 | 密码／私钥认证、口令和 Host Key |
| ShadowQUIC | 后续必须覆盖 | 专用认证与安全参数 |
| TrustTunnel | 后续必须覆盖 | 现有能力核对与协议专用字段 |
| Tailscale | 后续必须覆盖 | 认证与自身会话语义 |

以上来自当前 [ManualProtocols](backend/internal/node/registry.go)。后续新增协议另行评估；SSR 不在当前范围。模型复用按字段语义进行，不要求 QUIC、隧道或配置全文型协议都出现“传输＋TLS／REALITY”菜单。

每个协议完成需要覆盖条件字段、选择与自定义、凭据、保存重开、目标映射及对应验收。未支持 URI 的协议可以继续有正确的编辑和 Clash 能力；完成编辑改进不等于承诺新增其 URI 输出。

### 9.2 独立 Xray outbound

后续输出边界为有效客户端节点到固定验证 profile 的 Xray outbound，并返回转换差异。普通用户不必先选择大量核心版本。

完整 DNS／路由／inbounds／多个 outbounds 的配置组织、服务端部署及任意 Xray JSON 反向导入不属于该范围。目标版本不能只因项目依赖某版本而自动选定，也不能把在线最新字段、旧样例与项目依赖混为一个契约。Mihomo SMux 与 Xray Mux、不同传输别名均需独立语义映射。

---

## 十、影响评估、待细化契约与验收

### 10.1 受影响范围

| 模块／文档 | 影响 | 处理方式 |
|---|---|---|
| 节点表单、递归字段编辑器、节点 API 类型 | 条件显示、可编辑选择、凭据状态、局部 JSON 和错误定位 | 复用现有浮层与主题；不无条件改变共享组件的其他调用方 |
| 节点注册表与服务 | 活动条件、字段规范、选项、状态投影、凭据合并 | 服务端最终验证，统一样本约束 |
| 数据迁移与恢复 | `nodes` 当前元数据、活动参数、加密扩展、格式版本与修订号 | 同行同事务写入，覆盖迁移、备份恢复、配置导入密钥保护；不把现有配置导出误认为完整节点备份 |
| URI 解析和批量导入 | 编码、插件映射、当前状态初始化 | 保留逐行回执，统一归一化，不无声改写 |
| Clash／SR／generic 输出 | 活动投影、能力诊断、客户端差异 | 复用实际适配器，按 §7.4 执行生成门槛，按版本和入口验证 |
| Xray 检测与来源适配 | 检测字段与 manual 规范路径并不完全一致 | 接入共享输出前完成来源归一化，不开放 manual 式编辑，不因无旧数据省略这条路径 |
| 装配蓝图、版本、下载 | 新旧解释边界与最终检查 | 历史 manual 快照保持原语义；核对动态 Xray 路径 |
| Design／Build／Issue | 新设计、后续构建及问题闭环 | 本次只写 Design4；Build 待另行授权，问题未修复不闭环 |

### 10.2 构建前需要细化的内容

本节集中记录首批仍需补齐的设计契约，避免与第十一章重复维护待办。已确认行为不再作为待选方案；具体字段结构、组合矩阵和契约样例补齐后，才能作为可执行 Build 的完整依据。DDL、代码和测试夹具实现由 Build 承担，不要求先写代码再编写 Build。若细化中发现新的产品行为或兼容取舍，仍须按 AGENTS.md 与用户确认。

| 项目 | 已确定 | 尚待明确 |
|---|---|---|
| 当前状态与修订 | 取消独立表，在 `nodes` 同行保存最小当前状态；格式版本与修订号分开，旧修订返回 409（D4／D13／D22） | 元数据与活动字段的具体结构、配置写入入口的修订覆盖范围及一致读写契约 |
| 创建／更新／读取 API | 全部 manual 使用统一契约，其余 15 协议保留原表单；Xray 不建立人工编辑分支状态 | DTO、脱敏详情结构、凭据／扩展摘要、字段错误及冲突响应样例 |
| 条件元数据 | 后端 `FieldSchema` 承载活动条件、必填、分区、推荐值及重置依赖 | 具体 JSON 结构；四协议字段的归属和跨维度清空清单，条件正反样例 |
| 清空与凭据操作 | 按分区清空；A → B → A 不恢复旧值；重置合并基底后允许新输入，检查与保存共用流程 | 重置范围、保留／替换／清除的请求形状及新建／更新／重置后再填写样例 |
| 目标生成与诊断 | §7.4 已确定拒绝／跳过／警告和零有效链接行为 | 字段级诊断结构、预览／生成／覆盖层最终检查的接入与回执；动态 Xray 路径的对应契约 |
| 客户端 profile | Mihomo 1.19.29 与 CVR 2.5.2 为首批基线；Shadowrocket 待真机验证（D16） | 首批各协议普通候选组合清单、Clash YAML／SR URI／generic URI 字段级矩阵，标明完整／转换／可选损失／不支持及独立证据级别 |
| SS URI 与插件 | 普通 AEAD、插件条件表单与诊断为首批，SS 2022 完整支持后置（D18） | 普通插件逐字段映射、SIP003 规则和样例；明确哪些首批修复输出、哪些按 §7.4 诊断 |
| 未知扩展 | 来源／分区／目标明确；无法识别敏感路径时整体加密，保留／替换／清除，读取摘要；随分支清空（D10） | 扩展块结构、范围标识、脱敏摘要和向目标投影的契约样例 |
| 节点检查 | 同时接受新建及编辑草稿，复用实际适配器，使用有效凭据操作，不落库；旧响应不能覆盖新结果（D17） | 路径、请求／响应结构、草稿版本关联、字段诊断格式及凭据引用 |
| 初始化与来源适配 | manual 表单和 URI 导入统一入口；Xray 检测数据在共享输出前适配；无旧数据不免除新入口处理 | 各来源当前状态初始化及规范路径映射、逐行导入回执样例 |
| 前端具体交互 | 独立开关区、可编辑下拉、局部 JSON、分支清空与错误展开定位已确认 | 控件提交时机、未应用 JSON 的处理及桌面／移动交互验收样例 |

### 10.3 后续验收标准

| 类别 | 必须证明的行为 |
|---|---|
| 首批动态表单 | 四协议按自身模型切换；核心字段与当前连接参数易于发现；SS 插件不误套 VLESS |
| 开关与响应式 | 业务开关集中、按适用条件显示；更多开关可折叠；桌面／移动无横向溢出，保存和错误焦点可达 |
| 推荐与手动输入 | 搜索、选择、自定义、旧值回显、清空及输入法正常；无失焦改写；单值不变多值 |
| 同次编辑内切回 | WS → gRPC → WS、插件 A → B → A 均不恢复原参数或凭据；跨协议切回也不恢复 |
| 保存重开后切回 | 切换并保存后，数据库无原分支恢复副本；重开维持当前选择，再切回原分支仍需填写 |
| 清空范围 | 只清空声明范围和依赖字段；插件切换保留 SS 主密码；安全切换清空所属 SNI／ALPN／指纹；跨协议保留名称／地址／端口 |
| 草稿与落库 | 切换不立即写库；取消整个编辑或保存失败不改变已保存节点；必要凭据缺失时不能保存 |
| 折叠与功能关闭 | 收起面板及切换编辑模式不改变有效值、校验或输出；关闭有子配置功能清空其子参数，重开不恢复 |
| 默认与凭据 | 未设置／false／0 正确；未重置凭据可保留，重置后不得回填旧密文；重新填写的新凭据可正常校验保存 |
| JSON 与未知扩展 | 局部草稿应用／取消清楚，分支清空后旧草稿不能重入；扩展整体加密并返回摘要、仅在指定目标输出、所属分支清空时删除；脱敏输出无秘密泄漏 |
| 并发与检查 | 旧修订保存返回 409，保留前端草稿，数据库无部分更新；检查不落库、与保存共用凭据处理，迟到响应不覆盖新草稿 |
| 算法目录 | SS／VMess 规范值和别名分开；普通 AEAD 与插件诊断正确；SS 2022 明确后续/待验证，不在首批宣称完整支持；XHTTP 不将 `none` 当缺省 |
| 输出语义 | YAML 与 URI 路径分别验证；对算法改写、插件丢失、传输回退作明确诊断 |
| 四协议输出 | Trojan WS／gRPC、VLESS 安全与 Flow、VMess Cipher、SS 插件逐目标验证；核心语义无法表达时 SR／generic 跳过并说明、零链接拒绝，Clash 阻止生成且不自动改组成员；可选损失和未验证分别警告 |
| 过渡与来源 | 其余 15 协议可继续新建／编辑／保存；URI 导入进入统一当前状态；Xray 检测字段在共享输出前适配且来源所有权不变 |
| 持久化与备份 | 新库初始化、节点更新、删除、备份恢复覆盖当前元数据、加密扩展及修订号；配置导入密钥保护覆盖新密文存储位置 |
| 历史版本边界 | 当前无旧数据；设计落地后新版本使用新活动投影，已生成快照不被静默重写，重新装配才生成新产物 |
| 扩展模型 | TUIC 认证互斥和 WireGuard Peer 身份／凭据设计成立，不提前宣称其 UI 已完成 |
| 客户端实验 | 指定版本逐项导入并核对字段；代表组合完成连接实验后才记录相应连接级结论 |

获批构建后按受影响范围执行后端 `go build ./...`、`go vet ./...`、`go test ./...`，前端相关测试与 `npm run build`，以及 `git diff --check`；界面补充桌面／移动交互检查。适配器同时保留固定版本正反例，客户端升级时复核已知差异。本文不把上述未来验收标记为已完成。

### 10.4 设计推进流程

v1.2 已写入用户确认的清空规则、单节点行保存、协议过渡、生成门槛、扩展保护和并发方案。下一步补齐 §10.2 的首批契约与组合矩阵，按 §11.3 判断准备度，再由获授权的 Build 文档承接文件拆分、DDL、参考代码及测试实现。后续协议、SS 2022 和独立 Xray outbound 单独推进，不作为首批整体阻塞项。完成构建和验收后，才更新功能实现状态与相应 Issue 闭环。

---

## 十一、构建前检查与设计准备度

### 11.1 本轮确认后的结论

当前有效决策统一记录在第二章。取消分支保留可减少非激活数据存储、分支恢复和凭据跨分支合并；代价是切回需要重新填写。用户已确认该取舍，并确认取消独立状态表。必要的当前状态、条件校验、服务端投影、凭据保护与并发冲突检测仍保留，不能以简化为由省略。

原先“同协议分支参数共享还是分别恢复”的问题已由清空规则取代。首批与后续范围、目标生成门槛、未知扩展保护和并发处理已收口；尚未完成的具体契约统一见 §10.2。本次写入不等于这些契约、业务代码或客户端验收已经完成。

### 11.2 代码与证据检查结论

以下为本次设计审查核对的现有代码事实及对应构建影响，未修改业务代码：

| 检查项 | 现状与证据 | 对构建的影响 |
|---|---|---|
| 协议注册表 | [registry.go](backend/internal/node/registry.go) 覆盖 19 个 manual 协议，`FieldSchema` 可下发 | 扩展当前条件与重置归属；其余 15 个协议维持入口 |
| 递归编辑器 | [ProtocolFieldEditor.vue](frontend/src/components/ProtocolFieldEditor.vue) 支持 `fields/map/list`、未知键与对象 JSON | 复用组件，补齐清空、局部草稿及凭据摘要，不重写编辑平台 |
| 当前状态与输出过滤 | [render_clash.go](backend/internal/assembly/render_clash.go) 的 `clashProxy()` 输出归一化后的协议字段，未按新条件模型投影 | 显式当前状态与服务端投影不能由 UI 隐藏代替 |
| SR／generic 输出 | [links.go](backend/internal/assembly/links/links.go) 以 `reality-opts` 判断 VLESS REALITY，Trojan URI 未写入 WS／gRPC 传输参数 | 按组合与字段验证，执行 §7.4，不能只依赖协议级映射标记 |
| 空值保留凭据 | [node.go](backend/internal/node/node.go) 的 `mergeSensitive()` 对缺失／空值回填旧密文 | 必须区分正常保留与分支重置，覆盖 A → B → A 不复活旧凭据 |
| 并发更新 | `UpdateManual()` 事务外读取／合并后更新，尚无本设计的编辑修订号 | 同行写入仍需修订比较和原子更新，旧请求返回 409 |
| 未知参数与数组 | 当前更新重建 schema 顶层字段，敏感路径处理以对象映射为主 | 首批引入扩展块保护；后续 WireGuard 等继续设计稳定条目凭据，不能宣称已修复 |
| URI 导入 | [uri_import.go](backend/internal/node/uri_import.go) 独立准备参数并批量创建节点 | 接入统一归一化／当前状态保存，保持逐行回执 |
| Xray 来源字段 | [detect.go](backend/internal/xray/detect.go) 仍产生 `ws-path`、`ws-host` 等字段 | 共享输出前保留来源适配；不能将现有生产路径误判为旧数据兼容 |
| 历史版本 | [clash_plan.go](backend/internal/assembly/clash_plan.go) 的 `ManualProxies` 冻结 manual 配置 | 编辑与清空不改写已生成快照，重新装配才形成新产物 |
| 迁移与备份 | 现有版本化迁移与 SQLite 一致性备份可承载 `nodes` 新字段 | 验证初始化、删除和恢复；[export.go](backend/internal/config/export.go) 的密钥变更保护需覆盖新的密文位置，配置导出仍不等于节点备份 |

第八章记录的 41 个 Mihomo 配置检查与 9 个 CVR URI 样例仍为先前研究摘要，本轮未重跑，也尚未全部转成仓库夹具。Shadowrocket 仍只有版本／公告证据，无本轮真机结果。[3x-ui／Xray 对照研究](docs/Reference/Node-Editor-3xui-Xray-Research.md) 提供参考，不替代本项目各输出入口的验收。

### 11.3 设计前置、Build 任务与后续范围

| 阶段 | 必须完成的内容 | 与首批 Build 的关系 |
|---|---|---|
| 首批设计前置 | §10.2 的四协议组合与字段矩阵、重置归属、当前状态及 API 结构、凭据／扩展操作样例、来源适配和诊断契约；明确验收预期与证据等级 | 这些内容补齐后才能称为可执行 Build 的完整设计依据；不要求穷举所有参数笛卡尔积，但须覆盖普通候选和已知风险组合 |
| Build 实现与验证 | 具体 DDL／迁移编号、文件拆分、代码／伪代码、共享投影与校验实现、前端组件实现、测试夹具和验证命令 | 由后续获授权的 Build 分步承接；不要求先实现这些内容才能编写 Build |
| 后续专项 | SS 2022 完整密钥／URI／客户端兼容、其余 15 协议完整条件表单、独立 Xray outbound 的 profile 与导出 | 不作为首批整体阻塞项，分别设计与验收；TUIC／WireGuard 在首批仅作模型扩展验例，不能扩成完整 UI 交付 |

首批工程验收须覆盖 §10.3 的确定行为；编写 Build 前应明确可复现的输入和预期，不必先完成新适配器的连接实验。实际导入／连接证据在执行相应验收后记录，未执行时保持待验证，不能由单元测试或配置解析成功替代。当前状态为关键行为已确认、具体契约待补齐，不标记为全部设计定稿。

---

## 十二、变更记录

以下保留历史修订事实；v1.0／v1.1 关于分支保留和独立状态表的结论已由 v1.2 覆盖，不作为当前构建依据。

| 版本 | 日期 | 说明 |
|---|---|---|
| v1.0 | 2026-09-02 | 新建节点编辑器增量设计：记录协议驱动条件表单、常用优先与高级折叠、独立开关区、可搜索推荐选项与手动输入、活动配置及非激活状态、凭据与目标检查；确定首批 VLESS／VMess／Trojan／SS 和后续全部 19 协议目标；收录 41 个内核检查与 9 个 URI 样例结果，明确 Shadowrocket 证据限制、待细化契约和后续验收。仅文档，未构建。 |
| v1.1 | 2026-09-02 | 补充构建前决策确认与前期检查：通过 `ask_user_question` 确认独立 `node_edit_states` 表、跨协议整体清空、同协议保留非激活分支、Mihomo/CVR 目标基线、新增节点检查接口、SS 2022 后置；明确当前无业务数据、不做旧数据兼容；新增第十二章代码前期检查与单独待办清单。仅文档，未构建。 |
| v1.2 | 2026-09-02 | 按用户确认取消分支保留与恢复，明确协议／传输／安全／插件／功能关闭的清空范围、保存前草稿边界及凭据不复活；取消独立 `node_edit_states` 表，采用 `nodes` 最小当前状态与编辑修订号；确认其余协议过渡、目标生成门槛、未知扩展整体加密和 Xray 来源适配；同步界面、保存、检查、验收与影响评估，分离设计前置／Build 实现／后续专项并修正章节顺序。四协议矩阵及具体契约仍待补齐；仅更新 Design4，未构建，未重跑客户端实验。 |
