# Design1.md — VPN 订阅管理系统 设计记录（当前，完整重构基线）

> **文档定位：** 本文档是本系统的**当前设计记录**（设计大方向、架构构想、决策记录，非强制，供参考）。它是为**从 0 完整重构**而编制的设计基线：保留既有系统的全部设计功能，但不绑定旧的命名、表结构或 API 路径，以"功能需求"为中心组织。
> 编码约束遵循 [AGENTS.md](./AGENTS.md)（**唯一强要求**）；详细构建方案见 [BuildN.md](./BuildN.md)；问题追踪见 [IssueN.md](./IssueN.md)；历史构建记录见 [AchievedDocuments/](./AchievedDocuments/)（已存档，仅核查）。
> 设计状态：截至 2026-08-04，已完成对现有系统全部文档与代码的深度审查，本文档作为**从 0 完整重构的设计基线**。本地用户管理体系（源自 Future_Plan_01）与新增 11 项设计构想（双模式运行、模拟 OIDC、审批中心、验证码、SMTP、站点自定义、安装包分发、CDN 缓存分级、AntD UI 重构、限流扩展等，均经用户确认）已整合为正式设计章节。

---

## 一、产品定义与设计目标

### 1.1 产品定位

**自托管的 VPN 订阅管理系统**，面向 ≤10 人的小团队。管理员通过 Web UI 配置认证（OIDC 或本地账号）、管理用户、上传订阅配置文件（Clash / V2Ray / Shadowrocket 格式）和分流规则；普通用户登录后可通过「一键导入」将订阅添加到 VPN 客户端。

### 1.2 核心价值主张

- **一次获取，长期自动更新**：用户获得一次订阅链接后，即使不登录也能持续获得最新配置（通过长期下载 Token）；管理员更新订阅版本后，所有用户自动生效，无需重新分发。
- **零运维负担**：`docker compose up -d` 即可启动，所有运行时配置通过 Web UI 完成，无需 .env 文件、无需外部数据库、无需额外服务。

### 1.3 运行模式

| 模式 | 定位 | 说明 |
|------|------|------|
| **单实例自托管**（默认） | 小团队内部使用 | 单容器 + SQLite 嵌入式存储，部署在自有服务器；外部反代做 TLS 终止，或局域网直连简化模式 |
| **Dev 模式（调试/演示）** | 本地开发与功能演示 | 可启用**模拟 OIDC**（无需真实提供商）；使用**独立数据库** |
| **Production 模式（正式）** | 生产环境 | 仅允许真实 OIDC / 本地认证；独立数据库 |

**模式规则**：环境变量 `APP_MODE=dev|prod`（docker compose 传入）在**启动时**决定运行模式（修改后重启容器生效），**不在运行时切换**；两种模式使用不同数据库文件，数据互不干扰。

### 1.4 核心能力（能力清单，重构必须保留）

- **认证接入**：支持 Keycloak / Auth0 / 通用标准 OIDC 与本地用户管理（邮箱+密码）两种认证体系，可混合并存；PKCE + state 持久化 + 会话凭据；「记住我」控制会话时长；**模拟 OIDC**（仅 Dev 模式）支持无提供商环境下本地调试
- **订阅内容管理**：为每个客户端平台维护「默认 / 高级」两个级别的订阅，支持文件上传与文本编辑两种方式创建版本，最多保留 5 个历史版本，可预览、切换、回滚
- **订阅分级分发**：普通用户获得默认订阅，高级用户获得高级订阅；管理员可预览两种级别；未配置级别不降级、不升级
- **自定义订阅覆盖**：管理员可为「特定用户 + 特定平台」上传独立订阅，覆盖自动分配；支持版本管理与 Token 刷新
- **独立分享订阅**：不绑定任何用户的公开分享链接，支持版本管理、Token 刷新与吊销
- **分流规则管理**：通用规则模型（当前支持 Shadowrocket .conf），独立版本管理与下载 Token，支持一键导入
- **账号审批与启用**：自注册与 OIDC 各设独立审批开关（均默认关闭，关闭时新用户直接激活，开启后走白名单/审批流）；账号可禁用/启用
- **验证码防护**：支持 reCAPTCHA 与 Cloudflare Turnstile，管理员可勾选在注册/登录/找回密码页面启用（可全部不启用）
- **客户端安装包分发**：平台支持上传二进制/压缩包供公开下载（可被 CDN 缓存），与外部链接下载并存
- **SMTP 消息服务**：密码重置、审批结果通知、欢迎邮件（管理员勾选启用范围，默认全不启用）
- **站点自定义**：站点名称与 ICON（上传图片）自定义，ICON 走可缓存路径
- **长期下载 Token 体系**：用户 Token、分享 Token、规则 Token 三类独立体系，支持手动轮替、吊销、级联清理
- **访问日志**：记录全部下载请求（类型、用户、平台、IP、状态、失败原因），按日期查询，默认保留 90 天自动清理
- **分级运行日志**：debug / info / warn / error 四级，管理员可在面板配置中**运行时切换**（立即生效并持久化）
- **实时日志流**：SSE 实时推送运行日志（HTTP 请求/响应、调试输出，与 docker compose logs 同源），便于调试与对接 WAF
- **数据清理**：清空访问日志；一键清空所有数据（完全重置回 Setup，需确认词 + 二次确认）
- **密码救援**：忘记密码未配置 SMTP 时管理员可在用户管理直接重置用户密码（随机 8 位易读密码）；管理员自身密码救援通过 `RESET_ADMIN_PASSWORD` 环境变量进入应急恢复模式（仅此途径可重置密码）；数据库损坏/关键配置损坏自动触发的应急页仅提供重新初始化（见 3.8）
- **速率限制**：登录、注册、找回密码、下载 API 独立限流（默认登录 10/分钟、注册与找回 5/分钟、下载 20/分钟），超限返回 429 + Retry-After
- **全栈 Web UI**：Ant Design Vue + Tailwind 重构；首页订阅卡片、管理面板（订阅/分享/平台/用户/审批中心/规则/面板配置/日志）、个人中心、暗色模式、多端（手机/平板/台式机）友好

### 1.5 设计原则（重构时的决策准则）

| 原则 | 说明 |
|------|------|
| 简单轻量 | 不做不必要的抽象；后端单二进制 + 嵌入式存储，前端按需打包，项目自身资源零 CDN 运行时依赖（外部服务如验证码脚本除外） |
| 零配置启动 | 首次启动即进入 Setup 引导；除端口等运维参数外一切配置走 Web UI |
| 双模式运行 | Dev / Production 模式由启动参数（`APP_MODE`）决定，使用独立数据库；Dev 模式提供模拟 OIDC |
| 路径分级 | `/public` 静态资源（站点图片、安装包）可被 CDN 缓存；订阅/规则下载始终禁止缓存 |
| 性能优先、简约优先 | 多端（手机/平板/台式机）友好的响应式设计；组件库按需引入 |
| 安全底线不妥协 | 路径穿越防护、密钥加密存储、Token 脱敏日志、实时权限校验、订阅下载防缓存 |
| 数据一致性 | 版本创建/切换的并发安全（事务 + 行锁）、级联删除保证无孤儿数据 |
| 不过度防御 | 合理假设输入有效，不堆叠冗余的 nil/空值检查链与 fallback；网络安全边界由部署者控制 |

---

## 二、用户角色与权限模型

### 2.1 角色体系

系统只有两种角色，权限来源于持久化用户表（非会话 claims），**每次请求实时查库**，不做任何缓存：

| 角色 | 来源 | 能力边界 |
|------|------|---------|
| **管理员（admin）** | 首个完成认证流程的用户自动成为管理员（受「首管理员初始化标记」保护，见 2.5；Setup 只写配置、不创建任何用户） | 全部管理面板：用户/订阅/分享/平台/规则/审批中心/面板配置/日志；首页可预览两种订阅级别 |
| **普通用户（user）** | 管理员诞生后新登录的用户默认为普通用户；管理员手动创建的本地用户；审批通过的自注册用户；OIDC 白名单命中或审批通过的用户 | 仅首页（订阅卡片）+ 规则浏览页 + 个人中心；无管理入口 |

### 2.2 订阅分级体系（基于用户级别）

订阅按两个级别组织：**默认（default）** 与 **高级（advanced）**。用户级别由 `is_advanced` 布尔字段决定，由管理员在用户管理中设置。

**核心规则**：

- 新用户首次登录 → 普通用户 → 在所有平台只能获得**默认订阅**
- 管理员将用户设为高级 → 高级用户 → 在所有平台只能获得**高级订阅**
- 用户无权自行选择或切换订阅级别；高级用户不能降级到默认，普通用户不能获取高级
- 管理员自身恒为高级（后端强制，UI 不可修改）
- 用户级别变更（升/降级）→ 后端自动清除该用户全部旧下载 Token（防止旧级别 Token 绕过分级限制），用户下次访问首页按新级别重新生成

**未配置降级规则**（对称）：
- 某平台未配置 advanced 订阅：高级用户在该平台卡片显示「高级订阅未配置，请联系管理员」，不生成 Token；**不降级到 default**
- 某平台未配置 default 订阅：普通用户在该平台卡片显示「默认订阅未配置，请联系管理员」，不生成 Token；**不升级到 advanced**
- 管理员首页对应按钮显示「未配置」

### 2.3 自定义订阅（管理员强制覆盖）

管理员可为特定用户在特定平台上上传一份**完全独立**的订阅配置文件，覆盖该用户在该平台的默认/高级自动分配。用于例外场景（如某个高级用户需要与其他高级用户不同的节点配置）。

**覆盖规则**：

- 上传时必须指定适用平台；每用户每平台最多一份，同一平台再次上传即覆盖（创建新版本）
- 自定义订阅同样支持版本管理（上传/切换/预览/删除，最多 5 个版本）
- 自定义订阅与普通/高级订阅使用同一套用户下载 Token 机制，且支持手动刷新 Token
- 删除自定义订阅 → 用户自动恢复该平台默认/高级自动分配，级联清除关联 Token
- **用户首页显示**：有自定义订阅的平台卡片显示「已被分配自定义订阅」提示 + 自定义订阅操作按钮（替换默认/高级按钮）
- **管理员首页显示**：已有自定义订阅的平台同时显示 默认 + 高级 + 自定义 三组按钮（前两组用于预览）

### 2.4 独立分享订阅

不关联任何用户的独立分享订阅链接。任何人持有该链接即可下载订阅配置，无需登录。

**设计规则**：

- 与自定义订阅是**两个独立功能**：自定义订阅绑定用户+平台并替换自动分配；分享订阅不绑定用户、不区分平台、不区分 default/advanced——管理员上传什么内容，下载者就获得什么
- 每个独立分享订阅自动生成独立下载 Token；创建/刷新后链接即刻可用，吊销后链接立即失效但文件与版本历史保留
- 支持完整版本管理（同订阅逻辑）
- 使用场景：给外部人员临时访问、创建团队公共链接方便分发

### 2.5 管理员保护规则（四重防护）

防止误操作锁死系统：

1. 禁止管理员删除自己
2. 禁止删除最后一个管理员（系统内管理员数量必须 ≥ 1）
3. 禁止管理员修改自己的角色（role）
4. 禁止禁用最后一个活跃（未禁用）管理员（禁用场景延伸保护，见 4.6）

**首管理员初始化标记（改进后）**：Setup 完成时只标记系统已配置，**不创建任何用户账户**；**首个完成认证流程的用户**（OIDC 首次登录 / 首个自注册用户）触发管理员诞生，并写入「已初始化」标记。**当用户表为空（0 行）时忽略该标记**——数据库重置、迁移或全部用户被删除后，系统可自动重新开始（适用于所有认证模式）；已有用户时该标记防止意外产生第二个管理员。管理员自身 `is_advanced` 始终为 true，不可通过 UI 修改。

### 2.6 账号状态、来源与启用规则

**账号状态**：`待审批` / `已激活` / `已禁用`。仅「已激活」账号可登录；「已禁用」账号的会话与下载 Token 一并失效。

**来源标注**：OIDC / 本地创建 / 自注册，用于 UI 展示与区分。

**启用规则**（见第四章 4.6；两个独立审批开关，均默认关闭）：

- 自注册用户：受「自注册审批开关」控制——关闭（默认）：注册后**直接激活**；开启：注册后进入待审批，由管理员在审批中心激活或拒绝（**自注册开关默认关闭**；**用户表为空时首个注册者免审批直接成为管理员，不受审批开关影响**）
- OIDC 用户：受 OIDC 审批功能开关控制——关闭（默认）：新 OIDC 用户直接激活，白名单不生效；开启：Role/Group 白名单（值列表 + 可配置声明路径，见 3.4.7）命中 → 自动激活，未命中 → 进入待审批；**白名单未配置或为空时跳过校验直接激活**
- 管理员创建的本地用户：默认直接激活
- 用户表为空时，首个登录/注册者始终直接激活并成为管理员（不受开关影响，防死锁）

### 2.7 用户字段语义

| 字段 | 语义 |
|------|------|
| `user_id` | 主键。OIDC 用户取提供商返回的 subject 标识（按此去重）；本地用户由系统生成 |
| `username` / `email` | 由认证结果或注册/创建信息提取；邮箱**全局唯一**，用于展示与本地登录 |
| `role` | `admin` / `user` |
| `is_advanced` | 订阅分级核心字段：false=普通（默认订阅）、true=高级（高级订阅）；管理员强制 true |
| `groups` | JSON 数组，预留扩展用，当前版本仅存储不参与逻辑 |
| `password_hash` | 本地密码哈希（bcrypt）；为空 = 不可本地登录（OIDC-only 用户） |
| `user_source` | 来源标注：OIDC / 本地创建 / 自注册，用于 UI 展示 |
| `status` | 账号状态：待审批 / 已激活 / 已禁用；仅已激活可登录（审批拒绝即删除账号，无独立「已拒绝」状态） |

---

## 三、功能全景（重构目标功能清单）

### 3.1 首次部署与 Setup 引导

管理员执行 `docker compose up -d` 启动服务后（环境变量 `APP_MODE=dev|prod` 启动时决定运行模式，默认 prod，见 1.3）：

1. 访问网站任意路径 → 后端检测到系统未配置 → 前端路由守卫自动跳转到 Setup 首次配置页（页面展示当前模式：Dev / Production）
2. **选择部署入口（面向 0 基础用户的引导结构）**：Setup 仅写入系统配置，**不创建任何用户账户**；管理员由首个完成认证的用户产生（见 2.5）
   - **快速开始（推荐）**：本地账号模式——完成基础配置即可使用，无需任何外部依赖
   - **高级配置**：接入单点登录——Keycloak / Auth0 / 通用标准 OIDC / 模拟 OIDC（仅 Dev 模式可见可选）
3. （高级配置）填写提供商参数（Base URL、Realm/域名、Client ID、Client Secret）与回调地址、前端地址（模拟模式自动填充）
4. 点击「测试连接」验证 OIDC 配置正确性（不落库，仅验证；模拟模式始终通过）
5. 点击「完成配置」→ 系统自动生成签名密钥（同时作为加密密钥派生源）→ 写入配置存储 → 标记系统已配置
6. 跳转登录页 → 按所选模式登录 → 首个完成认证流程的用户自动成为管理员

**设计要点**：

- 切换提供商类型时**保留已填字段**（各提供商参数独立存储，互不覆盖）
- 回调地址与前端地址在 Setup 页预填当前浏览器域名，减少手动输入
- Setup 完成后可随时在管理面板修改 OIDC 参数（含切换提供商类型、重新测试连接），无需重启服务
- **认证模式关系**：**本地登录为基底（默认开启）**，OIDC 为可选叠加——Setup 后仍可在面板配置接入 OIDC；「认证模式只读」仅指 OIDC 提供商类型在三种提供商之间切换，本地与 OIDC 不互斥（见 3.4.7）
- **运行模式（Dev/Prod）由启动环境变量决定**，修改需重启容器生效（不在运行时切换，见 1.3）

### 3.2 认证与登录

**OIDC 登录**（标准 Authorization Code + PKCE）：

1. 用户点击登录 → 后端生成随机 state 与 code_verifier → 持久化（带 10 分钟 TTL）→ 下发 state 到浏览器 Cookie（HttpOnly）→ 302 重定向到提供商授权页
2. 提供商认证成功后回调 → 后端三重校验（Cookie state == 回调参数 state == 存储记录）→ 用 code_verifier 换取 token → 解析身份信息 → 查/建用户 → 签发会话凭据
3. 302 重定向到前端中转页（携带会话凭据）→ 前端提取并存到本地存储 → 清空 URL → 进入首页

**模拟 OIDC 登录**（仅 Dev 模式，模拟模式被选为认证方式时）：

- 登录页展示**模拟登录表单**：输入任意用户名/邮箱即可登录，无需真实提供商
- 可**附加模拟 role/group 属性**（勾选role/group后输入对应值），用于测试 Role/Group 白名单与审批逻辑（见 4.6）
- **email_verified 可勾选**（默认勾选=已验证），用于测试自动合并绑定分支（已验证/未验证）
- 模拟用户同样遵循账号状态与启用规则

**本地登录**（邮箱 + 密码；「允许本地登录」**默认开启**——本地登录为基底，OIDC 为可选叠加）：

- 邮箱为唯一标识；密码为 bcrypt 哈希校验
- 失败提示统一措辞（不区分「邮箱不存在」与「密码错误」，防枚举）；待审批/已禁用账号返回通用提示「账号未激活或已被禁用」
- 受速率限制；仅「已激活」状态账号可登录

**自注册**（仅当「允许自注册」开启，依赖本地登录开启；**默认关闭**，管理员显式开启后才出现注册入口）：

- 登录页提供注册入口 → 填写用户名 + 邮箱 + 密码（+ 确认密码）→ 提交校验（邮箱唯一）
- 注册后按「自注册审批开关」处理——关闭（默认）：直接激活可登录；开启：进入**待审批**状态，需管理员在**审批中心**激活后方可登录
- 用户表为空时，首个注册用户自动成为管理员（免审批）

**验证码防护**（reCAPTCHA / Cloudflare Turnstile）：

- 管理员在面板配置中**手动勾选**启用页面：注册 / 登录 / 找回密码（可全部不启用，默认不启用）
- 配置验证码提供商与站点/服务端密钥；**未配置密钥时跳过校验**（便于本地开发）
- 前端渲染验证码组件，后端调用提供商验证接口校验 token

**忘记密码**（本地账号）：

- 输入邮箱 → 已配置 SMTP 时生成一次性重置令牌（1 小时有效）并发送邮件；未配置时提示联系管理员（管理员可在用户管理直接重置该用户密码，见 3.4.4）
- 无论邮箱是否存在，均提示「若该邮箱已注册，重置链接已发送」（防枚举）
- 用户点击邮件链接 → 校验令牌 → 设置新密码 → 完成

**会话凭据**：

- 登录时可选「记住我」：勾选 → 会话 7 天；不勾选 → 24 小时
- 仅存用户标识 + 凭据版本号 + 标准声明，角色/级别等权限信息**不放入凭据**，每次请求实时查库（凭据版本号见 5.4）

**登录页布局**（区块化）：

- OIDC 登录区块：仅当 OIDC 已配置时显示（含模拟 OIDC 时显示模拟登录表单）
- 本地登录区块：仅当「允许本地登录」开启时显示（含记住我、忘记密码入口）
- 注册入口：仅当「允许自注册」开启时显示
- 暗色模式切换入口常驻；按管理员勾选在对应区块展示验证码组件

**设计要点**：

- state 记录用后立即删除（防重放）；过期记录定时清理
- OIDC 与本地登录共用同一套会话签发/验证机制（凭据格式统一）
- 同浏览器已登录用户访问登录页自动跳过

### 3.3 用户首页（Home）

**顶部栏**：左侧站点名称（面板配置的站点名称，未设置时显示默认标题）+ 订阅更新时间戳（各订阅当前版本的最新更新时间）；右侧：管理面板入口（仅管理员）、用户名 + 角色标签（普通/高级/管理员）、退出、暗色模式切换。

**主体 — 平台卡片网格**（响应式：大屏 3 列 / 中屏 2 列 / 小屏 1 列）：

- 平台名称、平台描述
- 订阅区段（显示规则见 2.2/2.3，普通用户一份、管理员多份预览）
- 三个操作按钮：**一键导入**（按客户端 scheme 拼接 URL 并跳转唤起客户端）、**复制链接**（弹窗展示完整订阅 URL，点击即复制）、**刷新链接**（轮替下载 Token，旧链接立即失效）
- **下载客户端按钮（双模式）**：平台上传了本地安装包 → 显示「本地下载」（公开路径，可 CDN 缓存）；配置了外部下载链接 → 显示「链接下载」；两者都有则显示两个按钮（见 4.7）

**其他区域**：公告栏卡片（管理员在面板配置中维护，有内容才显示）、分流规则入口卡片（跳转规则浏览页）。

### 3.4 管理面板（侧边栏导航）

管理面板为独立布局：左侧固定宽度侧边栏（当前路由高亮），右侧内容区。移动端侧边栏默认收起，通过汉堡按钮展开。包含 8 个模块：

#### 3.4.1 订阅管理

- 列表页按「平台 → 类型（默认/高级）」分组展示；创建订阅需指定平台与类型（每平台每类型唯一）
- 版本管理页：版本列表（版本号、创建/更新时间、当前激活标识）、**两种方式创建新版本**（文件上传 / 在线文本编辑）、切换当前版本、预览内容、删除旧版本
- 版本管理统一规则（见 4.1）：最多 5 个版本、不可删最后一个、上传/编辑后自动切换为当前

#### 3.4.2 分享订阅管理（独立模块）

- 独立列表页：名称、创建时间、当前版本号、Token 状态（有效/已吊销）
- 创建对话框：填写名称 → 上传第一个版本（文件或文本）→ 自动生成分享 Token
- 操作：版本管理（独立子页面）、复制分享链接（仅 Token 有效时）、刷新 Token（旧链接立即失效）、吊销 Token（链接不可用但文件与版本保留）、删除（级联删除文件 + Token）

#### 3.4.3 平台管理

- 平台 CRUD；系统初始化自动创建 3 个默认平台（Clash Verge / v2rayNG / Shadowrocket），内置各自的客户端导入 scheme
- 每个平台可配置：名称、描述、多个客户端 scheme（一键导入取第一个）、**客户端安装包**（两种来源并存）：
  - **本地上传**：上传二进制/压缩包文件（≤300MB），存至可缓存路径，公开下载（见 4.7）
  - **外部链接**：填写下载地址（原有 download_url 能力）
- 删除平台为危险操作：级联删除该平台全部订阅（含版本文件）、用户 Token、自定义订阅（含版本文件）、本地上传的安装包文件，需二次确认

#### 3.4.4 用户管理

- 用户列表：用户名、邮箱、角色标签、级别标签、**来源标注**（OIDC / 本地创建 / 自注册）、账号状态（待审批/已激活/已禁用）、自定义订阅平台标记、操作按钮
- 用户来源：OIDC 登录自动创建；管理员**手动创建本地用户**（用户名 + 邮箱 + 密码，邮箱唯一冲突提示）；自注册用户（激活受自注册审批开关控制）
- 操作：**编辑**（切换 is_advanced，管理员本人行禁用；变更级别时 UI 内联警告「将立即删除该用户所有下载链接」）、**上传自定义订阅**（选择适用平台 → 上传文件）、**删除自定义订阅**（恢复自动分配）、**吊销所有下载 Token**、**设置/重置密码**（已配置 SMTP 时可选触发重置邮件；**直接重置**：二次确认后系统随机生成 8 位密码——大小写字母 + 数字，去除易混淆字符 i I o O 0 l L，无特殊符号——对话框内展示供管理员复制，即时生效，**重置后该用户全部现有会话立即失效**；是忘记密码流程未配置 SMTP 时「联系管理员」的兜底手段）、**禁用/启用账号**、**删除用户**（管理员保护规则见 2.5）
- 待审批用户的激活/拒绝集中在**审批中心**处理（见 3.4.5）
- groups 字段仅存储不编辑；未设置则不展示

#### 3.4.5 审批中心（独立模块）

- 独立列表页：待审批用户（自注册用户 + 未命中 OIDC Role/Group 白名单的 OIDC 用户），展示来源、用户名、邮箱、申请时间、OIDC 附加角色/组信息（如有）
- 操作：**通过**（激活账号）、**拒绝**（删除账号，邮箱释放可重新注册）、批量通过；被拒用户无记录保留（与「拒绝即删除」一致），通过的用户在用户管理中可见
- 审批结果（通过/拒绝）均按配置发送通知邮件；**欢迎邮件在审批通过后发送**（自注册成功时仅创建待审批账号，不发欢迎邮件）；拒绝前可发送拒绝通知（见 3.4.7 SMTP 范围）

#### 3.4.6 规则管理

- 规则列表：名称、客户端类型（当前仅 Shadowrocket）、当前版本号、Token 状态
- 创建规则：名称 + 客户端类型 + 客户端 scheme（用于一键导入）→ 上传首个版本 → 自动生成规则 Token
- 操作：版本管理（同订阅模式）、复制下载链接、轮替 Token（旧链接立即失效）、删除（级联删除文件 + Token）

#### 3.4.7 面板配置

- **OIDC 配置**：查看/修改提供商参数（Client Secret 回显脱敏）、测试连接、切换提供商类型（保留已填字段）
- **OIDC 启用规则**：OIDC 审批功能开关（默认关闭；关闭时新 OIDC 用户直接激活）；开启后维护 **Role/Group 白名单**（值列表 + **可配置声明路径**，如 `realm_access.roles` / `groups` / `roles`，默认常见值下拉可选，兼容各类提供商），命中自动激活，未命中进入审批中心；**白名单未配置或为空时跳过校验直接激活**（见 2.6）
- **本地认证配置**：允许本地登录开关（**默认开启**——本地登录为基底，OIDC 为可选叠加）、允许自注册开关（依赖本地登录开启；**自注册默认关闭**）、自注册审批开关（独立于 OIDC 审批开关，**默认关闭：自注册用户直接激活**；开启后注册者进入审批队列；用户表为空的首个注册者始终免审批）
- **验证码配置**：选择提供商（reCAPTCHA / Turnstile / 关闭）+ 站点密钥 + 服务端密钥；勾选启用页面（注册/登录/找回密码，可全不选）
- **SMTP 配置**：服务器/端口/账号/密码（加密存储）/发件人/是否启用 TLS；**勾选启用范围**（密码重置邮件 / 审批结果通知 / 欢迎邮件，默认全不启用）
- **站点信息**：站点名称（浏览器标题/登录页/首页展示）、站点 ICON（上传图片文件，走可缓存路径）
- **运行模式信息**：展示当前模式（Dev / Production，由启动环境变量决定，修改需重启容器）
- **速率限制配置**：登录（默认 10/min）、注册（默认 5/min）、找回密码（默认 5/min）、下载（默认 20/min）；均按 **IP 维度**计数，NAT/反代共享 IP 场景由部署者调高阈值
- **日志级别**：debug / info / warn / error **运行时切换**（环境变量 `LOG_LEVEL` 为初始值；面板修改立即生效并持久化，运行日志与实时日志流同步生效）
- **系统公告栏**：编辑公告内容（首页展示）
- **调试模式开关**：开启后 5xx 错误响应返回详细内部信息（生产默认关闭，状态持久化，重启保留）
- **危险操作区**：一键清空所有数据（确认词如 `RESET` + 二次确认 → 完全重置回 Setup，见 4.8）

#### 3.4.8 日志查看

模块内两个视图（页签切换）：

**访问日志视图**：

- 按日期筛选访问日志；展示下载类型、用户、平台、IP、成功/失败状态、失败原因
- 覆盖全部下载途径：用户订阅下载、分享订阅下载、自定义订阅下载、规则下载（后两者用户可为空）
- 日志自动保留 90 天，后端定时清理
- **清空日志**按钮（ConfirmDialog 二次确认）：删除全部访问日志记录

**实时日志流视图**（SSE 单向推送，见 4.8）：

- 实时展示运行日志（HTTP 请求/响应、调试输出，与 docker compose logs 同源）
- 支持按级别过滤、暂停/继续、清屏（前端操作）；仅管理员可访问
- 便于本地调试与对接 WAF 排查

**数据清理归属说明**：

- 清空访问日志按钮在本模块；**一键清空所有数据**的入口位于面板配置「危险操作区」（见 3.4.7）：确认词（如 `RESET`）+ 二次确认后执行，清空全部业务数据 + 系统配置（含签名密钥与 configured 标记）+ 数据文件（版本文件、/public 资源）→ 系统回到**未配置状态**（无需重启，前端路由守卫检测 configured=false 自动进入 Setup 首次配置，见 4.8）

### 3.5 用户规则浏览页

面向**所有登录用户**（含普通用户）的规则浏览页面：

- 规则卡片列表：名称、客户端类型、当前版本信息
- 操作：下载当前版本、一键导入（按规则 scheme 唤起客户端）
- 仅浏览与下载，不可管理版本（管理入口仅在管理面板）

### 3.6 用户个人中心（/profile）

面向所有登录用户（预留扩展能力，当前实现基础设置）：

| 功能 | 说明 |
|------|------|
| 查看个人信息 | 用户名、邮箱、角色、订阅级别 |
| 修改用户名 | 直接修改，即时生效 |
| 修改邮箱 | 新邮箱被占用即拒绝（防账号接管，见 4.6）；修改成功后凭据版本号递增，**所有设备会话立即失效**并引导重新登录 |
| 设置/修改本地密码 | OIDC 用户可在此绑定本地密码；本地用户可修改密码 |
| **绑定 OIDC** | 自注册/本地用户跳转 OIDC 授权完成绑定（见 4.6），绑定后可用 OIDC 登录同一账号 |
| 记住我设备管理 | 已确认远期实现，本期不做（见第九章 9.1） |

**设置密码安全性**：

- 已设密码的用户修改前需验证当前密码
- OIDC 用户首次设置密码无需旧密码，但须在已登录会话中操作（以登录状态确认身份）

### 3.7 全局体验

| 体验项 | 说明 |
|--------|------|
| 暗色模式 | 手动切换，本地持久化偏好；Ant Design Vue 暗色主题 + Tailwind `dark:` 前缀联动，全页面自动跟随 |
| 多端适配 | AntD 响应式组件 + 栅格布局，手机/平板/台式机自适应；弹窗宽度自适应（移动端 90% 视口或全屏）；长表格移动端卡片化或横向滚动 |
| 通知系统 | Ant Design 通知组件（message/notification），统一封装调用 |
| 加载与空状态 | 统一 loading / 空状态组件，关键列表页三态分支（加载中/空/列表） |
| 404 页面 | 未匹配路由显示友好 404 + 返回首页 |
| 路由导航 | 全局顶部进度条；路由切换即时反馈 |
| 删除确认 | 所有删除操作统一确认对话框（标题 + 提示 + 确认回调），不使用系统原生 confirm |

### 3.8 应急恢复模式（管理员密码救援）

面向管理员忘记/丢失密码或数据库损坏的最后救援手段，系统进入**应急模式**（阻塞式特殊界面，暂停对外提供正常服务）。

**触发条件（能力分级，见「设计要点」）**：

- **手动触发**：启动时检测到环境变量 `RESET_ADMIN_PASSWORD` 已设置（值非空）→ 进入应急模式（管理员密码遗忘/丢失等场景均通过此变量触发）
- **自动触发**（仅以下两类，页面不含重置密码能力）：
  - 数据库无法连接/损坏（含完整性检查不通过）
  - 关键配置损坏（如 configured=true 但签名密钥缺失）
- 其他密码类异常不做自动检测（bcrypt 哈希无法静态判定密码有效性）；空表死锁（全部用户被删且自注册关闭，无登录途径）等场景由部署者设置该环境变量重启进入应急页救援

**应急页能力**：

1. **重置管理员密码**（仅环境变量触发且数据库可读时提供）：选择管理员账号 → 设置新密码（复杂度 ≥8 字符）→ 确认后更新密码；完成后**阻止服务继续运行**，页面提示「已从 docker-compose 移除 `RESET_ADMIN_PASSWORD` 环境变量并重启容器」；未移除则下次启动再次进入应急页（防遗留）；**用户表为空时无任何账号可选，此能力不可用，仅可重新初始化**
2. **重新初始化（应急全清）**：一键清空所有数据回到 Setup（确认词 + 二次确认，同面板配置危险操作区，见 4.8）——数据库损坏/空表死锁/配置错乱时的最后手段；**自动触发的应急页仅保留此按钮**

**设计要点**：

- **能力分级（安全收紧）**：重置管理员密码仅由环境变量门控（手动触发）；自动触发（数据库损坏/关键配置损坏）的应急页仅保留重新初始化按钮，降低无凭据重置任意管理员密码的安全隐患；环境变量已设置但数据库不可读时，页面自动降级为仅重新初始化（重置技术上不可执行）
- 应急页无需登录（正常认证体系可能已不可用）
- **应急端点仅在应急模式下暴露，正常运行时屏蔽（不注册路由）**；应急模式为极低频救援场景且正常服务已暂停，不额外加限流/验证码防护，保持最简
- 运行日志输出警告：应急模式已触发（含触发原因）/ 重置已执行 / 等待移除环境变量重启
- 环境变量每次启动均生效（不做一次性标记，保持简单）；部署文档提醒重置成功后移除该变量

---

## 四、核心机制设计（重构重点）

### 4.1 版本管理（统一适用于订阅/规则/自定义订阅/分享订阅）

**版本模型**：每种内容资源维护一个版本列表，每个版本包含：版本号、文件引用、创建时间、更新时间。当前激活版本通过「当前」指针指向具体版本。

**统一规则**：

- 版本号 = 已有最大编号 + 1（不可用「列表长度 + 1」，防止删除版本后编号冲突）
- 最多保留 5 个版本，超出自动删除最旧的（文件 + 记录）
- 不可删除最后一个版本
- 上传新版本 / 编辑当前版本内容保存 → 自动创建新版本号并切换为当前
- 切换当前版本时更新该版本时间戳（用于首页更新时间戳计算）
- 删除资源 → 级联删除其全部版本文件

**并发安全**（关键设计）：

- 版本号计算与版本列表更新必须在**单个事务 + 行级锁**内完成，防止并发上传导致版本号冲突或文件覆盖
- 「当前」指针切换采用**原子替换**（临时指针 + rename），避免下载过程中读到半切换状态
- 所有资源类型的版本创建路径统一失败清理模式（创建记录 → 写文件 → 更新列表，任一步失败完整回滚清理）

### 4.2 下载 Token 体系（三类独立）

| Token 类型 | 绑定关系 | 生命周期 | 主要操作 |
|-----------|---------|---------|---------|
| **用户下载 Token** | 用户 + 平台 +（订阅类型 或 自定义订阅标识） | 长期有效，无过期时间 | 首次获取时自动生成并复用；用户点击「刷新链接」轮替；管理员吊销全部；用户被删除/升降级/自定义订阅被删时级联清除 |
| **分享 Token** | 分享订阅 | 长期有效 | 创建时自动生成；管理员刷新（旧失效）或吊销（链接不可用但内容保留） |
| **规则 Token** | 规则 | 长期有效 | 创建时自动生成；管理员轮替（旧失效） |

**用户下载 Token 复用规则**：

- 同一用户 + 平台 + 订阅类型的 Token 复用（不重复创建）
- 自定义订阅场景下 Token 以「用户 + 平台 + 自定义订阅标识」为复用键，订阅类型字段不参与判定
- 普通/高级订阅与自定义订阅使用同一张 Token 存储，通过标识字段区分指向哪份内容

**Token 生命周期联动**（保证分级不被绕过）：

- 用户级别变更（升级/降级）→ 清除该用户全部 Token，下次访问首页按新级别重新生成
- 删除用户自定义订阅 → 清除指向该订阅的 Token，下次访问该平台重新生成默认/高级 Token
- 删除平台订阅 → 清除该平台+类型的非自定义 Token
- 删除用户 → 清除其全部 Token 与自定义订阅
- 账号禁用 → 其全部 Token 失效；重新启用后**不恢复原 Token**，用户下次访问首页重新生成（与升降级逻辑一致）

### 4.3 订阅内容下载分发

**统一输出行为**：所有下载端点始终以纯文本（`text/plain`）输出订阅配置内容（类似 GitHub RAW），VPN 客户端可直接读取。

**兼容例外（Clash Verge）**：当下载平台为 Clash Verge 时额外返回专用响应头：
- `Content-Disposition`（客户端依赖识别文件名）
- `profile-update-interval: 300`（建议每 300 分钟自动更新）
- `profile-web-page-url`（用户可跳转到管理页面）

**四种下载途径**：

| 途径 | 认证方式 | 用途 |
|------|---------|------|
| 会话凭据下载 | Bearer 会话凭据 | Web UI 预览订阅内容（浏览器直接查看）；管理员可指定级别预览 |
| 用户 Token 下载 | `?token=` 查询参数 | VPN 客户端实际使用（无需登录）；Token 指向自定义订阅时返回自定义内容 |
| 分享 Token 下载 | `?token=` 查询参数 | 分享订阅公开下载（无需登录） |
| 规则 Token 下载 | `?token=` 查询参数 | 规则公开下载（无需登录） |

**设计要点**：

- 下载时始终返回「当前」指针指向的最新版本
- 所有下载端点返回**禁止缓存**响应头（`no-store` 等），确保客户端每次拉取最新配置
- 所有下载端点受速率限制并记录访问日志（含成功/失败状态与失败原因）
- 会话凭据路径与 Token 路径互斥使用，不会同时出现两种凭据

### 4.4 级联删除规则（数据一致性）

| 删除对象 | 级联范围 |
|---------|---------|
| 用户 | 该用户全部下载 Token + 自定义订阅（含版本文件） |
| 自定义订阅 | 指向该订阅的下载 Token（用户下次访问恢复自动分配） |
| 平台订阅 | 该平台+类型的非自定义下载 Token |
| 规则 | 该规则的下载 Token + 版本文件 |
| 分享订阅 | 分享 Token + 版本文件 |
| 平台 | 该平台全部订阅（含版本文件）+ 下载 Token + 自定义订阅（含版本文件）；需二次确认 |

### 4.5 更新生效链路

管理员上传新版本（自动切换为当前）→ 首页更新时间戳更新 → VPN 客户端按更新间隔自动拉取（或用户手动刷新链接）→ 客户端通过 Token 获取到最新「当前」版本内容 → 全员自动生效，无需重新分发链接。

### 4.6 账号与认证机制（本地用户体系）

**用户身份模型**（登录能力由**是否设有密码**决定，不依赖认证来源列）：

| 用户类型 | 标识 | 密码 | 登录方式 |
|---------|------|------|---------|
| OIDC 用户（未设密码） | 提供商 subject | 无 | 仅 OIDC |
| OIDC 用户（已设密码） | 提供商 subject | bcrypt 哈希 | OIDC 或 邮箱+密码 |
| 本地用户（管理员创建） | 系统生成 | bcrypt 哈希 | 邮箱+密码 |
| 自注册用户 | 系统生成 | bcrypt 哈希 | 邮箱+密码（激活受自注册审批开关控制，见 2.6） |

**邮箱唯一性与身份绑定**：

- 邮箱全局唯一约束；本地登录以邮箱为唯一标识
- **自动合并绑定（仅当 OIDC 返回 `email_verified=true`，且目标邮箱账号未绑定其他 OIDC 身份、状态为待审批/已激活）**：OIDC 登录时，若 subject 对应用户不存在但邮箱已被占用且已验证 → 视为同一用户，自动合并并绑定 OIDC 身份（更新标识为 subject）；**邮箱未经验证、目标账号已绑定其他 OIDC 或账号已禁用时不做自动合并**，提示冲突，防止账号接管（与单绑定规则一致）
- **手动绑定**：自注册/本地用户在个人中心点击「绑定 OIDC」→ 跳转 OIDC 授权 → 回调校验该 OIDC subject 未绑定其他账号 → 绑定到当前账号（绑定基于 subject，不要求邮箱一致）；绑定后可用 OIDC 或邮箱+密码登录同一账号
- **单 OIDC 绑定**：每个账号最多绑定一个 OIDC 身份（subject）；已绑定账号遇到其他 OIDC 登录（subject 不同）→ 不覆盖现有绑定，提示冲突
- 管理员创建本地用户 / 自注册时邮箱被占用 → 明确提示冲突

**账号状态流转与启用**（两个独立审批开关，均默认关闭）：

- 自注册 → 受自注册审批开关控制——关闭（默认）→ **直接激活**；开启 → **待审批** → 管理员审批 → **已激活** / **拒绝（即删除账号，邮箱释放可重新注册，被拒用户可再次注册进入审批队列，滥用由注册限流+验证码兜底）**（用户表为空时首个自注册者免审批直接成为管理员，不受审批开关影响）
- OIDC 首次登录：受 OIDC 审批功能开关控制——关闭（默认）→ **直接激活**（白名单不生效）；开启 → 白名单命中 → **已激活**，未命中（白名单非空）→ **待审批** → 审批中心处理；白名单未配置或为空 → 直接激活
- 用户表为空时，首个登录/注册者始终直接激活并成为管理员（不受开关影响）
- 待审批用户从未获得会话，不存在下载 Token，审批通过/拒绝无需特殊 Token 清理逻辑
- 管理员可禁用/启用任意非管理员账号：禁用后不可登录，会话（凭据版本号机制，见 5.4）与下载 Token 一并失效；重新启用后 Token 不恢复，用户下次访问首页重新生成
- **禁用管理员保护**：允许禁用管理员账号，但禁止禁用最后一个活跃（未禁用）管理员（系统内活跃管理员数必须 ≥ 1，防管理面锁死）；被怀疑滥用的管理员可由其他管理员禁用或删除（删除受「最后一个管理员」保护约束）
- **修改邮箱**成功后递增该用户凭据版本号，所有设备旧会话立即失效（与「修改后需重新登录」一致）
- **密码重置**（管理员直接重置/邮件重置完成/个人中心自助修改）成功后递增该用户凭据版本号，其全部现有会话立即失效（适用于账号疑似被盗用场景）
- 仅「已激活」账号可登录

**验证码机制**（reCAPTCHA / Cloudflare Turnstile）：

- 面板配置：提供商 + 站点密钥 + 服务端密钥 + 启用页面勾选（注册/登录/找回密码，默认全不选）
- 前端在勾选页面渲染验证码组件；后端对受保护接口校验验证码 token（服务端调用提供商验证接口）
- 未配置密钥或未勾选页面 → 跳过校验（本地开发不受阻）

**密码重置令牌机制**（本地账号）：

- 一次性令牌，1 小时有效，用后即删；管理员触发与用户自助触发共用同一机制
- 无论邮箱是否存在，响应一致（防枚举）
- 批量能力：管理员可为所有无密码用户生成重置链接并发送邮件（用于 OIDC 断开后的存续）
- **SMTP 未配置或发送失败不阻断主流程**：业务操作（审批/重置等）仍正常完成，记录错误日志并在 UI 提示「邮件发送失败」

**会话时长**：

- 「记住我」勾选 → 会话 7 天；不勾选 → 24 小时；过期后需重新认证
- OIDC 与本地登录的会话凭据格式统一（同一套签发/验证机制）

**OIDC 断开后的用户存续**（仅管理员手动触发——清空/移除 OIDC 配置，系统不自动判定）：

- OIDC 重定向登录失效；已设本地密码的用户不受影响，仍可邮箱+密码登录
- 未设密码的 OIDC 用户暂时无法登录 → 管理员可为其设置密码（自动发送重置邮件），或使用批量操作
- 纯本地模式不存在此场景（无 OIDC 依赖）

### 4.7 软件安装包与路径分级

**客户端安装包分发**（平台级）：

- 每个平台支持**两种下载来源并存**：本地上传文件（二进制/压缩包，≤300MB）+ 外部下载链接
- 本地上传文件**公开下载**（无需登录），存储于可缓存路径，可被 CDN 缓存；上传新文件即覆盖（文件名带时间戳，URL 变化以突破 CDN 旧缓存；**覆盖时同步删除旧时间戳文件**，避免数据卷无限膨胀）
- 首页「下载客户端」按钮：有本地文件 → 「本地下载」；有外部链接 → 「链接下载」；两者都有 → 显示两个按钮
- 删除平台时级联删除其安装包文件
- **安装包下载为公开静态资源，不记访问日志、不限流**（由 CDN/反代缓存承载，区别于订阅下载）

**路径分级与缓存策略**（避免缓存不一致）：

| 路径 | 内容 | 缓存策略 |
|------|------|---------|
| `/public/*` | 站点图片（logo/favicon）、平台安装包 | **可缓存**（public + max-age） |
| `/assets/*` | 前端构建产物（文件名含哈希） | immutable（长期缓存） |
| 订阅/规则/分享下载端点 | 订阅配置内容 | **禁止缓存**（no-store 等，保持每次拉取最新） |

**站点品牌资源**：站点 ICON/logo 上传后存至 `/public/site/` 固定路径（覆盖即更新），由前端页面引用。

### 4.8 运行日志、实时日志流与数据清理

**分级运行日志**（与访问日志区分）：

- 运行日志输出到 stdout（docker compose logs 可见），级别：debug / info / warn / error
- 环境变量 `LOG_LEVEL` 提供初始级别（默认 info）；**面板配置可运行时切换**（持久化到配置存储，立即全局生效，无需重启）
- 与「调试模式开关」（5xx 错误响应是否返回内部详情）独立：日志级别控制输出详细度，调试开关控制错误响应内容，二者可独立配置

**实时日志流（SSE 单向流）**：

- 运行日志经统一日志管道同时输出到 stdout 与**内存环形缓冲**（如最近 500 条）
- 管理面板通过 SSE 端点订阅：先推送缓冲历史，再实时推送增量；客户端自动重连；连接断开自动清理订阅
- **认证方式：`?token=` 查询参数携带会话凭据**（EventSource 原生 API 不支持自定义 Header；复用现有 token 脱敏机制）；响应 `no-cache`
- **并发订阅上限**（如 5 个连接），超出拒绝新连接并提示（防资源耗尽）
- 仅管理员可订阅（实时日志暴露内部信息）；支持按级别过滤（前端过滤即可，保持简洁）
- 与访问日志（access_logs 持久化业务记录）明确区分：实时流 = 运行日志（HTTP 请求/响应、错误、调试），不落库

**数据清理**：

- **清空日志**：删除全部访问日志记录（二次确认）；实时日志缓冲为内存态，随清空/重启自然重置
- **一键清空所有数据**（确认词 + 二次确认）：清空全部业务数据（用户/订阅/平台/规则/分享/自定义订阅/各类 Token/访问日志/认证状态/密码重置令牌）+ 系统配置（含签名密钥、configured 标记）+ 数据文件（版本文件、/public 资源）→ 系统回到**未配置状态**，**无需重启**（前端路由守卫检测 configured=false 自动进入 Setup 首次配置，重新生成密钥；管理员由首个完成认证的用户重新产生，见 2.5）；实时日志缓冲同步重置
- 清空操作为破坏性操作，执行前必须确认词校验（防误操作）

---

## 五、目标架构设计（重构方向）

### 5.1 技术选型

> 以下选型在现有系统已验证可行（纯静态编译、零 CGO、小体积、低成本维护），重构时作为推荐基线；如无充分理由不建议更换。

| 层 | 技术 | 选型理由 |
|----|------|---------|
| 后端语言 | Go | 单二进制分发、静态编译、并发安全天然契合下载型服务 |
| Web 框架 | Gin | 成熟中间件生态（认证/限流/日志） |
| 数据库 | SQLite（纯 Go 驱动） | 嵌入式零依赖、WAL 模式、单文件备份；按运行模式分离数据库文件 |
| 日志 | 结构化日志库 | 分级输出（debug/info/warn/error）、JSON/Console 双格式、`?token=` 自动脱敏 |
| OIDC | 标准 OIDC 客户端库 + JWT 库 | PKCE 流程 + 会话凭据签发/验证；Dev 模式内置模拟实现 |
| 验证码 | reCAPTCHA / Cloudflare Turnstile（服务端校验） | 防自动化注册/登录/爆破 |
| 加密 | AES-256-GCM | 敏感配置（Client Secret、SMTP 密码）加密落库 |
| 前端框架 | Vue 3（Composition API + `<script setup>`） | 组合式开发效率 |
| 构建工具 | Vite | 快速开发与按需打包 |
| 组件库 | **Ant Design Vue**（按需引入，全局中文 locale，暗色主题） | 多端响应式组件成熟（用户确认 2026-08-04） |
| 样式 | Tailwind CSS（辅助） | 与 AntD 搭配，处理组件库难以覆盖的自定义样式与布局微调 |
| 状态与路由 | Pinia + Vue Router | 前端标准方案 |
| HTTP | Axios | 统一 baseURL、401 自动登出拦截 |

**约束**：所有依赖必须可纯静态编译/打包，不依赖 CGO、不依赖外部数据库进程。

### 5.2 模块划分（逻辑分层）

> 重构时建议保持清晰的职责分层，但命名与文件组织可重新设计（不绑定旧命名）。

```
接入层（HTTP）
  ├── 公开端点：健康检查、系统状态、公告、规则下载（Token）、分享下载（Token）、/public 静态可缓存资源（图片/安装包）、应急恢复页（应急模式下，见 3.8）
  ├── 认证端点：OIDC 登录/回调、本地登录、注册、忘记/重置密码、绑定 OIDC、当前用户（限流；验证码仅用于表单类端点：本地登录/注册/找回密码）
  ├── 用户端点：首页平台数据、更新时间、刷新 Token、个人中心（需会话）
  ├── 订阅下载：会话预览 / Token 下载（限流 + 防缓存）
  └── 管理端点：用户/订阅/分享/平台/规则/审批中心/面板配置/日志（含实时日志流 SSE、清空日志、一键清空数据）（需会话 + 管理员）
        │
业务层（领域服务）
  ├── 认证服务：OIDC 流程（含模拟 OIDC）、PKCE、本地登录（bcrypt）、自注册、验证码校验、OIDC 绑定、密码重置、会话签发/验证
  ├── 用户服务：用户管理、管理员保护、级别变更 Token 清理、账号状态管理
  ├── 审批服务：待审批用户列表、激活/拒绝、通知触发
  ├── 订阅服务：订阅 CRUD、版本管理、用户 Token 生成/复用/轮替
  ├── 自定义订阅服务：覆盖分配、版本管理、Token 联动
  ├── 分享订阅服务：分享 CRUD、版本管理、Token 刷新/吊销
  ├── 规则服务：规则 CRUD、版本管理、Token 轮替
  ├── 平台服务：平台 CRUD、安装包上传/删除
  ├── 系统配置服务：OIDC/白名单/本地认证开关/审批开关/验证码/SMTP/站点信息/限流/日志级别/公告/调试开关
  ├── 邮件服务：SMTP 发送（按勾选范围：密码重置 / 审批通知 / 欢迎邮件）
  ├── 日志服务：访问日志写入与查询、运行日志管道（分级输出 + 环形缓冲）、实时日志流广播（SSE）
  └── 数据清理服务：清空访问日志、一键清空所有数据（完全重置）
        │
数据层（持久化）
  ├── 配置存储（键值）
  ├── 业务存储（用户/平台/订阅/规则/分享/自定义订阅）
  ├── 令牌存储（用户 Token/分享 Token/规则 Token）
  ├── 密码重置令牌存储（一次性令牌，1 小时 TTL）
  ├── 日志存储（访问日志）
  └── 认证状态存储（OIDC state，TTL 过期清理）
```

**职责约束**：接入层只做协议解析与响应；业务层承载规则（分级、覆盖、级联、版本）；数据层封装 SQL 与文件读写。接入层不直接操作存储，业务层不感知 HTTP。

### 5.3 数据实体设计（实体关系）

> 实体名与字段名可重新设计；以下为**功能需求层面**的实体清单与关系语义。

| 实体 | 关键属性 | 关系语义 |
|------|---------|---------|
| 系统配置 | 键值对 | 承载：认证参数（含加密密文）、OIDC 白名单、本地认证开关、验证码参数、SMTP 参数与启用范围、站点信息、运行模式、日志级别、签名密钥、限流参数、公告内容、系统状态标记、调试开关 |
| 用户 | 标识、用户名、邮箱（唯一）、角色、级别、密码哈希、来源、状态、凭据版本号（会话失效机制，见 5.4）、预留分组 | 1:N 用户下载令牌；1:N 自定义订阅；1:N 密码重置令牌 |
| 平台 | 标识、名称、描述、客户端 scheme 列表、安装包文件引用、外部下载链接 | 1:N 平台订阅；初始化预置 3 个默认平台 |
| 订阅 | 标识、名称、平台、级别（默认/高级） | 平台+级别唯一；1:N 版本；关联用户下载令牌 |
| 版本 | 版本号、文件引用、创建/更新时间 | 所有内容实体共享的版本列表；「当前」指针指向激活版本 |
| 自定义订阅 | 标识、用户、平台 | 用户+平台唯一；1:N 版本；替换自动分配 |
| 分享订阅 | 标识、名称 | 1:N 版本；1:N 分享令牌 |
| 规则 | 标识、名称、客户端类型、客户端 scheme 列表 | 1:N 版本；1:N 规则令牌 |
| 用户下载令牌 | 令牌值、用户、平台、类型/自定义标识 | 唯一键：用户+平台+类型 或 用户+平台+自定义标识 |
| 分享令牌 | 令牌值、分享订阅 | 每分享订阅一份，可刷新/吊销 |
| 规则令牌 | 令牌值、规则 | 每规则一份，可轮替 |
| 认证状态 | 状态值、验证器、随机数、创建时间 | 10 分钟 TTL，用后即删 |
| 密码重置令牌 | 令牌值、所属用户、过期时间、使用标记 | 一次性；1 小时 TTL；用后即删 |
| 访问日志 | 用户（可空）、IP、下载类型、平台、资源标识、状态、失败原因、时间 | 按日期查询；90 天自动清理 |

**关键约束**：内容实体的版本列表与「当前」指针更新必须事务化（见 4.1）；令牌的创建/轮替/吊销/级联清除必须原子（见 4.2/4.4）。

### 5.4 认证与安全架构

- **会话凭据最小化**：仅含用户标识 + **凭据版本号** + 标准声明（签发/过期时间）；角色、级别等权限**不入凭据**，每次请求实时查库。**会话立即失效机制**：禁用/删除账号、一键清空数据、修改邮箱、密码重置（管理员重置/自助修改完成）时递增用户或全局凭据版本号，请求时查库比对，版本不符即返回 401——保证「禁用账号会话一并失效」等场景，符合实时查库原则
- **权限校验链**：会话验证（解析凭据 → 查库取用户）→ 管理员校验（仅管理端点）；两个中间件独立、可组合
- **凭据存储**：浏览器本地存储（非 Cookie），API 通过 Authorization 头传递
- **下载令牌**：仅通过查询参数传递，不进入日志（自动脱敏）

### 5.5 内容文件存储设计

- 全部版本文件按内容域分目录存放：订阅 / 规则 / 自定义（按用户+平台隔离）/ 分享
- 每个版本独立文件（`v1`、`v2`…），「当前」为指向激活版本的符号指针（原子替换切换）
- **可缓存静态资源**存放于 `/public` 目录：站点图片（logo/favicon）、平台安装包（文件名带时间戳以突破 CDN 缓存）
- **数据库按运行模式分离**：Dev 与 Production 使用不同数据库文件（如 `app-dev.db` / `app-prod.db`），启动时按 `APP_MODE` 选择对应文件（不在运行时切换）；**模式切换时数据不迁移**（Dev 定位临时调试，数据不需长期保留）
- 所有用户可控路径必须经过路径穿越防护（禁止 `..` 与绝对路径逃逸）
- 统一存放于持久化卷（与数据库同卷，便于整体备份）

### 5.6 部署架构

```
用户浏览器 ──HTTPS──▶ 外部反向代理（TLS 终止）
                        └─ /* → 127.0.0.1:8080（单容器）
                              ├─ API 路由
                              ├─ /assets + /public 静态资源（缓存策略见 4.7）
                              └─ 其余路径 → 前端入口（SPA 回退）
                                      │
                              持久化卷
                    ┌───────┬──────┬──────┬────────┐
              数据库×2 订阅文件 规则文件 分享文件 /public
```

- **单容器**：后端同时服务 API + 静态资源（前端产物 + `/public` 可缓存资源）+ SPA 回退；对外只暴露一个端口
- **端口隔离（按接入方式）**：外部反代接入时容器端口仅绑定本机回环地址（`127.0.0.1`），不直接暴露公网；局域网直连模式由部署者直接映射端口，网络边界由部署者自行控制（防火墙/内网）
- **单数据卷**：数据库与全部内容文件统一持久化，备份即打包该卷
- **环境变量**：`APP_MODE`（dev/prod，启动时决定）+ 监听端口、日志级别、日志格式可选覆盖；业务配置一律走 Web UI

---

## 六、安全设计

### 6.1 认证安全

- OIDC 登录采用 PKCE（验证器与 state 绑定持久化），防授权码截获
- state 三重校验（Cookie == 回调参数 == 持久化记录）+ 用后即删，防 CSRF 与重放
- 本地密码 **bcrypt 哈希**存储，绝不落明文
- 会话凭据有效期：记住我 7 天 / 非记住我 24 小时；过期后需重新认证
- 本地登录 / 自注册 / 忘记密码端点均受速率限制；失败与不存在提示统一措辞（防邮箱枚举）
- **验证码**：reCAPTCHA / Turnstile 服务端校验（按管理员勾选的页面启用，未配置跳过）
- 密码重置令牌：一次性、1 小时有效、用后即删；响应不区分邮箱是否存在
- 仅「已激活」账号可登录（待审批 / 已禁用拦截）；禁用账号的会话与 Token 一并失效
- 认证端点速率限制独立配置：登录（默认 10/分钟）、注册（默认 5/分钟）、找回密码（默认 5/分钟）

### 6.2 密钥与凭据存储

- 签名密钥在 Setup 时**自动随机生成**，不硬编码、不依赖 .env
- 敏感配置（认证 Client Secret）以 **AES-256-GCM 加密**后落库；加密密钥由签名密钥派生（单一密钥管理，无需额外配置）
- SMTP 密码同样以 AES-256-GCM 加密落库（复用同一密钥派生逻辑，不新增密钥管理复杂度）
- 若配置存储被清空，全部会话与令牌立即失效（安全兜底）

### 6.3 输入与路径安全

- 所有含用户输入的路径操作经路径穿越防护（防 `..` 逃逸）
- 资源标识格式校验（小写字母数字连字符）
- 文件上传大小分档限制：订阅/规则/自定义/分享文件 ≤50MB；**安装包 ≤300MB**（前端 + 后端双重校验；安装包上传需**流式落盘**避免整读内存；外部反代 `client_max_body_size` 需相应调大）
- 管理端点全部要求管理员权限（会话校验 + 角色校验双层）

### 6.4 下载安全与防滥用

- 下载认证仅凭令牌（用户/分享/规则三类独立），令牌无过期时间但可轮替/吊销
- **缓存分级**：订阅/规则/分享下载响应返回**禁止缓存**头（`no-store` 等），防止代理缓存旧配置；`/public` 静态资源（图片/安装包）返回**可缓存**头，供 CDN 加速（见 4.7）
- 下载端点统一速率限制（默认 20/分钟，**按 IP 维度**计数；NAT/反代共享 IP 场景由部署者在面板调高阈值），超限返回 429 + Retry-After 提示
- 真实客户端 IP 解析：信任本机反向代理，正确读取转发头（用于限流与日志）
- 安装包上传校验：大小 ≤300MB、扩展名白名单（zip/tar.gz/7z/exe/dmg/apk/ipk 等常见格式）；部署文档中反代 `client_max_body_size` 同步调整

### 6.5 日志与隐私

- 日志记录中令牌查询参数一律脱敏为 `***`
- 下载失败记录失败原因（令牌无效 / 文件缺失 / 版本缺失 / 限流等），便于排查且不泄露敏感信息
- 5xx 内部错误默认脱敏返回通用错误；仅调试模式开启时返回详细信息（状态持久化，生产默认关闭）
- **实时日志流仅管理员可订阅**（暴露运行细节，不对外公开）
- **破坏性操作保护**：清空访问日志需二次确认；一键清空所有数据需**确认词 + 二次确认**（防误操作）

---

## 七、部署与运维

### 7.1 部署形态

- **Docker Compose 单服务**：`docker compose up -d` 一键启动；镜像多阶段构建（前端打包 → 后端编译 → 最小运行时），单镜像分发
- **两种接入方式（面向不同基础的用户）**：
  - **外部反代接入（推荐，公网部署）**：容器端口仅绑定 `127.0.0.1`，由部署者已有的反向代理承担 TLS 终止与域名接入（OIDC 回调要求 HTTPS 域名）
  - **局域网直连简化模式（0 基础/内网场景）**：docker-compose 直接映射端口（如 `8080:8080`），跳过外部反代；适用于内网/无域名场景（本地账号模式不依赖 HTTPS）；**注意：该模式下 Token 与密码经 HTTP 明文传输**，Setup/面板提供一次性风险提示，网络边界由部署者自行控制（防火墙/内网）
- 健康检查端点 `/health` 供反代/监控探活
- 可选环境变量：**`APP_MODE=dev|prod`（启动时决定运行模式，默认 prod，修改需重启容器）**、**`RESET_ADMIN_PASSWORD`（非空时启动进入应急恢复模式重置管理员密码，见 3.8）**、**`LOG_LEVEL`（初始日志级别，默认 info；面板可运行时切换并持久化）**、监听端口（默认 8080）、日志格式（默认 console，可选 JSON）
- **部署向导**：README/部署指引提供完整向导（域名 + 反代 + HTTPS 配置示例，或局域网直连两种路径）

### 7.2 持久化与备份

- **单一数据卷**承载全部持久化数据：按模式分离的数据库文件（Dev/Prod 各一）+ 订阅/规则/自定义/分享版本文件 + `/public` 静态资源
- 备份 = 打包该卷（tar 即可）；恢复 = 解包到卷
- 数据库使用 WAL 模式，单文件可安全拷贝备份；Dev 与 Prod 数据库互不干扰，切换运行模式（重启后）不会交叉污染数据；**模式切换时数据不迁移**（Dev 定位临时调试）

### 7.3 可观测性

- 日志输出到 stdout（`docker logs` 查看），分级控制（debug 排查 / info 常规 / warn 精简）
- 支持 JSON 结构化输出，便于接入日志收集系统
- 访问日志与运行日志分离：业务下载记录在 Web UI「日志查看」中按日期查询

### 7.4 升级与维护

- 镜像自动构建发布（GitHub Actions 推送到容器仓库），`docker compose pull && up -d` 完成升级
- 数据卷不随容器重建丢失；无状态升级（业务配置全在数据库）

---

## 八、重构技术债清单（从现有系统继承，重构时必须解决）

以下为现有系统审查发现的已知问题与重构要点，重构时应一并改进（非新功能，属工程质量）：

| # | 问题 | 重构要点 |
|---|------|---------|
| 1 | HTTP 处理器单文件臃肿（81 个函数集中于一个文件） | 按业务域拆分文件，每个文件独立管理依赖 |
| 2 | 4 个内容服务（订阅/规则/分享/自定义）的版本管理代码重复 >80% | 提取通用版本管理事务组件，统一失败清理模式 |
| 3 | 自定义订阅 Token 刷新存在「先读后写」竞争条件 | 改为基于业务键的原子更新 |
| 4 | 服务实例用包级全局变量，难以单测 | 新代码强制构造注入（结构体 Handler + 依赖传入）；旧代码逐步迁移 |
| 5 | 下载响应头中前端地址每次查库 | 启动时缓存，管理员修改后重启生效 |
| 6 | 登录发起参数无长度校验 | 增加长度限制（保留 OIDC 规范值白名单注释供未来启用） |
| 7 | 版本文件失败清理模式不统一 | 统一为事务模板 + defer 清理（见 4.1） |
| 8 | 前端构建产物过大（约 1.1MB / gzip 365KB）；旧 UI 为 Tailwind + Element Plus 5 组件极简策略 | **UI 全面重构**：切换为 Ant Design Vue + Tailwind（用户确认 2026-08-04）；路由级代码分割 + 按需加载管理面板子页面 |
| 9 | 部分 UI 遗留未使用配置/变量 | 清理无用 Tailwind 配置、初始化调用位置统一到应用入口 |
| 10 | 版本管理相关页面重复实现 | 抽取通用版本管理视图组件（列表/上传/预览） |

---

## 九、后续设计候选（待规划，非承诺）

> 新构想先记录于此（设计构想，非强制），经用户确认后由 Build 文档承接为分步构建 Step。
> **本地用户管理体系**（源自 `Future_Plan_01.md`）已于 2026-08-04 经用户确认**纳入本次重构实施范围**，其设计已整合为正式章节（角色权限见第二章、认证见 3.1-3.2、管理见 3.4.4-3.4.7、个人中心见 3.6、认证机制见 4.6、架构见 5.2-5.3、安全见 6.1-6.2），本节仅保留尚未排期的候选。

### 9.1 远期候选

| 候选项 | 说明 | 状态 |
|--------|------|------|
| 记住我设备管理 | 个人中心查看/撤销「记住我」会话 | 已确认远期实现，本期不做 |
| 多版本下载支持 | 规则页允许用户选择历史版本下载（当前仅当前版本） | 待评估 |
| 版本管理通用组件 | 前端 3 个版本页抽取统一组件（含卡片化样式） | 重构过程衍生 |
| 严格 OIDC prompt 白名单 | 校验仅允许规范值（none/login/consent/select_account） | 待评估 |

### 9.2 已确认参数（2026-08-04）

| 参数 | 结论 | 出处 |
|------|------|------|
| 本地密码复杂度要求 | ≥ 8 字符 | 用户确认 2026-08-04 |
| 密码重置令牌有效期 | 1 小时 | 用户确认 2026-08-04 |

---

## 十、设计决策记录（重构基线已确认）

| # | 决策点 | 结论 | 出处 |
|---|--------|------|------|
| 1 | 部署形态 | 单容器（后端 serve API + 静态文件 + SPA 回退），外部反代仅做 TLS 终止 | 现有系统已验证 |
| 2 | 存储 | SQLite 嵌入式（纯 Go 驱动，WAL），单一数据卷 | 现有系统已验证 |
| 3 | 前端组件策略 | ~~仅保留 5 个高复杂度组件，其余 Tailwind 原生，零 CDN~~（**已废弃**，由 #25 AntD 方案替代） | 现有系统已验证 |
| 4 | 权限模型 | 会话凭据最小化（仅用户标识），角色/级别实时查库不缓存 | 安全约束 |
| 5 | 订阅分发 | 按 is_advanced 自动决定级别，用户不可自选；未配置不升降级 | 产品定义 |
| 6 | 覆盖机制 | 自定义订阅（用户+平台）优先级高于自动分配 | 产品定义 |
| 7 | 分享机制 | 分享订阅不绑定用户、独立 Token 体系 | 产品定义 |
| 8 | 版本管理 | 最多 5 版本；版本号 max+1；当前指针原子切换；事务+行锁 | 并发安全要求 |
| 9 | Token 生命周期 | 用户升降级/自定义订阅删除时级联清 Token，防绕过 | 分级安全要求 |
| 10 | 密钥管理 | Setup 自动生成签名密钥，敏感配置 AES-256-GCM 加密落库，单密钥派生 | 安全约束 |
| 11 | 限流 | 登录/下载独立可配置限流，429 + Retry-After | 防滥用要求 |
| 12 | 日志 | 全部下载途径记日志（含失败原因），90 天自动清理 | 可观测性要求 |
| 13 | 首管理员 | 「已初始化」标记防二次产生管理员；首个登录用户为管理员 | 安全约束 |
| 14 | 错误处理 | 5xx 默认脱敏，调试模式可开关详细信息 | 安全约束 |
| 15 | 认证体系 | 本地用户管理纳入本次重构实施范围；OIDC 与本地登录可混合并存 | 用户确认 2026-08-04 |
| 16 | 认证模式关系 | ~~Setup 完成后认证模式只读，不支持运行时切换~~（**已修订，见 #41**：本地为底+OIDC 可叠加；只读仅指 OIDC 提供商类型间切换） | Future_Plan_01 设计 |
| 17 | OIDC 断开触发 | 仅管理员手动触发（清空/移除 OIDC 配置），系统不自动判定 | 用户确认 2026-08-04 |
| 18 | 自注册审批 | ~~注册后进入待审批状态，需管理员激活方可登录~~（**已修订，见 #75**：受自注册审批开关控制，默认关闭 = 直接激活）；用户表为空时首个注册者免审批成为管理员 | 用户确认 2026-08-04 |
| 19 | 记住我 | 会话 7 天 / 非记住我 24 小时；设备管理（查看/撤销）远期实现 | 用户确认 2026-08-04 |
| 20 | 邮箱唯一性 | 全局唯一约束；OIDC 与本地用户按邮箱绑定去重 | Future_Plan_01 设计 |
| 21 | 本地密码与重置令牌 | bcrypt 哈希存储；复杂度 ≥8 字符；重置令牌一次性 1 小时（**已确认，见 #44**） | Future_Plan_01 建议 |
| 22 | 运行模式 | Dev / Production 双模式：`APP_MODE` 环境变量定初始值（**已修订：仅启动时决定，见 #34**），独立数据库 | 用户想法 2026-08-04 |
| 23 | 模拟 OIDC | 仅 Dev 模式可用：模拟登录表单（任意用户名/邮箱 + 可模拟 role/group 属性） | 用户确认 2026-08-04 |
| 24 | OIDC 启用规则 | Role/Group 白名单命中自动激活，未命中进入审批中心 | 用户确认 2026-08-04 |
| 25 | UI 框架 | Ant Design Vue（按需）+ Tailwind 辅助，多端（手机/平板/台式机）友好 | 用户确认 2026-08-04 |
| 26 | 安装包下载 | 平台可上传二进制/压缩包；**公开下载 + CDN 缓存**；与外部链接并存，首页双按钮 | 用户确认 2026-08-04 |
| 27 | 验证码 | reCAPTCHA / Turnstile；管理员勾选启用页面（注册/登录/找回密码，默认不启用）；未配置密钥跳过 | 用户确认 2026-08-04 |
| 28 | SMTP 范围 | 管理员勾选启用（密码重置/审批结果通知/欢迎邮件），默认全不启用 | 用户确认 2026-08-04 |
| 29 | 邮箱绑定 | OIDC 与自注册可共存；邮箱相同自动合并绑定 + 个人中心手动绑定并存 | 用户确认 2026-08-04 |
| 30 | 站点自定义 | 站点名称存配置；ICON 上传图片文件，走 `/public` 可缓存路径 | 用户确认 2026-08-04 |
| 31 | 缓存分级 | `/public`（图片/安装包）可缓存供 CDN；`/assets` immutable；订阅/规则/分享下载禁止缓存 | 用户想法 2026-08-04 |
| 32 | 限流扩展 | 登录（10/min）、注册（5/min）、找回密码（5/min）、下载（20/min）独立可配置 | 用户想法 2026-08-04 |
| 33 | 部署形态 | 外部反代为主（公网）+ **局域网直连简化模式**（端口映射、可跳过 TLS，内网/无域名场景）；部署向导双路径 | 用户确认 2026-08-04 |
| 34 | 模式切换简化 | 仅启动时由 `APP_MODE` 决定（改环境变量 + 重启），**移除运行时 UI 切换与数据层热切换** | 用户确认 2026-08-04 |
| 35 | 自动合并安全 | 仅当 OIDC 返回 `email_verified=true` 才自动合并绑定；未验证仅支持手动绑定，防账号接管 | 用户确认 2026-08-04 |
| 36 | 安装包上限 | 上传上限 **300MB**（**已修订**，由 1GB 下调，见 #60；前端+后端双重校验，反代 client_max_body_size 同步调整） | 用户确认 2026-08-04 |
| 37 | Setup 引导 | **本地模式默认推荐**（快速开始），OIDC 折叠为高级配置入口 | 用户确认 2026-08-04 |
| 38 | 白名单提取 | Role/Group 白名单支持**可配置声明路径**（默认 `realm_access.roles`/`groups`/`roles` 下拉） | 用户确认 2026-08-04 |
| 39 | 验证码方案 | 保持 reCAPTCHA + Turnstile 两种提供商，不增加内置图形验证码兜底 | 用户确认 2026-08-04 |
| 40 | 自注册默认 | **默认关闭**自注册，管理员显式开启后才出现注册入口并进入审批流 | 用户确认 2026-08-04 |
| 41 | 认证模式关系 | **本地为底（默认开启）+ OIDC 可叠加**；「认证模式只读」仅指 OIDC 提供商类型间切换，本地与 OIDC 不互斥 | 用户确认 2026-08-04 |
| 42 | OIDC 绑定数 | **单 OIDC 绑定**：每账号最多一个 subject；已绑定后其他 OIDC 登录不覆盖、提示冲突 | 用户确认 2026-08-04 |
| 43 | 审批拒绝 | **拒绝即删除**待审批账号，邮箱释放可重新注册；状态枚举保持三种（无「已拒绝」状态） | 用户确认 2026-08-04 |
| 44 | 参数确认 | 本地密码复杂度 **≥8 字符**；密码重置令牌有效期 **1 小时**（已确认，9.2 清空） | 用户确认 2026-08-04 |
| 45 | 分级运行日志 | debug/info/warn/error 四级；`LOG_LEVEL` 为初始值，面板**运行时切换**（立即生效并持久化）；与调试模式开关独立 | 用户想法 2026-08-04 |
| 46 | 实时日志流 | **SSE 单向流**：环形缓冲（约 500 条）+ 广播推送；仅管理员可订阅；与访问日志明确区分（运行日志不落库） | 用户确认 2026-08-04 |
| 47 | 清空日志 | 仅清访问日志记录（二次确认）；实时日志缓冲为内存态随重启自然重置 | 用户确认 2026-08-04 |
| 48 | 一键清空所有数据 | **完全重置回 Setup**（业务数据 + 系统配置含密钥/configured + 数据文件全清）；**确认词 + 二次确认** | 用户确认 2026-08-04 |
| 49 | SSE 认证方式 | 实时日志流端点经 **`?token=` 查询参数**认证（EventSource 不支持自定义 Header，复用 token 脱敏机制）；响应 no-cache；**并发订阅上限 5** | 用户确认 2026-08-04 |
| 50 | 全清后进入 Setup | 清空后**无需重启**：前端路由守卫检测 configured=false 自动进入 Setup（重新生成密钥与管理员） | 用户确认 2026-08-04 |
| 51 | 审批记录 | **移除**「已处理记录可查看」：被拒用户无记录（与拒绝即删除一致），通过的用户在用户管理可见 | 用户确认 2026-08-04 |
| 52 | 模拟 email_verified | 模拟登录表单提供 **email_verified 勾选项**（默认已验证），可分别测试自动合并的已验证/未验证分支 | 用户确认 2026-08-04 |
| 53 | OIDC 白名单空值行为 | 白名单未配置/为空时跳过校验，OIDC 新用户直接激活；白名单仅在审批功能开启时生效 | 用户确认 2026-08-04 |
| 54 | 会话立即失效机制 | 凭据内记录「凭据版本号」，禁用/删除/全清时递增，每次请求查库比对（符合实时查库原则） | 用户确认 2026-08-04 |
| 55 | 下载限流维度 | 保持按 IP 限流（部署者可通过面板调高阈值应对 NAT 共享场景） | 用户确认 2026-08-04 |
| 56 | Dev/Prod 双库 | 保留双库；文档注明「切换模式数据不迁移」，Dev 定位临时调试，不补充文件隔离策略 | 用户确认 2026-08-04 |
| 57 | 首管理员产生 | 统一为「首个完成认证者触发」；Setup 只写配置、不创建任何用户（见 #70） | 用户确认 2026-08-04 |
| 58 | 全清数据入口 | 放在面板配置页「危险操作区」（从日志查看模块移出） | 用户确认 2026-08-04 |
| 59 | HTTP 直连风险 | 部署文档 + Setup/面板一次性提示（不过度防御，网络边界由部署者控制） | 用户确认 2026-08-04 |
| 60 | 安装包上限修订 | 由 1GB **降至 300MB**（降低反代/CDN 兼容性风险，保留流式落盘要求，修订 #36） | 用户确认 2026-08-04 |
| 61 | SMTP 失败处理 | 发送失败不阻断主流程，记录错误日志 + UI 提示「邮件发送失败」 | 用户确认 2026-08-04 |
| 62 | 改邮箱防接管 | 新邮箱被占用即拒绝；自动合并仅对「待审批/已激活」账号生效，不合并已禁用账号 | 用户确认 2026-08-04 |
| 63 | 拒绝后重注册 | 被拒用户可重新注册、再次进入审批队列；滥用由注册限流 + 验证码兜底 | 用户确认 2026-08-04 |
| 64 | 审批开关体系 | **两个独立开关**：① 自注册审批开关 ② OIDC 审批功能开关；均默认关闭；用户表为空时首个登录/注册者始终直接激活成为管理员（不受开关影响） | 用户确认 2026-08-04 |
| 65 | OIDC 审批开关语义 | 关闭（默认）：所有 OIDC 新用户直接激活，白名单不生效；开启：白名单命中→自动激活，未命中→待审批 | 用户确认 2026-08-04 |
| 66 | 自注册审批开关语义 | 独立于 OIDC 审批开关；**已修订（见 #76）**：默认关闭 = 自注册用户直接激活；开启后才进入审批队列（免审批例外：用户表为空的首个注册者） | 用户确认 2026-08-04 |
| 67 | 待审批用户 Token | 待审批用户从未获得会话，不存在下载 Token，无需特殊清理逻辑 | 用户确认 2026-08-04 |
| 68 | 禁用账号 Token | 禁用时 Token 失效；重新启用后不恢复原 Token，用户下次访问首页重新生成 | 用户确认 2026-08-04 |
| 69 | SMTP 启用范围 | 保持三项独立勾选（密码重置 / 审批结果通知 / 欢迎邮件），不合并 | 用户确认 2026-08-04 |
| 70 | Setup 定位 | Setup 仅写系统配置（认证模式、参数、密钥生成），不创建任何用户账户 | 用户确认 2026-08-04 |
| 71 | 用户密码重置兜底 | 忘记密码未配置 SMTP 时提示联系管理员；管理员在用户管理「直接重置」：二次确认 → 随机 8 位密码（大小写字母+数字，去除 i I o O 0 l L，无特殊符号）→ 对话框展示供复制，即时生效 | 用户确认 2026-08-04 |
| 72 | 应急恢复模式 | 手动触发（`RESET_ADMIN_PASSWORD` 非空，每次启动生效）或自动触发（数据库无法连接/损坏、关键配置损坏）进入阻塞式应急页；重置密码（完成后阻止运行，须移除变量重启；用户表为空时不可选）+ 重新初始化；其他密码异常不做自动检测（bcrypt 无法静态判定）；应急页无需登录；空表死锁由部署者设变量进应急页重建 | 用户确认 2026-08-04 |
| 73 | 应急端点暴露与防护 | 应急端点**仅应急模式下暴露，正常运行时屏蔽**；极低频救援场景不额外加限流/验证码防护，保持最简 | 用户确认 2026-08-04 |
| 74 | 应急页能力分级 | **重置管理员密码仅由环境变量门控**；自动触发（数据库损坏/关键配置损坏）的应急页仅保留重新初始化按钮；环境变量已设但数据库不可读时自动降级为仅重新初始化 | 用户确认 2026-08-04 |
| 75 | 自注册审批开关赋义 | **保留双开关并赋予实际语义**（修订 #64/#66）：自注册审批开关默认关闭 = 自注册用户直接激活；开启后才进审批队列；消除死开关 | 用户确认 2026-08-04 |
| 76 | 禁用管理员保护 | 允许禁用管理员账号，但**禁止禁用最后一个活跃（未禁用）管理员**（活跃管理员数 ≥ 1）；三重保护扩展为禁用场景 | 用户确认 2026-08-04 |
| 77 | 凭据版本号递增场景 | 修改邮箱、密码重置（管理员重置/自助修改完成）成功后同步递增凭据版本号，全部现有会话立即失效（与禁用/删除/全清一致） | 用户确认 2026-08-04 |

> **用途说明：** 每个决策点记录「决策点 → 结论 → 出处」，任何取舍都有据可查；冲突时以用户确认为准。

---

## 十一、设计变更流程

1. 新设计构想先在本文件（Design1）「后续设计候选」或新章节记录（设计构想，非强制）；
2. 存在方案取舍时，使用提问工具附推荐选项与用户确认；
3. 确认后由 [BuildN.md](./BuildN.md) 承接为分步构建方案（Step 含目标/前置/参考代码/验收命令）；
4. 构建完成后更新本文件「功能全景」与「设计决策记录」。

---

## 十二、变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-08-04 | 初始版本：完整重构设计基线（产品定义、角色权限、功能全景、核心机制、目标架构、安全设计、部署运维、技术债清单、演进候选、决策记录） |
| v1.1 | 2026-08-04 | 结合 Future_Plan_01 完善第九章：本地用户管理体系完整演进设计（认证模式选择、混合模式、管理员初始化改进、自注册流程、个人设置页、邮件与密码重置、登录页改版、数据变更概要、未决事项），并补充 3.1/3.2 交叉引用 |
| v1.2 | 2026-08-04 | 经用户确认（纳入本次重构、OIDC 断开手动触发、自注册管理员审批、记住我设备管理远期），本地用户管理体系整合为正式设计章节（2.x/3.x/4.6/5.x/6.x）；第九章改为「后续设计候选」，决策记录追加 #15-21 |
| v1.3 | 2026-08-04 | 整合用户新增 11 项设计构想（经两轮确认）：Dev/Prod 双模式与模拟 OIDC、OIDC Role/Group 白名单与审批中心、验证码（reCAPTCHA/Turnstile）、SMTP 勾选范围、站点名称/ICON 自定义、安装包上传与公开下载、/public 缓存分级、Ant Design Vue UI 重构、登录/注册/找回密码限流；决策记录追加 #22-32 |
| v1.4 | 2026-08-04 | 以「0 基础一键部署」为目标深度核验并修正（经两轮确认）：① 部署形态增加局域网直连简化模式 + 部署向导；② 模式切换简化为仅启动时 APP_MODE 决定（移除运行时切换）；③ 自动合并绑定要求 email_verified=true；④ 安装包上限 1GB；⑤ Setup 本地模式默认推荐（OIDC 折叠高级入口）；⑥ 白名单支持可配置声明路径；⑦ 自注册默认关闭；⑧ 修正决策 #3 废弃标注与第九章引用；决策记录追加 #33-40 |
| v1.5 | 2026-08-04 | 深入核验并修正 6 处内部冲突（决策 #22、5.2/5.5/5.6 遗留「模式切换」表述、状态枚举缺项、格式瑕疵）；经确认新增 4 项决策：① 本地为底+OIDC 可叠加；② 单 OIDC 绑定；③ 审批拒绝即删除；④ 密码复杂度 ≥8、重置令牌 1 小时（9.2 清空）；决策记录追加 #41-44 |
| v1.6 | 2026-08-04 | 细节核对修正：① 明确本地模式 Setup 创建管理员时同步置位「已初始化」标记（防双管理员）；② 自动合并绑定补充「目标账号未绑定其他 OIDC」前置条件（与单绑定规则一致）；③ 5.2 明确验证码仅用于表单类端点；④ 审批中心明确拒绝通知与欢迎邮件发送时机 |
| v1.7 | 2026-08-04 | 新增 3 项设计（经确认）：① 分级运行日志（debug/info/warn/error）面板运行时切换；② 实时日志流（SSE 单向流 + 环形缓冲，仅管理员，便于调试与对接 WAF）；③ 清空日志（仅访问日志）+ 一键清空所有数据（完全重置回 Setup，确认词+二次确认）；决策记录追加 #45-48 |
| v1.8 | 2026-08-04 | 构建视角核验修正（经确认）：① SSE 认证改 query token + 并发订阅上限 + no-cache；② 一键清空后无需重启自动进入 Setup；③ 移除审批「已处理记录」表述；④ 模拟 OIDC 增加 email_verified 勾选项；另修正事实性不一致（2.6/4.6 免审批例外、5.6 端口隔离双模式表述、5.1 缺 error 级别、1.5 零 CDN 限定、6.3 流式落盘）；决策记录追加 #49-52 |
| v1.9 | 2026-08-04 | 深度核验轮（成熟项目/真实场景/构建视角，经四轮确认，18 项新决策）：① Setup 定位修正为仅写配置不创建用户，首管理员统一由首个认证者触发（修正 2.1/2.5/3.1 矛盾）；② 审批体系改为两个独立开关（自注册审批 / OIDC 审批功能），均默认关闭，白名单空值行为与开关语义明确（2.6/3.4.7/4.6）；③ 会话立即失效采用凭据版本号机制（5.4）；④ 限流明确 IP 维度（3.4.7/6.4）；⑤ 安装包上限 1GB → 300MB（修订 #36）；⑥ 全清入口移至面板配置危险操作区（3.4.7/3.4.8）；⑦ HTTP 直连一次性风险提示（7.1）；⑧ SMTP 失败不阻断主流程（4.6）；⑨ 改邮箱防接管与自动合并状态检查（3.6/4.6）；⑩ 禁用/启用账号 Token 行为、拒绝后重注册、待审批无 Token 约定（4.2/4.6）；⑪ 双库备份说明简化（模式切换数据不迁移，5.5/7.2）；决策记录追加 #53-70 |
| v2.0 | 2026-08-04 | 细节完善（经确认）：① 忘记密码未配置 SMTP 时提示联系管理员，用户管理新增「直接重置密码」（二次确认 + 随机 8 位易读密码：大小写字母+数字，去除 i I o O 0 l L，无特殊符号，对话框展示供复制）；② 新增 §3.8 应急恢复模式：`RESET_ADMIN_PASSWORD` 环境变量触发阻塞式应急页（重置管理员密码 + 应急全清），密码异常/数据库异常/重置未完成无变量也触发，纯 OIDC 管理员不参与健康检查（5.2/7.1 同步补充）；决策记录追加 #71-72 |
| v2.1 | 2026-08-04 | 二轮核验修正 §3.8 应急恢复模式（经确认简化）：① 无变量触发仅限「数据库无法连接」，移除不可实现的「密码异常自动检测」（bcrypt 无法静态判定）；② 用户表为空时重置密码不可用，仅可应急全清重建；空表死锁救援路径＝设变量进应急页全清；③ 应急端点仅应急模式暴露、正常运行屏蔽，不额外加防护；④ 同步修正 3.2 会话凭据缺凭据版本号表述、5.3 用户实体补充凭据版本号字段；决策 #72 重写、追加 #73 |
| v2.2 | 2026-08-04 | §3.8 能力分级改进（经确认）：① **重置管理员密码仅由环境变量门控**，自动触发的应急页仅保留重新初始化按钮，降低无凭据重置密码的安全隐患；② 自动触发项扩展为「数据库无法连接/损坏 + 关键配置损坏（configured=true 但签名密钥缺失）」；③ 环境变量已设但数据库不可读时自动降级为仅重新初始化；决策 #72 修订、追加 #74 |
| v2.3 | 2026-08-04 | 三轮交叉一致性核验修正（经确认，4 项决策）：① 自注册审批开关赋予实际语义——默认关闭时自注册用户直接激活，开启后才进审批队列（修订 #64/#66，同步改 2.6/3.2/3.4.7/4.6）；② 新增禁用管理员保护——允许禁用管理员但禁止禁用最后一个活跃管理员（2.5 关联，4.6）；③ 修改邮箱成功后递增凭据版本号、全部会话失效（3.6/4.6/5.4）；④ 密码重置成功后递增凭据版本号、现有会话全部失效（3.4.4/4.6/5.4）；决策记录追加 #75-77 |
| v2.4 | 2026-08-04 | 四轮核验表述一致性修正（无需新决策）：① 4.6 身份模型表自注册用户登录描述同步审批开关语义；② 4.8 一键清空「重新创建管理员」修正为「管理员由首个认证者重新产生」（与 #57/#70 一致）；③ 3.4.4 删除用户保护引用改为见 2.5（四重防护）；④ 4.7 安装包覆盖时同步删除旧时间戳文件（防卷膨胀）；⑤ 3.3 顶部栏标题改为站点名称（与站点自定义一致）；⑥ 5.2 系统配置服务补审批开关；⑦ 决策 #18 标注已被 #75 修订 |
