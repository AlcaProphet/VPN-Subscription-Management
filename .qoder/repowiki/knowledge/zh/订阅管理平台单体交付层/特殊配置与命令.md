一键部署：`docker compose up -d`（或 `docker compose up -d --build` 从源码构建）；升级：`docker compose pull && docker compose up -d`；本地开发：后端 `cd backend && go run ./cmd/server`，前端 `cd frontend && npm run dev`（Vite 代理 `/api` 到 127.0.0.1:8080）。