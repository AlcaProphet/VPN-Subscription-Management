- 通过 `envOr` 辅助函数集中读取环境变量并提供默认值，避免在多处重复 os.Getenv 判断。
- 数据库迁移脚本采用数字前缀命名（如 0001_*.sql、1001_*.sql），配合 embed.FS 与 store.Migrate 实现按文件名排序的增量升级。
- 关键故障路径（DB 无法打开、迁移失败、紧急触发）统一收敛到 runEmergencyMode，复用同一套应急服务装配逻辑而非各自退出。
- 可嵌入资源包（migrations、web）仅声明 `//go:embed` 指令与导出 FS 变量，不包含业务代码，保持纯资源注入职责。