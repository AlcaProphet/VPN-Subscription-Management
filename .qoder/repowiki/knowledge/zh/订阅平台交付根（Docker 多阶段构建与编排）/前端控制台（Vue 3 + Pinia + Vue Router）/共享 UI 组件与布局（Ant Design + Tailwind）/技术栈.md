Vue 3 `<script setup>` + TypeScript；Ant Design Vue（`Layout`/`Menu`/`Modal`/`Message`/`Notification`/`Skeleton`/`Result`/`Empty`/`Button`/`Dropdown`/`Switch`/`Tag`/`Input`）；Tailwind CSS（通过 `@tailwind base/components/utilities` 注入）；`vue-router` 用于导航；`@ant-design/icons-vue` 图标库。