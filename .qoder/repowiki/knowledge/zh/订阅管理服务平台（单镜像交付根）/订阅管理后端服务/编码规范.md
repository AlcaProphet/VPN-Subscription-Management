- 每个内部包均配套同名 `_test.go` 单元测试文件，测试与实现同目录组织
- 业务错误统一通过 `internal/response` 返回标准 JSON 响应结构
- 可配置项集中通过 `internal/config` Service 读写，敏感字段在存储前加解密
- 对外 API 使用短码（slug）作为资源标识，由 `internal/slug` 生成与解析
- 对 IP 维度的请求限制通过 `internal/ratelimit` 固定窗口限流器统一处理