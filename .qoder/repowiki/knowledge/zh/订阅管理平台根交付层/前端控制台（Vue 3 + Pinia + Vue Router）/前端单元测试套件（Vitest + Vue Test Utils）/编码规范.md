- 通过 vi.mock 对 @/api/* 模块进行方法级替换，并在 beforeEach 中对 mock 函数执行 .mockReset() 以保证用例隔离。
- 每个 describe 块的 beforeEach 中统一初始化 Pinia 实例（setActivePinia(createPinia())）并清空 localStorage，避免状态泄漏。
- 组件测试使用 @vue/test-utils 的 mount 配合 createMemoryHistory 创建内存路由，并通过 global.plugins 注入 router 实例。
- 断言采用 BDD 风格（describe/it/expect），对异步流程使用 await flushPromises 或 setTimeout 等待微任务后再断言。
- 针对 AxiosError 构造 401 响应时，直接设置 http.defaults.adapter 抛出错误以走完整拦截器链，而非替换 http 实例。