# Issue13.md — VPN 订阅管理系统问题追踪（当前）

> **文档定位：** 本文记录对 Build17～Build20 进行严格对齐核验后发现的节点编辑器、目标检查与输出问题，承接已归档的 [Issue1～Issue12](docs/reports/Issue/)，以及当前的 Issue13。当前核验以 [Design4.md](Design4.md) 为设计基线，并参考 [BuildReport3.md](docs/reports/BuildReport/BuildReport3.md)；编码约束以 [AGENTS.md](AGENTS.md) 为准。

---

## 一、本轮说明与总体结论

- **创建时间：** 2026-09-03
- **调查来源：** [检查 Build17 至 Build20 对齐问题](thread://01a06527-aa9f-7572-b6f9-c13703503cc1) 任务的最终调查结论、浏览器评论与当前工作区代码定位。
- **核验范围：** Build17～Build20 的落地产物；当前提交 `db14912` 已包含 Build21 的后续修复，因此 Build21 引入的输出回归也纳入现状记录。
- **运行时核对：** 节点管理页实际返回的 JS/CSS 与本次源码构建结果一致，已排除浏览器使用旧静态资源造成的假象；本次调查没有保存新节点，也未修改业务代码。
- **自动化结果：** 后端 `node`、`server`、`assembly`、`assembly/links`、`xray` 五个包测试通过，`go build ./...`、`go vet ./...` 通过；前端相关 4 个测试文件、26 个用例通过，`npm run build` 通过；调查时 `git status --short` 干净，`git diff --check` 通过。
- **验收结论：** Build17 的保存基础基本完成；Build18 的元数据、活动投影和检查接口已实现但仍有契约缺口；Build19 未达到 Design4 的完整表单与交互验收要求；Build20 的统一入口和输出门槛已有实现，但完整输出验收尚未闭环。自动化通过不能替代真实元数据到浏览器交互链路和固定版本客户端的完整验收。
- **范围边界：** 固定版本客户端的实际导入/连接验证仍是 Design4/Build 文档中的人工待办，不在本 Issue 中重复登记为缺陷；Issue12 的 R26-02～R26-07 也不重复登记。

---

## 二、问题记录与修复状态

### R27-01 可编辑下拉把当前显示值当作搜索词，隐藏其他连接方式与 REALITY

- **来源：** 引用任务的浏览器评论 1、5；节点管理页 `/admin/nodes` 的 VLESS 新建表单。
- **现象：** 传输默认显示 `TCP`，打开下拉后只看到 `TCP`；清空输入文字后才出现 WebSocket、gRPC、HTTP/2、HTTP、XHTTP。安全默认显示“无”，打开后只看到“无”；清空文字后才出现 TLS、REALITY。REALITY 字段本身能够在选中后出现，但正常打开下拉时难以发现，因此用户会认为 REALITY 参数缺失。
- **快速验证：** 当前浏览器实际复现上述两组下拉行为；选择 REALITY 后，公钥、Short ID、SNI、客户端指纹均会出现。`frontend/src/components/EditableCombobox.vue:68`、`:115` 在打开下拉时将当前标签写入与用户搜索共用的 `text`，候选过滤继续使用该值，导致列表被当前显示值过滤。该问题同时解释了浏览器评论 1 的“缺少 VLESS Reality 配置区域”和评论 5 的“选取参数不完整”。
- **可能根因：** 当前值回显、下拉展开和用户主动搜索没有分离；控件把显示值直接当成过滤关键词，而不是在展开时清空搜索态或使用独立的 `displayValue`/`query`。
- **影响范围：** 影响所有使用可编辑下拉的传输、安全、协议或插件字段；尤其会隐藏 REALITY 等合法推荐选项，造成表单能力与实际 schema 不一致的判断，并增加误填自定义值的风险。
- **修复方向：** 分离当前选中值和搜索词：展开时显示完整合法选项，只有用户主动输入后才过滤；保留明确的推荐选项与自定义值入口，并补充传输、安全（含 REALITY）和插件候选的浏览器/组件回归测试。
- **待确认事项：** 无产品语义疑点；自定义值是否允许仍应由字段的 `allow_custom` 契约决定，不能通过控件默认行为放宽。
- **状态：** ☑ 已修复（R27-01 代码与回归测试已实施，详见“二·补充”与变更记录）

### R27-02 递归对象字段未执行条件判断，插件参数全部混排

- **来源：** 引用任务的浏览器评论 2、3、6；SS `obfs` 表单与协议元数据接口核对。
- **现象：** 新建 SS 并选择 `obfs` 后，插件参数区域仍同时显示密码、路径、TLS、指纹、证书校验、Mux、HTTP Upgrade、Restls Script 等属于其他插件模式的字段；“更多功能 / 高级参数”与连接区因此混杂大量当前组合不适用的内容。
- **快速验证：** 协议接口已下发子字段 `when.plugin`，但 `frontend/src/views/admin/NodesView.vue:119` 只过滤顶层字段；`frontend/src/components/ProtocolFieldEditor.vue:319` 进入对象后直接遍历全部 `properties`，递归编辑器没有接收当前连接状态，也没有对子字段执行条件判断。另有 `backend/internal/node/registry.go:639` 附近的插件模式候选共用一份清单，`obfs` 菜单会同时出现 HTTP、TLS、WebSocket，而不是按插件拆分。
- **可能根因：** 条件 schema 只在顶层动态字段渲染路径中生效，递归 `fields`/对象编辑路径没有复用同一套条件求值；后端注册表还把不同插件模式的候选集合放在同一层。
- **影响范围：** 影响 SS 插件、嵌套传输参数以及其他递归对象的表单可理解性和数据正确性；用户可能填写当前插件不会消费的参数，保存后又被归一化、清空或静默忽略，无法判断实际生效配置。
- **修复方向：** 让递归编辑器接收当前 `network`、`security`、`plugin`、`features` 状态，并对每个子字段及其子对象执行统一 `when` 判断；插件候选按插件模式拆分，隐藏非当前组合字段，补充至少 SS `obfs`、`v2ray-plugin`、无插件三组回归测试。
- **待确认事项：** 无产品决策；字段显示范围应遵循 Design4 的当前组合模型，未知键继续由既有扩展/JSON 保护契约处理。
- **状态：** ☑ 已修复（R27-02 代码与回归测试已实施，详见“二·补充”与变更记录）

### R27-03 多路复用关闭后未清空嵌套参数，违反关闭即清空规则

- **来源：** 引用任务最终调查中的 VLESS 草稿交互复现；Design4 §3.4 的功能关闭契约。
- **现象：** 在未保存的 VLESS 草稿中将多路复用最大连接数设置为 `7`，启用多路复用后再关闭，数值仍保留为 `7`；其他 SMux 子参数及 Brutal 参数仍可见或仍留在草稿中。重新开启后旧值可能继续出现，违反“关闭功能清空子参数、重新开启不恢复”的约定。
- **快速验证：** `frontend/src/views/admin/NodesView.vue:600` 的 `setField()` 只处理少数顶层布尔功能的关闭，没有处理 `smux.enabled` 等嵌套开关；递归更新逻辑只是合并对象。`backend/internal/node/registry.go:115` 的 VLESS、VMess SMux schema 也没有相应的功能重置声明。
- **可能根因：** 清空规则按顶层字段硬编码，未由嵌套 schema 的开关、条件和 `reset_on` 元数据统一驱动；对象更新又采用浅层合并，关闭动作无法删除旧子树。
- **影响范围：** 影响 VLESS、VMess 及其他带嵌套功能开关的节点编辑、保存、检查和后续输出；会产生 UI 已关闭但协议 JSON 仍含旧参数的状态漂移。
- **修复方向：** 为 `smux.enabled`、Brutal 等嵌套功能补齐重置归属与条件元数据；由统一草稿更新器按关闭事件删除指定子树和凭据/扩展，重新开启只生成默认空状态，不恢复旧值；补充关闭前后草稿、保存与输出投影测试。
- **待确认事项：** 无产品决策；清空范围应严格按 Design4 已确认的协议、传输、安全、插件和功能关闭规则执行。
- **状态：** ☑ 已修复（SS/VLESS/VMess 的 SMux/Brutal 代码与自动化回归已实施；浏览器/客户端实机复核见 ProdTestList）

### R27-04 legacy 字段仍作为可编辑入口，与新的连接模型并存

- **来源：** 引用任务的浏览器评论 3、6；Build21 后的注册表、归一化和节点表单核对。
- **现象：** VLESS 在 TCP 状态下仍显示 `WebSocket Path`、`WebSocket Headers`；切换到 WebSocket 后，连接区出现新的结构化 WebSocket 参数，高级区仍保留旧入口。选择 REALITY 后，高级区还会出现一个默认关闭的 `TLS` 开关，与当前安全选择形成矛盾反馈。
- **快速验证：** `backend/internal/node/registry.go:354` 附近仍保留旧 TLS/WS 字段，旧 WS 字段在 `:439` 附近仅被移入高级分组，没有增加 `when.network=ws` 或清空依赖；`backend/internal/node/normalize.go:30` 附近的服务端归一化会把 WS 别名合入规范对象并删除，但页面没有解释两个入口的关系或优先级。
- **可能根因：** Build21 调整了字段分组，却没有清理旧字段入口或将其改成受条件约束的兼容显示；旧 `tls` bool 与新的 `security` 选择也没有在 UI 上形成单一权威来源。
- **影响范围：** 影响 VLESS/VMess/Trojan 的新建、编辑和旧数据回显；用户可能在两个入口填写同一语义的不同值，保存后又被服务端归一化为另一种形态，导致连接参数不可信。
- **修复方向：** 将新连接模型作为唯一可编辑入口；legacy 字段只在必要的兼容读取/迁移路径使用，不在当前组合的表单中重复展示。对旧值先完成服务端归一化，再按 `network`、`security` 条件渲染当前传输与 TLS/REALITY 字段，并补充冲突值回显、切换、保存和输出投影测试。
- **待确认事项：** 已确认不保留只读迁移提示；从当前编辑表单移除 legacy 入口，旧值仅走归一化/导入/输出兼容路径。
- **状态：** ☑ 已修复（前后端、自动化与本地浏览器核心流程已验证；客户端验收边界见“二·补充”）

### R27-05 外层分组已存在，但内部顺序、层次与控件尺寸仍未对齐 Design4

- **来源：** 引用任务的浏览器评论 2、3、4、6；Design4 §3.1～§3.3 的节点表单分层要求。
- **现象：** 表单虽然有“认证与密钥”“连接方式与当前参数”“更多功能 / 高级参数”等外层区块，但内部仍出现层次和尺寸混乱：VLESS + REALITY 的顺序为 Flow、ALPN、传输、REALITY 对象、安全、SNI、客户端指纹，安全选择位于部分依赖参数之后；WebSocket 的 Early Data、HTTP Upgrade 直接铺在连接区；SMux 内的开关卡片与标签加输入框混排，当前视口中“启用/填充”卡片约高 54px，而“统计/仅 TCP”约高 40px；“结构化编辑/高级 JSON”还使用与运行参数开关相同的 Switch 表达。
- **快速验证：** `frontend/src/views/admin/NodesView.vue:736` 按注册表顺序渲染字段；`frontend/src/components/ProtocolFieldEditor.vue:254` 对对象子字段统一采用网格平铺，未建立子区域的常用/高级层次。浏览器评论标注的实际 DOM 与上述顺序、网格拉伸和控件高度差一致。
- **可能根因：** 仅完成了外层 group 的迁移，没有按“选择控件 → 当前组合参数 → 高级参数”的依赖顺序重新组织 schema；对象字段、运行开关和编辑模式控件复用了同一布局/表达组件。
- **影响范围：** 影响所有协议的表单可读性、移动端操作和字段发现；用户难以建立“当前选择决定哪些参数”的心智模型，也容易误以为不适用字段是有效连接参数。
- **修复方向：** 按 Design4 重新定义字段排序和子区块：基础信息/凭据、连接方式（传输与安全选择先行）、当前组合参数、独立运行开关、兼容与性能、高级 JSON/目标检查分别组织；统一字段行、开关卡片和列表控件的尺寸；用按钮或分段控件表达编辑模式，避免与运行时启用开关混淆。
- **待确认事项：** 已确认允许调整可见字段范围与常用/高级分类；首批四协议字段基线见“二·补充”R27-05。
- **状态：** ☑ 已修复（前后端、全量自动化与本地浏览器核心布局/交互；Production 与客户端边界见 ProdTestList）

### R27-06 `allow_custom=false` 在 API 序列化中丢失，安全选择错误开放自定义值

- **来源：** 引用任务最终调查的协议接口响应核对；VLESS/VMess 安全字段行为。
- **现象：** 安全下拉实际出现“使用自定义值：无”。字段在后端注册表中明确不允许自定义，但接口响应缺少 `allow_custom` 后，前端把缺失值解释为允许自定义，用户可以把任意字符串作为安全值提交。
- **快速验证：** `backend/internal/node/registry.go:25` 的 `AllowCustom` 使用 `omitempty`，因此 `false` 不出现在 JSON 中；`frontend/src/components/ProtocolFieldEditor.vue:349` 以 `field.allow_custom !== false` 判断，字段缺失时反而放行。实际协议响应确认 VLESS、VMess 的安全字段没有 `allow_custom`。
- **可能根因：** 后端将“false”当作可省略的默认值，但前端需要区分“明确禁止”和“未声明”；双方没有统一布尔字段的序列化契约。
- **影响范围：** 影响安全、传输、插件等所有禁止自定义的枚举字段；非法自定义值可能进入保存/检查链，破坏协议状态约束并造成前后端校验不一致。
- **修复方向：** 对 `allow_custom` 使用明确的非省略 JSON 布尔值，或建立严格的前端响应适配并将缺失视为禁止；补充 VLESS/VMess 安全、传输和插件字段的真实接口响应测试，确保自定义入口只在明确允许时出现。
- **待确认事项：** 已确认后端改为 `*bool`，区分未声明/禁止/允许；前端仅在 `allow_custom === true` 时开放自定义。
- **状态：** ☑ 已修复（前后端契约、真实协议接口响应与自动化回归已验证）

### R27-07 新建节点的嵌套敏感字段错误显示“已保存（留空保留）”

- **来源：** 引用任务最初在 SS 嵌套插件凭据上复现；R27-02 拆分插件参数后，该具体字段已变化，但调查确认同一缺陷仍覆盖嵌套敏感字段，并暴露 WireGuard `peers[]` 数组凭据没有稳定身份、未进入完整加密链的问题。
- **现象：** 新建或已重置的敏感字段可能错误显示“已保存（留空保留）”；数组凭据若按索引处理，在 Peer 重排后还可能把旧密文保留到错误条目。
- **快速验证：** 原实现仅按 schema 中的敏感路径推断保存状态，递归组件没有服务端“实际已保存”摘要；后端路径读取、合并、加密、脱敏和解密只支持固定对象路径，WireGuard `peers[].pre-shared-key` 未进入同一链路。
- **可能根因：** “字段敏感”与“节点已有可保留密文”被混为一谈；数组条目又缺少跨重排稳定的凭据身份，无法安全地把 schema 通配路径映射到具体凭据。
- **影响范围：** 影响顶层、嵌套对象和对象数组凭据的状态理解、保留、替换、清空、检查与输出；数组重排时还存在凭据错配风险。
- **修复方向：** 服务端返回实际密文摘要 `saved_sensitive_paths`；递归编辑器按完整路径区分未配置、已保存、待替换和已重置。数组敏感路径使用稳定 UUID 条目标识展开，统一接入归一化、加密、合并、脱敏、检查、解密和输出清理，并升级历史 WireGuard Peer 数据。
- **待确认事项：** 已确认扩大 R27-07，本次一并重构数组敏感凭据模型；具体契约与实施结果见“二·补充”。
- **状态：** ☑ 已修复（固定／嵌套／数组凭据契约、历史数据升级与前后端回归已验证）

### R27-08 目标检查成功响应的 `diagnostics: null` 导致检查面板崩溃

- **来源：** 引用任务最终调查的 SS + `obfs` 草稿“不落库检查”复现；目标检查前端测试核对。
- **现象：** 目标检查接口返回 HTTP 200，Clash 结果为 `status: "ok"`、`diagnostics: null`；浏览器报错 `Cannot read properties of null (reading 'length')`，目标检查面板消失。该问题连续复现两次。
- **快速验证：** 修复前 `backend/internal/node/check.go` 以 nil 初始化诊断数组，`frontend/src/components/NodeCheckPanel.vue` 直接读取 `result.targets[key].diagnostics.length`；前端测试 mock 又只使用 `diagnostics: []`，因此未覆盖真实接口的 null 形态。现已由非 nil 服务端初始化、真实 HTTP 数组断言及前端 null mock 回归锁定。
- **可能根因：** 后端成功结果把“无诊断”序列化为 JSON `null`，而前端把诊断集合当作必定为数组；接口类型、后端初始化和前端渲染没有形成统一的非空数组契约。
- **影响范围：** 影响所有无诊断的目标检查结果，直接破坏节点检查 UI；也会掩盖真实检查状态，使用户无法看到 Clash/SR/generic 的检查结果和预览。
- **修复方向：** 统一接口契约：无诊断时返回 `[]`，并在前端边界适配中对 null 做安全归一化；补充真实响应形态、成功无诊断、多目标混合诊断和迟到响应回归测试。
- **待确认事项：** 已确认后端统一返回 `[]`，前端对 null 做安全归一化。
- **状态：** ☑ 已修复（后端非空数组契约、前端 null 兼容与自动化回归已验证）

### R27-09 SS 插件 Clash 输出格式回归，检查仍错误标记为 `ok`

- **来源：** 引用任务最终调查的 SS + `obfs` 目标检查响应；Build21 输出映射核对；固定 Mihomo 1.19.29 源码契约。
- **现象：** 当前实际检查响应生成的 Clash YAML 为：

  ```yaml
  plugin: obfs-local;obfs=http
  ```

  同时没有 `plugin-opts`。检查结果仍标记为 `status: "ok"`，没有提示插件语义丢失或目标输出不完整。
- **快速验证：** Step 10 已移除 URI 插件渲染中被忽略的目标参数并拆分 SR/generic 可表达性；Clash 输出仍通过 Step 11 前 legacy helper 生成插件字符串并删除结构化 `plugin-opts`。固定版本 Mihomo 1.19.29 的 Shadowsocks 处理按独立插件名称（如 `obfs`）匹配分支，并从 `plugin-opts` 读取参数；因此 Clash 直接回归仍待 Step 11 修复。
- **可能根因：** Build21 新增的目标插件转换复用了 URI/Shadowrocket 风格的字符串映射，却将其直接用于 Clash/Mihomo YAML，并在转换后删除了 Clash 仍需要的结构化参数；目标检查只验证生成成功，没有验证固定版本内核的语义形态。
- **影响范围：** 影响普通 SS + `obfs` 节点的 Clash/Mihomo 订阅与不落库目标检查；可能造成配置可解析但插件不生效。该条是输出与固定版本源码契约的核对结论，本轮未做真实连接实验。
- **修复方向：** 按目标分别实现插件映射：Clash/Mihomo 保留独立 `plugin` 与 `plugin-opts` 结构，SR/generic URI 再按目标客户端要求转换为 `obfs-local` 等名称；目标检查增加结构化输出断言和插件语义诊断，不能仅以 YAML 生成成功标记 `ok`。
- **待确认事项：** 已确认先按官方模板/离线契约修复 Clash 结构化输出与 URI 映射；固定版本真机导入/连接验证保留为人工待办，不宣称完整兼容。
- **状态：** ◐ 修复中（Build21 Step 7～10 的合同/schema、幂等归一化、未知参数保存回显、固定字段与私钥敏感路径、SIP002 及 URI 目标分流已验收；Step 11～14 尚待实施）


## 二·补充、研究解决方案与已确认决策（2026-09-03）

> 本节在 R27-01～R27-09 的原始记录基础上，补充逐项深研后的解决方案与用户决策确认。R27-01～R27-08 已完成代码与回归测试；R27-09 已完成 Build21 Step 7～10 的合同/schema、幂等归一化、未知参数保存回显、固定字段与私钥敏感路径、SIP002 与 URI 目标分流，Step 11～14 仍待实施；具体实机验证边界以各项记录为准。

### 解决方案总览

| 编号 | 问题 | 已确认方案 | 状态 |
|---|---|---|---|
| R27-01 | 可编辑下拉把当前值当搜索词 | 分离显示值与搜索词；展开显示全部合法选项，仅用户输入后过滤；空值项始终可点击 | 已修复（前端组件与回归测试） |
| R27-02 | 递归对象未执行条件判断 | 递归编辑器接收当前连接状态并统一执行 `when`；`plugin-opts` 拆为每插件独立对象/字段，存储层也拆分 | 已修复（前后端与回归测试） |
| R27-03 | 多路复用关闭未清空嵌套参数 | 由统一重置元数据驱动功能关闭清空；前端草稿、后端保存与投影同时清除关闭功能的子树 | 已修复（前后端与回归测试；实机复核待办） |
| R27-04 | legacy 字段仍可编辑 | 从当前编辑表单移除 `ws-path`、`ws-headers`、`tls bool` 可编辑入口；不保留只读迁移提示 | 已修复（前后端、自动化与本地浏览器核心流程） |
| R27-05 | 表单层次/顺序/尺寸未对齐 | 四协议选择先行、递归高级折叠、运行开关集中、模式按钮与尺寸统一；保留规范路径与数据契约 | 已修复（前后端、全量自动化与本地浏览器核心流程） |
| R27-06 | `allow_custom=false` 序列化丢失 | 后端改为 `*bool`，区分未声明/禁止/允许；前端仅在 `allow_custom === true` 时开放自定义 | 已修复（前后端契约与回归测试） |
| R27-07 | 嵌套敏感字段误显示已保存 | 后端返回 `saved_sensitive_paths`；前端按完整路径呈现凭据状态；WireGuard Peer 以稳定 UUID 接入数组凭据全链路 | 已修复（前后端、历史升级与回归测试） |
| R27-08 | `diagnostics: null` 导致前端崩溃 | 后端统一返回 `[]`，前端对 null 做安全归一化 | 已修复（前后端与回归测试） |
| R27-09 | SS 插件 Clash 输出格式回归 | Clash/Mihomo 保留结构化 `plugin` + `plugin-opts`；SR/generic URI 使用目标字符串映射；真机验证保留人工待办 | 修复中（Build21 Step 7～10 已验收） |

### R27-01 研究解决方案

- 核心思路：`EditableCombobox.vue` 将“当前值回显文本”与“搜索输入”拆成两个状态。
- 关闭时输入框显示当前值标签；打开时清空搜索态，展示全部合法候选；用户主动输入后才过滤。
- 保留“不使用插件/无”等空值候选为可点击项，不依赖用户手动清空输入。
- 需要补充测试：当前值为 TCP/无时打开下拉能看到全部传输/安全选项；`allow_custom=false` 不出现自定义入口。

### R27-02 研究解决方案

- 让 `ProtocolFieldEditor.vue` 接收当前 `CurrentState`（network/security/plugin/features），并在对象、列表递归时继续传递。
- 每个子字段及其子对象统一调用现有 `when` 条件判断，后端 `ProjectActive` 已具备递归匹配能力，前端需要对齐。
- SS 插件按用户决策拆成独立对象/字段，且存储层同步拆分：
  - `obfs-opts`
  - `v2ray-plugin-opts`
  - `shadow-tls-opts`
  - `restls-opts`
- 涉及同步调整：URI 导入归一化、敏感路径/加密/脱敏、Clash/SR/generic 输出映射、插件切换清空、扩展 scope、回归测试。
- 对未验证插件（如 `shadow-tls`、`restls`）保留诊断，不宣称完整支持。

### R27-03 研究解决方案

- 将功能关闭清空从“前端顶层硬编码”改为“schema 元数据 + 统一草稿更新器”驱动。
- 明确 `smux`、`brutal` 等嵌套功能所属的 `feature` 范围；当嵌套 `enabled` 由 true 变 false 时，删除该功能子参数、凭据、扩展和局部 JSON 草稿。
- 重新开启只生成默认空状态，不恢复旧值。
- 后端活动投影同步过滤已关闭功能的子树，确保保存、检查、输出三者一致。
- 实施落点：后端 `FieldSchema.feature` 声明功能名/控制字段，`features.go` 提供递归派生、关闭清理和重置路径判断；前端 `nodeFeatures.ts` 统一处理结构化与 JSON 候选值，按作用域清除错误、凭据、扩展草稿及组件内部 JSON 文本。SMux 关闭保留 `{enabled:false}`，Brutal 独立关闭保留其他 SMux 参数；新增 `feature.smux.brutal` 子 scope，父 scope 关闭不允许子扩展被 keep 复活。
- 实施范围：SS/VLESS/VMess 共用 SMux/Brutal，回归既有顶层功能。未扩展 Trojan 内层 SS；未处理 R27-04～R27-09；不修改数据库结构和历史版本快照。
- 自动化覆盖：修复前新增测试已复现存储/投影残留、Brutal 重置合并复活和检查/保存关闭失效；修复后覆盖关闭再开启、已有关闭状态的 JSON 残留、作用域扩展/凭据、无关草稿保留、旧功能状态读取/投影、保存失败不写库及实际 Clash YAML 片段。浏览器真实接口联调与固定版本客户端连接仍未在本轮执行，见 [ProdTestList.md](ProdTestList.md)。
- 验证结果（2026-09-03）：后端全量 `go test ./... -count=1`、`go build ./...`、`go vet ./...`，节点/装配/服务端三包 `go test -race` 通过；前端全量 40 文件 / 179 用例及 `npm run build` 通过，保留 Vite 大于 600 kB 的 chunk 警告；`git diff --check` 通过。

### R27-04 研究解决方案

- 用户已确认：**直接移除 legacy 入口，不保留只读迁移提示**。
- 新连接模型成为唯一可编辑入口：
  - VLESS/VMess 使用 `network` + `ws-opts/grpc-opts/...`
  - 使用 `security` + TLS/REALITY 字段
- 旧 `ws-path`、`ws-headers`、`tls bool` 只保留在 URI 导入/旧数据归一化/输出适配路径，不再出现在手工表单。
- 实施落点：`GetProtocols()` 对外生成可编辑 schema，内部注册表继续用于兼容输入、字段校验、更新合并和输出。移除 VLESS 的顶层 `ws-path/ws-headers/tls` 与 VMess 的顶层 `tls`；Trojan 原本没有上述入口。HTTP/SOCKS5 的 TLS 与 SS 插件内部 TLS 参数不受影响。
- 草稿与归一化：前端回填 `current_state.security` 后移除 VLESS/VMess 草稿顶层 `tls`；WS 别名转换下沉到读取/保存/检查/投影共用路径，规范值优先、旧值只补缺项，列表/详情仅转换响应副本。无数据库迁移和历史版本重写。
- 自动化：修复前新增测试复现了协议接口下发旧入口、读取保留 WS 别名、TLS 草稿残留；修复后覆盖真实接口、冲突值回显、检查不写库、凭据保留、保存及实际 YAML/URI 安全字段。后端全量 `go test ./... -count=1`、`go build ./...`、`go vet ./...`，前端 40 文件 / 183 用例与 `npm run build` 均通过；保留既有 chunk 大于 600 kB 提示；`git diff --check` 通过。
- 浏览器：本轮最新生产前端构建与隔离 Dev 后端完成 VLESS TCP/WS、TLS/REALITY 入口核对，以及 VLESS/VMess 新建、保存重开、TLS 切换 none 后保存重开；核心走查无浏览器控制台错误。临时实例和页面已关闭。
- 验证边界：目标检查面板的交互、固定版本客户端导入/连接仍待人工复核。新增测试观察到未改动的 VMess SR URI 映射没有显式 `tls` 查询参数，其客户端语义需独立核验；本轮自动化安全输出断言覆盖 VLESS 的 Clash/generic/SR 与 VMess 的 Clash/generic，不冒充 VMess SR TLS 验收。详见 Build21 补充 Step 与 ProdTestList。

### R27-05 研究解决方案

- 用户已确认可按需调整字段可见范围与常用/高级分类。
- 已批准的首批四协议字段基线：
  - VLESS：auth=UUID；connection=network、security、flow（TCP+TLS/REALITY）、对应传输参数、TLS/REALITY 身份参数；switches=UDP、packet-addr、xudp、tfo；advanced=smux、packet-encoding、encryption、mptcp 等。
  - VMess：auth=UUID；connection=network、security、cipher、传输参数、TLS 身份参数；switches=UDP、xudp、packet-addr、tfo；advanced=alterId、smux、packet-encoding、global-padding、authenticated-length、mptcp 等；REALITY 不开放。
  - Trojan：auth=password；connection=network、TLS 身份参数、ws/grpc 参数；switches=UDP、tfo；advanced=ss-opts、mptcp 等；REALITY 不开放。
  - SS：auth=password；connection=cipher、plugin、插件专属参数、client-fingerprint（仅适用插件）；switches=UDP、udp-over-tcp、tfo；advanced=smux、udp-over-tcp-version、mptcp 等。
- 配套 UI 要求：统一字段行/开关卡片/列表控件尺寸；用按钮或分段控件表达“结构化编辑/高级 JSON”，避免与运行开关混用。
- 补充确认：所有首批运行开关集中到“独立开关”，高级开关进入“更多开关”；SMux 等嵌套开关标明所属功能，参数仍留在结构化高级区域。保留原 JSON 路径，不做数据库迁移；对象数组控件继续归属条目。
- 已实施：后端重排四协议 schema、标记 WS/插件高级子字段、补齐 SS `client-fingerprint` 的 `shadow-tls`/`restls` 条件与插件清空归属；前端按祖先条件收集开关并复用原更新链，递归编辑器消费 `advanced`、保留折叠区草稿、使用编辑模式按钮，补齐错误展开定位与手机 Switch 尺寸。
- 验证：后端全量测试、编译与 vet 通过；前端 41 文件 / 191 用例与生产构建通过（保留既有大 chunk 警告）。本地浏览器使用真实 API、最新生产前端构建和全新临时 Dev 数据库：四协议核心表单、VLESS WS 高级值保存/重开、SMux 关闭再开、SS 指纹切换清空，以及 375px 手机布局和明暗主题通过；开关卡片 44px、本体 22px，手机无横向溢出。
- 边界：未处理 R27-06～R27-09；SS 插件凭据的“已保存”误提示仍属于 R27-07，输出与目标检查未标为完成。固定版本客户端及更完整 Production 回归仍保留人工待办。

### R27-06 研究解决方案

- 用户已确认：后端 `AllowCustom` 从 `bool` 改为 `*bool`。
- JSON 语义：
  - `true`：明确允许自定义。
  - `false`：明确禁止。
  - `null` / 未声明：未声明，按禁止自定义处理（除非字段本身无枚举约束）。
- 前端改为只允许 `field.allow_custom === true` 时显示“使用自定义值”。
- 后端校验同步：枚举字段未声明或不允许时不得接受清单外值。
- 补充真实接口响应测试，确保三种形态可区分。
- 已实施：`FieldSchema.AllowCustom` 改为 `*bool` 并保留 `omitempty`；VLESS/VMess `security` 显式下发 `false`，原本允许自定义的传输、算法与插件继续显式下发 `true`；后端校验及前端动态编辑器统一为仅显式 `true` 放行，前端类型覆盖 `null`。
- 自动化覆盖：后端验证 `true`/`false`/未声明的 JSON 三态、真实协议接口的安全/传输/算法/插件字段以及允许/拒绝路径；前端覆盖 `true`、`false`、`null` 和缺失四种动态编辑器行为。后端全量测试、编译与 vet 通过；前端 41 文件 / 192 用例与生产构建通过，保留既有大 chunk 警告。
- 边界：不修改数据库、节点存储、历史数据、输出适配或其他 R27 项；本项为元数据与表单契约修复，无需客户端连接实验。

### R27-07 研究解决方案与实施结果

- 用户已确认：先新增后端 `saved_sensitive_paths`、前端按完整路径传递凭据状态；随后扩大 R27-07，本次一并重构数组敏感凭据模型。
- 后端响应契约：创建、详情和列表中的 `saved_sensitive_paths` 始终为数组；只把存储值确为 `enc:v1:` 密文的具体路径计入，空集合序列化为 `[]`。响应仍对凭据值本身留空。
- 路径与稳定身份契约：schema 用 `peers[].pre-shared-key` 声明数组敏感字段，具体操作和响应使用 `peers[<uuid>].pre-shared-key`；`FieldSchema.item_id_field` 声明内部条目标识 `_credential_id`。UUID 随 Peer 持久化并在重排时保持不变，重复标识拒绝保存，数字索引不能作为敏感凭据操作路径。
- 数组凭据已接入归一化、保留／替换／清除合并、加密、脱敏、节点检查、解密和装配输出链。`_credential_id` 只用于内部编辑身份，按 schema 在正式客户端输出前剥离，不会污染订阅配置；同名未知扩展不会被全局误删。
- 启动升级会幂等地为历史 WireGuard Peer 补齐 UUID，并把既有明文 `pre-shared-key` 加密后在同一事务写回；升级不改变业务编辑修订号。读取阶段先关闭游标再进入配置／写事务，兼容 SQLite 单连接配置。
- 前端递归对象和对象数组均传递完整路径状态：新建／已重置显示“未配置”，已有密文显示“已保存（留空保留）”，输入新值显示“待替换”。A → B → A 的重置失效状态持续保留到保存或显式重置草稿，不会因最终选择回到原值而复活旧凭据；数组新增与重排使用稳定 UUID。
- 自动化覆盖：后端覆盖固定路径摘要、两 Peer 加密与回显、重排保留、替换／清除、非法索引、历史升级幂等、检查脱敏和输出元数据清理；前端覆盖递归状态、事件传播、稳定条目身份、保存回显及 A → B → A 失效。`go test ./...`、`go build ./...`、`go vet ./...` 均通过；前端 41 个测试文件／195 个用例和生产构建通过，保留既有大 chunk 警告。
- 边界：本项没有修改 R27-08／R27-09；客户端固定版本连接验收仍沿用原人工边界，本轮通过自动化验证内部条目标识不会进入客户端产物。

### R27-08 研究解决方案

- 后端 `TargetCheckResult.Diagnostics` 始终为非 nil 数组，JSON 输出 `[]` 而不是 `null`。
- 前端在读取 `.length` 前统一执行 `diagnostics ?? []`，并对 `sortedDiagnostics` 同样防御。
- 测试覆盖：成功无诊断、多目标混合诊断、迟到旧响应不覆盖新结果。
- 已实施：目标检查成功路径以非 nil 空诊断数组初始化；校验和渲染产生的诊断继续按原顺序追加，状态计算、预览、脱敏与不落库语义不变。前端通过单一诊断读取入口将 `null` 归一化为 `[]`，长度判断和排序共用该入口。
- 自动化覆盖：后端 service 与真实 HTTP route 断言成功无诊断序列化为 `[]`，多目标正反例断言有诊断／无诊断混合响应；前端以真实 `diagnostics: null` mock 验证面板继续显示“未发现诊断”和其他目标诊断，既有迟到响应回归继续通过。`go test ./...`、`go build ./...`、`go vet ./...`、前端 41 个测试文件／195 个用例及生产构建均通过，保留既有大 chunk 警告。
- 边界：不修改数据库、节点保存、目标状态算法、诊断内容或 R27-09 输出适配；正式前端类型仍以非空数组表达服务端契约，`null` 仅作为运行时兼容输入处理。

### R27-09 研究解决方案

- 用户已确认：先按官方模板/离线契约修复，真机验证保留人工待办。
- Build21 Step 7 已验收：新增四个已知插件的集中合同、`plugin_not` 补集条件与 `map_value_type=string`；真实协议接口、后端保存前类型拒绝、前端条件/JSON 校验及全量构建测试通过。
- Build21 Step 8 已验收：已知旧 `plugin-opts` 只递归补缺且规范新对象优先，未知插件字符串 map 贯通创建、数据库、详情、列表、更新、服务重载与检查；空字符串 flag 保留，普通 `password/token/secret` 不进入敏感路径，读取/检查不写库或递增修订。固定插件字段、URI/Clash 投影和目标诊断仍按 Step 9～14 串行处理。
- Build21 Step 9 已验收：四个已知插件 schema 与 Mihomo 1.19.29 集中合同对齐，移除 v2ray-plugin `version` 与 Restls `path` 的正式支持暗示；Clash 必需项只在目标检查生效，不阻止不完整草稿保存；新增两条插件私钥固定敏感路径并通过加密、脱敏摘要、留空保留、清除、替换和插件重置回归。
- Build21 Step 10 已验收：SIP002 编解码器可稳定往返特殊字符、Unicode、百分号和 bare flag，并拒绝重复键、空键与坏转义；URI 导入保留未知字符串参数并恢复四插件已知字段类型，SR/generic 分别消费合同，目标不支持或无法无损回读时由渲染器显式报错。Clash 结构化投影、正式装配诊断和未知插件前端编辑仍按 Step 11～14 串行处理。
- Clash/Mihomo YAML：
  - 保留 `plugin: obfs` / `v2ray-plugin` / `shadow-tls` / `restls`。
  - 保留结构化 `plugin-opts` 对象，不再拼接成 `obfs-local;obfs=http` 字符串。
  - 如果存储已按 R27-02 拆分为独立插件对象，输出层负责映射回 Clash 的 `plugin-opts`。
- SR/generic URI：
  - 保留目标字符串映射，如 `obfs-local;obfs=http;obfs-host=...`。
  - `shadow-tls`、`restls` 未验证项继续输出诊断。
- 目标检查增加结构化输出断言，不能仅以 YAML 生成成功标记 `ok`。

---

## 三、后续处理边界

1. 本 Issue 只登记本次对齐核验已确认的现状问题；本轮不修改业务代码、不更新测试、不改变 Design4 的既有产品决策。
2. R27-01～R27-09 的解决方案与用户决策已在本文件“二·补充”确认；后续直接按该节拆分到新的 Build Step，不再重复确认已拍板项。实施仍不得以局部 CSS、前端隐藏或单元测试 mock 调整替代连接模型、状态清空和目标输出契约修复。
3. 完成修复后，至少需要执行后端 `go test ./...`、`go build ./...`、`go vet ./...`，前端全量测试与 `npm run build`、`git diff --check`，并补充真实 schema 浏览器交互、固定 Mihomo 版本输出语义和客户端导入/连接证据。
4. Issue12 已闭环的 R26-02～R26-07（检查/保存校验统一、URI 归一化、自定义传输、兼容字段、未知字段和 403）不因本轮发现再次打开；若新修复影响这些契约，应另行记录回归问题。

---

## 四、问题记录格式

```markdown
### R27-XX 问题标题

- **来源：** 浏览器页面、URL、用户标注和必要的截图/交互步骤。
- **现象：** 可复现的用户可见行为或逻辑结果。
- **快速验证：** 浏览器验证结果、接口响应、测试结果或最小静态代码证据。
- **可能根因：** 当前阶段基于证据的定位；未确认部分明确标注不确定性。
- **影响范围：** 受影响的页面、接口、数据或用户流程。
- **修复方向：** 仅给出大致方向，不在本轮实施。
- **待确认事项：** 需要用户决定的产品语义、兼容性或实现取舍。
- **状态：** ☐ 待确认 / ☐ 待修复（本轮仅分析）
```

## 五、变更记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v1.0 | 2026-09-03 | 根据“检查 Build17 至 Build20 对齐问题”任务的最终调查结论，新建 Issue13，登记 R27-01～R27-09：可编辑下拉过滤、递归条件缺失、SMux 关闭不清空、legacy 字段重复入口、表单层次与尺寸、`allow_custom` 序列化、嵌套凭据状态、目标检查 null 崩溃及 SS 插件 Clash 输出回归；本轮仅文档整合，未修改业务代码。 |
| v1.1 | 2026-09-03 | 补充“二·研究解决方案与已确认决策”：逐项深研 R27-01～R27-09，通过 `ask_user_question` 确认插件拆分（UI 与存储均拆）、legacy 直接移除、表单字段基线、`allow_custom` 改为 `*bool`、新增 `saved_sensitive_paths`、诊断数组统一 `[]`、SS 插件先修结构化输出并保留真机待办；R27 状态同步更新为“方案已确认，待实施”；本轮仅文档整理，未修改业务代码。 |
| v1.2 | 2026-09-03 | 实施 R27-01 可编辑下拉修复：打开时清空搜索态、展示全部合法选项，空值候选始终可点击；补充 `EditableCombobox` 组件回归测试；前端全量测试与构建通过。其余 R27-02～R27-09 未在本轮处理。 |
| v1.3 | 2026-09-03 | 实施 R27-02 递归条件与 SS 插件拆分：前端递归编辑器接收当前状态并执行 `when`；SS 插件参数拆为 `obfs-opts`/`v2ray-plugin-opts`/`shadow-tls-opts`/`restls-opts`，存储、URI 导入、敏感路径、清空和输出适配同步调整；新增前后端回归测试，全量后端与前端测试/构建通过。其余 R27-03～R27-09 未在本轮处理。 |
| v1.4 | 2026-09-03 | 实施 R27-03：以功能元数据统一 SMux/Brutal 草稿、保存、检查及输出清理，补齐嵌套重置、扩展/凭据保护与局部 JSON 失效，并同步 Design4/Build18/Build19 和人工复核清单。R27-04～R27-09 保持待实施。 |
| v1.5 | 2026-09-03 | 完成 R27-04：对外表单移除旧 WS/TLS 入口、前端清除重复 TLS 草稿、服务端共用 WS 归一化；全量自动化与最新构建浏览器核心流程通过。同步 Design4/Build21/ProdTestList，记录 VMess SR 输出差异待独立核验；R27-05～R27-09 未处理。 |
| v1.6 | 2026-09-03 | 完成 R27-05：四协议排序与高级元数据、集中运行开关、模式按钮、控件尺寸、错误定位及 SS 指纹归属；全量测试/构建和本地浏览器核心流程通过。同步 Design4/Build21/ProdTestList，R27-06～R27-09 保持待实施。 |
| v1.7 | 2026-09-04 | 完成 R27-06：后端 `AllowCustom` 改为三态指针，VLESS/VMess 安全字段明确下发 `false`；前后端仅显式 `true` 允许自定义，补充真实接口、校验和动态编辑器回归。全量后端测试/编译/vet及前端 41 文件 / 192 用例与生产构建通过；R27-07～R27-09 保持待实施。 |
| v1.8 | 2026-09-04 | 完成扩大的 R27-07：新增非空数组 `saved_sensitive_paths` 与递归完整路径凭据状态；以稳定 UUID 支持 WireGuard Peer 数组凭据的归一化、迁移、加密、合并、脱敏、检查、解密和输出清理。后端全量测试/编译/vet及前端 41 文件 / 195 用例与生产构建通过；R27-08～R27-09 保持待实施。 |
| v1.9 | 2026-09-04 | 完成 R27-08：目标检查成功无诊断响应统一为 `diagnostics: []`，前端对运行时 `null` 做集中归一化；补充 service、真实 HTTP、多目标混合和面板回归。后端全量测试/编译/vet及前端 41 文件 / 195 用例与生产构建通过；R27-09 保持待实施。 |
| v1.10 | 2026-09-04 | 开始实施 R27-09，完成 Build21 Step 7：集中 SS 插件目标合同、`plugin_not`、字符串 map schema 与前后端类型校验已通过定向/全量测试、编译、vet 和生产构建；R27-09 保持修复中，Step 8～14 尚未实施。 |
| v1.11 | 2026-09-04 | 完成 R27-09 Build21 Step 8：修复已知插件新旧对象优先级与未知参数静默删除，贯通数据库/响应/重载/检查、空 flag、重置、敏感路径隔离和只读零写入回归；后端相关包、全量、竞态、编译与 vet 均通过，Step 9～14 尚未实施。 |
| v1.12 | 2026-09-04 | 完成 R27-09 Build21 Step 9：对齐四个已知插件固定字段、Mihomo 目标证据、Clash 限定必需项与 v2ray/shadow-tls 私钥敏感路径；默认 mode 不批量入库、旧未知键保留、私钥全生命周期及后端定向/全量/竞态/编译/vet 验收通过，Step 10～14 尚未实施。 |
| v1.13 | 2026-09-05 | 完成 R27-09 Build21 Step 10：新增 SIP002 特殊字符转义、稳定序列化及严格反向解析；未知参数导入/URI 输出不丢失，四插件类型恢复与 SR/generic 可表达性分流由合同约束，不支持或复杂值由渲染器报错；后端定向、竞态、全量、编译/vet 与前端生产构建通过，Step 11～14 尚未实施。 |
