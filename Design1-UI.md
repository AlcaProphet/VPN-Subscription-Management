# Design1-UI.md — GUI 样式规格（承载 Design1.md 全部界面部件）

> **文档定位：** 本文档是 [Design1.md](./Design1.md) 全部界面部件的 GUI 样式规格：布局结构、Ant Design Vue 组件映射、状态分支、关键交互与响应式规则。功能行为与业务规则一律以 Design1.md 为准（本文不重复定义）；编码约束遵循 [AGENTS.md](./AGENTS.md)（**唯一强要求**）；暂缓项预留见 [DesignOnHold.md](./DesignOnHold.md)。
> 本规格面向第一次构建可直接实施；与 Design1.md 冲突时以 Design1.md 为准并提示用户。

---

## 一、全局风格基础

### 1.1 风格基调（已确认决策）

| 决策点 | 结论 |
|--------|------|
| 主色 | AntD 默认科技蓝 `#1677FF`，零定制 |
| 布局基调 | **全浅色布局**（浅色侧边栏 + 浅色顶栏），用户端与管理端风格统一 |
| 暗色模式 | 手动切换 + localStorage 持久化，`ConfigProvider theme.darkAlgorithm` 全局联动（Design1.md 3.7） |
| 移动端长表格 | **<768px 统一卡片化**，由通用「表格/卡片双态列表」组件承载（见 1.5） |
| 分页与搜索 | 访问日志/用户管理走**后端分页（默认 20 条/页）+ 关键字搜索**；组/平台/分享/规则等小列表全量渲染不分页 |
| 实时日志区样式 | **终端风深色底**（不随主题变化）+ 等宽字体 + 级别色块 |
| 版本管理形态 | **独立子路由页**（可深链直达；路由见第七章） |
| 侧边栏结构 | **平铺不分组**（与 Design1.md 3.4 的 9 模块 + 1 预留表述一致） |

### 1.2 色彩与主题 Token

- 主色 `#1677FF`；成功 `#52C41A` / 警告 `#FAAD14` / 错误 `#FF4D4F`（AntD 默认语义色）
- 页面底色 `#F5F7FA`（浅色）；卡片白底 `#FFFFFF`、圆角 `8px`、AntD `boxShadowTertiary` 轻阴影
- 暗色模式：AntD 暗色算法整体联动，页面底色 `#141414`，不做逐组件手工适配
- 危险操作（删除/清空/吊销）按钮统一 `danger` 属性红色，破坏性确认弹窗标题带警示图标

### 1.3 布局与排版

- 字体：AntD 默认字体栈；正文 14px、正文强调 16px、页面标题 20px、卡片标题 16px、表格 14px
- 圆角：卡片 8px、按钮/输入框 AntD 默认 6px
- 间距：内容区统一 24px 内边距（移动端 16px），卡片间 16px
- **响应式三档断点**：台式 ≥1200 / 平板 768~1199 / 手机 <768；本文各部件以 `<768` 与 `768~1199` 注记差异

### 1.4 关键页视觉预览（观感参考，非像素级依据）

- 首页：`~/.qoder-cn/vibe_images/ui-home_1786070658.png`
- 登录页：`~/.qoder-cn/vibe_images/ui-login_1786070715.png`
- 管理面板-订阅管理：`~/.qoder-cn/vibe_images/ui-admin-subscription_1786070717.png`

> 构建启动前建议将三张图拷贝至仓库 `docs/ui/` 目录归档；视觉图仅确认观感方向，实施以本文结构规格为准。

### 1.5 通用组件约定（复用件）

| 组件 | 职责 | 要点 |
|------|------|------|
| `TriStateList` 三态列表 | 加载中 / 空 / 列表三态分支统一封装（Design1.md 3.7） | 加载=Skeleton 骨架；空=Empty + 操作引导文案；错误=message.error + 重试按钮 |
| `ResponsiveTable` 表格/卡片双态列表 | ≥768 渲染 `a-table`；<768 自动切换卡片列表（**已决策：统一卡片化**） | 卡片展示前 3~4 个关键字段 + 操作折叠进 Dropdown；列定义与卡片字段共用一份配置 |
| `ConfirmModal` 删除确认 | 全部删除/危险操作统一确认对话框（Design1.md 3.7，禁用原生 confirm） | 标题 + 影响提示 + 确认回调；需确认词的场景（RESET/IMPORT）内嵌输入框，词不正确时确认按钮禁用 |
| `Notify` 通知封装 | `message`（操作即时反馈）与 `notification`（异步结果/邮件类结果）统一封装调用 | 成功=success、失败=error（展示后端返回的可展示信息；5xx 按调试开关决定详情） |
| `PageHeader` 页面头 | 管理面板各模块标题 + 面包屑 + 右侧操作区（新建/批量按钮） | 移动端操作按钮收进 Dropdown |
| `CopyField` 复制字段 | Token/链接/标识展示 + 一键复制 | 复制成功 message.success；敏感链接复制时弹出 Design1.md 规定的警示文案 |

---

## 二、无登录态页面

### 2.1 Setup 首次配置向导（Design1.md 3.1）

**整体结构**：独立全屏路由，居中单列卡片（max-width 720px）；顶部站点 ICON + 「首次配置」标题 + 当前模式徽标（`a-tag`：Dev 蓝 / Production 绿）；右上角暗色模式切换。主体为 `a-steps` 步骤条（认证方式 → 参数配置 → 测试连接 → 完成），导入分支跳过步骤 2~3 直达完成。

**步骤 1 — 选择认证方式**（三张 `a-card` 单选卡，选中态蓝色描边 + 勾选角标）：

| 卡片 | 内容 | 可见性 |
|------|------|--------|
| 快速开始（推荐） | 「本地账号模式，零配置一键完成」，右上角 `a-tag` 推荐 | 始终显示 |
| 高级配置 | 「接入单点登录 OIDC」；选中后展开提供商 `a-radio-group`：Keycloak / Auth0 / 通用 OIDC / 模拟 OIDC | 始终显示（模拟 OIDC 仅 Dev 模式可选） |
| 导入已有配置 | `a-upload`（配置文件）+ 导出密码输入框 | **仅 Production 模式渲染** |

**步骤 2 — 参数表单**（`a-form` 垂直布局）：按提供商类型动态渲染 Base URL / Realm / 域名 / Client ID / Client Secret（`a-input-password`）；「高级」折叠面板（`a-collapse`）内为前端地址与回调地址两个输入框（预填推导值，提示「手动值优先，修改需重启容器生效」）；切换提供商类型保留已填字段（各提供商表单独立缓存）；模拟模式各字段自动填充且只读。

**步骤 3 — 测试连接**：「测试连接」按钮（loading 态）+ 结果 `a-alert`（成功 success / 警告「不支持 client_credentials，已降级」/ 失败 error 展示具体错误）；测试不落库。

**完成页**：`a-result success` + **显著 `a-alert warning` 步骤式提示「请部署者本人立即注册/登录成为管理员」（含抢注风险提示）** + 「前往登录」主按钮。

**状态分支**：导入分支校验失败 → 不做任何变更 + 错误原因 alert；完成写入失败 → error + 可重试。`<768`：步骤条转竖向、卡片纵向堆叠。

### 2.2 登录页（Design1.md 3.2）

**整体结构**：全屏居中卡片（max-width 420px），顶部站点 ICON + 站点名称；右上角暗色模式切换（常驻）。区块化垂直堆叠，区块间 `a-divider`：

1. **OIDC 登录区块**（仅 OIDC 已配置时渲染）：主按钮「使用 OIDC 登录」；模拟 OIDC 时替换为**模拟登录表单**：邮箱输入框 + role/group 附加属性勾选输入 + `email_verified` 勾选框（默认勾选），标题标注「Dev 模拟登录」
2. **本地登录区块**（仅允许本地登录时渲染）：邮箱、密码、`a-checkbox` 记住我 + 「忘记密码？」链接、登录按钮；失败统一措辞（后端返回文案直接展示）
3. **注册入口**（允许自注册开启，或用户表为空且本地登录开启时**始终显示**）：文字链「没有账号？注册」→ 注册表单页（用户名 + 邮箱 + 密码 + 确认密码）；表空时区块顶部 `a-alert info`「系统尚未配置管理员，首个注册用户将成为管理员」
4. **验证码组件**：按面板配置的启用页面，在对应表单提交按钮上方渲染 reCAPTCHA/Turnstile 组件；脚本加载失败显示错误文案（不静默卡死）

**忘记密码页**（独立路由）：邮箱输入 → 提交后统一提示「若该邮箱已注册，重置链接已发送」（`a-result`）；重置页（token 校验）：新密码 + 确认密码。

**`/pending` 待审批页**：`a-result info` 图标 + 「账号待审批，等待管理员激活」说明 + 返回登录按钮；无凭据独立页，不经过凭据中转页。

**响应式**：<768 卡片宽度 90% 视口。

### 2.3 404 页（Design1.md 3.7）

`a-result 404` + 「返回首页」按钮；未匹配路由统一兜底。

---

## 三、应急恢复页（Design1.md 3.8）

**整体结构**：独立全屏路由（应急模式下前端据系统状态端点 `emergency` 标记强制路由至此）；居中卡片，顶部红色警示图标 + 「应急恢复模式」+ 触发原因说明（`a-alert error`）。

**交互序列**：

1. **操作码输入**（第一步，必经）：8 位操作码输入框（大字号等宽）+ 校验按钮；校验失败提示「操作码已失效，请重新从运行日志获取」（每次提交即消耗）
2. 校验通过后按能力分级渲染：
   - **重置管理员密码**（仅环境变量触发且数据库可读）：`a-select` 管理员账号选择（验码前不暴露名单）→ 新密码 + 确认密码 → 确认；完成提示「进程即将退出重启，请移除 RESET_ADMIN_PASSWORD 环境变量后重启容器」
   - **重新初始化**（自动触发场景仅此按钮）：`ConfirmModal` + 二次确认 → 执行全清；完成提示同上（重启后进入 Setup）

**状态分支**：应急期间业务 API 503 → 本页不依赖业务 API，仅调用应急端点与系统状态/站点信息端点。

---

## 四、用户端页面

### 4.1 用户首页（Design1.md 3.3）

**顶部栏**（吸顶，白底，高度 64px）：

- 左侧：站点名称（未设置时默认标题）+ 订阅更新时间戳文案（普通用户=可见订阅最大值；管理员=全池最大值；无可见订阅显示「暂无订阅」）
- 右侧：管理面板入口按钮（仅管理员）、用户名 + 角色标签（`a-tag`：管理员蓝 / 普通灰）+ 所属组名标签、暗色模式切换、退出（Dropdown）
- `<768`：时间戳隐藏至次行，右侧收进头像 Dropdown

**主体 — 平台卡片网格**：`a-row a-col` 栅格，**大屏 3 列 / 中屏 2 列 / 小屏 1 列**。每张平台卡片（`a-card`）：

| 区域 | 内容 |
|------|------|
| 头部 | 平台名称 + 平台描述 |
| 订阅区段 | 普通用户：组选定的一份订阅（名称 + 版本时间戳）；未选定 → 灰色占位「未分配，请联系管理员」且**三按钮隐藏**；有自定义订阅 → `a-alert info`「已被分配自定义订阅」+ 自定义操作按钮；管理员：池内全部订阅折叠列表（非当前平台默认折叠），每份三按钮 |
| 操作按钮组 | 「一键导入」（主按钮；无 scheme 时隐藏）、「复制链接」（弹窗展示 URL + 复制；绑定类 Token 复制时弹警示文案）、「刷新链接」（ConfirmModal 后刷新） |
| 底部 | 「下载客户端」：本地下载 / 链接下载按钮（并存时两个都显示；均无则不渲染） |

**其他区域**：公告栏卡片（有内容才显示，纯文本转义渲染）；分流规则入口卡片（跳转规则浏览页）。

### 4.2 规则浏览页（Design1.md 3.5）

`PageHeader`「分流规则」+ 公开提示 `a-alert warning`「规则内容公开，链接请谨慎分发」；规则卡片网格（同首页栅格）：名称、客户端类型标签、当前版本信息；每卡片两按钮：「下载」（会话凭据，需登录）、「一键导入」（全局规则 Token）。

### 4.3 个人中心 /profile（Design1.md 3.6）

单列卡片 + `a-tabs` 三页签：

1. **基本信息**：`a-descriptions` 展示用户名/邮箱/角色/所属组；「修改用户名」与「修改邮箱」行内编辑按钮（邮箱修改成功后弹「所有设备会话已失效，请重新登录」并跳转登录页）
2. **密码管理**：设置/修改密码表单（已设密码需先验证当前密码；OIDC 用户首次设置免旧密码）
3. **OIDC 绑定**（仅 OIDC 已配置时显示该页签）：已绑定 → 展示绑定状态（subject 脱敏展示）；未绑定 → 「绑定 OIDC」按钮跳转授权

---

## 五、管理面板

### 5.0 面板整体布局与「订阅装配」占位页（Design1.md 3.4 / 3.9）

**布局**：`a-layout` — 左侧 `a-layout-sider` 浅色主题，展开 220px / 收起 64px（当前路由高亮）；右侧内容区白底卡片容器（24px 内边距）。**<768 侧边栏默认收起，汉堡按钮唤出 Drawer 抽屉式菜单**。路由级代码分割，子页面按需加载（Design1.md 第八章 #7）。

**侧边栏菜单**（9 模块 + 1 预留，图标 + 文字，**平铺不分组**）：订阅、用户组、分享、平台、用户、审批中心、规则、面板配置、日志、**订阅装配（预留）**。

**订阅装配占位页**（第一次构建唯一落地形态）：`a-result info` 风格卡片：标题「订阅装配」+ 功能一句话说明（勾选模块动态拼接双语法订阅）+ `a-tag`「即将推出」+ 暂缓提示文案（详见 DesignOnHold.md）；**无任何表单与接口调用**。

### 5.1 订阅管理（3.4.1）

- **列表页**：`PageHeader`（标题 + 「新建订阅」按钮）；主体按平台分组 `a-collapse`（默认全展开），组内订阅行（≥768 表格 / <768 卡片）：名称、当前版本 `a-tag`、关联组标签组、「被 N 个组选定中」蓝色提示标签、操作（版本管理 / 编辑 / 删除）
- **新建订阅弹窗**：`a-modal` 表单（平台 Select、名称、标识输入（实时格式校验 + 唯一性提示）、关联组多选）；**创建成功后引导弹窗两步：「关联用户组 → 每平台选定」（直达组管理按钮 + 「跳过」）**
- **编辑弹窗**：名称 + 关联组多选（平台只读展示；取消关联若被选定则后端拒绝并提示）
- **版本管理页**（订阅详情子页）：版本表格（版本号、创建/更新时间、当前激活 `a-tag`）；操作：创建新版本（文件上传 `a-upload` / 在线文本编辑双页签，文本编辑保存前 YAML 语法提示 `a-alert warning` 不阻断）、切换当前（确认弹窗）、预览（`a-modal` 宽屏 `a-typography-paragraph code` 纯文本，禁 HTML）、删除（ConfirmModal；当前激活版本禁删提示先切换）
- **装配生成入口预留**：创建新版本的操作区**不渲染**装配入口（Design1.md 3.9，UI 暂不暴露）

### 5.2 用户组管理（3.4.2）

- **分发引导**：首管理员首次登录一次性 `a-alert` 引导条「创建第一份订阅」（直达订阅管理，可关闭）；存在 `needs_reselect` 标记的组：行高亮（警示底色）+ 「重新选定」快捷按钮（平台删除不触发）
- **组列表**（双态列表）：组名（预置默认组带 `a-tag` 且无删除操作）、关联订阅数、组内用户数、操作（编辑/重新选定/删除——默认组不可删）
- **组编辑弹窗**：改名（唯一校验）+ **每平台选定**区（每平台一行 `a-select`，从关联订阅中选；选定变更时提示「影响 N 名用户」）+ 关联订阅多选（取消正被选定的订阅 → 拒绝并提示先改选）
- **删除组**：ConfirmModal「组内 N 名用户将自动迁入默认组」

### 5.3 分享订阅管理（3.4.3）

双态列表：名称、创建时间、当前版本、Token 状态（`a-badge`：有效绿 / 已吊销红）；操作：改名、版本管理（复用 5.1 版本组件）、复制分享链接（仅 Token 有效）、刷新 Token（ConfirmModal）、吊销（ConfirmModal 危险）、删除（ConfirmModal 危险）。创建对话框：名称 + 首版本上传（文件/文本页签）→ 成功后显著展示自动生成的标识供复制。

### 5.4 平台管理（3.4.4）

- **列表**：双态列表——名称、标识（只读复制）、scheme 数量、安装包状态（本地已传/外链/无）、操作（编辑/删除危险）；**新建平台成功后引导「为各用户组选定该平台的订阅」**
- **编辑页**（表单较长，用独立页而非弹窗）：名称、描述、scheme 动态列表（`a-form-list`，含 `{url}` 占位符格式提示）、**附加响应头键值对编辑器**（动态行：键 + 值输入，控制字符即时校验报错；`{frontend_url}` 占位符提示）、安装包区（`a-upload` 本地上传 ≤300MB 进度条 + 删除按钮 / 外部链接输入框）、标识只读展示
- **删除平台**：ConfirmModal **逐项列出影响清单**（N 份订阅、M 个 Token、K 份自定义订阅 + 文件不可恢复提示）

### 5.5 用户管理（3.4.5）

- **列表**（双态列表，**后端分页默认 20 条/页 + 搜索框：用户名 / 邮箱模糊匹配**；字段多，卡片态展示前 4 字段）：用户名、邮箱（无邮箱标注灰 tag）、角色标签、所属组标签、来源标注（OIDC/本地创建/自注册）、状态（`a-badge`：待审批橙 / 已激活绿 / 已禁用灰）、自定义订阅平台角标、操作 Dropdown
- **头部批量操作**：「为所有无密码用户发送密码设置链接」按钮（未配置 SMTP 置灰 + 提示；执行后回执提示排除范围）
- **操作 Dropdown 项**：编辑（分组 Select 换组）、角色变更（ConfirmModal；降级提示级联清除显式 Token）、上传自定义订阅（平台 Select + 文件/文本上传；成功展示 custom- 标识）、删除自定义订阅、吊销所有 Token（ConfirmModal 危险）、**设置/重置密码**（专属弹窗：二选一 — 触发重置邮件 / 直接重置；直接重置确认后**展示随机 8 位密码供复制** + 提示会话已失效；待审批账号拒绝并提示去审批中心；管理员本人入口禁用）、清除 OIDC 绑定（无密码用户先弹显著警告）、禁用/启用（禁用自己禁用项置灰）、删除（ConfirmModal；待审批账号与「拒绝」同效果说明）
- **新建用户弹窗**：用户名 + 邮箱 + 密码表单（邮箱冲突即时提示）

### 5.6 审批中心（3.4.6）

双态列表（后端分页）：来源标签、用户名、邮箱、申请时间（UTC 本地化展示）、OIDC claims 展开（`a-typography-paragraph` JSON 只读，如有）；行首 Checkbox 支持批量选择；行操作：**通过**（批量通过=勾选后头部按钮）/**拒绝**（ConfirmModal：账号将删除、邮箱释放，拒绝通知按配置发送）。空状态 Empty「暂无待审批用户」。

### 5.7 规则管理（3.4.7）

双态列表：名称、客户端类型标签（Shadowrocket）、当前版本、Token 生成/上次刷新时间；创建弹窗：名称 + 标识（手填，命名空间交叉校验即时提示）+ 客户端类型 + scheme + 首版本上传；操作：版本管理（复用组件，创建后类型/scheme 只读）、复制链接、刷新 Token、删除。

### 5.8 面板配置（3.4.8）

**页面骨架**：左侧 `a-anchor` 锚点导航 + 右侧分区 `a-card` 堆叠（<768 锚点转为顶部 Select 定位）：

| 分区 | 组件要点 |
|------|---------|
| OIDC 配置 | 提供商参数表单（Secret 脱敏回显 `a-input-password`）+ 「测试连接」+ 「清空 OIDC 配置」（危险确认）+ 高级折叠（前端地址/回调地址，保存时「需重启容器生效」提示）；切换提供商前提示旧绑定失效 |
| OIDC 启用规则 | 审批开关 + 白名单值列表（Tag 输入）+ 声明路径 Select（默认值下拉）；开关开且白名单空 → 显著警告 |
| 本地认证 | 三个开关（本地登录默认开/自注册/自注册审批）；本地登录关且 OIDC 不可用 → 禁止保存 + 显著警告 |
| 验证码 | 提供商 Radio + 双密钥输入 + 启用页面 Checkbox 组；勾选未配密钥 → 校验拦截 |
| SMTP | 服务器/端口/账号/密码（加密）/发件人/TLS + 启用范围 Checkbox 组 + 「发送测试邮件」按钮（结果回执） |
| 站点信息 | 名称（≤50 字符计数）+ ICON `a-upload`（≤2MB，png/jpeg/webp/ico 校验）+ 「删除恢复默认」 |
| 速率限制 | 四个数字输入（登录/注册/找回/下载每分钟）+ 当前 IP 解析策略展示 + auto 模式伪造风险警示 |
| 日志级别 | `a-radio-group` 四级（切换立即生效） |
| 系统公告 | `a-textarea` ≤2000 字符计数（纯文本提示） |
| 调试模式 | `a-switch`（生产警示文案） |
| 配置导入/导出 | 仅 Production 渲染：导出（设置导出密码 ≥8 字符 → 下载）/ 导入（上传 + 密码 + `IMPORT` 确认词 ConfirmModal；完成提示含整体覆盖与迁移核对双重警告）；Dev 模式显示说明文案 |
| 危险操作区 | 红色边框卡片：「一键清空所有数据」→ ConfirmModal 内嵌 `RESET` 确认词输入 + 二次确认 |

### 5.9 日志查看（3.4.9）

`a-tabs` 双页签：

1. **访问日志**：日期 `a-range-picker` 筛选 + 双态列表（**后端分页，默认 20 条/页**；下载类型、用户（可空展示 —）、平台、IP、状态 Badge、失败原因）+ 右上角「清空日志」按钮（ConfirmModal）
2. **实时日志流**：工具栏（级别过滤 Select + 暂停/继续 + 清屏）+ 日志容器（**终端风深色底，不随主题变化**；等宽字体；级别色块：info 白 / warn 黄 / error 红 / debug 灰；滚动跟随；断线自动重连提示；连接数达上限提示文案）；SSE 短期 Token 换取过程对 UI 透明

---

## 六、全局交互约定

| 场景 | 约定 |
|------|------|
| 脱敏回显 | OIDC Client Secret、SMTP 密码一律 `a-input-password` 回显脱敏；操作码/密码类一次性值仅在对话框内展示供复制 |
| 需重启字段 | 前端地址/回调地址保存时 `a-alert warning`「需重启容器生效」；导入覆盖同提示 |
| 防枚举措辞 | 登录失败/忘记密码响应直接展示后端统一文案，前端不做存在性区分 |
| 时间展示 | 后端 UTC → 前端按本地时区格式化（版本时间戳/日志/审批时间统一） |
| 401 处理 | Axios 拦截器统一清除本地凭据并跳转登录页（凭据版本号失效同路径） |
| 一键导入无响应 | scheme 跳转后提供「请确认已安装对应客户端」提示入口 |
| 复制敏感链接 | 用户绑定类 Token 复制时弹出「该链接与您的账号绑定，请勿分享」 |
| 暗色模式 | 全站跟随 ConfigProvider 主题算法，不单独维护暗色样式表；图表/代码块均用主题 token；**实时日志区例外：固定终端深色底**（见 5.9） |

---

## 七、路由表与全局约定

### 7.1 前端路由总表

| 路径 | 页面 | 布局 | 懒加载分组 |
|------|------|------|-----------|
| `/setup` | Setup 首次配置向导 | 无登录态居中 | setup |
| `/login`、`/register`、`/forgot`、`/reset/:token` | 登录 / 注册 / 忘记密码 / 重置密码 | 无登录态居中 | auth |
| `/pending` | 待审批提示页 | 无登录态居中 | auth |
| `/emergency` | 应急恢复页 | 无登录态居中（警示基调） | emergency |
| `/` | 用户首页 | 用户端（顶栏） | home |
| `/rules` | 规则浏览页 | 用户端 | home |
| `/profile` | 个人中心 | 用户端 | home |
| `/admin/subscriptions`、`/admin/subscriptions/:id/versions` | 订阅管理 / 版本管理子页 | 管理面板 | admin-sub |
| `/admin/groups` | 用户组管理 | 管理面板 | admin-sub |
| `/admin/shares`（含版本子页同构） | 分享订阅管理 | 管理面板 | admin-sub |
| `/admin/platforms`、`/admin/platforms/:id/edit` | 平台管理 / 平台编辑页 | 管理面板 | admin-sub |
| `/admin/rules`（含版本子页同构） | 规则管理 | 管理面板 | admin-sub |
| `/admin/users` | 用户管理 | 管理面板 | admin-user |
| `/admin/approvals` | 审批中心 | 管理面板 | admin-user |
| `/admin/settings` | 面板配置 | 管理面板 | admin-settings |
| `/admin/logs` | 日志查看 | 管理面板 | admin-settings |
| `/admin/assembly` | 订阅装配占位页（预留） | 管理面板 | admin-settings |
| `/:pathMatch(.*)*` | 404 | 就近布局 | 主包 |

> 版本管理子页路由模式四类内容资源同构：`/admin/subscriptions|shares|rules|customs/:id/versions`（自定义订阅版本页从用户管理入口进入）。

### 7.2 路由守卫

| 守卫 | 规则 |
|------|------|
| emergency | 系统状态端点返回 emergency=true → 全站强制跳转 `/emergency`（`/emergency` 自身与静态资源除外） |
| configured | configured=false → 任意路径跳转 `/setup`；`/setup` 在已配置时跳 `/login` |
| 登录态 | 无有效凭据访问受保护路由 → `/login`（Design1.md 3.1 前端路由守卫） |
| 角色 | `/admin/**` 实时校验管理员（与后端中间件双保险，后端为准） |
| 登录页跳过 | 已登录访问 `/login` 自动跳 `/`（Design1.md 3.2） |

### 7.3 错误码 → UI 映射（对齐 AGENTS.md 4.8）

| 响应 | 前端展示 |
|------|---------|
| 400 校验错误 | 表单场景定位到字段错误提示；非表单场景 message.error |
| 401 | Axios 拦截器清除本地凭据 → 跳 `/login`（含凭据版本号失效场景） |
| 403 权限不足 | message.error「权限不足」 |
| 409 重复冲突 | message.error 展示后端冲突描述（标识/邮箱/组名占用等） |
| 429 限流 | message.warning「操作过于频繁，请 X 秒后重试」（读 Retry-After）；下载端点 429 对用户端不可见（客户端行为） |
| 500 | 默认通用文案；调试模式开启时展示后端返回详情（Design1.md 3.4.8） |
| 200 业务错误注释块 | 下载预览内容以 `# error:` 开头时，预览弹窗顶部 `a-alert warning` 转人话提示，原文仍完整展示 |

### 7.4 表单与弹窗规范

| 项 | 约定 |
|----|------|
| 弹窗宽度档位 | 普通表单 480px；复杂表单（用户操作/平台编辑弹窗化场景）720px；预览类 960px（或 90% 视口）；**<768 统一 90% 视口或全屏**（Design1.md 3.7） |
| 校验时机 | 输入中实时格式校验（标识/邮箱/控制字符），提交时统一服务端校验回显 |
| 防重复提交 | 异步提交期间按钮强制 loading 并禁用；`ConfirmModal` 确认后同理 |
| Modal 生命周期 | 含表单的 Modal 关闭即重置（destroyOnClose），不残留上次输入 |
| 长表单 | 平台编辑等长表单用独立页面（见 5.4），不塞弹窗 |

### 7.5 空状态文案表（Empty 统一口径）

| 页面 | 文案 | 引导操作 |
|------|------|---------|
| 首页（普通用户无可见订阅） | 暂无订阅 | —（顶栏同口径提示） |
| 订阅管理 | 还没有订阅 | 「新建订阅」按钮 |
| 用户组 | 暂无用户组（预置默认组始终存在，此态仅理论出现） | — |
| 分享订阅 | 还没有分享订阅 | 「创建分享」按钮 |
| 平台 | 暂无平台（默认平台始终存在，此态仅理论出现） | — |
| 用户管理 | 暂无用户（Setup 后首个认证用户产生前） | — |
| 审批中心 | 暂无待审批用户 | — |
| 规则管理 | 还没有规则 | 「创建规则」按钮 |
| 访问日志 | 所选日期范围内无记录 | — |

### 7.6 全局细节

- **顶部进度条**：路由切换细进度条（NProgress 风格，主色蓝），切换完成即隐（Design1.md 3.7）
- **语言与 locale**：仅中文；AntD `ConfigProvider locale` 全局 zh_CN；日期时间经 dayjs 本地时区格式化（后端 UTC，见第六章）
- **图标**：统一 AntD 官方图标集（`@ant-design/icons-vue`）按需引入，不自绘图标
- **排版**：标识/令牌/版本号等代码性内容用 `a-typography-text code` 样式；长文说明用段落 + 列表，不用大段密集文字
- **暗色模式切换入口**：用户端顶栏 / 登录页右上角 / 管理面板内容区右上角，三处均为图标按钮，状态同源（localStorage）
- **无障碍底线**：图标按钮均带 title 提示；危险操作不用颜色作为唯一区分手段（附文字/图标）

---

## 八、变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-08-07 | 初始版本：13 个页面/部件单元的 GUI 结构规格 + 3 张关键页视觉预览 |
| v1.1 | 2026-08-07 | 完善：新增第七章（路由总表/路由守卫/错误码映射/表单弹窗规范/空状态文案/全局细节）；补入 4 项新决策（后端分页策略/终端风日志区/版本独立子页/侧边栏平铺）及用户管理、审批中心、日志页的相应规格 |
