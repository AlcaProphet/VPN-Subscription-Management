# ProdTestList.md — 待用户自行执行的 Production 测试清单

> **定位：** 记录当前无法在本环境自动完成的 Production 模式测试项，由用户后续自行在真实/临时 Production 实例中执行。
> 关联：[R16-09](docs/reports/Issue/Issue3.md)、[R17-07](docs/reports/Issue/Issue3.md)、[R19-05](docs/reports/Issue/Issue4.md)、[Issue12.md](docs/reports/Issue/Issue12.md)（R26 系列人工回归）。

---

## 一、.smoke-test.sh Production 模式验证

- **背景：** `.smoke-test.sh` 中的 v2 导出/导入必须运行在 Production 模式（`app_mode=prod`），否则会因 Dev 模式 403 导致假绿或直接失败。
- **当前状态：** 已新增 [.smoke-test-prod.sh](.smoke-test-prod.sh) 可自动拉起临时 Production 容器并执行四类装配器 + v2 导出/导入往返；仍需要用户在有 Docker 的环境中执行。
- **执行方式：**
  ```bash
  bash .smoke-test-prod.sh
  ```
- 也可以继续使用手动方式（见下）。

### 待用户手动执行

1. 准备一个 Production 模式实例（可通过环境变量 `APP_MODE=prod` 启动，或临时部署）。
2. 执行：
   ```bash
   BASE=http://127.0.0.1:18080 .smoke-test.sh
   ```
3. 确认脚本完整跑完并输出：
   ```text
   === SMOKE ALL DONE ===
   ```
4. 重点检查第 16 步：
   - HTTP 必须为 `200`；
   - 导出文件不能是 JSON 错误；
   - 文件大小 > 0。
5. 可选导入回环：
   ```bash
   SMOKE_IMPORT=1 BASE=http://127.0.0.1:18080 .smoke-test.sh
   ```

---

## 二、Production 专项人工核查

- [ ] v2 导出文件可在新库上正常导入；
- [ ] 导入后实例/节点/独立账号/装配蓝图引用正确；
- [ ] 素材池大数据量同步期间其他 API 仍可响应；
- [ ] 装配生成后“去版本管理激活”不再出现 15s `context canceled`；
- [ ] 重启后平台数据不丢失、平台列表正常（对应 R20-11；原环境已不可用，未能复现，需在真实环境人工验证）；
- [ ] OIDC Mock 登录邮箱可通过；
- [ ] 代理组管理在“订阅装配”Tab 内可用；
- [ ] REALITY 新字段可保存并在链接中正确输出。
- [ ] Clash 下载响应头含 `filename*`/RFC5987 与真实 emoji（无 `\U` 转义）；
- [ ] 覆盖层装配（Merge + Rules/Proxies/Groups Seq）预览、生成、下载内容一致；
- [ ] 节点 URI 批量导入回执正确：合法 2 条 / 非法 1 条，刷新后节点列表可见。

---

## 三、Build11 专项人工核查

- [ ] Production 实例中 `/admin` 概览可访问，服务状态/Checklist/计数/最近待审批/最近访问日志正常；
- [ ] 重置链接四态（valid / missing / used / expired）页面表现正确，已使用/过期链接不渲染密码表单；
- [ ] 非应急状态下 `/emergency` 显示正常提示，不显示操作码输入；
- [ ] 装配预览修改配置后出现“配置已变化，请重新预览”，确认生成被禁用；
- [ ] 设置页六分组可快速定位，未保存提示与离开拦截正确；
- [ ] 手机宽度（≤430px）下复杂表单使用全屏 Drawer，底部操作可见且命中区 ≥44px；
- [ ] 浅色/暗色主题下 AntD 与 Tailwind 颜色一致，无纯黑断层与明显对比度问题。

---

## 四、Build19/Build20 节点编辑器与客户端人工验收

- [ ] 在 Mihomo 1.19.29 / Clash Verge Rev 2.5.2 上导入代表组合（VLESS WS/TLS/REALITY、VMess、Trojan WS/gRPC、SS obfs/v2ray-plugin）；
- [ ] 真实连接后另行记录连接级结论；未完成前相关状态保持 `unverified`；
- [ ] Shadowrocket 保持“待真机验证”；
- [ ] 前端动态表单、分支清空、局部 JSON 应用/放弃、目标检查与 409 重新加载在浏览器中人工走查。
- 已自动化覆盖：未知扩展摘要/新增/替换/清除、分支切换 Toast 提示、未应用 JSON 草稿保存拦截；仍建议在浏览器中人工复核交互。

### Issue13 R27-03 功能关闭清空（代码与自动化已修复，待实机复核）

- [ ] 使用最新前后端及真实协议接口，在 SS/VLESS/VMess 草稿中启用 SMux，填写最大连接数 `7`、Brutal 上下行值；关闭 SMux 后仅保留关闭状态，重开不恢复旧值。
- [ ] 单独关闭 Brutal：上下行和未知子键清空，SMux 最大连接数与其他配置保留；关闭父功能则连同 Brutal 一起清空。
- [ ] 在高级 JSON 应用 `enabled:false` 与残留子参数（包含原来就关闭的情形）；应用后文本与有效草稿均清理，再次应用或重新开启不能恢复旧参数。
- [ ] 已有节点包含父/子功能扩展时，关闭清除对应扩展及未应用替换草稿；公共扩展、无关凭据、其他对象 JSON 草稿保留；关闭再开启后保存和重开均不复活旧内容。
- [ ] 检查不写库，旧检查结果随草稿变化失效；取消编辑不改数据库；折叠面板/切换编辑模式不清空有效配置。成功无诊断目标应显示“未发现诊断”，面板不得因空诊断响应消失。
- [ ] 在固定版本客户端核对重新装配的输出没有关闭功能残留参数；客户端连接结果单独记录，不以本地 YAML 输出测试代替。

### Issue13 R27-04 单一连接编辑入口

- 本轮本地验证（2026-09-03）：最新生产前端构建 + 真实后端 + 临时 Dev 数据库，测试管理员角色/状态/组为 `admin/active/1`。VLESS TCP/WS、TLS/REALITY 的重复入口核对，以及 VLESS/VMess 新建、TLS 保存重开、切到 none 后保存重开均通过；核心走查无控制台错误。测试页面和临时服务已关闭。
- [ ] 在 Production 模式复核 VLESS TCP/WS/REALITY、VMess TLS 下无旧 `WebSocket Path`、`WebSocket Headers` 和顶层 TLS 开关；HTTP/SOCKS5 与 SS v2ray-plugin 的有效 TLS 开关仍可操作。
- [ ] 覆盖 WS→TCP→WS、REALITY→none→TLS 的往返切换、局部 JSON、保存后重开，确认旧参数不恢复；同时复核无诊断与有诊断目标可在检查面板混合显示。
- [ ] 在固定 Mihomo/Clash Verge Rev 与 Shadowrocket 客户端核对导入及真实连接结果，不能以表单或 YAML/URI 自动化断言替代。
- [ ] 独立核验既有 VMess SR URI 的 TLS 语义：本轮实际适配器输出未显式包含 `tls` 查询参数；尚未确认客户端解释，也未修改该映射。R27-04 自动化安全输出断言只覆盖 VLESS Clash/generic/SR 与 VMess Clash/generic。

---

### Issue13 R27-05 表单顺序、分层与集中开关

- 本轮本地验证（2026-09-03）：最新生产前端构建 + 真实 API + 全新临时 Dev 数据库，合成 Admin 实际为 admin/active。四协议核心布局、VLESS REALITY/WS 顺序、WS Early Data 折叠保存重开、SMux 关闭再开、SS 指纹随插件切换清空，以及桌面/375px 手机和明暗主题均已核对；手机无横向溢出，开关卡片 44px、本体 22px。
- [ ] 在隔离 Production 模式覆盖四协议的全部常用组合：选择控件先于依赖参数，VMess/Trojan 不开放 REALITY，运行开关不与数值字段混排。
- [ ] 复核 320px/平板等其它宽度、长标签/长列表、键盘焦点、JSON 未应用保存定位和嵌套区域折叠；折叠不得清空数据，开关修改后旧 JSON 草稿不能重新覆盖新值。
- [ ] 复核非首批 15 协议的对象/对象数组/列表与高级 JSON，确认字段能力未减少；动态数组开关仍位于所属条目内。
- [ ] 在 R27-06～09 修复后联动复核安全选项、插件凭据提示、目标检查与实际客户端输出；当前不能把 R27-05 的表单验收当作这些问题已闭环。

---

## 五、Issue12 R26 系列人工回归核查

> **状态说明：** 以下 7 项已从 [Issue12.md](docs/reports/Issue/Issue12.md) 的人工验收范围迁移至本清单，统一标记为“暂时完成”。“暂时完成”仅表示清单迁移和人工测试准备完成，不代表人工测试已经通过；后续由用户在隔离的 Production/临时实例中逐项手动执行，并在本节补充执行日期、环境、证据和最终结果。

### R26-01 Clash YAML 头部表单

- **状态：** 暂时完成，待人工后续手动处理。
- [ ] 在桌面和移动宽度打开 Clash YAML 头部编辑，确认端口、Geo 数据、DNS、更多参数四个区块默认折叠且可独立展开。
- [ ] 核对个人模板默认值、开关位置、DNS 数组增删、分区级结构化编辑/高级 JSON 和未知顶层键保留；切换 Shadowrocket 头部后确认既有行为未回归。
- **预期：** 页面分区、默认值、编辑模式和响应式布局与 Issue12 修复方案一致，不能因使用高级 JSON 而丢失未知键或破坏既有 `fixed_params` 语义。

### R26-02 检查与正式保存一致性

- **状态：** 暂时完成，待人工后续手动处理。
- [ ] 准备一份带非法 inactive 分支字段或非法字段类型的节点草稿，分别执行“检查当前节点”和正式保存。
- [ ] 准备一份合法草稿，确认检查通过后正式保存成功；核对检查失败时返回的字段路径与保存失败原因一致。
- **预期：** 检查与保存使用同一套字段/当前状态校验语义，不出现检查成功而保存返回 400 的分叉；检查不落库。

### R26-03 URI 导入归一化与非法组合

- **状态：** 暂时完成，待人工后续手动处理。
- [ ] 批量导入合法 URI 2 条、非法组合 URI 1 条，核对逐行成功/跳过回执，并刷新节点列表确认合法节点出现。
- [ ] 检查 VLESS/VMess/Trojan 的 WS、gRPC 等 URI 导入结果只保留规范嵌套路径（如 `ws-opts`、`grpc-opts`），不再同时出现旧顶层 `path`、`host` 别名；确认非法 URI 不落库。
- **预期：** 手动保存与 URI 导入的规范化、当前状态和组合校验结果一致；非法行有可定位错误，不污染节点列表。

### R26-04 Trojan 自定义传输与目标诊断

- **状态：** 暂时完成，待人工后续手动处理。
- [ ] 分别用 Trojan 的自定义 `h2`、`http`、`xhttp` 传输创建/编辑节点并保存，再执行目标检查。
- [ ] 核对保存层保留自定义值；对不能完整表达的目标显示明确诊断或降级警告，不把自定义值误报为普通完整支持。
- **预期：** 表单允许保存 schema 明确允许的自定义传输，Clash/SR/generic 目标按实际能力给出诊断，不能无提示硬拒绝或静默改写。

### R26-05 `security` 与旧 `tls` 兼容投影

- **状态：** 暂时完成，待人工后续手动处理。
- [ ] 准备含历史 `tls=false` 的 VLESS/VMess 节点，编辑时选择 `security=tls`，保存并重新打开；另测 `security=none` 和 REALITY 组合。
- [ ] 核对表单当前状态、节点列表/详情、目标检查和实际输出均以新的 `security` 为权威，不残留与选择冲突的旧 `tls` 语义。
- **预期：** 旧兼容字段不会覆盖新安全选择；保存、重开、检查和输出之间状态一致，关闭/切换安全方式遵循清空规则。

### R26-06 未知顶层协议字段保护

- **状态：** 暂时完成，待人工后续手动处理。
- [ ] 通过创建、更新、检查和 URI 导入入口提交一个不在当前 FieldSchema 中的顶层协议字段。
- [ ] 核对接口返回带字段路径的明确校验错误，节点原有 `protocol_json`、当前状态和扩展数据保持不变；同时确认已登记字段与允许的嵌套未知字段仍按设计工作。
- **预期：** 未知顶层字段不再静默丢弃，也不会未经明确归属进入实际输出；失败操作不产生部分更新。

### R26-07 Xray 节点禁止编辑的错误码

- **状态：** 暂时完成，待人工后续手动处理。
- [ ] 对来源为 Xray 且禁止手动编辑的节点发送 PUT 更新请求，分别构造权限/资源限制、普通字段校验错误和修订冲突请求。
- [ ] 核对禁止编辑返回 HTTP 403，字段校验返回 400，修订冲突返回 409；前端提示应能区分三类错误。
- **预期：** PUT 接口遵循 AGENTS.md 错误码契约，不把 `ErrForbidden` 错误误报为 400。

### 人工执行记录

| 项目 | 执行日期 | 环境/版本 | 结果 | 证据或备注 |
|---|---|---|---|---|
| R26-01～R26-07 | 待填写 | 待填写 | 待人工测试 | 待填写 |

---

## 六、变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-08-21 | 初始版本：记录 Production 冒烟与人工核查清单，待用户后续自行执行。 |
| v1.1 | 2026-08-22 | 新增 `.smoke-test-prod.sh` 自动拉起临时 Production 容器；R20-11 标记为“原环境不可用，未能复现，转人工验证”。 |
| v1.2 | 2026-08-28 | 新增 Build11 专项人工核查：管理员概览、重置四态、应急正常态、装配 stale 预览、设置六分组、手机 Drawer、双主题一致性。 |
| v1.3 | 2026-09-02 | 新增 Build19/Build20 节点编辑器与客户端人工验收待办；自动测试通过不代表客户端连接验收完成。 |
| v1.4 | 2026-09-02 | 记录前端收口：未知扩展摘要/增删改、分支切换提示、未应用 JSON 草稿保存拦截已实现并有自动化测试；客户端真机导入/连接仍为人工待办。 |
| v1.5 | 2026-09-03 | 从 Issue12 迁移 R26-01～R26-07 的人工回归范围，新增节点表单、检查/保存、URI、Trojan 自定义传输、security/tls、未知字段和 Xray 403 共 7 项人工核查；统一标记为“暂时完成”，待用户后续手动执行。 |
| v1.6 | 2026-09-03 | 新增 Issue13 R27-03 修复后的真实 schema 浏览器交互、SMux/Brutal 关闭再开启、JSON/扩展及客户端输出人工复核；不将自动化通过标为实机通过。 |
| v1.7 | 2026-09-03 | 记录 R27-04 最新构建本地浏览器核心流程通过，保留 Production/客户端与往返切换补充验收；新增 VMess SR URI 缺少显式 TLS 参数的独立核验项。 |
| v1.8 | 2026-09-03 | 记录 R27-05 本地四协议核心布局、集中开关、保存回显、SS 指纹清空与手机/主题核验；保留 Production、更多设备/协议及 R27-06～09 联动人工验收。 |
