# ProdTestList.md — 当前人工核验清单

> 本文件只记录仍需用户亲自执行或修复后重新执行的 Production、浏览器和真实客户端人工核验。已完成且未发现问题的项目从清单移除；工程问题和自动化证据写入对应的 Issue/Build 文档。

## 1. R26-07 Xray 节点禁止编辑的错误码

> 这里只保留尚未完成的人工 API/UI 核验。不得使用真实凭据或生产数据。

- **状态：** ☐ 待人工核验。
- **准备：** 使用隔离 Production 或等价真实 API 环境，准备一个来源为 `xray`、由实例维护且不可手工编辑的节点，以及一个可编辑的 `manual` 节点用于构造 `400`/`409` 对照请求；记录节点 ID、实例状态、管理员角色、应用版本和请求时间。
- **步骤：**
  1. 对 Xray 节点发送其他字段合法的 `PUT /api/admin/nodes/:id`，确认返回 `403` 且数据未改变。
  2. 对 manual 节点构造协议、端口或必填字段等非法请求，确认返回 `400` 并记录前端提示。
  3. 对 manual 节点使用过期 `base_revision` 更新，确认返回 `409` 并带当前修订信息。
  4. 从节点详情/编辑界面触发上述失败路径，确认页面分别显示禁止编辑、请求校验错误和修订冲突，不混为同一种错误。
- **预期：** 三类失败均不写入节点数据；Xray 节点不因对照请求而期待 `400`/`409`。

### 执行记录

| 项目 | 执行日期 | 环境/版本 | 结果 | 证据或备注 |
|---|---|---|---|---|
| Xray PUT `403` / manual `400` / `409` 与前端提示 | 待填写 | 待填写 | 待人工测试 | 待填写 |

## 变更记录

| 日期 | 说明 |
|---|---|
| 2026-09-14 | 清理已完成人工项目和历史展开，仅保留 Build11 R30-01、R26-07 两项当前核验。 |
| 2026-09-14 | 补记重置链接四态现行合同（Issue16 DC-01）：一次性、1 小时 TTL、`used=1` 记录保留、过期历史由清理任务处理。 |
| 2026-09-14 | 用户确认 Build11 邮件重置链接四态人工核验完成；移除 Issue16 R30-01 当前人工项目。 |
| 2026-09-14 | 用户更正 R26-07 尚未完成；恢复本节当前人工核验项目。 |
