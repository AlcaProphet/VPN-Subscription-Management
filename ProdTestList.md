# ProdTestList.md — 当前人工核验清单

> 本文件只记录仍需用户亲自执行或修复后重新执行的 Production、浏览器和真实客户端人工核验。已完成且未发现问题的项目从清单移除；工程问题和自动化证据写入对应的 Issue/Build 文档。

## 当前人工核验项目

### Issue17 OIDC 修复（代码与自动化隔离验证完成，真实 IdP/浏览器/反代人工项待执行）

> **前置与安全边界：** 仅使用隔离测试部署、隔离 IdP tenant/client、测试账号与测试 Secret；不要在真实 Production 数据或真实用户账号上制造损坏配置、重放旧回调或执行并发竞态。开始前保留一个已验证可用的本地管理员登录通道，并记录测试站点的公开前端地址、实际回调地址、提供商类型及 IdP 端登记的 redirect URI。下列结果须逐项记录，未执行项保持待验收。

1. [ ] **PT-OIDC-01 真实提供商基础登录（R31-02/R31-06）：** 在 Production 模式配置至少一个受支持的真实提供商，确认 Base URL 为 HTTPS、管理端测试连接成功；用真实浏览器完成授权码 + PKCE 登录及已有账号绑定，确认 discovery、授权、token、JWKS、回调、登录 ticket/会话和用户身份均正常。检查应用与反代日志不出现 Client Secret、token、原始密文或签名密钥。Production 中不得出现或使用 mock 登录入口；Dev mock 回归须另用隔离 Dev 实例执行。
2. [ ] **PT-OIDC-02 提供商切换与进行中授权隔离（R31-03）：** 准备字段和测试账号可明确区分的两个提供商配置；切换时确认只加载目标提供商自己的 Base URL/Realm/Client ID，Secret 始终为空且“已配置”状态属于目标。验证未保存草稿的丢弃提示、未改字段时空 Secret 可复用、任一非 Secret 字段修改后空 Secret 保存被拒且原配置不变、显式新 Secret 保存后新登录成功。发起提供商 A 授权后切换并保存 B，再完成 A 的旧回调，应失败且不得用 B 的地址/Secret、不得绑定账号或签发 ticket/会话；切回 A 后新发起登录应成功。
3. [ ] **PT-OIDC-03 损坏状态与恢复界面（R31-04）：** 只在可丢弃的隔离数据库副本中分别准备 JSON 损坏、Secret 损坏和 `signing_key` 故障。真实浏览器确认 JSON 可解析时保留非 Secret 字段、坏 JSON 不猜测字段、Secret/密文/签名密钥始终不回显；坏 Secret 必须输入新 Secret 才能保存并恢复真实 IdP 登录；`signing_key` 故障显示独立系统错误并禁用保存、测试与 Secret 重填，不能由 OIDC 页面生成或替换密钥。完成后销毁该测试副本，不把损坏数据带回正常环境。
4. [ ] **PT-OIDC-04 地址即时生效与反代边界（R31-05）：** 在真实浏览器和至少一种 HTTPS 终止/非默认域名反代部署中验证：保存 `frontend_url` 或独立 `callback_url` 后无需重启，新授权立即使用新地址；独立回调优先，点“恢复推导”并保存后改用“前端地址 + 回调路径”；清除草稿在切换提供商后仍保留。授权进行中改址时，旧流程仍使用发起时 redirect URI，新流程使用新地址。独立回调与前端地址跨 host/协议时须看到 state Cookie 警示，并从回调地址所在 host/协议发起登录；记录实际成功或因 Cookie 边界失败的结果，不把警示展示等同于跨 host 登录成功。
5. [ ] **PT-OIDC-05 Production mock 与导入拒绝（R31-06）：** 在可恢复的隔离 Production 部署中，确认 Setup、管理端保存、测试连接、登录/绑定直接入口和公开状态均拒绝或隐藏 mock；分别用 v1/v2、Setup/管理端导入含 `oidc_provider_type=mock` 或任意 `oidc_params_mock` 键（含空值）的测试文件，确认整体拒绝且现有配置不变。该项只验收现行配置导入，不包含尚未实施的 Design5 整站恢复。
6. [ ] **PT-OIDC-06 停用、重启用与流程失效（R31-07）：** 选择“暂未启用”后先确认仅形成未保存草稿及丢弃提示；保存停用并刷新，确认仍停用但保留 provider、各提供商参数和既有身份绑定，公开登录页、个人绑定入口及直接 API 均不能发起新流程。停用前已发起的回调和未兑换 ticket 在停用后、以及重新启用后的原 TTL 内均须失败且不能绑定或签发会话；停用前已有会话仍可用。重新启用原提供商和另一提供商后分别发起新登录；另核对“清空配置”会清参数而“停用”不会。本地登录关闭时停用应被拒且配置不变；使用两个独立浏览器/API 客户端交错提交停用与关闭本地登录、绑定或 ticket 兑换，确认不会出现双登录入口关闭，也不会在停用提交后产生新绑定或新会话。

> Issue17 的 HTTP 降级、恶意 discovery endpoint、凭据重定向、损坏矩阵、故障注入及并发顺序已有隔离自动化覆盖；人工核验聚焦真实 IdP、真实浏览器、实际反代/Cookie 行为和用户可见状态，不在外部真实服务上主动构造 Secret 泄露测试。

### Build28 邮件内容定制（工程/自动化与隔离 smoke 已完成，正式人工项待执行）

> **核验结论（2026-09-15）：** 已复跑 Build28 代码与自动化门禁，工程修复完成；以下项目缺少真实 SMTP、收件箱、邮件客户端、浏览器或实际反代证据，保持待验收。

1. [ ] **PT-B28-01 真实 SMTP 五分支投递：** 使用管理员配置的隔离 SMTP 测试环境，分别触发密码重置、审批通过、审批拒绝、本地欢迎、OIDC 欢迎，确认发送成功到测试收件箱。
2. [ ] **PT-B28-02 收件箱内容与开关：** 核对五分支主题/正文与管理员保存内容一致；审批通过只到达一封且由 `approval_notify` 控制；仅开启 `welcome` 的既有站点不再收到审批通过邮件；SMTP 连接测试邮件仍为固定文案。
3. [ ] **PT-B28-03 邮件客户端链接表现：** 在至少一个真实邮件客户端中确认 HTML 版本锚点的 href、可见文字与纯文本版本 URL 完全一致，完整 URL 不被省略、改写或附加别名；邮件无图片、CID、附件或远程资源。
4. [ ] **PT-B28-04 真实浏览器设置页：** 确认邮件内容卡片位于 SMTP 卡片及测试区之后，五分支切换、光标插入变量、HTML/纯文本预览、sandbox iframe、保存/恢复默认、damaged 警示、未保存切换提示与页面离开保护可用。
5. [ ] **PT-B28-05 边界与快照复验：** 验证直接书写的完整 URL 仅按空白 token 保守识别，紧邻标点、括号或混合文字时保持普通文本；非法 URL 在保存/预览时被明确拒绝；模板往返与备份快照在测试环境仍保留覆盖：备份只验证从 tar.gz 提取 `app.db` 后五个模板键值保留，Build28 不含恢复工具，不执行真实备份恢复。
6. [ ] **PT-B28-06 跨反代/实际部署差异：** 在至少一种反向代理、HTTPS 终止和非默认域名部署下，确认邮件内容卡片、预览 iframe、保存/恢复 API、页面离开保护及邮件内完整 URL 仍指向公开前端地址；核对 Cookie/Authorization、同源策略与安全头差异。

> Build28 的工程自动化、构建、隔离 SQLite 与本地 SMTP mock 结果见 [docs/reports/Build/Build28.md](docs/reports/Build/Build28.md)，不得与上述真实人工核验混记。

Xray 相关人工核验已拆分至 [XrayRelated1.md](XrayRelated1.md)；当前聚焦基础模式，高级模式暂不跟进。

## 变更记录

| 日期 | 说明 |
|---|---|
| 2026-09-14 | 清理已完成人工项目和历史展开，仅保留 Build11 R30-01、R26-07 两项当前核验。 |
| 2026-09-14 | 补记重置链接四态现行合同（Issue16 DC-01）：一次性、1 小时 TTL、`used=1` 记录保留、过期历史由清理任务处理。 |
| 2026-09-14 | 用户确认 Build11 邮件重置链接四态人工核验完成；移除 Issue16 R30-01 当前人工项目。 |
| 2026-09-14 | 用户更正 R26-07 尚未完成；恢复本节当前人工核验项目。 |
| 2026-09-14 | 将 R26-07 完整迁移至 [XrayRelated1.md](XrayRelated1.md)；本清单不再重复承载 Xray 相关项目。 |
| 2026-09-15 | 新增 Build28 邮件内容定制正式 SMTP、收件箱、客户端链接、真实浏览器与边界复验人工项；工程/自动化与隔离 smoke 见 [docs/reports/Build/Build28.md](docs/reports/Build/Build28.md)。 |
| 2026-09-15 | 修正 Build28 备份人工项为快照校验并明确恢复工具不在范围；新增跨反代/实际部署差异人工项。 |
| 2026-09-15 | 核验 Issue17 R31-02～R31-07 已完成代码与自动化隔离验证；新增真实 IdP、提供商切换、损坏恢复界面、地址/反代、Production mock 拒绝及停用/重启用六组人工验收步骤。 |
| 2026-09-15 | 复跑 Build28 后端 build/vet/全量与定向测试、前端 build/全量测试均通过；将六组正式人工项统一编号为 PT-B28-01～06 并保持未完成。 |
