# ProdTestList.md — 待用户自行执行的 Production 测试清单

> **定位：** 本文件只记录当前仍需要用户亲自执行或修复后重新执行的 Production、浏览器和真实客户端人工核验。已完成且未发现问题的人工项目不再列入当前清单；工程性问题、自动化证据门禁和人工测试中发现的问题分别由 [Issue14.md](Issue14.md) 与 [Issue15.md](Issue15.md) 跟踪。
> 关联：[Build21.md](Build21.md)、[Issue13.md](docs/reports/Issue/Issue13.md)、[Issue14.md](Issue14.md)、[Issue15.md](Issue15.md)、[Issue12.md](docs/reports/Issue/Issue12.md)（R26 历史人工回归来源）。

---

## 一、当前说明

- PT-28-01～PT-28-05（Production smoke、Mihomo/Clash Verge、Shadowrocket、Production 浏览器、动态插件输入、控制台、保存重开及移动端交互）已由用户于 2026-09-09 确认完成，未发现问题，已从本清单移除。
- R27-01～R27-04 及其对应的核心浏览器/客户端人工项目不再作为当前待办；R27-05 的额外尺寸、焦点与折叠边界仍待补充核验。
- R26-01～R26-06 的人工项目不再作为当前待办；R26-07 Xray 节点 PUT 错误码核验按用户要求在本清单完整保留。
- R29-03 批量 URI 导入回执 UI 已完成代码修复、自动化验证和用户真机验证，已从当前待人工复验项目移除；闭环记录见 [Issue15.md](Issue15.md) R29-03。
- R29-02 素材池跨池同步已完成代码修复、前后端回归、用户提供 URL 的自动化集成验证，并由用户于 2026-09-10 确认问题已修复，已从当前待人工复验项目移除；闭环记录见 [Issue15.md](Issue15.md) R29-02。
- R29-08 素材池同步取消后继续运行的问题已完成原子终态与取消/完成竞态修复、受控及真实 URL 自动化验证，并由用户于 2026-09-10 确认实际运行测试通过；该问题已直接闭环，不加入当前待人工复验项目，记录见 [Issue15.md](Issue15.md) R29-08。
- R29-07 节点清空提示位置与 Select 切换滚动跳顶已完成代码修复、自动化验证和用户浏览器核验，已从当前待人工复验项目移除；本次确认不替代 R29-06 其余控件语义、移动端、主题和保存重开范围，闭环记录见 [Issue15.md](Issue15.md) R29-07。
- `.smoke-test-prod.sh` 的正式脚本工程复验已完成，属于 Build21/Issue14 的工程证据，不在本清单重复作为待人工项目。

## 二、Issue15 问题修复后的人工复验

以下项目来自 [Issue15.md](Issue15.md) 的实际人工测试报告。当前记录的是修复或澄清后的重新核验，不把原先已观察到的问题写成“通过”。

### R29-04 手机主页管理入口

- **状态：** ◐ 代码、自动化与 416×928 浏览器人工核验已通过；待用户后续安排真实手机/Production 复验。
- **核验内容：** 在不超过 430px 的手机视口、实际 Production 构建和主流移动浏览器中打开主页，确认进入管理面板的入口可见、可点击，并继续核对页面无横向溢出。
- **预期：** 手机宽度下主页仍提供可见、可操作的管理面板入口。
- **关联：** [Issue15.md](Issue15.md) R29-04。

### R29-06 节点动态表单选择、数字与列表控件

- **状态：** ☐ 代码与自动化验证完成，待用户真实运行核验。
- **核验内容：** 在真实运行的“新建节点/编辑节点”中检查 VLESS、VMess、Trojan、SS 和 WireGuard：推荐字段应与“协议”一样呈现带箭头的按钮式选择框，点击整框展开且不出现搜索输入；`allow_custom=true` 才出现“其他（自定义）”，输入未应用时不得改写实际字段，保存与目标检查均被阻止，应用后分支清空仅触发一次，取消恢复实际值。另核对旧自定义值回显、空值候选、ALPN 推荐/自定义有序追加、可选数字未设置与显式 0、WireGuard 整数列表新增不自动产生 0。
- **显示范围：** 同时覆盖桌面、375px、浅色和深色主题；确认独立自定义/列表草稿区不横向溢出，键盘 Enter/Escape/Tab 与输入法不会误提交。
- **预期：** 标准下拉外观和交互与协议选择一致；“其他”仅为前端状态，不进入 API/数据库/当前状态/输出；未应用草稿不可保存或检查，分支切换和取消编辑会丢弃所属草稿；数字和列表不产生隐式 0。
- **关联：** [Issue15.md](Issue15.md) R29-06；[Design4.md](Design4.md) §4.1。

---

## 三、Production 专项人工核查

- [ ] 修复 R29-01 后，在未启用高级模式的 Production 实例中使用 v2 导出文件导入新库；确认不再错误要求同时输入 `IMPORT` 和 `RESET`，且不提示高级模式已开启。
- [ ] 导入成功后核对实例、节点、独立账号和装配蓝图的引用关系、数量及可用状态；本项与 R29-01 的导入失败问题一并复验。
- [ ] 在真实可用环境复核重启后平台数据不丢失、平台列表正常（R20-11；原环境不可用，尚未完成真实环境核验）。
- [ ] 核对 OIDC Mock 登录邮箱可以完成登录，并记录使用的隔离环境、版本和结果。

## 四、Build11 专项人工核查

- [ ] 核对重置链接四态（valid / missing / used / expired）的页面表现；已使用或过期链接不得渲染密码表单。

## 五、R27 后续人工核查

### R27-05 额外尺寸与交互边界

- **状态：** ☐ 待人工核验。
- [ ] 复核 320px、平板等其他宽度，以及长标签、长列表、键盘焦点、JSON 未应用保存定位和嵌套区域折叠。
- [ ] 确认折叠不会清空数据，开关修改后旧 JSON 草稿不会重新覆盖新值。
- **预期：** 所有控件可见、可操作且无横向溢出；错误定位明确；折叠只改变显示，不改变有效配置。

## 六、R26-07 Xray 节点禁止编辑的错误码

> 本节完整迁移自已归档的 [Issue12.md](docs/reports/Issue/Issue12.md) R26-07 人工回归范围。R26-07 的代码修复和自动化回归已完成；本节只保留尚未完成的人工 API/UI 核验，不重新打开已关闭的工程问题。

- **状态：** ☐ 待人工核验。
- **背景：** Xray 来源节点由实例检测维护，禁止管理员通过手工节点编辑入口修改；人工核验需同时覆盖权限/资源限制、普通字段校验和修订冲突三类响应。
- **准备：** 使用隔离 Production 或等价真实 API 环境，准备一个来源为 `xray`、由实例维护且不可手工编辑的节点，以及一个可编辑的 `manual` 节点用于构造 400/409 对照请求；记录节点 ID、实例状态、管理员角色、应用版本和请求时间。不得使用真实凭据或生产数据。
- **步骤 1：禁止编辑：** 对该 Xray 节点发送一个其他字段合法的 `PUT /api/admin/nodes/:id` 更新请求，确认服务端拒绝手工编辑且数据未改变。
- **步骤 2：普通字段校验：** 对 `manual` 节点构造协议、端口或必填字段等非法请求，记录 HTTP 状态、错误码/消息和前端提示。
- **步骤 3：修订冲突：** 对 `manual` 节点使用过期的 `base_revision` 发送更新请求，记录 HTTP 状态、冲突标识和当前修订号。
- **步骤 4：前端区分：** 从节点详情/编辑界面触发上述可复现的三类失败路径，确认页面分别显示禁止编辑、请求校验错误和修订冲突，不把它们合并成同一种错误。由于更新服务先判断节点来源，400/409 对照请求使用 `manual` 节点，不在 Xray 节点上强行期待这两种响应。
- **预期：** 禁止编辑返回 HTTP `403`；字段校验错误返回 HTTP `400`；修订冲突返回 HTTP `409` 并带当前修订信息；三类失败均不写入节点数据。
- **实现依据：** [Issue12.md](docs/reports/Issue/Issue12.md) R26-07 的工程修复记录；当前代码已有 `TestNodeUpdateForbiddenReturns403`、修订冲突和字段校验回归，人工核验不以自动化结果替代。

### R26-07 人工执行记录

| 项目 | 执行日期 | 环境/版本 | 结果 | 证据或备注 |
|---|---|---|---|---|
| Xray PUT 403/400/409 与前端提示 | 待填写 | 待填写 | 待人工测试 | 待填写 |

---

## 七、Build22 来源状态与装配回执人工核验

> Build22 Step 8/9 的代码与自动化验收已完成；以下项目仍必须在用户真实运行环境执行。自动化结果不得外推为真实浏览器、真实手机或真实客户端结论；未执行项目不标记为通过。

### A. Build22 Step 8 来源状态与 pending 操作

- **状态：** ☐ 代码与自动化验收完成，待用户真实浏览器核验。
- **准备条件：** 使用隔离的 Production 或等价真实运行环境，不使用真实凭据和真实业务数据；准备包含 never synced、active、pending、failed、failed+old active 且最终恢复 active 的来源样例（可用隔离环境构造 URL 来源、手工条目、同步历史与待激活快照）；记录应用版本、环境地址和视口。
- **操作步骤：**
  1. 在 1440px 桌面视口打开素材池详情，核对每个 URL 来源卡片：`display_url`、`source_mode`、active/pending/failed/待同步主徽标；
  2. 核对 never synced、active、pending、failed 四类主状态；确认主徽标只由最近一次尝试决定；
  3. 构造“最近失败但旧 active 仍生效”，确认同时显示“同步失败，继续使用旧活动快照”和旧 active 摘要；再让该来源同步成功后，确认主状态恢复 active、历史 failed 不再控制主徽标；
  4. 核对 input、recognized、accepted、excluded、rejected、duplicates；核对 v1 stats 的 evidence codes、rule_counts、comparison、decision；对 `schema_version=0` 确认显示“历史统计不可用”且不解释为零变化；
  5. 核对长诊断的换行/截断，确认页面无明显横向溢出；
  6. 对 pending 来源点击“激活”，确认 ConfirmModal 展示旧 active 与新 pending 的 input、accepted、format、profile 和诊断差异；确认激活后来源状态与素材条目刷新，`activated_at` 只显示服务端值；
  7. 对另一个 pending 来源点击“丢弃”，确认丢弃后来源状态与素材条目刷新且当前 active 不受影响；
  8. 确认状态卡片只显示脱敏后的 `display_url`；进入编辑后仍为原始 URL，保存后原始 URL 不被脱敏展示值覆盖；
  9. 打开浏览器控制台确认无新增异常；将视口切换到 390px 窄屏，重复 pending 操作、诊断卡片和长文本检查。
- **预期结果：** 主状态由 latest_attempt 决定；old active 与最新失败可同时辨认；历史 failed 不会永久标红；诊断无明文敏感凭据；桌面和窄屏可操作且无明显横向溢出；`display_url` 不会提交回编辑 API。
- **人工执行记录：**

  | 项目 | 执行日期 | 环境/版本 | 视口 | 结果 | 证据或备注 |
  |---|---|---|---|---|---|
  | 四类主状态与 latest_attempt | 待填写 | 待填写 | 1440px | 待人工核验 | 待填写 |
  | failed + old active 与恢复 | 待填写 | 待填写 | 1440px | 待人工核验 | 待填写 |
  | v1 stats / version 0 / 长诊断 | 待填写 | 待填写 | 1440px | 待人工核验 | 待填写 |
  | pending 激活确认与 activated_at | 待填写 | 待填写 | 1440px | 待人工核验 | 待填写 |
  | pending 丢弃与刷新 | 待填写 | 待填写 | 1440px | 待人工核验 | 待填写 |
  | display_url 与编辑原始 URL 边界 | 待填写 | 待填写 | 1440px | 待人工核验 | 待填写 |
  | 控制台无新增错误 | 待填写 | 待填写 | 1440px | 待人工核验 | 待填写 |
  | 窄屏可操作与无横向溢出 | 待填写 | 待填写 | 390px | 待人工核验 | 待填写 |

- **自动化已证明：** `frontend/tests/pool-detail.spec.ts` 的 22 项组件测试覆盖状态卡片、pending 展示/确认/激活/丢弃、激活后状态与条目刷新、服务端 activated_at、v1 stats/version 0、failed+active、恢复 active、never_synced、display_url 不进入编辑提交、API/操作错误与长诊断样式；后端状态 API 与脱敏字段由 Go 测试固定。
- **自动化不能证明：** 1440px/390px 实际浏览器渲染与真实布局溢出、真实交互和触控、真实控制台无异常、真实运行环境中 pending/失败状态的实际生成与迁移。

### B. Build22 Step 9 装配回执

- **状态：** ☐ 代码与自动化验收完成，待用户真实浏览器核验。
- **准备条件：** 使用隔离的 Production 或等价真实运行环境；准备能产生 direct_output、equivalent_conversions、skipped_unsupported、target_validation_failed 和 final_output 差异的装配输入；记录应用版本、目标类型与环境。
- **操作步骤：**
  1. 在真实运行页面完成一次 preview，确认预览步骤在警告/跳过信息附近显示完整六项回执；
  2. 与当前 preview 接口响应逐项核对数字一致；
  3. 修改目标、素材池或自定义规则，确认旧回执消失且预览进入 stale 状态；
  4. 重新预览后执行 generate，确认生成成功页显示本次 generate 返回的回执；构造 preview 与 generate 回执不同的场景，确认页面以 generate 为准、不混用旧 preview 回执；
  5. 在回执缺省时确认页面保持兼容且不显示虚假零值；
  6. 核对桌面和窄屏布局无横向溢出，控制台无新增错误。
- **预期结果：** 过期回执不冒充当前结果；preview 与 generate 各自使用对应响应；缺省 receipt 保持兼容；页面显示与真实接口响应一致。
- **人工执行记录：**

  | 项目 | 执行日期 | 环境/版本 | 视口 | 结果 | 证据或备注 |
  |---|---|---|---|---|---|
  | preview 六项回执与接口一致 | 待填写 | 待填写 | 1440px | 待人工核验 | 待填写 |
  | 目标/输入/素材池/规则变化后旧回执消失 | 待填写 | 待填写 | 1440px | 待人工核验 | 待填写 |
  | generate 使用本次 receipt 不混用 | 待填写 | 待填写 | 1440px | 待人工核验 | 待填写 |
  | 缺省 receipt 兼容 | 待填写 | 待填写 | 1440px | 待人工核验 | 待填写 |
  | 窄屏布局与控制台 | 待填写 | 待填写 | 390px | 待人工核验 | 待填写 |

- **自动化已证明：** 后端原始 JSON 合同测试固定 generate 响应中 receipt 六项 snake_case 字段及数值来自本次 Render；前端 25 项定向测试覆盖 preview 回执保存/传递/渲染、stale 清除、generate 以本次响应为准和缺省 receipt 兼容；前后端构建通过。
- **自动化不能证明：** 真实运行页面完成 preview/generate 的操作结果、真实接口响应与页面展示一致性、桌面/窄屏实际布局和控制台状态。

---

## 八、变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v2.11 | 2026-09-10 | 新增 Build22 Step 8 来源状态/pending 操作与 Step 9 装配回执的人工核验章节：记录代码与自动化已完成、实际浏览器/真实环境项目待用户核验、每项准备条件/操作/预期/记录表及自动化证据边界；未将自动化结果写成人工通过。保留 R29-04、R29-06、R27-05、R26-07 等既有待办。 |
| v1.0 | 2026-08-21 | 初始版本：记录 Production 冒烟与人工核查清单，待用户后续自行执行。 |
| v1.1 | 2026-08-22 | 新增 `.smoke-test-prod.sh` 自动拉起临时 Production 容器；R20-11 标记为“原环境不可用，未能复现，转人工验证”。 |
| v1.2 | 2026-08-28 | 新增 Build11 专项人工核查。 |
| v1.3 | 2026-09-02 | 新增 Build19/Build20 节点编辑器与客户端人工验收待办。 |
| v1.4 | 2026-09-02 | 记录前端收口与客户端真机导入/连接人工边界。 |
| v1.5 | 2026-09-03 | 从 Issue12 迁移 R26-01～R26-07 的人工回归范围，统一标记为待用户后续手动执行。 |
| v1.6 | 2026-09-03 | 新增 Issue13 R27-03 修复后的真实 schema 浏览器交互、功能清空、JSON/扩展及客户端输出人工复核。 |
| v1.7 | 2026-09-03 | 记录 R27-04 浏览器核心流程与 VMess SR URI 的独立核验边界。 |
| v1.8 | 2026-09-03 | 记录 R27-05 本地核心布局通过，同时保留更多设备、焦点和折叠边界。 |
| v1.9 | 2026-09-08 | 同步 Build21 Step 15 的自动化结果，保留真实客户端人工边界。 |
| v2.0 | 2026-09-08 | 从 Build21 Step 14 和 Issue13 集中整理用户需要亲自执行的项目，工程问题转由 Issue14 跟踪。 |
| v2.1 | 2026-09-09 | 同步 R28-01 工程修复的自动化结果。 |
| v2.2 | 2026-09-09 | 同步 R28-03 工程修复与正式 Production smoke 原样全链路通过结果。 |
| v2.3 | 2026-09-09 | 关联 Issue15 R29-03，记录批量导入回执 UI 问题。 |
| v2.4 | 2026-09-09 | 将批量 URI 导入功能结果与回执 UI 结果拆开记录。 |
| v2.5 | 2026-09-09 | 用户确认 PT-28-01～PT-28-05 完成且未发现问题，移除已完成项目。 |
| v2.6 | 2026-09-09 | 清理已完成人工核验，只保留当前待人工复验；恢复 R20-11 的真实环境核验，并完整迁移 R26-07 Xray 的 403/400/409、前端提示和不写入核验范围。 |
| v2.7 | 2026-09-09 | 新增 R29-06 修复后的真实运行核验：标准推荐下拉、自定义应用/取消与草稿阻断、数字未设置/显式 0、列表推荐/自定义/整数新增，并保留桌面/375px/主题/键盘与输入法边界。 |
| v2.8 | 2026-09-09 | 同步 R29-04 闭环结果：管理员手机主页入口、44px 触控高度和文案居中已完成代码、自动化及 416×928 浏览器核验；仅保留真实手机/Production 复验。 |
| v2.9 | 2026-09-09 | 同步 R29-03 闭环结果：批量 URI 导入回执已完成桌面固定布局、长文本局部断行、手机卡片和区域滚动修复，并经自动化与用户真机验证通过；从当前待人工复验项目移除。 |
| v2.10 | 2026-09-10 | 同步 R29-07 闭环结果：节点清空说明已改为表单内持久提示，Select 正常选值不再因错误回焦导致弹窗滚动跳顶；代码、定向/全量前端测试、生产构建及用户浏览器核验通过。R29-06 其余人工范围保持待办。 |
