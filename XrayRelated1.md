# XrayRelated1.md — Xray 相关问题跟踪

> **文档定位：** 集中跟踪 Xray 节点、Xray 实例维护及其管理端 API/UI 行为相关的问题、人工核验和后续处理。当前项目聚焦基础模式完善；高级模式暂不跟进，除非后续获得单独确认并更新本文件范围。
> **当前状态：** R26-07 的工程修复与自动化回归已完成，Xray 节点禁止编辑的人工 API/UI 核验尚未完成。
> **关联文档：** [AGENTS.md](AGENTS.md)（唯一强要求）、[TODOLIST.md](TODOLIST.md)、[ProdTestList.md](ProdTestList.md)、归档的 [Issue12.md](docs/reports/Issue/Issue12.md)。

## 一、范围与跟踪规则

1. 本文件是 Xray 相关问题的当前跟踪入口；Xray 问题不再重复登记在 `TODOLIST.md` 或 `ProdTestList.md`。
2. 当前优先完善基础模式中的 Xray 节点管理、错误码契约、不可编辑边界和基础 API/UI 行为。
3. 高级模式、复杂出站组合及未在当前基础模式合同中确认的扩展能力暂不跟进；不得将其标记为已实现、已验证或当前待办。
4. 工程实现、自动化验证、隔离环境验证、浏览器/API 人工核验分别记录，不以其中一类证据替代其他验收层级。
5. 新增 Xray 问题须补充编号、现象、影响范围、处理状态和对应验收边界；完成后保留历史结论，不在其他当前清单重复展开。

## 二、问题与处理状态

### R26-07：Xray 节点禁止编辑的错误码

- **来源：** API 错误码核验；AGENTS.md §四.8 规定 403 表示权限不足。
- **现象：** 节点检查接口遇到禁止编辑的 Xray 节点时正确返回 403，但节点更新 PUT 接口把 `ErrForbidden` 与 `ErrBadRequest` 一并映射为 400，调用方无法区分请求校验错误和权限/资源状态限制。
- **影响范围：** 管理端前端和外部 API 客户端会将权限/资源限制误判为表单错误，影响错误提示、重试策略和接口契约一致性。
- **工程修复：** 将 `ErrForbidden` 独立映射为 HTTP 403，并补充更新接口的权限错误、校验错误和冲突错误状态码测试。
- **工程状态：** ✅ 代码修复与自动化回归已完成。
- **人工状态：** ☐ 待人工核验。
- **当前模式边界：** 仅核验基础模式下的 Xray 节点禁止编辑、manual 节点基础字段错误及修订冲突；高级模式不在本次任务范围内。

## 三、人工核验任务

> 不得使用真实凭据或生产数据。使用隔离 Production 或等价真实 API 环境，并记录节点 ID、实例状态、管理员角色、应用版本和请求时间。

### 核验目标

确认以下三类失败均不写入节点数据，且 API 与前端提示保持区分：

1. Xray 来源节点禁止编辑返回 `403`。
2. manual 节点基础字段校验失败返回 `400`。
3. manual 节点使用过期 `base_revision` 返回 `409`，并带当前修订信息。

### 核验步骤

1. 准备一个来源为 `xray`、由实例维护且不可手工编辑的节点，以及一个可编辑的 `manual` 节点。
2. 对 Xray 节点发送其他字段合法的 `PUT /api/admin/nodes/:id`，确认返回 `403` 且数据未改变。
3. 对 manual 节点构造协议、端口或必填字段等基础字段非法请求，确认返回 `400` 并记录前端提示。
4. 对 manual 节点使用过期 `base_revision` 更新，确认返回 `409` 并带当前修订信息。
5. 从节点详情/编辑界面触发上述失败路径，确认页面分别显示禁止编辑、请求校验错误和修订冲突，不混为同一种错误。
6. 核对三类失败均未写入节点数据；Xray 节点不因对照请求而期待 `400`/`409`。

### 执行记录

| 项目 | 执行日期 | 环境/版本 | 结果 | 证据或备注 |
|---|---|---|---|---|
| Xray PUT `403` / manual `400` / `409` 与前端提示 | 待填写 | 待填写 | 待人工测试 | 待填写 |

## 四、后续处理边界

- 当前只处理基础模式的 Xray 相关契约和人工核验。
- 高级模式暂不创建实施任务，不纳入当前 TODO、ProdTestList 或本文件的“已完成”统计。
- 如人工核验发现新的代码问题，新增独立编号并记录根因、影响、修复授权和验收证据；不得直接将人工发现改写为通过。

## 五、变更记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v1.0 | 2026-09-14 | 新建 Xray 相关问题跟踪文档；将 R26-07 的完整问题描述、工程状态和人工 API/UI 核验任务从 TODOLIST/ProdTestList 迁入；明确基础模式优先、高级模式暂不跟进。 |
